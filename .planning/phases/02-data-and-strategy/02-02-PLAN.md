---
phase: 02-data-and-strategy
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - pyproject.toml
  - Makefile
  - .env.example
  - config/app.yaml
  - src/trading_platform/core/settings.py
  - src/trading_platform/services/data.py
  - src/trading_platform/services/calendar.py
  - src/trading_platform/services/market_data_access.py
  - src/trading_platform/db/models/symbol.py
  - src/trading_platform/db/models/market_session.py
  - src/trading_platform/db/models/daily_bar.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0003_phase2_metadata_and_calendar.py
  - scripts/sync_symbol_metadata.py
  - src/trading_platform/worker/__main__.py
  - tests/test_db_migrations.py
  - tests/test_market_data_access.py
autonomous: true
requirements:
  - REQ-04
  - REQ-06
  - REQ-11
user_setup:
  - "Set `TRADING_PLATFORM_MARKET_DATA__POLYGON__API_KEY` in `.env` or the shell before running live metadata refreshes."
must_haves:
  truths:
    - "The platform can resolve which `XNYS` trading sessions should exist for a date range and can tell when stored daily bars are missing or stale."
    - "Symbol metadata and market-session context are persisted separately from raw bar ingestion and can be refreshed deterministically."
    - "Strategies and later backtests can read session-aware bar windows through a reusable market-data access layer instead of calling Polygon directly."
  artifacts:
    - "An enriched `symbols` model and a persisted `market_sessions` table exist with an Alembic revision."
    - "A session-aware market-data access service exists for lookback and as-of queries."
    - "A metadata refresh command exists for the configured universe."
  key_links:
    - "The calendar service provides the session truth used to judge ingest completeness and latest-session availability."
    - "Polygon ticker metadata upserts into the symbol catalog that the bar layer and strategy layer both reference."
    - "Automated tests prove session-aware reads and missing-session detection on persisted data."
---

<objective>
Turn raw ingested daily bars into a reusable market-data foundation by persisting symbol metadata, storing exchange-session context, and exposing session-aware data access patterns for later strategy use.

Purpose: This plan separates provider sync concerns from market-data consumption concerns so the strategy plan can operate on stable, reusable reads instead of making Polygon-specific assumptions.
Output: Symbol metadata refresh, persisted `XNYS` session context, and reusable market-data access services that answer calendar-aware read queries.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-and-strategy/02-RESEARCH.md
@.planning/phases/02-data-and-strategy/02-01-SUMMARY.md
@src/trading_platform/core/settings.py
@src/trading_platform/services/data.py
@src/trading_platform/services/polygon.py
@src/trading_platform/services/ingestion.py
@src/trading_platform/db/models/daily_bar.py
@tests/test_market_data_ingestion.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist symbol enrichment and exchange-session context</name>
  <files>pyproject.toml, .env.example, config/app.yaml, src/trading_platform/core/settings.py, src/trading_platform/db/models/symbol.py, src/trading_platform/db/models/market_session.py, src/trading_platform/db/models/daily_bar.py, src/trading_platform/db/models/__init__.py, alembic/versions/0003_phase2_metadata_and_calendar.py</files>
  <action>Add the calendar and metadata pieces that Phase 2 still lacks. Introduce `exchange-calendars` as the runtime source of `XNYS` session truth, add any settings needed for calendar code or metadata refresh policy, enrich the `symbols` table with provider metadata that matters for eligibility and auditability, and create a `market_sessions` table that persists session date, open/close timestamps, and early-close state. Keep the schema focused on daily workflow needs rather than forecasting later intraday complexity.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_db_migrations.py tests/test_market_data_access.py -q` passes after the migration and model updates.</verify>
  <done>The database holds both symbol metadata and persisted session context, so the platform can reason about expected daily data without guessing from weekdays alone.</done>
</task>

<task type="auto">
  <name>Task 2: Implement calendar-backed metadata sync and reusable market-data read services</name>
  <files>src/trading_platform/services/data.py, src/trading_platform/services/calendar.py, src/trading_platform/services/market_data_access.py, scripts/sync_symbol_metadata.py, src/trading_platform/worker/__main__.py</files>
  <action>Split the market-data boundary into explicit responsibilities: a calendar service that understands `XNYS`, a metadata-refresh path that pulls Polygon ticker overview data for the configured universe, and a reusable access layer that can answer questions such as "latest completed session", "bars for the last N sessions", and "which sessions are missing bars for symbol X". The access layer should operate on persisted data and hide provider-specific request semantics from callers. Keep the API surface CLI-first and do not add operator-facing HTTP endpoints yet unless a thin read route is required for debugging.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_market_data_access.py -q` passes and `PYTHONPATH=src .venv/bin/python scripts/sync_symbol_metadata.py --help` exits successfully.</verify>
  <done>The codebase has a session-aware market-data read layer and a dedicated metadata refresh flow that can support strategy lookbacks and stale-data checks later.</done>
</task>

<task type="auto">
  <name>Task 3: Prove session-aware queries, metadata refresh, and missing-session detection</name>
  <files>Makefile, tests/test_db_migrations.py, tests/test_market_data_access.py</files>
  <action>Add deterministic tests that seed persisted bars and sessions, then verify the reusable access layer returns the expected session windows and flags gaps correctly. Cover at least: metadata upsert behavior, `latest_completed_session` or equivalent resolution, bar-window retrieval by sessions rather than naive dates, and explicit missing-session detection for stale or incomplete symbols. Keep verification entirely local through fixtures and temporary PostgreSQL databases.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_market_data_access.py tests/test_db_migrations.py -q` passes.</verify>
  <done>The platform can prove that it understands which trading days exist, which bars are missing, and how strategies can safely request lookback windows from persisted data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_market_data_access.py tests/test_db_migrations.py -q` passes
- [ ] `PYTHONPATH=src .venv/bin/python scripts/sync_symbol_metadata.py --help` exits successfully
- [ ] Session-aware read helpers return the expected window across weekends and market holidays in tests
- [ ] Missing-session detection is covered by automated tests for at least one incomplete symbol
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The platform stores enough symbol and calendar context to run daily workflows consistently
- Later strategy and backtest work can consume session-aware bar reads without embedding Polygon- or calendar-specific logic
  </success_criteria>

<output>
After completion, create `.planning/phases/02-data-and-strategy/02-02-SUMMARY.md`
</output>
