---
phase: 02-data-and-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - Makefile
  - .env.example
  - config/app.yaml
  - src/trading_platform/core/settings.py
  - src/trading_platform/services/data.py
  - src/trading_platform/services/polygon.py
  - src/trading_platform/services/ingestion.py
  - src/trading_platform/db/models/symbol.py
  - src/trading_platform/db/models/daily_bar.py
  - src/trading_platform/db/models/market_data_ingestion_run.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0002_phase2_market_data_foundation.py
  - scripts/ingest_polygon_bars.py
  - src/trading_platform/worker/__main__.py
  - tests/test_db_migrations.py
  - tests/test_market_data_ingestion.py
  - tests/fixtures/polygon_daily_bars.json
autonomous: true
requirements:
  - REQ-04
  - REQ-06
  - REQ-11
user_setup:
  - "Set `TRADING_PLATFORM_MARKET_DATA__POLYGON__API_KEY` in `.env` or the shell before running live Polygon syncs."
must_haves:
  truths:
    - "Polygon daily bars can be fetched, normalized, and persisted as one deterministic row per symbol per session without manual cleanup."
    - "Re-running the same ingest window is idempotent and records a provider-backed ingestion run instead of duplicating rows."
    - "Provider credentials and ingest defaults remain externalized through typed settings."
  artifacts:
    - "Runtime config includes a Polygon provider block and ingest defaults."
    - "Persisted `symbols`, `daily_bars`, and `market_data_ingestion_runs` tables exist with an Alembic revision."
    - "A CLI or worker entrypoint exists for historical bar ingestion."
  key_links:
    - "The ingest command flows through typed settings into a dedicated Polygon client and then into the database layer."
    - "Normalized `session_date`, provider timestamps, and the `adjusted` flag persist together so later phases can reproduce the bar source."
    - "Automated tests mock Polygon payloads and prove pagination plus upsert behavior without live network calls."
---

<objective>
Build the first real market-data path for the platform: Polygon provider configuration, a deterministic daily-bar ingestion pipeline, and normalized persistence for historical bars.

Purpose: This plan turns the Phase 1 placeholder market-data boundary into a real provider-backed workflow while keeping the scope limited to reproducible daily-bar ingestion, not strategy logic or calendar-aware reads.
Output: Polygon provider settings, normalized bar and ingest-run persistence, and a CLI/worker entrypoint that can backfill the initial universe reproducibly.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-and-strategy/02-RESEARCH.md
@pyproject.toml
@Makefile
@config/app.yaml
@src/trading_platform/core/settings.py
@src/trading_platform/services/data.py
@src/trading_platform/db/session.py
@src/trading_platform/worker/__main__.py
@tests/test_db_migrations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend runtime settings and the market-data contract for Polygon-backed ingestion</name>
  <files>pyproject.toml, .env.example, config/app.yaml, src/trading_platform/core/settings.py, src/trading_platform/services/data.py</files>
  <action>Add the minimum runtime surface required to talk to Polygon without hardcoding credentials or request behavior. Promote `httpx` to a runtime dependency, add typed settings for the Polygon base URL, API key, adjusted-bar behavior, retry/backoff policy, and ingest window defaults, and replace the placeholder `get_daily_bars(symbols)` shape with typed request/response contracts that can represent per-symbol daily bars, ingest windows, and provider metadata. Keep the service synchronous and avoid pulling pandas or async infrastructure into the codebase here.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_app_boot.py -q` passes and `python -m compileall src` succeeds after the settings and contract updates.</verify>
  <done>The codebase has typed Polygon provider settings and a market-data boundary that can support real ingestion without leaking raw ad-hoc dictionaries through the platform.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the Polygon daily-bar client, normalized persistence, and idempotent ingest flow</name>
  <files>src/trading_platform/services/polygon.py, src/trading_platform/services/ingestion.py, src/trading_platform/db/models/symbol.py, src/trading_platform/db/models/daily_bar.py, src/trading_platform/db/models/market_data_ingestion_run.py, src/trading_platform/db/models/__init__.py, alembic/versions/0002_phase2_market_data_foundation.py, scripts/ingest_polygon_bars.py, src/trading_platform/worker/__main__.py</files>
  <action>Implement a dedicated Polygon client around the stocks daily-bar endpoint that handles pagination, adjusted-bar requests, and explicit request parameters. Add the first Phase 2 persistence tables: a minimal `symbols` catalog, normalized `daily_bars`, and `market_data_ingestion_runs`. Build an ingest orchestration path that upserts symbols and bars for the configured universe, records run status and request metadata, and uses a natural uniqueness boundary so re-running the same history window updates instead of duplicating. Do not add trading-calendar tables, signal logic, or backtest outputs in this plan.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_market_data_ingestion.py tests/test_db_migrations.py -q` passes, and `PYTHONPATH=src .venv/bin/python scripts/ingest_polygon_bars.py --help` exits successfully.</verify>
  <done>The repo can ingest mocked Polygon daily bars into PostgreSQL through a real CLI path, and the resulting schema is ready for later session-aware reads.</done>
</task>

<task type="auto">
  <name>Task 3: Harden the operator workflow and test the ingest path with provider fixtures</name>
  <files>Makefile, tests/test_db_migrations.py, tests/test_market_data_ingestion.py, tests/fixtures/polygon_daily_bars.json</files>
  <action>Add or update operator commands so the ingest path is discoverable through the same CLI-first workflow established in Phase 1, for example `make ingest-bars` or an equivalent worker subcommand wrapper. Use stable JSON fixtures and mocked HTTP interactions to prove normalization, pagination handling, clear failure behavior when the API key is absent, and idempotent upserts across repeat ingest runs. Keep the verification local and deterministic; do not require live Polygon access to pass the automated checks.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_market_data_ingestion.py tests/test_db_migrations.py -q` passes.</verify>
  <done>Phase 2 has a reproducible ingest workflow with deterministic tests that validate the provider boundary and persistence behavior without live network dependence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_market_data_ingestion.py tests/test_db_migrations.py -q` passes
- [ ] `python -m compileall src` succeeds
- [ ] `PYTHONPATH=src .venv/bin/python scripts/ingest_polygon_bars.py --help` exits successfully
- [ ] Repeated ingest of the same mocked time window does not duplicate `daily_bars` rows
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Historical daily bars for the initial universe can be normalized and persisted reproducibly from Polygon
- Market-data ingestion is explainable through typed config and ingestion-run records rather than opaque ad-hoc scripts
  </success_criteria>

<output>
After completion, create `.planning/phases/02-data-and-strategy/02-01-SUMMARY.md`
</output>
