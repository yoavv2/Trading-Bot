---
phase: 02-data-and-strategy
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - Makefile
  - config/strategies/trend_following_daily.yaml
  - src/trading_platform/core/settings.py
  - src/trading_platform/strategies/base.py
  - src/trading_platform/strategies/signals.py
  - src/trading_platform/strategies/registry.py
  - src/trading_platform/strategies/trend_following_daily/strategy.py
  - scripts/generate_signals.py
  - src/trading_platform/worker/__main__.py
  - tests/test_strategy_registry.py
  - tests/test_trend_following_strategy.py
autonomous: true
requirements:
  - REQ-03
  - REQ-11
user_setup: []
must_haves:
  truths:
    - "`TrendFollowingDailyV1` computes its configured moving-average indicators from persisted normalized daily bars and emits deterministic entry and exit signals."
    - "Strategy parameters such as universe, moving-average windows, and exit rules remain externalized in strategy config instead of being hardcoded in the implementation."
    - "The strategy remains registry-resolved and ready to be reused by later backtest and paper-execution phases."
  artifacts:
    - "The base strategy contract exposes typed signal-generation hooks and signal payload types."
    - "The trend-following strategy config and implementation include explicit indicator windows and exit behavior."
    - "Deterministic strategy tests exist for warmup, entry, exit, and no-signal cases."
  key_links:
    - "The strategy reads typed config from settings and consumes the session-aware market-data layer from Phase 2."
    - "Registry metadata still resolves the same strategy ID while surfacing the updated config-backed strategy shape."
    - "Signal generation stays free of broker, order, and risk-sizing logic so later phases can compose on top of it."
---

<objective>
Implement the first real strategy logic in the platform by extending the strategy contract, externalizing indicator and exit settings, and making `TrendFollowingDailyV1` emit deterministic entry and exit signals from persisted daily bars.

Purpose: This plan completes Phase 2 by proving the platform can turn normalized historical data into reproducible strategy decisions without yet entangling those decisions with backtesting, risk, or execution flows.
Output: A config-driven trend-following strategy implementation, typed signal outputs, and deterministic tests for the first signal-generation path.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-and-strategy/02-RESEARCH.md
@.planning/phases/02-data-and-strategy/02-01-SUMMARY.md
@.planning/phases/02-data-and-strategy/02-02-SUMMARY.md
@config/strategies/trend_following_daily.yaml
@src/trading_platform/core/settings.py
@src/trading_platform/strategies/base.py
@src/trading_platform/strategies/registry.py
@src/trading_platform/strategies/trend_following_daily/strategy.py
@src/trading_platform/services/market_data_access.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the strategy contract and config schema for real signal generation</name>
  <files>config/strategies/trend_following_daily.yaml, src/trading_platform/core/settings.py, src/trading_platform/strategies/base.py, src/trading_platform/strategies/signals.py, src/trading_platform/strategies/registry.py</files>
  <action>Promote the Phase 1 strategy shell into a reusable strategy contract that can produce typed signals. Add or refine typed settings for indicator windows, warmup requirements, and exit rules inside the existing strategy YAML and settings loader. Introduce explicit signal and indicator snapshot types so the strategy output is stable, inspectable, and easy to test. Keep the registry API compatible with the existing strategy ID and metadata flow; this plan should extend the contract, not replace the registry model built in Phase 1.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_strategy_registry.py -q` passes after the contract and settings updates.</verify>
  <done>The platform has a stable contract for strategy signal generation, and the strategy's configurable parameters live in typed config rather than inside method bodies.</done>
</task>

<task type="auto">
  <name>Task 2: Implement `TrendFollowingDailyV1` indicators and deterministic entry/exit logic</name>
  <files>src/trading_platform/strategies/trend_following_daily/strategy.py, scripts/generate_signals.py, src/trading_platform/worker/__main__.py</files>
  <action>Implement the strategy logic exactly within the MVP boundary: load the configured universe, request the required lookback window from the session-aware market-data layer, compute the 50-day and 200-day moving averages, and emit deterministic long-only signals. Respect the locked first-version behavior: entry when `close > SMA 200` and `SMA 50 > SMA 200`, exit when `close < SMA 50`, and no entry when there is insufficient history. Add a CLI or worker entrypoint that can evaluate the strategy for an explicit as-of session and return or print structured signal output. Do not introduce risk sizing, order intent creation, portfolio accounting, or broker integration here.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_trend_following_strategy.py -q` passes and `PYTHONPATH=src .venv/bin/python scripts/generate_signals.py --help` exits successfully.</verify>
  <done>The initial strategy can turn persisted daily bars into deterministic, typed entry and exit signals through a real operator entrypoint.</done>
</task>

<task type="auto">
  <name>Task 3: Prove signal determinism and guard the strategy boundary with automated tests</name>
  <files>Makefile, tests/test_strategy_registry.py, tests/test_trend_following_strategy.py</files>
  <action>Create deterministic tests that seed fixed bar histories and prove the strategy behavior at the boundaries that matter now: insufficient-history gating, entry generation when the trend filters pass, exit generation when price closes below the configured exit moving average, and no accidental signal churn when the same input window is evaluated twice. Update the operator workflow so the signal path is reachable through a clear command such as `make generate-signals`. Keep verification local and fixture-driven rather than depending on live provider calls.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_trend_following_strategy.py tests/test_strategy_registry.py -q` passes.</verify>
  <done>The strategy behavior is covered by deterministic tests, and the registry plus operator surface still expose the same strategy cleanly after the contract expansion.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_trend_following_strategy.py tests/test_strategy_registry.py -q` passes
- [ ] `PYTHONPATH=src .venv/bin/python scripts/generate_signals.py --help` exits successfully
- [ ] The strategy emits the same signals for the same persisted bar window across repeat test runs
- [ ] Strategy config remains the source of truth for universe, moving-average windows, and exits
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `TrendFollowingDailyV1` computes indicators and emits deterministic signals for configured symbols
- The strategy implementation stays isolated from risk, execution, and backtest concerns so later phases can compose on top of it cleanly
  </success_criteria>

<output>
After completion, create `.planning/phases/02-data-and-strategy/02-03-SUMMARY.md`
</output>
