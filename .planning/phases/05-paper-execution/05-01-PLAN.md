---
phase: 05-paper-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/app.yaml
  - src/trading_platform/core/settings.py
  - src/trading_platform/services/alpaca.py
  - src/trading_platform/services/execution.py
  - src/trading_platform/services/paper_execution.py
  - src/trading_platform/db/models/paper_order.py
  - src/trading_platform/db/models/strategy_run.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0008_phase5_paper_orders.py
  - src/trading_platform/worker/__main__.py
  - scripts/submit_paper_orders.py
  - Makefile
  - tests/test_alpaca_execution.py
  - tests/test_db_migrations.py
autonomous: true
requirements:
  - REQ-08
  - REQ-06
  - REQ-11
user_setup: []
must_haves:
  truths:
    - "Approved execution candidates can be turned into Alpaca paper orders through a real execution service instead of the current placeholder contract."
    - "Submitted paper orders persist with durable local idempotency keys and broker identifiers so later lifecycle sync and reconciliation can reuse them."
    - "Broker-specific request and response mapping stays isolated from strategy and risk code."
  artifacts:
    - "Typed broker and execution settings exist for Alpaca paper execution."
    - "A normalized `paper_orders` model plus Alembic migration and Phase 5 run-type support exist."
    - "A CLI and worker submission entrypoint plus broker-focused tests exist."
  key_links:
    - "The submission flow consumes approved Phase 4 execution candidates rather than raw strategy signals."
    - "Alpaca-specific HTTP behavior lives in `src/trading_platform/services/alpaca.py`, while orchestration stays in a separate execution service."
    - "Persisted paper orders anchor to `strategy_runs` so every execution batch remains auditable."
---

<objective>
Build the Phase 5 execution foundation by adding typed Alpaca broker settings, a real paper-execution service, and durable paper-order persistence for submitted orders.

Purpose: This plan replaces the placeholder execution seam with the minimal real broker integration needed to submit paper orders without mixing in scheduling, fill syncing, or reconciliation policy too early.
Output: Typed Alpaca settings, a thin broker client, normalized paper-order storage, and a CLI-first submission flow for approved execution candidates.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-paper-execution/05-RESEARCH.md
@.planning/phases/05-paper-execution/05-VALIDATION.md
@.planning/phases/04-risk-and-portfolio/04-02-SUMMARY.md
@src/trading_platform/services/execution.py
@src/trading_platform/worker/__main__.py
@src/trading_platform/core/settings.py
@src/trading_platform/db/models/strategy_run.py
@src/trading_platform/services/polygon.py
@src/trading_platform/strategies/signals.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed Alpaca paper settings and a thin broker client</name>
  <files>config/app.yaml, src/trading_platform/core/settings.py, src/trading_platform/services/alpaca.py, src/trading_platform/services/execution.py, tests/test_alpaca_execution.py</files>
  <action>Extend the typed settings surface with an explicit broker or execution block for Alpaca paper trading, including auth fields, base URL, timeout or retry controls, and any deterministic defaults the execution layer needs. Then add a thin `httpx`-based Alpaca client that mirrors the explicit style already used by `PolygonClient`: narrow methods, explicit request mapping, and normalized response payloads. Replace the placeholder execution-service contract with typed order-intent and order-result objects, but keep provider-specific HTTP semantics isolated inside the Alpaca module so strategy, portfolio, and risk code remain provider-agnostic.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_alpaca_execution.py -q` passes for auth, payload mapping, retry behavior, and response normalization.</verify>
  <done>The codebase has typed broker configuration and a real Alpaca paper client plus execution-service seam without leaking broker details into unrelated layers.</done>
</task>

<task type="auto">
  <name>Task 2: Add paper-order persistence and Phase 5 batch anchoring</name>
  <files>src/trading_platform/db/models/paper_order.py, src/trading_platform/db/models/strategy_run.py, src/trading_platform/db/models/__init__.py, alembic/versions/0008_phase5_paper_orders.py, tests/test_db_migrations.py</files>
  <action>Add the normalized storage needed for submitted paper orders, including a `paper_orders` model with local idempotency key, broker order identifier, symbol, side, quantity, intended session, local status, and broker status snapshot fields that later lifecycle syncing can update. Extend `strategy_runs` with the Phase 5 run-root shape needed to represent a paper-execution batch rather than inventing a second orchestration root. Keep fills, scheduling metadata, and reconciliation findings out of this first migration so the schema stays focused on submission and auditability.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_db_migrations.py -q` passes and confirms the new Phase 5 order schema lands at Alembic head.</verify>
  <done>Submitted paper orders have durable PostgreSQL storage and every submission batch has a canonical run root for later syncing and inspection.</done>
</task>

<task type="auto">
  <name>Task 3: Wire a CLI-first order-submission flow for approved candidates</name>
  <files>src/trading_platform/services/paper_execution.py, src/trading_platform/worker/__main__.py, scripts/submit_paper_orders.py, Makefile, tests/test_alpaca_execution.py</files>
  <action>Implement the first paper-execution orchestration flow that consumes approved execution candidates from the completed Phase 4 risk pipeline, assigns deterministic client-order IDs, submits the orders through the Alpaca client, persists the submission results, and prints a concise JSON summary for the operator. Expose that flow through both a standalone script and a worker subcommand so later scheduling can reuse the same service rather than duplicating broker logic. Keep this entrypoint focused on one-time submission only: no scheduler loop, no fill polling, and no reconciliation policy yet.</action>
  <verify>`PYTHONPATH=src .venv/bin/python scripts/submit_paper_orders.py --help` succeeds, `PYTHONPATH=src .venv/bin/python -m trading_platform.worker submit-paper-orders --help` succeeds, and `PYTHONPATH=src .venv/bin/pytest tests/test_alpaca_execution.py tests/test_db_migrations.py -q` passes.</verify>
  <done>The operator can submit approved paper orders through a real broker adapter and persist the results without scheduling or lifecycle logic bleeding into this first slice.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_alpaca_execution.py tests/test_db_migrations.py -q` passes
- [ ] `PYTHONPATH=src .venv/bin/python scripts/submit_paper_orders.py --help` passes
- [ ] `PYTHONPATH=src .venv/bin/python -m trading_platform.worker submit-paper-orders --help` passes
- [ ] Mocked order submission persists `paper_orders` rows with stable client-order IDs and broker identifiers
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The placeholder execution service is replaced by a real paper-order submission seam
- Submitted paper orders are durably stored with enough metadata for lifecycle sync and reconciliation in later plans
  </success_criteria>

<output>
After completion, create `.planning/phases/05-paper-execution/05-01-SUMMARY.md`
</output>
