---
phase: 05-paper-execution
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - config/app.yaml
  - src/trading_platform/core/settings.py
  - src/trading_platform/services/paper_execution.py
  - src/trading_platform/services/alpaca.py
  - src/trading_platform/db/models/paper_order.py
  - src/trading_platform/db/models/paper_fill.py
  - src/trading_platform/db/models/position.py
  - src/trading_platform/db/models/account_snapshot.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0009_phase5_order_lifecycle.py
  - src/trading_platform/worker/__main__.py
  - scripts/run_paper_session.py
  - scripts/sync_paper_state.py
  - Makefile
  - tests/test_paper_execution.py
  - tests/test_db_migrations.py
autonomous: true
requirements:
  - REQ-08
  - REQ-06
user_setup: []
must_haves:
  truths:
    - "One idempotent paper-session runner can execute the daily flow without manual intervention once scheduled."
    - "Order status changes, fills, positions, and account snapshots persist across process restarts."
    - "Broker-derived live state updates reuse the same submission foundation from 05-01 rather than building a second execution path."
  artifacts:
    - "A scheduled paper-session runner and worker entrypoint exist."
    - "Normalized `paper_fills` persistence and migration support exist alongside order lifecycle updates."
    - "Broker-sync commands and automated lifecycle tests exist."
  key_links:
    - "The paper-session runner resolves a target session and consumes approved execution candidates before submitting missing orders."
    - "Positions and account snapshots are updated from broker reads, not only from local assumptions."
    - "Lifecycle sync reuses the `paper_orders` records and Alpaca client from 05-01 instead of duplicating submission logic."
---

<objective>
Build the day-to-day operating loop for paper trading by adding an idempotent session runner, lifecycle syncing, fill ingestion, and broker-derived live-state updates.

Purpose: This plan turns the raw submission seam from `05-01` into a usable daily workflow that can be scheduled and resumed safely while keeping internal state aligned with broker responses.
Output: A session-aware paper runner, order-lifecycle sync and fill ingestion, and updated live positions and account snapshots derived from the broker.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-paper-execution/05-RESEARCH.md
@.planning/phases/05-paper-execution/05-VALIDATION.md
@.planning/phases/05-paper-execution/05-01-SUMMARY.md
@.planning/phases/04-risk-and-portfolio/04-02-SUMMARY.md
@src/trading_platform/services/paper_execution.py
@src/trading_platform/services/alpaca.py
@src/trading_platform/services/market_data_access.py
@src/trading_platform/worker/__main__.py
@src/trading_platform/db/models/strategy_run.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build an idempotent paper-session runner and scheduling surface</name>
  <files>config/app.yaml, src/trading_platform/core/settings.py, src/trading_platform/services/paper_execution.py, src/trading_platform/worker/__main__.py, scripts/run_paper_session.py, Makefile, tests/test_paper_execution.py</files>
  <action>Introduce the paper-session orchestration that resolves the target trading session from persisted market sessions, loads the approved execution candidates for that session, and submits only the missing orders through the existing `05-01` execution seam. Add any scheduling settings needed for a fixed daily paper-trading cadence, but keep the business logic inside a reusable session runner that can be called manually or from a scheduler rather than burying the logic inside a forever-loop. The runner must be idempotent for a session so repeated invocations after a restart do not re-submit the same orders.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_paper_execution.py -q` passes for session resolution, idempotency, and runner behavior.</verify>
  <done>The codebase has a deterministic paper-session runner that is safe to schedule and safe to re-run for the same session.</done>
</task>

<task type="auto">
  <name>Task 2: Persist lifecycle updates, fills, positions, and account snapshots from broker sync</name>
  <files>src/trading_platform/services/paper_execution.py, src/trading_platform/services/alpaca.py, src/trading_platform/db/models/paper_order.py, src/trading_platform/db/models/paper_fill.py, src/trading_platform/db/models/position.py, src/trading_platform/db/models/account_snapshot.py, src/trading_platform/db/models/__init__.py, alembic/versions/0009_phase5_order_lifecycle.py, scripts/sync_paper_state.py, tests/test_paper_execution.py, tests/test_db_migrations.py</files>
  <action>Add the lifecycle persistence Phase 5 needs after submission: sync broker order statuses, ingest fills, and update the Phase 4 live-state tables for positions and account snapshots from broker reads. Introduce only the normalized schema needed to represent the broker lifecycle cleanly, such as `paper_fills` and any missing order timestamps or status fields. Keep the state-update logic broker-derived: local assumptions may inform provisional summaries, but the durable position and account state should be refreshed from broker data whenever the sync path runs.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_paper_execution.py tests/test_db_migrations.py -q` passes and confirms fills plus lifecycle schema land correctly.</verify>
  <done>Broker lifecycle changes, fills, positions, and account snapshots persist durably and can be recovered after a restart.</done>
</task>

<task type="auto">
  <name>Task 3: Expose operator entrypoints and regression coverage for the scheduled loop</name>
  <files>src/trading_platform/worker/__main__.py, scripts/run_paper_session.py, scripts/sync_paper_state.py, Makefile, tests/test_paper_execution.py</files>
  <action>Expose the paper-session runner and broker-sync flow through the worker and standalone scripts, and add any Makefile shortcuts needed to keep the operator workflow consistent with the rest of the repository. Expand tests so they cover repeat invocation of the same session, partial lifecycle progression, and state sync after order submission. Keep the operator surface strictly at the execution and sync layer: do not pull reconciliation policy or unsafe-state stops forward from `05-03`.</action>
  <verify>`PYTHONPATH=src .venv/bin/python scripts/run_paper_session.py --help` succeeds, `PYTHONPATH=src .venv/bin/python -m trading_platform.worker run-paper-session --help` succeeds, and `PYTHONPATH=src .venv/bin/pytest tests/test_paper_execution.py tests/test_alpaca_execution.py tests/test_db_migrations.py -q` passes.</verify>
  <done>The operator can run and schedule the paper-execution loop with CLI-first entrypoints, and the lifecycle behavior is covered by deterministic tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_paper_execution.py tests/test_alpaca_execution.py tests/test_db_migrations.py -q` passes
- [ ] `PYTHONPATH=src .venv/bin/python scripts/run_paper_session.py --help` passes
- [ ] `PYTHONPATH=src .venv/bin/python -m trading_platform.worker run-paper-session --help` passes
- [ ] `PYTHONPATH=src .venv/bin/python scripts/sync_paper_state.py --help` passes
- [ ] The paper-session runner is idempotent for the same session and broker sync updates fills, positions, and account snapshots
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The platform can run a deterministic daily paper-execution session without manual order submission
- Lifecycle sync and fill ingestion keep local live state aligned with broker responses across restarts
  </success_criteria>

<output>
After completion, create `.planning/phases/05-paper-execution/05-02-SUMMARY.md`
</output>
