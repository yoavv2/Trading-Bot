---
phase: 05-paper-execution
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src/trading_platform/services/reconciliation.py
  - src/trading_platform/services/paper_execution.py
  - src/trading_platform/services/alpaca.py
  - src/trading_platform/db/models/execution_event.py
  - src/trading_platform/db/models/paper_order.py
  - src/trading_platform/db/models/strategy_run.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0010_phase5_reconciliation_guards.py
  - src/trading_platform/worker/__main__.py
  - scripts/reconcile_paper_execution.py
  - Makefile
  - tests/test_execution_reconciliation.py
  - tests/test_paper_execution.py
  - tests/test_db_migrations.py
autonomous: true
requirements:
  - REQ-08
  - REQ-06
user_setup: []
must_haves:
  truths:
    - "New paper execution is blocked when broker and local state diverge or repeated execution failures exceed the configured safety policy."
    - "Restarts do not create duplicate broker submissions or leave in-flight execution state ambiguous."
    - "The operator can inspect reconciliation findings, retries, and stop reasons through durable records instead of inferring them from logs."
  artifacts:
    - "A reconciliation service and normalized persistence for execution or reconciliation events exist."
    - "Restart-safe idempotency and repeated-failure guard logic exist in the paper-execution orchestration path."
    - "CLI and worker reconciliation entrypoints plus automated reconciliation tests exist."
  key_links:
    - "Reconciliation compares broker orders, fills, positions, and account state against the persisted local execution tables."
    - "Unsafe-state findings feed execution gating before the next paper-session run."
    - "Restart-safe submission reuses the existing `paper_orders` client-order IDs instead of inventing a second deduplication path."
---

<objective>
Finish the Phase 5 operating model by adding reconciliation, restart-safe execution recovery, and explicit stop conditions for unsafe broker state.

Purpose: This plan makes the paper-trading loop trustworthy in practice by preventing silent drift, duplicate submissions, and unsafe continued execution when broker state no longer matches local records.
Output: Reconciliation services, durable execution-stop events, restart-safe replay logic, and operator commands for inspecting or resolving unsafe state.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-paper-execution/05-RESEARCH.md
@.planning/phases/05-paper-execution/05-VALIDATION.md
@.planning/phases/05-paper-execution/05-02-SUMMARY.md
@src/trading_platform/services/paper_execution.py
@src/trading_platform/services/alpaca.py
@src/trading_platform/worker/__main__.py
@src/trading_platform/db/models/strategy_run.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement broker-to-local reconciliation and unsafe-state persistence</name>
  <files>src/trading_platform/services/reconciliation.py, src/trading_platform/services/paper_execution.py, src/trading_platform/services/alpaca.py, src/trading_platform/db/models/execution_event.py, src/trading_platform/db/models/paper_order.py, src/trading_platform/db/models/__init__.py, alembic/versions/0010_phase5_reconciliation_guards.py, tests/test_execution_reconciliation.py, tests/test_db_migrations.py</files>
  <action>Add a dedicated reconciliation service that compares broker orders, fills, positions, and account state against the persisted local execution records, then records any mismatch as a durable execution or reconciliation event with severity, details, and whether the mismatch blocks new execution. Keep the mismatch model explicit enough for next-day inspection rather than hiding it in log messages or transient exceptions. Reuse the existing Phase 5 persistence shapes rather than inventing shadow state.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_execution_reconciliation.py tests/test_db_migrations.py -q` passes for mismatch detection and schema coverage.</verify>
  <done>Unsafe broker drift is durably detectable and inspectable before the next paper-execution session runs.</done>
</task>

<task type="auto">
  <name>Task 2: Add restart-safe submission recovery and repeated-failure guardrails</name>
  <files>src/trading_platform/services/paper_execution.py, src/trading_platform/db/models/paper_order.py, src/trading_platform/db/models/strategy_run.py, tests/test_execution_reconciliation.py, tests/test_paper_execution.py</files>
  <action>Update the paper-execution orchestration so restarts and retries are safe: reuse persisted client-order IDs, resume or sync in-flight orders before any resubmission attempt, track repeated submission or sync failures, and block new execution when the configured failure threshold or unresolved mismatch policy is reached. Keep these guardrails in the execution orchestration layer, not in strategy generation or risk evaluation. The goal is to make paper execution fail closed when state becomes ambiguous.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_execution_reconciliation.py tests/test_paper_execution.py -q` passes for restart-safe recovery and stop-condition behavior.</verify>
  <done>The paper-execution loop can recover from restarts without double-submitting, and it halts cleanly when repeated failures or unresolved drift make execution unsafe.</done>
</task>

<task type="auto">
  <name>Task 3: Expose reconciliation commands and lock the unsafe-state behavior with regression tests</name>
  <files>src/trading_platform/worker/__main__.py, scripts/reconcile_paper_execution.py, Makefile, tests/test_execution_reconciliation.py, tests/test_paper_execution.py</files>
  <action>Expose the reconciliation and unsafe-state inspection flow through both worker and standalone CLI entrypoints, and extend tests so they cover operator-visible stop reasons, mismatch reporting, and re-entry after a clean reconciliation. Keep the operator surface minimal and factual: report what drift was found, whether execution is blocked, and which persisted records need review. Do not fold Phase 6 API or dashboard work into this plan.</action>
  <verify>`PYTHONPATH=src .venv/bin/python scripts/reconcile_paper_execution.py --help` succeeds, `PYTHONPATH=src .venv/bin/python -m trading_platform.worker reconcile-paper-execution --help` succeeds, and `PYTHONPATH=src .venv/bin/pytest tests/test_execution_reconciliation.py tests/test_paper_execution.py tests/test_alpaca_execution.py tests/test_db_migrations.py -q` passes.</verify>
  <done>The operator can inspect or run reconciliation from the terminal, and the paper-trading loop now blocks unsafe execution instead of silently drifting.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_execution_reconciliation.py tests/test_paper_execution.py tests/test_alpaca_execution.py tests/test_db_migrations.py -q` passes
- [ ] `PYTHONPATH=src .venv/bin/python scripts/reconcile_paper_execution.py --help` passes
- [ ] `PYTHONPATH=src .venv/bin/python -m trading_platform.worker reconcile-paper-execution --help` passes
- [ ] Reconciliation findings persist durably and block new execution when unresolved or repeatedly failing
- [ ] Restarting the same paper session does not create duplicate broker orders
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Broker reconciliation and restart-safe execution make the paper-trading loop fail closed when state is unsafe
- The operator can inspect submitted, filled, retried, blocked, and mismatched execution outcomes without reading raw logs
  </success_criteria>

<output>
After completion, create `.planning/phases/05-paper-execution/05-03-SUMMARY.md`
</output>
