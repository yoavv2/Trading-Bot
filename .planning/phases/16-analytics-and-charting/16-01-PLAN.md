---
phase: 16-analytics-and-charting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_platform/services/analytics.py
  - tests/test_analytics_service.py
autonomous: true
requirements: [ANLX-01]
must_haves:
  truths:
    - "The GET /api/v1/analytics/strategies/{strategy_id}?backtest_run_id=... response includes an equity_curve array under the backtest block"
    - "equity_curve carries the already-computed session_date + total_equity series produced by materialize_backtest_report — no new computation"
    - "A pytest asserts equity_curve is present and non-empty in the summarized backtest block"
  artifacts:
    - path: "src/trading_platform/services/analytics.py"
      provides: "equity_curve passthrough in StrategyAnalyticsService._summarize_backtest"
      contains: "equity_curve"
    - path: "tests/test_analytics_service.py"
      provides: "Test asserting equity_curve is serialized into the backtest block"
      contains: "equity_curve"
  key_links:
    - from: "src/trading_platform/services/analytics.py:_summarize_backtest"
      to: "materialize_backtest_report report['equity_curve']"
      via: "add report['equity_curve'] to the returned dict"
      pattern: "equity_curve.*report\\[.equity_curve.\\]"
---

<objective>
Serialize the already-computed `equity_curve` field into the existing analytics
response so the console can chart it. This is the single authorized backend
change for Phase 16 (ROADMAP Known Gaps #2, operator-approved 2026-07-07):
read-only exposure of already-computed state, not a new capability.

Purpose: `materialize_backtest_report()` already computes `equity_curve`, but
`StrategyAnalyticsService._summarize_backtest()` filters it out before it reaches
`GET /api/v1/analytics/strategies/{strategy_id}`. Stop filtering it out.
Output: `backtest.equity_curve` present in the analytics response, covered by a
service-level pytest.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-analytics-and-charting/16-CONTEXT.md

@src/trading_platform/services/analytics.py
@src/trading_platform/services/backtest_reporting.py
@tests/test_analytics_service.py

<interfaces>
<!-- The report dict returned by materialize_backtest_report() already contains
     equity_curve. _summarize_backtest currently drops it. -->

materialize_backtest_report() returns (src/trading_platform/services/backtest_reporting.py:73-85):
```python
{
  "run_id": str, "strategy_id": str, "status": str, "trigger_source": str,
  "started_at": str, "completed_at": str | None,
  "summary": dict, "assumptions": dict, "metrics": dict, "trades": list,
  "equity_curve": list,   # <-- already computed, currently filtered out
}
```

Each equity_curve row (backtest_reporting.py _load_equity_rows, serialized):
```python
{
  "session_date": "YYYY-MM-DD",     # ISO date
  "cash": float, "gross_exposure": float,
  "total_equity": float,            # <-- the series the chart plots
  "realized_pnl": float, "unrealized_pnl": float,
  "open_positions": int,
}
```

_summarize_backtest currently returns ONLY (analytics.py:133-140):
```python
{"run_id", "status", "started_at", "completed_at", "summary", "metrics"}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Serialize equity_curve into the backtest block + cover with a service test</name>
  <files>src/trading_platform/services/analytics.py, tests/test_analytics_service.py</files>
  <action>
    In `StrategyAnalyticsService._summarize_backtest()` (src/trading_platform/services/analytics.py, the return dict at lines ~133-140), add one key to the
    returned dict:

      "equity_curve": report["equity_curve"],

    That is the ONLY change to analytics.py. Do NOT add computation, transformation,
    a new route, or a new method. `report["equity_curve"]` is already fully serialized
    by `materialize_backtest_report()` / `_load_equity_rows()` (list of dicts with
    session_date + total_equity + pnl fields). Read-only passthrough of already-computed
    state — this is the exact scope of the ROADMAP Known Gaps #2 exception; anything more
    is out of scope.

    Then extend the existing test in tests/test_analytics_service.py named
    `test_strategy_analytics_service_summarizes_backtest_and_paper_state` (it already
    runs a real backtest and summarizes it) with assertions that equity_curve is now
    present and usable:
      - assert "equity_curve" in summary["backtest"]
      - assert len(summary["backtest"]["equity_curve"]) > 0
      - assert "session_date" in summary["backtest"]["equity_curve"][0]
      - assert "total_equity" in summary["backtest"]["equity_curve"][0]

    Prefer extending that existing test (it already has a seeded backtest fixture) over
    adding a brand-new test, to reuse its DB/backtest setup. If placing the assertions in
    that test is awkward, add a focused new test in the same file that reuses the same
    fixtures (migrated_analytics_db, strategy_config_override) and the same run_backtest
    seeding pattern already present at the top of that test.
  </action>
  <verify>
    <automated>python -m pytest tests/test_analytics_service.py::test_strategy_analytics_service_summarizes_backtest_and_paper_state -x -q</automated>
  </verify>
  <done>The analytics service backtest block includes a non-empty equity_curve list whose rows carry session_date + total_equity; the pytest asserting this passes. No new route/method/computation was added.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_analytics_service.py -q` passes (no regressions in the analytics suite).
- `git diff src/trading_platform/services/analytics.py` shows exactly one added line (the equity_curve passthrough) and no other logic change.
</verification>

<success_criteria>
- `GET /api/v1/analytics/strategies/{strategy_id}?backtest_run_id=...` returns `backtest.equity_curve` (list of {session_date, total_equity, ...}).
- The change is a pure passthrough of already-computed state — no new route, method, or computation.
- A service-level pytest asserts equity_curve presence and shape.
</success_criteria>

<output>
After completion, create `.planning/phases/16-analytics-and-charting/16-01-SUMMARY.md`
</output>
