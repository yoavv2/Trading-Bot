---
phase: 16-analytics-and-charting
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified: []
autonomous: false
requirements: [ANLX-01, ANLX-02]
must_haves:
  truths:
    - "Operator selects a backtest run and sees its equity curve rendered as a Recharts line chart against live backend data"
    - "Operator sees the labeled summary metrics — Sharpe, max drawdown, win rate, total return %, trade count — for that run"
    - "Operator confirms a non-backtest run shows no analytics section, and an absent/empty equity_curve shows an honest 'not available' state (not a broken chart)"
    - "Operator confirms an API-down analytics fetch renders an ErrorState naming the endpoint"
  artifacts: []
  key_links: []
---

<objective>
Operator live-verifies the Phase 16 analytics view end-to-end against a running
FastAPI backend and console dev server, consistent with the Phase 14 (14-05) and
Phase 15 (15-03) checkpoints, and signs off ANLX-01 + ANLX-02.

Purpose: prove the equity chart + summary metrics render correctly on real data,
degrade honestly when data is absent, and fail honestly when the API is down —
before the phase (and the v1.2 milestone) is marked complete.
Output: operator approval (or a diagnosed defect list to feed a gaps plan).
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-analytics-and-charting/16-CONTEXT.md
@.planning/phases/14-strategy-and-runs-inspection/14-05-SUMMARY.md

Prerequisites: 16-01 (equity_curve serialized into the analytics response) and
16-02 (recharts + EquityCurveChart + SummaryMetricsPanel + BacktestAnalyticsSection
mounted on backtest run-detail pages) are complete and committed.

Data-availability note: a real backtest run with a persisted equity curve must
exist in the operator's DB for the populated-chart path to be verified. If none
exists, the operator can run a backtest (existing worker/CLI path) to seed one, or
approve the honest-not-available path only and note that populated-chart rendering
remains live-unverified until a backtest run is present — mirroring the 15-03
precedent where broker-empty data was the checkpoint's designed condition.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator verifies the analytics view end-to-end</name>
  <action>Pause execution and present the verification steps below to the operator against a live FastAPI backend + console dev server. Do not mark ANLX-01/ANLX-02 verified or Phase 16 complete without operator sign-off.</action>
  <what-built>
Phase 16 delivered the analytics view on the Phase 14 run-detail surface:
- Backend (16-01): the existing `/api/v1/analytics/strategies/{id}` response now includes `backtest.equity_curve` (read-only passthrough of already-computed state — the one authorized backend change).
- Console (16-02): on a backtest run's detail page, a `BacktestAnalyticsSection` renders a Recharts equity curve (total_equity over session_date) and a labeled summary-metrics panel (Sharpe, max drawdown, win rate, total return %, trade count), both from a SINGLE fetch of `/api/v1/analytics/strategies/{id}?backtest_run_id=...`. Non-backtest runs render no analytics section; absent equity_curve/metrics render honest "not available" states; fetch failure renders an endpoint-named ErrorState. The existing MetricsPanel (RUNS-06 raw table) is unchanged.
  </what-built>
  <how-to-verify>
Preconditions: FastAPI backend running; console dev server running (`cd console && npm run dev`); open http://localhost:3000.

1. NAVIGATE: open the Runs table, filter to run_type = backtest, and open a backtest run's detail page. Confirm an equity-curve / analytics section appears below the existing run panels, with its own "as of …" FetchMeta timestamp + Refresh; press Refresh and confirm the timestamp advances.
2. EQUITY CURVE (ANLX-01): confirm a line chart renders total_equity over session_date — a real curve with axes and a hover tooltip, NOT a blank frame or broken image. If the selected run has no persisted equity curve, confirm the panel instead shows an explicit "equity curve not available" message (honest state). Where a run WITH a curve exists, verify the populated path.
3. SUMMARY METRICS (ANLX-02): confirm all five labeled figures render — Sharpe, Max drawdown %, Win rate %, Total return %, Trade count — and that their values agree with the raw Metrics panel (RUNS-06) on the same page (both read the same backend metrics). If metrics are absent, confirm an explicit "summary metrics not available" state.
4. BACKTEST-ONLY GATING: open a non-backtest run (operator_control / risk / paper) and confirm NO equity-curve/analytics section appears (equity curves are backtest-only per CONTEXT).
5. HONEST FAILURE (CONS-02 inheritance): stop the FastAPI backend and Refresh the analytics section; confirm it renders an ErrorState NAMING the `/api/v1/analytics/strategies/...` endpoint and status (HTTP code or "unreachable") — never a blank or fake-success render. Restart the backend, Refresh, and confirm full recovery.
6. Report any rendering/labeling/gating defect. Per the checkpoint rules, a directly in-scope one-line rendering bug may be fixed live (as in 14-05); anything larger is diagnosed for a gaps plan via `/gsd:plan-phase 16 --gaps`.
  </how-to-verify>
  <resume-signal>Type "approved" once steps 1-6 pass, or list the specific failures (panel + expected vs actual) so gaps can be planned.</resume-signal>
  <verify>Operator responds "approved" after completing steps 1-6 against live data.</verify>
  <done>Operator confirmed live: a backtest run's detail page renders a Recharts equity curve and labeled Sharpe/max-drawdown/win-rate/total-return/trade-count metrics agreeing with the raw metrics table; non-backtest runs show no analytics section; absent equity_curve/metrics render honest not-available states; and the section degrades to an endpoint-named ErrorState on API down with correct recovery. ANLX-01 and ANLX-02 verified.</done>
</task>

</tasks>

<verification>
- Operator has exercised steps 1-6 against live data and either approved or produced a diagnosed defect list.
</verification>

<success_criteria>
- ANLX-01 and ANLX-02 are operator-confirmed: equity curve chart and labeled summary metrics render on a backtest run, non-backtest runs are correctly excluded, honest not-available and endpoint-named error states hold.
- Phase 16 (and the v1.2 Operator Console v0 milestone) is ready to be marked complete on approval.
</success_criteria>

<output>
After completion, create `.planning/phases/16-analytics-and-charting/16-03-SUMMARY.md`
</output>
