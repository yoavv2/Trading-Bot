---
phase: 04-risk-and-portfolio
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/trading_platform/services/portfolio.py
  - src/trading_platform/services/risk.py
  - src/trading_platform/services/market_data_access.py
  - src/trading_platform/db/models/risk_event.py
  - src/trading_platform/db/models/strategy_run.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0007_phase4_risk_pipeline.py
  - src/trading_platform/worker/__main__.py
  - scripts/evaluate_risk.py
  - tests/test_risk_pipeline.py
  - tests/test_db_migrations.py
autonomous: true
requirements:
  - REQ-07
  - REQ-06
user_setup: []
must_haves:
  truths:
    - "Every generated signal is deterministically approved or rejected by a mandatory risk-validation pipeline before any execution path exists."
    - "Rejected signals are persisted with machine-readable rule codes and human-readable explanations."
    - "Phase 5 can consume approved, risk-evaluated execution candidates without re-implementing the Phase 4 guardrails."
  artifacts:
    - "A concrete `RiskService` exists for signal evaluation against portfolio state and stale-data rules."
    - "A normalized persistence shape exists for approved or rejected risk decisions, plus any required run-root changes."
    - "A CLI and worker entrypoint exist for running and inspecting the signal-to-risk evaluation flow."
  key_links:
    - "The risk pipeline consumes `strategy.generate_signals(...)` and the `04-01` portfolio service instead of embedding strategy logic or broker logic."
    - "Persisted risk-decision rows anchor to `strategy_runs` or an equivalent batch root plus symbols and sessions for later inspection."
    - "Automated tests prove stale-data blocking, duplicate prevention, allocation caps, and approved-sizing behavior against seeded PostgreSQL data."
---

<objective>
Turn Phase 4 into a real execution gate by implementing the risk-validation pipeline, persisting signal decisions, and exposing a CLI-first evaluation flow that later paper execution can reuse.

Purpose: This plan makes the risk engine mandatory in practice by forcing every generated signal through deterministic validation and persisted auditability before broker submission exists.
Output: A concrete risk service, per-signal decision persistence, and an operator command that runs the full signal to risk evaluation loop.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-risk-and-portfolio/04-RESEARCH.md
@.planning/phases/04-risk-and-portfolio/04-01-SUMMARY.md
@.planning/phases/01-foundation-platform/01-03-SUMMARY.md
@src/trading_platform/services/risk.py
@src/trading_platform/services/portfolio.py
@src/trading_platform/services/market_data_access.py
@src/trading_platform/strategies/signals.py
@src/trading_platform/worker/__main__.py
@tests/test_backtest_runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the placeholder risk contract with a real validation pipeline</name>
  <files>src/trading_platform/services/portfolio.py, src/trading_platform/services/risk.py, src/trading_platform/services/market_data_access.py, tests/test_risk_pipeline.py</files>
  <action>Implement the Phase 4 `RiskService` around typed inputs and outputs that accept strategy signals, the evaluation session, and the persisted portfolio state from `04-01`, then emit a deterministic decision for each symbol. The pipeline must enforce the v1 rules Phase 4 owns: stale-data blocking, duplicate open-position prevention, max-position limits, strategy allocation caps, total allocation caps, and size calculation from the portfolio service. Represent broker reconciliation and repeated-order-failure guards only to the extent needed to keep the interface extensible; do not pull actual broker-state or order-submission behavior forward from Phase 5.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_risk_pipeline.py -q` passes for the core validation behavior.</verify>
  <done>Every signal can be deterministically converted into either an approved execution candidate or a rejected decision before any broker adapter exists.</done>
</task>

<task type="auto">
  <name>Task 2: Persist approved and rejected signal decisions with audit-ready context</name>
  <files>src/trading_platform/db/models/risk_event.py, src/trading_platform/db/models/strategy_run.py, src/trading_platform/db/models/__init__.py, alembic/versions/0007_phase4_risk_pipeline.py, tests/test_db_migrations.py</files>
  <action>Add the normalized persistence the roadmap requires for blocked-trade explanations and later operator inspection: one decision record per evaluated signal carrying strategy run context, symbol, session date, signal direction, decision outcome, machine-readable rejection code, human-readable reason, and any proposed sizing metadata needed by Phase 5. If the evaluation flow needs a new run type or batch anchor, extend `strategy_runs` rather than inventing a second orchestration root. Keep actual orders, fills, and broker lifecycle tables out of scope for this plan.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_db_migrations.py tests/test_risk_pipeline.py -q` passes and confirms the new audit schema lands at Alembic head.</verify>
  <done>Approved and blocked signals are durably inspectable in PostgreSQL with enough context to explain or reuse the decision later.</done>
</task>

<task type="auto">
  <name>Task 3: Wire a CLI-first signal-to-risk evaluation flow and regression coverage</name>
  <files>src/trading_platform/worker/__main__.py, scripts/evaluate_risk.py, tests/test_risk_pipeline.py</files>
  <action>Expose a worker and standalone CLI command that resolves the evaluation session, generates strategy signals through the existing strategy boundary, loads current portfolio state, runs the risk pipeline, persists the decisions, and prints a concise summary for the operator. Add integration coverage for stale-data rejection, duplicate-position blocking, max-position rejection, and approved-sizing output. Keep the command strictly at the gating layer: it may produce approved execution candidates, but it must not submit orders, talk to Alpaca, or mutate broker-order state.</action>
  <verify>`PYTHONPATH=src .venv/bin/python scripts/evaluate_risk.py --help` exits successfully, `PYTHONPATH=src .venv/bin/python -m trading_platform.worker evaluate-risk --help` exits successfully, and `PYTHONPATH=src .venv/bin/pytest tests/test_risk_pipeline.py tests/test_portfolio_service.py tests/test_db_migrations.py tests/test_backtest_runner.py -q` passes.</verify>
  <done>The operator can run the full signal-to-risk loop from the terminal, and Phase 5 has a stable, persisted execution-gating surface to build on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_risk_pipeline.py tests/test_portfolio_service.py tests/test_db_migrations.py tests/test_backtest_runner.py -q` passes
- [ ] The risk pipeline has no bypass path from signal generation to execution candidate creation
- [ ] Approved and rejected decisions persist with both machine-readable codes and human-readable reasons
- [ ] CLI or worker evaluation commands run through the same risk service instead of duplicating gating logic
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Every strategy signal now flows through a real, persisted risk-validation pipeline before execution
- Phase 5 can build on approved risk-evaluated candidates without redesigning the Phase 4 audit trail
  </success_criteria>

<output>
After completion, create `.planning/phases/04-risk-and-portfolio/04-02-SUMMARY.md`
</output>
