---
phase: 04-risk-and-portfolio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/app.yaml
  - src/trading_platform/core/settings.py
  - src/trading_platform/services/portfolio.py
  - src/trading_platform/db/models/position.py
  - src/trading_platform/db/models/account_snapshot.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0006_phase4_portfolio_state.py
  - tests/test_portfolio_service.py
  - tests/test_db_migrations.py
autonomous: true
requirements:
  - REQ-07
  - REQ-06
  - REQ-11
user_setup: []
must_haves:
  truths:
    - "The platform can derive deterministic cash, exposure, and open-position state for the single-user portfolio from normalized PostgreSQL records."
    - "Position sizing is deterministic, whole-share based, and constrained by typed Phase 4 configuration rather than hidden constants."
    - "Phase 5 can read a real portfolio-state foundation instead of inferring live state from backtest artifacts or in-memory objects."
  artifacts:
    - "Typed portfolio or risk runtime settings exist for Phase 4 allocation and freshness rules."
    - "Normalized `positions` and `account_snapshots` models plus an Alembic revision exist for the live trading path."
    - "A dedicated portfolio service and automated tests exist for sizing and exposure accounting."
  key_links:
    - "The portfolio service composes on existing strategy metadata and persisted market data instead of embedding strategy logic."
    - "Live portfolio-state tables remain separate from Phase 3 backtest trade and equity tables."
    - "Migration and service tests prove the new state foundation is deterministic and does not regress the existing backtest path."
---

<objective>
Build the Phase 4 foundation by adding typed portfolio settings, durable portfolio-state persistence, and a deterministic service for sizing and exposure accounting.

Purpose: This plan isolates the state and sizing foundation so the risk-validation pipeline can land in a smaller follow-up plan without mixing migration risk, operator flow, and per-signal decision logic.
Output: Typed Phase 4 config, normalized portfolio-state models, and a reusable portfolio service for cash, exposure, and proposed sizing.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-risk-and-portfolio/04-RESEARCH.md
@.planning/phases/03-backtest-and-reporting/03-03-SUMMARY.md
@src/trading_platform/core/settings.py
@src/trading_platform/services/backtesting.py
@src/trading_platform/services/market_data_access.py
@src/trading_platform/db/models/strategy_run.py
@tests/test_backtest_runner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed Phase 4 settings and a deterministic portfolio service surface</name>
  <files>config/app.yaml, src/trading_platform/core/settings.py, src/trading_platform/services/portfolio.py, tests/test_portfolio_service.py</files>
  <action>Extend the typed settings surface with the account-level controls this phase owns, such as strategy allocation caps, total portfolio allocation caps, stale-data policy knobs, and any missing sizing inputs that should not live as hidden constants. Then add a dedicated portfolio service with typed state objects that can load or represent cash, open positions, exposure, and a proposed whole-share order size from the current portfolio state plus a candidate entry price. Reuse the fixed-fraction and equal-weight ideas already visible in the codebase where they still fit, but keep the service independent from backtest-specific models and independent from per-signal rejection logic that belongs in `04-02`.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_portfolio_service.py -q` passes for the typed settings and sizing surface.</verify>
  <done>The codebase has a typed Phase 4 portfolio configuration path and a deterministic service that can calculate portfolio state and proposed order sizes without broker integration.</done>
</task>

<task type="auto">
  <name>Task 2: Add normalized portfolio-state tables and the Phase 4 migration</name>
  <files>src/trading_platform/db/models/position.py, src/trading_platform/db/models/account_snapshot.py, src/trading_platform/db/models/__init__.py, alembic/versions/0006_phase4_portfolio_state.py, tests/test_db_migrations.py</files>
  <action>Add normalized models and migration support for the live portfolio state that later paper execution must read and update: current or historical positions plus account snapshots. Keep this schema intentionally focused on one operator, one account, and one portfolio in v1, but include the strategy and timestamp fields future paper-order flow will need. Do not repurpose `backtest_trades` or `backtest_equity_snapshots` for this job; Phase 3 artifacts remain historical research data, while Phase 4 introduces the durable state Phase 5 will mutate.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_db_migrations.py -q` passes and confirms the new tables land at Alembic head.</verify>
  <done>PostgreSQL can persist the live portfolio state Phase 5 will build on, and the migration chain remains clean.</done>
</task>

<task type="auto">
  <name>Task 3: Lock sizing and exposure accounting with deterministic tests</name>
  <files>tests/test_portfolio_service.py</files>
  <action>Expand the portfolio-service tests so they cover the Phase 4 truths that must remain stable: fixed-fraction or cap-aware sizing, whole-share rounding, exposure math, open-position counting, and account-level allocation enforcement. Add only the minimum regression slice needed from existing suites to prove the new portfolio foundation does not bleed into the Phase 3 backtest runner. Keep this task focused on deterministic state accounting, not on signal rejection flows that belong to `04-02`.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_portfolio_service.py tests/test_db_migrations.py tests/test_backtest_runner.py -q` passes.</verify>
  <done>The Phase 4 portfolio foundation is deterministic, migratable, and safe to build the real risk pipeline on top of.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_portfolio_service.py tests/test_db_migrations.py tests/test_backtest_runner.py -q` passes
- [ ] Alembic head creates the Phase 4 portfolio-state tables cleanly
- [ ] The portfolio service computes deterministic sizing and exposure from typed settings and persisted state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The codebase has a durable, typed, and testable portfolio-state foundation for live risk evaluation
- Existing Phase 3 backtest behavior remains intact after the new portfolio-state work
  </success_criteria>

<output>
After completion, create `.planning/phases/04-risk-and-portfolio/04-01-SUMMARY.md`
</output>
