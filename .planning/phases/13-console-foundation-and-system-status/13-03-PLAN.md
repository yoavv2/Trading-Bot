---
phase: 13-console-foundation-and-system-status
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - console/src/lib/api.ts
  - console/src/lib/api.test.ts
  - console/src/lib/useApiQuery.ts
  - console/src/components/ErrorState.tsx
  - console/src/components/FetchMeta.tsx
  - console/src/components/KillSwitchBanner.tsx
  - console/src/app/layout.tsx
  - console/vitest.config.ts
  - console/package.json
autonomous: true
requirements: [CONS-02, CONS-03, KILL-01]
must_haves:
  truths:
    - "Every fetch resolves to an explicit success-or-error result that always names the endpoint; there is no code path that renders fake success on failure"
    - "An HTTP error yields {ok:false, endpoint, status, message} and a network/proxy failure yields {ok:false, endpoint, status:null, message} — both carry the as-of timestamp"
    - "Every query records an as-of timestamp and exposes a manual refetch, rendered by shared FetchMeta (timestamp + Refresh button)"
    - "A tripped kill switch shows a red banner on every screen; a failed kill-switch fetch shows an amber 'state unknown' banner — silence only when verifiably armed"
  artifacts:
    - path: "console/src/lib/api.ts"
      provides: "fetchApi wrapper returning discriminated ApiResult<T>, prefixing /backend"
      contains: "ApiResult"
    - path: "console/src/lib/api.test.ts"
      provides: "Vitest unit tests for success / HTTP error / network error / non-JSON body classification"
      contains: "fetchApi"
    - path: "console/src/lib/useApiQuery.ts"
      provides: "Client hook: loading/result state, asOf timestamp, refetch()"
      contains: "useApiQuery"
    - path: "console/src/components/ErrorState.tsx"
      provides: "Shared error rendering naming endpoint + status + message"
      contains: "endpoint"
    - path: "console/src/components/FetchMeta.tsx"
      provides: "Shared as-of timestamp + manual Refresh control"
      contains: "Refresh"
    - path: "console/src/components/KillSwitchBanner.tsx"
      provides: "Global banner consuming /api/v1/system/kill-switch, refetching on route change"
      contains: "kill-switch"
  key_links:
    - from: "console/src/components/KillSwitchBanner.tsx"
      to: "/backend/api/v1/system/kill-switch"
      via: "useApiQuery"
      pattern: "api/v1/system/kill-switch"
    - from: "console/src/app/layout.tsx"
      to: "console/src/components/KillSwitchBanner.tsx"
      via: "rendered in root layout above children"
      pattern: "KillSwitchBanner"
    - from: "console/src/lib/useApiQuery.ts"
      to: "console/src/lib/api.ts"
      via: "fetchApi call"
      pattern: "fetchApi"
---

<objective>
Build the shared fetch/error/freshness pattern every v1.2 screen inherits (CONS-02, CONS-03) and mount the global kill-switch banner in the root layout (KILL-01).

Purpose: This is the honesty contract of the whole console. Phases 14-16 must not invent their own fetch handling — they consume `useApiQuery` + `ErrorState` + `FetchMeta` built here. The banner must exist in the shell before any inspection screen is built on top (roadmap phase-ordering rationale).
Output: Typed API client with vitest coverage, query hook, shared error/freshness components, and a layout-mounted kill-switch banner with an honest "unknown" degraded state.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-console-foundation-and-system-status/13-01-SUMMARY.md
@.planning/phases/13-console-foundation-and-system-status/13-02-SUMMARY.md
@console/src/app/layout.tsx
@console/next.config.ts

<interfaces>
<!-- Contract from plan 13-01 — GET /api/v1/system/kill-switch (via proxy: /backend/api/v1/system/kill-switch): -->
```json
{
  "name": "global_kill_switch",
  "state": "armed" | "tripped",
  "is_tripped": false,
  "last_changed_at": "2026-07-08T00:00:00+00:00",
  "last_change_actor": "local_operator" | null,
  "last_change_reason": "..." | null,
  "last_change_run_id": "uuid" | null
}
```
Errors: 503 + `{"detail": "..."}` if the control row is missing; 500/502 HTML from the Next dev proxy if the API process is down (response body may NOT be JSON — the client must tolerate that).

<!-- Contract this plan defines for downstream phases (14/15/16 build against these exact types): -->
```typescript
// console/src/lib/api.ts
export type ApiSuccess<T> = { ok: true; data: T; endpoint: string; asOf: Date };
export type ApiFailure = {
  ok: false;
  endpoint: string;          // always populated — CONS-02 requires naming the failing endpoint
  status: number | null;     // null = network/proxy unreachable
  message: string;
  body?: unknown;            // parsed JSON error body when available (e.g. /ready degraded payload)
  asOf: Date;
};
export type ApiResult<T> = ApiSuccess<T> | ApiFailure;
export async function fetchApi<T>(endpoint: string): Promise<ApiResult<T>>;

// console/src/lib/useApiQuery.ts
export type QueryState<T> = { loading: boolean; result: ApiResult<T> | null; refetch: () => void };
export function useApiQuery<T>(endpoint: string): QueryState<T>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API client (fetchApi) with vitest coverage, plus useApiQuery hook</name>
  <files>console/src/lib/api.ts, console/src/lib/api.test.ts, console/src/lib/useApiQuery.ts, console/vitest.config.ts, console/package.json</files>
  <behavior>
    console/src/lib/api.test.ts (vitest, node environment, `vi.stubGlobal("fetch", ...)`, relative import `./api` to avoid alias config):
    - Test 1 (success): fetch resolves 200 with JSON → `{ok: true}`, data parsed, `endpoint === "/api/v1/system"`, asOf is a Date.
    - Test 2 (HTTP error, JSON body): fetch resolves 503 with `{"detail":"db down"}` → `{ok: false, status: 503}`, message includes "db down", `body.detail === "db down"`, endpoint preserved.
    - Test 3 (HTTP error, non-JSON body): fetch resolves 502 with HTML text → `{ok: false, status: 502}`, message still meaningful (e.g. "HTTP 502"), no throw from JSON parsing.
    - Test 4 (network failure): fetch rejects with TypeError → `{ok: false, status: null}`, message names unreachability, endpoint preserved. NO code path throws out of fetchApi.
    Run tests RED against a stub, then implement api.ts GREEN.
  </behavior>
  <action>
    Install dev deps in console/: `npm install -D vitest`. Add `"test": "vitest run"` script.
    `console/vitest.config.ts`: `{ test: { environment: "node", include: ["src/**/*.test.ts"] } }`.

    Implement `fetchApi<T>(endpoint)` per the interface block: prefixes `/backend` onto the endpoint
    (`fetch(\`/backend${endpoint}\`, { cache: "no-store" })`), records `asOf = new Date()` when the
    response settles (success OR failure), never throws. Keep `endpoint` in the result as the
    UN-prefixed API path (e.g. `/api/v1/system`) — that is what the operator recognizes.

    Implement `useApiQuery<T>(endpoint)` ("use client" module): fetches on mount and whenever
    `endpoint` changes, exposes `{loading, result, refetch}`. `refetch` re-runs the fetch and
    replaces the result (this is the CONS-03 manual-refresh primitive). Guard against state updates
    after unmount. No polling, no caching layer, no SWR/TanStack dependency — this is a deliberate
    minimal instrument; do not add data-fetching libraries.
  </action>
  <verify>
    <automated>cd console && npx vitest run && npm run build</automated>
  </verify>
  <done>All four classification tests pass; fetchApi never throws; hook compiles under strict TS; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Shared ErrorState + FetchMeta components and global KillSwitchBanner in layout</name>
  <files>console/src/components/ErrorState.tsx, console/src/components/FetchMeta.tsx, console/src/components/KillSwitchBanner.tsx, console/src/app/layout.tsx</files>
  <action>
    `ErrorState.tsx` (props: `failure: ApiFailure`, optional `title`): renders a visually loud error
    block showing — verbatim, no softening — the endpoint path, the status (`HTTP 503` or
    `unreachable` when status is null), and the message. This is the ONLY approved way screens render
    fetch failures (CONS-02: never an empty or fake-success render).

    `FetchMeta.tsx` (props: `asOf: Date | null`, `loading: boolean`, `onRefresh: () => void`):
    renders "as of HH:MM:SS" (local time, seconds precision) plus a Refresh button that calls
    onRefresh and shows a disabled/loading state while a fetch is in flight (CONS-03). Screens place
    one per data panel.

    `KillSwitchBanner.tsx` ("use client"): uses `useApiQuery` on `/api/v1/system/kill-switch` and
    `usePathname()` from next/navigation with an effect that refetches on pathname change (so the
    banner is fresh on every screen navigation, not just first load). Render logic — three honest
    states, in priority order:
    1. result ok && data.is_tripped → full-width red banner: "KILL SWITCH TRIPPED — order submission halted"
       plus last_changed_at, last_change_actor, last_change_reason, and a small FetchMeta (asOf + refresh).
    2. result not ok → full-width amber banner: "Kill-switch state UNKNOWN — GET /api/v1/system/kill-switch failed
       ({status or 'unreachable'})" with refresh control. A safety indicator must never fail silent.
    3. result ok && armed → render nothing (status screen still shows armed state explicitly per STAT-03).

    Mount `<KillSwitchBanner />` in `console/src/app/layout.tsx` at the placeholder from plan 13-02
    (below nav, above {children}) so every current and future screen inherits it (KILL-01).
  </action>
  <verify>
    <automated>cd console && npm run build && grep -q "KillSwitchBanner" src/app/layout.tsx</automated>
  </verify>
  <done>Build passes; banner mounted in root layout; three-state render logic implemented (tripped/red, unknown/amber, armed/hidden); ErrorState and FetchMeta exported for reuse by plans 13-04 and phases 14-16.</done>
</task>

</tasks>

<verification>
- `cd console && npx vitest run` — API client classification tests green.
- `cd console && npm run build` — strict TS + lint pass.
- `grep -rn "fetch(" console/src/app console/src/components | grep -v fetchApi` returns nothing outside lib/api.ts — all data access goes through the shared client.
- Live behavior (banner visible when tripped, amber when API down) is verified in plan 13-04's checkpoint.
</verification>

<success_criteria>
- CONS-02: shared client always names endpoint + status on failure; components exist to render it; no fake-success path.
- CONS-03: asOf timestamp + manual refetch built into the shared hook and FetchMeta control.
- KILL-01: banner in root layout, refreshes per navigation, honest tri-state including "unknown".
</success_criteria>

<output>
After completion, create `.planning/phases/13-console-foundation-and-system-status/13-03-SUMMARY.md`
</output>
