---
phase: 06-analytics-and-apis
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/trading_platform/api/app.py
  - src/trading_platform/api/dependencies.py
  - src/trading_platform/api/routes/analytics.py
  - src/trading_platform/api/routes/runs.py
  - src/trading_platform/api/routes/operations.py
  - src/trading_platform/api/routes/strategies.py
  - src/trading_platform/api/routes/system.py
  - tests/test_api_reads.py
  - tests/test_app_boot.py
autonomous: true
requirements:
  - REQ-09
  - REQ-10
user_setup: []
must_haves:
  truths:
    - "Versioned FastAPI read endpoints expose strategy analytics, run history, and recent operational state through stable JSON contracts."
    - "Route handlers reuse the shared Phase 6 analytics and inspection services instead of embedding raw database queries in the API layer."
    - "The operator and future dashboard can inspect recent runs, orders, fills, positions, snapshots, and blocking events through read-only APIs without mutating platform state."
  artifacts:
    - "New versioned route modules exist for analytics, runs, and operational inspection."
    - "FastAPI app bootstrap registers the new routers and shared dependencies cleanly."
    - "Seeded API integration tests cover happy paths, filters, empty states, and missing-resource behavior."
  key_links:
    - "Analytics endpoints return the same values as the Phase 6 CLI analytics surface by calling the same shared services."
    - "Operational endpoints join symbol and run context through the shared read layer rather than through route-local SQL."
    - "System and strategy route output is rich enough for later dashboard consumption without direct database access."
---

<objective>
Expose a stable, versioned FastAPI read surface for strategy analytics and operational inspection on top of the Phase 6 service-layer foundation.

Purpose: This plan turns the platform’s new analytics and inspection capabilities into clean operator-facing APIs that a future dashboard can consume without direct database access or duplicated query logic.
Output: Versioned read-only FastAPI endpoints for analytics, runs, and operational entities plus seeded API integration coverage.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-analytics-and-apis/06-RESEARCH.md
@.planning/phases/06-analytics-and-apis/06-01-SUMMARY.md
@src/trading_platform/api/app.py
@src/trading_platform/api/routes/health.py
@src/trading_platform/api/routes/strategies.py
@src/trading_platform/api/routes/system.py
@src/trading_platform/services/analytics.py
@src/trading_platform/services/operator_reads.py
@tests/test_app_boot.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add versioned analytics and run inspection routes</name>
  <files>src/trading_platform/api/dependencies.py, src/trading_platform/api/routes/analytics.py, src/trading_platform/api/routes/runs.py, src/trading_platform/api/routes/strategies.py, tests/test_api_reads.py</files>
  <action>Create versioned read-only endpoints for strategy detail, strategy analytics, run listing, and run detail, using the Phase `06-01` services as the only data source. Support simple query filters such as strategy, run type, status, session window, and limit where they materially help inspection. Return explicit 404 responses for missing strategies or runs, keep response contracts JSON-first and stable, and avoid route-local SQL or ORM serialization shortcuts that will be hard to keep consistent with the CLI analytics path.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_api_reads.py -q` passes for analytics, run-detail, filtering, and missing-resource behavior.</verify>
  <done>The API exposes stable strategy-analytics and run-inspection reads that are backed by the shared Phase 6 service layer instead of bespoke route logic.</done>
</task>

<task type="auto">
  <name>Task 2: Expose operational inspection endpoints for paper-trading state and audit trails</name>
  <files>src/trading_platform/api/routes/operations.py, src/trading_platform/api/routes/system.py, src/trading_platform/api/dependencies.py, tests/test_api_reads.py</files>
  <action>Add versioned endpoints for recent paper orders, fills, positions, account snapshots, risk events, and execution events, including the blocking or non-blocking context the operator needs to interpret reconciliation drift and kill-switch-related failures later. Include consistent query parameters for strategy and limit, and return human-readable symbol and session context rather than raw foreign keys alone. Keep the surface strictly read-only and dashboard-ready; do not mix in new mutating controls or run-triggering actions in this plan.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_api_reads.py -q` passes for orders, fills, positions, snapshots, risk events, execution events, and empty-state responses.</verify>
  <done>The operator can inspect current and recent operational paper-trading state through stable FastAPI reads instead of only through scripts and logs.</done>
</task>

<task type="auto">
  <name>Task 3: Register the new routers and lock API behavior with seeded integration tests</name>
  <files>src/trading_platform/api/app.py, src/trading_platform/api/routes/system.py, tests/test_api_reads.py, tests/test_app_boot.py</files>
  <action>Wire the new versioned routers into the FastAPI app, update the system route or boot metadata as needed so the operator can see that the analytics and inspection surface is available, and add integration tests that prove the app boots with the new routes registered. Cover default behavior, not-found behavior, filter semantics, and empty-state stability in seeded tests. Keep startup and route registration simple; this plan should still leave the API read-only and local-first.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_api_reads.py tests/test_app_boot.py -q` passes and confirms the app boots with the Phase 6 routers installed.</verify>
  <done>The FastAPI application now exposes the new operator read surface reliably, and seeded tests lock the contract so future dashboard work can depend on it safely.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_api_reads.py tests/test_app_boot.py -q` passes
- [ ] Strategy analytics returned by the API match the shared Phase 6 analytics service rather than a separate route implementation
- [ ] Operational endpoints are read-only and expose recent orders, fills, positions, snapshots, risk events, and execution events with stable ordering
- [ ] Missing strategy or run lookups return explicit not-found responses instead of uncaught exceptions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The platform exposes a versioned operator-facing FastAPI read surface for analytics and inspection
- Future dashboard work can consume these reads without direct database access or route-by-route query rewrites
  </success_criteria>

<output>
After completion, create `.planning/phases/06-analytics-and-apis/06-02-SUMMARY.md`
</output>
