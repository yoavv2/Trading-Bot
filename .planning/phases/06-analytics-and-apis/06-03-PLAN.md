---
phase: 06-analytics-and-apis
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - src/trading_platform/services/operator_controls.py
  - src/trading_platform/services/operator_status.py
  - src/trading_platform/services/paper_execution.py
  - src/trading_platform/services/reconciliation.py
  - src/trading_platform/db/models/strategy.py
  - src/trading_platform/db/models/strategy_run.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0012_phase6_operator_controls.py
  - src/trading_platform/core/logging.py
  - src/trading_platform/worker/__main__.py
  - scripts/operator_control.py
  - scripts/operator_status.py
  - tests/test_operator_controls.py
  - tests/test_paper_execution.py
  - tests/test_db_migrations.py
autonomous: true
requirements:
  - REQ-10
  - REQ-06
user_setup: []
must_haves:
  truths:
    - "The operator can enable or disable a strategy intentionally, and new paper execution honors that state before broker submission begins."
    - "Kill-switch changes and blocked execution attempts persist as durable audit records instead of living only in logs or transient CLI output."
    - "Daily-use observability surfaces recent failures, blocking conditions, and current control state without requiring raw log inspection."
  artifacts:
    - "A durable operator-control service and audit path exist."
    - "Paper execution and worker surfaces enforce kill-switch state and return explicit blocked summaries."
    - "CLI or worker status commands plus richer structured log context expose the latest strategy state and recent operational health."
  key_links:
    - "Control actions update `Strategy.status` and persist durable audit events under `strategy_runs` plus `execution_events` or an equivalent normalized record."
    - "Paper session and paper-order submission consult control state before broker work and persist explicit blocked reasons."
    - "Operator status output reuses the Phase 6 analytics or inspection services plus reconciliation findings instead of re-implementing query logic."
---

<objective>
Finish Phase 6 by turning strategy disable or enable into a real operator control, enforcing kill-switch behavior in paper execution, and surfacing current status plus failures through durable observability outputs.

Purpose: This plan makes the platform safe and reviewable for daily use by ensuring the operator can intentionally stop new execution, see why the system is blocked, and inspect the latest control or failure state without reconstructing it from raw logs.
Output: Durable operator controls, kill-switch enforcement in paper execution, an operator status surface, and richer structured observability output.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-analytics-and-apis/06-RESEARCH.md
@.planning/phases/06-analytics-and-apis/06-02-SUMMARY.md
@.planning/phases/05-paper-execution/05-03-SUMMARY.md
@src/trading_platform/core/logging.py
@src/trading_platform/db/models/strategy.py
@src/trading_platform/db/models/strategy_run.py
@src/trading_platform/services/paper_execution.py
@src/trading_platform/services/reconciliation.py
@src/trading_platform/services/operator_reads.py
@src/trading_platform/worker/__main__.py
@tests/test_paper_execution.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement durable strategy controls and kill-switch audit records</name>
  <files>src/trading_platform/services/operator_controls.py, src/trading_platform/db/models/strategy.py, src/trading_platform/db/models/strategy_run.py, src/trading_platform/db/models/__init__.py, alembic/versions/0012_phase6_operator_controls.py, tests/test_operator_controls.py, tests/test_db_migrations.py</files>
  <action>Turn strategy disable or enable into a real persisted control flow instead of an unused schema field. Reuse `Strategy.status` for the current state, but add the durable audit trail Phase 6 needs so the operator can see who changed the status, when it changed, and why. The cleanest path is to persist control actions under `strategy_runs` with a dedicated control-oriented run type and attach normalized event data rather than relying on free-form logs or environment variables. Keep the control surface single-user and local-first.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_operator_controls.py tests/test_db_migrations.py -q` passes for status transitions, audit persistence, and Alembic head coverage.</verify>
  <done>Strategy disable or enable is now a real operator control with durable audit records instead of a dormant enum field.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce the kill switch in paper execution and persist blocked outcomes</name>
  <files>src/trading_platform/services/paper_execution.py, src/trading_platform/services/reconciliation.py, src/trading_platform/services/operator_controls.py, tests/test_operator_controls.py, tests/test_paper_execution.py</files>
  <action>Update paper-order submission and scheduled paper-session execution so new broker work fails closed when the strategy is disabled. Persist blocked outcomes with explicit reasons that are queryable later through the existing execution-event or audit surface. Keep read-only inspection, analytics, and reporting available while the strategy is disabled; the kill switch is for halting new execution, not for hiding the system state. Preserve the existing reconciliation fail-closed behavior and make the blocked reason operator-visible in returned reports.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_operator_controls.py tests/test_paper_execution.py -q` passes for disabled-strategy blocking, returned summaries, and non-execution read behavior.</verify>
  <done>The platform refuses new paper execution while disabled and records the blocked reason durably for next-day inspection.</done>
</task>

<task type="auto">
  <name>Task 3: Add operator status commands and richer structured observability output</name>
  <files>src/trading_platform/services/operator_status.py, src/trading_platform/core/logging.py, src/trading_platform/worker/__main__.py, scripts/operator_control.py, scripts/operator_status.py, tests/test_operator_controls.py, tests/test_paper_execution.py</files>
  <action>Introduce a shared operator-status surface that reports current strategy status, latest account snapshot, latest paper-session outcome, and recent blocking or failure events by reusing the Phase 6 read services. Expose that through standalone and worker CLI commands, and standardize log context helpers so the major execution and control flows consistently emit fields such as `strategy_id`, `run_id`, `session_date`, `strategy_status`, and `blocked_reason`. Do not treat raw log tailing as the primary operator interface; the status command should be the durable daily-use summary, with logs providing structured supporting detail.</action>
  <verify>`PYTHONPATH=src .venv/bin/python scripts/operator_control.py --help` succeeds, `PYTHONPATH=src .venv/bin/python scripts/operator_status.py --help` succeeds, `PYTHONPATH=src .venv/bin/python -m trading_platform.worker operator-status --help` succeeds, and `PYTHONPATH=src .venv/bin/pytest tests/test_operator_controls.py tests/test_paper_execution.py tests/test_api_reads.py -q` passes.</verify>
  <done>The operator can inspect current controls and recent failures from a stable CLI surface, and execution or control logs now carry the context needed for durable observability.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_operator_controls.py tests/test_paper_execution.py tests/test_api_reads.py tests/test_db_migrations.py -q` passes
- [ ] `PYTHONPATH=src .venv/bin/python scripts/operator_control.py --help` passes
- [ ] `PYTHONPATH=src .venv/bin/python scripts/operator_status.py --help` passes
- [ ] `PYTHONPATH=src .venv/bin/python -m trading_platform.worker operator-status --help` passes
- [ ] New paper execution fails closed when the strategy is disabled and the blocked reason persists durably
- [ ] Operator status output surfaces current control state plus recent blocking conditions without requiring raw log inspection
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The platform has a real kill-switch and control-audit path for daily paper-trading operations
- Observability now includes durable status and blocked-state visibility instead of relying on raw logs alone
  </success_criteria>

<output>
After completion, create `.planning/phases/06-analytics-and-apis/06-03-SUMMARY.md`
</output>
