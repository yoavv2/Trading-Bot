---
phase: 06-analytics-and-apis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_platform/services/analytics.py
  - src/trading_platform/services/operator_reads.py
  - src/trading_platform/services/backtest_reporting.py
  - src/trading_platform/services/bootstrap.py
  - src/trading_platform/db/models/backtest_metric.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0011_phase6_analytics_metrics.py
  - src/trading_platform/worker/__main__.py
  - scripts/report_strategy_analytics.py
  - tests/test_analytics_service.py
  - tests/test_backtest_reporting.py
  - tests/test_db_migrations.py
autonomous: true
requirements:
  - REQ-09
  - REQ-06
user_setup: []
must_haves:
  truths:
    - "Backtest analytics expose a richer strategy-performance profile while remaining derived strictly from persisted run artifacts."
    - "Paper-trading analytics stay honest to the state persisted today and summarize execution, account, and blocking conditions without inventing unsupported closed-trade metrics."
    - "Historical inspection reads exist behind a shared service layer that later FastAPI routes can consume without embedding SQL in route handlers."
  artifacts:
    - "A real analytics service and operator-read query layer exist for backtest and paper-trading inspection."
    - "The `backtest_metrics` persistence shape covers the additional Phase 6 performance fields."
    - "A CLI and worker analytics-report surface exists for strategy-level summary and recent operational inspection."
  key_links:
    - "Expanded backtest analytics reuse Phase 3 reporting artifacts instead of re-running the simulator."
    - "Paper summaries read from `risk_events`, `paper_orders`, `paper_fills`, `positions`, `account_snapshots`, and `execution_events` under `strategy_runs`."
    - "The Phase 6 API plan can call the shared read services directly rather than issuing fresh ORM queries inside each route."
---

<objective>
Build the Phase 6 read foundation by replacing the placeholder analytics contract, expanding trustworthy backtest metrics, and adding shared historical inspection reads for the paper-trading loop.

Purpose: This plan gives the platform one reusable source of truth for operator analytics and inspection so later API routes and status surfaces do not duplicate fragile SQL or reporting logic.
Output: A real analytics service, shared inspection queries, richer backtest metrics, and a CLI-first strategy analytics report surface.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-analytics-and-apis/06-RESEARCH.md
@.planning/phases/03-backtest-and-reporting/03-03-SUMMARY.md
@.planning/phases/04-risk-and-portfolio/04-02-SUMMARY.md
@.planning/phases/05-paper-execution/05-03-SUMMARY.md
@src/trading_platform/services/analytics.py
@src/trading_platform/services/backtest_reporting.py
@src/trading_platform/services/paper_execution.py
@src/trading_platform/services/reconciliation.py
@src/trading_platform/services/bootstrap.py
@src/trading_platform/db/models/backtest_metric.py
@src/trading_platform/db/models/strategy_run.py
@tests/test_backtest_reporting.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the placeholder analytics seam with a real strategy analytics service</name>
  <files>src/trading_platform/services/analytics.py, src/trading_platform/services/backtest_reporting.py, src/trading_platform/services/bootstrap.py, src/trading_platform/db/models/backtest_metric.py, src/trading_platform/db/models/__init__.py, alembic/versions/0011_phase6_analytics_metrics.py, tests/test_analytics_service.py, tests/test_backtest_reporting.py, tests/test_db_migrations.py</files>
  <action>Replace `PlaceholderAnalyticsService` with a real implementation that can summarize both backtest and paper-trading state from persisted artifacts. Extend the Phase 3 backtest metric surface to include the missing project-level statistics that are derivable from persisted trades and equity snapshots, such as CAGR, Sharpe, Sortino, expectancy, turnover, and best or worst trade values. Keep the calculations deterministic and no-trade-safe. For paper trading, expose only metrics that are trustworthy from the current persistence model, such as latest account equity, exposure, submitted and filled order counts, blocked-session counts, and recent reconciliation state. Do not invent closed-paper-trade PnL metrics that the current schema cannot support honestly.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_analytics_service.py tests/test_backtest_reporting.py tests/test_db_migrations.py -q` passes for expanded metrics, paper-summary behavior, and Alembic head.</verify>
  <done>The codebase has a real analytics service with richer backtest metrics and an honest paper-operational summary derived only from persisted state.</done>
</task>

<task type="auto">
  <name>Task 2: Add shared historical inspection reads for runs and operational entities</name>
  <files>src/trading_platform/services/operator_reads.py, src/trading_platform/services/analytics.py, tests/test_analytics_service.py</files>
  <action>Introduce a shared read layer that returns serializable inspection payloads for strategy runs, run detail, orders, fills, positions, account snapshots, risk events, and execution events. Include explicit filters for strategy ID, run type, status, session window, and limit. Join symbol tickers and run metadata so the output is already operator-readable and future-API-ready. Keep ordering deterministic, newest-first where appropriate, and hide SQLAlchemy internals behind typed return structures so later route handlers do not open-code joins or sorting rules.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_analytics_service.py -q` passes for inspection payload shape, ordering, filters, and empty-state behavior.</verify>
  <done>One shared query layer now provides stable historical inspection reads for every Phase 6 entity the operator needs to review.</done>
</task>

<task type="auto">
  <name>Task 3: Expose a CLI-first strategy analytics report surface</name>
  <files>src/trading_platform/worker/__main__.py, scripts/report_strategy_analytics.py, src/trading_platform/services/analytics.py, src/trading_platform/services/operator_reads.py, tests/test_analytics_service.py</files>
  <action>Expose the new analytics and inspection foundation through a standalone script and a worker subcommand that print a strategy analytics summary plus recent operational inspection data in JSON or markdown. Default to the latest successful backtest and latest synced paper state for the default strategy, while still allowing explicit strategy and run selectors. Keep the output concise, machine-readable, and aligned with the service layer that the API will reuse in `06-02`; do not create a second reporting path with separate metric or query logic.</action>
  <verify>`PYTHONPATH=src .venv/bin/python scripts/report_strategy_analytics.py --help` succeeds, `PYTHONPATH=src .venv/bin/python -m trading_platform.worker report-strategy-analytics --help` succeeds, and `PYTHONPATH=src .venv/bin/pytest tests/test_analytics_service.py tests/test_backtest_reporting.py -q` passes.</verify>
  <done>The operator can review strategy analytics and recent activity from the terminal without raw SQL or log spelunking, and the API plan has a reusable report foundation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_analytics_service.py tests/test_backtest_reporting.py tests/test_db_migrations.py -q` passes
- [ ] `PYTHONPATH=src .venv/bin/python scripts/report_strategy_analytics.py --help` passes
- [ ] `PYTHONPATH=src .venv/bin/python -m trading_platform.worker report-strategy-analytics --help` passes
- [ ] Expanded backtest metrics remain derived strictly from persisted artifacts and stay zero-safe for no-trade runs
- [ ] Paper analytics only expose metrics that are honest to the currently persisted order, fill, snapshot, and event model
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The platform has a real analytics and inspection foundation instead of a deferred placeholder
- Phase 6 API and operator-status work can reuse one shared analytics or inspection layer rather than duplicating query logic
  </success_criteria>

<output>
After completion, create `.planning/phases/06-analytics-and-apis/06-01-SUMMARY.md`
</output>
