---
phase: 07-correctness-kernel
plan: 03
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - src/trading_platform/db/models/system_control.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0015_phase7_global_kill_switch.py
  - src/trading_platform/services/operator_controls.py
  - src/trading_platform/services/paper_execution.py
  - src/trading_platform/services/operator_reads.py
  - src/trading_platform/services/operator_status.py
  - src/trading_platform/worker/__main__.py
  - tests/test_operator_controls.py
  - tests/test_paper_execution.py
  - tests/test_db_migrations.py
autonomous: true
requirements:
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04
  - SAFE-05
user_setup: []
must_haves:
  truths:
    - "The kill switch is a durable global submission halt, not an in-process flag or per-strategy overload."
    - "Every batch-entry and pre-submit path checks the persisted switch, while reconciliation, logging, and read-only safety flows continue operating."
    - "Trip and reset actions are explicit operator events with restart-safe state and durable audit visibility."
  artifacts:
    - "A persisted global kill-switch state model/table exists."
    - "Worker CLI commands exist to trip and reset the switch with reason/actor metadata."
    - "Operator status/read surfaces show current switch state and recent blocked-submission outcomes."
  key_links:
    - "Existing per-strategy enable/disable controls remain separate from the emergency global submission halt."
    - "Paper-session orchestration records blocked runs/events instead of silently aborting or skipping visibility."
    - "Restarting the worker re-reads the persisted kill switch before any new broker submission is possible."
---

<objective>
Add the restart-safe global kill switch for Phase 7 by persisting platform-wide submission-halt state, enforcing it at batch-entry and pre-submit boundaries, and surfacing its state through the existing operator-control/read paths.

Purpose: This plan gives the operator one durable emergency brake that blocks new broker side effects without blinding the platform. Reconciliation, logging, and investigation must continue even while new submissions are halted.
Output: Persisted global kill-switch state, CLI trip/reset commands, enforced submission gating, and operator-visible blocked-run reporting.
</objective>

<execution_context>
@/Users/yoavhevroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-correctness-kernel/07-CONTEXT.md
@.planning/phases/07-correctness-kernel/07-01-SUMMARY.md
@.planning/phases/07-correctness-kernel/07-02-SUMMARY.md
@src/trading_platform/services/operator_controls.py
@src/trading_platform/services/paper_execution.py
@src/trading_platform/services/operator_reads.py
@src/trading_platform/services/operator_status.py
@src/trading_platform/worker/__main__.py
@tests/test_operator_controls.py
@tests/test_paper_execution.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist global kill-switch state and audit actions</name>
  <files>src/trading_platform/db/models/system_control.py, src/trading_platform/db/models/__init__.py, alembic/versions/0015_phase7_global_kill_switch.py, src/trading_platform/services/operator_controls.py, tests/test_db_migrations.py, tests/test_operator_controls.py</files>
  <action>Add a durable system-level control model for the global kill switch, including current state, last-changed metadata, and any reason/actor fields needed for auditability. Extend the operator-control service with explicit trip/reset flows that persist state and audit rows without overloading per-strategy `Strategy.status`. Keep the existing strategy enable/disable controls intact and clearly separate from the global switch semantics.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_operator_controls.py tests/test_db_migrations.py -q` passes for migration shape, trip/reset persistence, restart-safe reads, and audit-event creation.</verify>
  <done>The platform has a durable global kill-switch record and explicit audited operator actions to trip and reset it.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce the switch at session entry and before every broker submission</name>
  <files>src/trading_platform/services/paper_execution.py, src/trading_platform/services/operator_controls.py, tests/test_paper_execution.py</files>
  <action>Make paper-session orchestration and direct submission paths consult the persisted kill switch before any broker side effect. While tripped, still run preflight/reconciliation/read-only safety work where applicable, then persist blocked-run summaries and events explaining that submission was halted by the global switch. Keep behavior consistent between batch entry and per-order pre-submit checks so mid-run trips take effect on the next submission boundary without a restart.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_paper_execution.py tests/test_operator_controls.py -q` passes for global blocking behavior, read-only/reconciliation continuity, mid-run enforcement, and manual-reset-only semantics.</verify>
  <done>New submissions are impossible while the switch is tripped, but the operator can still inspect and reconcile platform state safely.</done>
</task>

<task type="auto">
  <name>Task 3: Surface kill-switch state in CLI and operator inspection reads</name>
  <files>src/trading_platform/services/operator_reads.py, src/trading_platform/services/operator_status.py, src/trading_platform/worker/__main__.py, tests/test_operator_controls.py, tests/test_paper_execution.py</files>
  <action>Add worker commands or arguments to trip/reset/show the global kill switch using the existing CLI-first control pattern. Extend operator-status and related read payloads so the current kill-switch state, last change metadata, and recent blocked submissions are visible without raw DB inspection. Keep the default operator view condensed while including the switch state in the top-level summary and durable blocked-run context in drilldown feeds.</action>
  <verify>`PYTHONPATH=src .venv/bin/python -m trading_platform.worker operator-control --help` shows the updated control surface, and `PYTHONPATH=src .venv/bin/pytest tests/test_operator_controls.py tests/test_paper_execution.py -q` passes for CLI/report payload coverage.</verify>
  <done>The operator can trip, reset, and inspect the global kill switch from the existing CLI/status surface, and blocked submission outcomes are clearly visible.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_operator_controls.py tests/test_paper_execution.py tests/test_db_migrations.py -q` passes
- [ ] Global kill-switch state persists across service reload / cache clear / fresh session reads
- [ ] Batch entry and per-submit paths both honor the switch before broker side effects
- [ ] Reconciliation and operator logging/status flows still run while submissions are halted
- [ ] CLI/operator status clearly distinguish global kill switch from per-strategy disable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 7 has a durable global submission halt with clear auditability
- Operator control and status surfaces explain both the switch state and any blocked paper-session outcomes
  </success_criteria>

<output>
After completion, create `.planning/phases/07-correctness-kernel/07-03-SUMMARY.md`
</output>
