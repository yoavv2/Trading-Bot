---
phase: 07-correctness-kernel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_platform/db/models/paper_order.py
  - src/trading_platform/db/models/order_event.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0013_phase7_order_state_kernel.py
  - src/trading_platform/services/order_state_machine.py
  - src/trading_platform/services/paper_execution.py
  - src/trading_platform/services/reconciliation.py
  - tests/test_order_state_machine.py
  - tests/test_paper_execution.py
  - tests/test_execution_reconciliation.py
  - tests/test_db_migrations.py
autonomous: true
requirements:
  - ORDER-01
  - ORDER-02
  - ORDER-03
  - ORDER-04
  - ORDER-05
  - ORDER-06
user_setup: []
must_haves:
  truths:
    - "Paper-order lifecycle is a closed enum state machine, not freeform string mutation."
    - "All accepted and rejected transitions flow through one `apply_order_transition(order_id, event)` boundary that performs no broker I/O."
    - "Every lifecycle change leaves an append-only durable event trail; the order row is only the latest projection."
  artifacts:
    - "A persisted `order_events` model/table exists with typed event and state fields."
    - "A dedicated order-state-machine service owns legal transition mapping and typed illegal-transition errors."
    - "Existing submission and reconciliation flows call the transition boundary instead of mutating `PaperOrder.status` directly."
  key_links:
    - "Phase 5/6 operator auditability stays intact because transition events become the canonical drilldown history."
    - "Reconciliation and paper execution share one lifecycle vocabulary instead of each mapping raw statuses independently."
    - "Later idempotency and kill-switch work can build on the closed lifecycle kernel without reopening persistence design."
---

<objective>
Build the closed order-lifecycle kernel for Phase 7 by replacing direct `PaperOrder.status` mutation with enum-backed state, append-only transition events, and one typed transition entry point.

Purpose: This plan establishes the non-negotiable correctness boundary for every later execution path. If order state can still change through ad hoc writes, the rest of Phase 7 cannot be made trustworthy.
Output: Enum-backed order lifecycle persistence, `OrderEvent` audit history, a single transition service, and tests that forbid bypassing it.
</objective>

<execution_context>
@/Users/yoavhevroni/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-correctness-kernel/07-CONTEXT.md
@src/trading_platform/db/models/paper_order.py
@src/trading_platform/db/models/execution_event.py
@src/trading_platform/services/paper_execution.py
@src/trading_platform/services/reconciliation.py
@src/trading_platform/services/execution.py
@tests/test_paper_execution.py
@tests/test_execution_reconciliation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce enum-backed order lifecycle persistence and append-only order events</name>
  <files>src/trading_platform/db/models/paper_order.py, src/trading_platform/db/models/order_event.py, src/trading_platform/db/models/__init__.py, alembic/versions/0013_phase7_order_state_kernel.py, tests/test_db_migrations.py</files>
  <action>Replace the freeform `PaperOrder.status` string surface with closed enum-backed lifecycle fields and add a dedicated append-only `order_events` persistence model that records from-state, event, to-state, accepted/rejected outcome, timestamp, and structured details. Keep the current `paper_orders` row as the latest-state projection only. The migration must preserve existing Phase 5/6 paper-order data by mapping current states into the new closed lifecycle vocabulary rather than dropping history.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_db_migrations.py -q` passes with assertions for new lifecycle columns, `order_events` table shape, enum values, and migration compatibility.</verify>
  <done>The schema exposes a closed order lifecycle and a durable per-transition event table instead of ad hoc string status writes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the single transition boundary and illegal-transition enforcement</name>
  <files>src/trading_platform/services/order_state_machine.py, src/trading_platform/db/models/order_event.py, tests/test_order_state_machine.py</files>
  <action>Create `apply_order_transition(order_id, event)` plus the closed legal transition map, typed `IllegalOrderTransition`, and helpers needed to persist accepted and rejected transitions. Keep this service pure with respect to broker I/O: it may load and persist DB state, but it must not call Alpaca or other side-effecting services. Rejected transitions must still append a durable `OrderEvent` row with enough context for operator inspection.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_order_state_machine.py -q` passes for legal transitions, illegal transitions, append-only event persistence, and no-I/O boundary behavior.</verify>
  <done>One service now owns all legal order-state transitions, and illegal state changes fail loudly with durable audit history.</done>
</task>

<task type="auto">
  <name>Task 3: Route existing paper-execution and reconciliation flows through the kernel</name>
  <files>src/trading_platform/services/paper_execution.py, src/trading_platform/services/reconciliation.py, tests/test_paper_execution.py, tests/test_execution_reconciliation.py</files>
  <action>Replace direct status mutation in paper submission, broker-sync, and reconciliation-recovery paths with calls to the new transition service. Preserve current business behavior where valid, but make the new lifecycle state the canonical source shown in summaries and persisted records. Add a module-boundary regression test that fails if these services start mutating lifecycle state directly again.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_order_state_machine.py tests/test_paper_execution.py tests/test_execution_reconciliation.py tests/test_db_migrations.py -q` passes for kernel integration, regression coverage, and migrated lifecycle behavior.</verify>
  <done>All current order lifecycle updates pass through the closed transition kernel, and bypassing it is test-visible breakage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_order_state_machine.py tests/test_paper_execution.py tests/test_execution_reconciliation.py tests/test_db_migrations.py -q` passes
- [ ] No remaining direct `PaperOrder.status` lifecycle mutation exists outside the transition service or migration/test fixtures
- [ ] Illegal transitions produce typed failures and durable rejected-event rows
- [ ] Accepted transitions append events; the order row is not the sole lifecycle record
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 7 has a closed lifecycle kernel with one transition boundary
- Future submission, retry, and kill-switch work can reuse the same lifecycle vocabulary and event trail
  </success_criteria>

<output>
After completion, create `.planning/phases/07-correctness-kernel/07-01-SUMMARY.md`
</output>
