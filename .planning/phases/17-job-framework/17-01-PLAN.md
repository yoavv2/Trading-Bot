---
phase: 17-job-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_platform/db/models/job.py
  - src/trading_platform/db/models/job_dependency.py
  - src/trading_platform/db/models/job_event.py
  - src/trading_platform/db/models/job_log.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0018_phase17_job_framework.py
  - tests/test_db_migrations.py
autonomous: true
requirements: [JOB-01, JOB-05, JOB-06, JOB-07]

must_haves:
  truths:
    - "A Job row's status column can only hold queued/running/succeeded/failed/cancelled — PostgreSQL rejects any other literal"
    - "A Job can record a stable failure reason (worker_lost, lease_expired, cancellation_timeout, handler_error) and an outcome_uncertain flag (D-01, D-03, D-09)"
    - "A Job can record a dependency-driven cancellation cause plus the blocking Job ID, that Job's terminal status, and the root ancestor Job ID (D-04, D-05)"
    - "A Job records its cancellation history: requester identity, optional reason, requested_at, acknowledged_at, and terminal cause (D-10)"
    - "A Job can declare zero or more dependency edges on other Jobs; a self-dependency edge is rejected by the database (D-06)"
    - "A Job can record a progress snapshot: optional 0-100 percent, step message, current/total counters (D-11)"
    - "Job logs are append-only rows with a deterministic per-Job ordering key (D-13)"
    - "Progress and log rows remain queryable for the lifetime of the Job record; no pruning, compaction, or TTL is defined (D-14)"
    - "alembic upgrade head creates jobs, job_dependencies, job_events, job_logs with the declared enum types"
  artifacts:
    - path: "src/trading_platform/db/models/job.py"
      provides: "Job ORM model plus JobStatus, JobFailureReason, JobCancellationCause enums"
      contains: "class JobStatus(StrEnum)"
    - path: "src/trading_platform/db/models/job_dependency.py"
      provides: "JobDependency self-referential edge model"
      contains: "ck_job_dependencies_job_not_self_dependent"
    - path: "src/trading_platform/db/models/job_event.py"
      provides: "JobEvent append-only lifecycle/cancellation audit model"
      contains: "class JobEventType(StrEnum)"
    - path: "src/trading_platform/db/models/job_log.py"
      provides: "JobLog append-only structured log model with sequence ordering"
      contains: "sequence"
    - path: "alembic/versions/0018_phase17_job_framework.py"
      provides: "Phase 17 schema migration"
      contains: "down_revision = \"0017_phase11_query_perf_indices\""
  key_links:
    - from: "src/trading_platform/db/models/__init__.py"
      to: "src/trading_platform/db/models/job.py"
      via: "barrel export"
      pattern: "from trading_platform.db.models.job import"
    - from: "alembic/versions/0018_phase17_job_framework.py"
      to: "src/trading_platform/db/base.py NAMING_CONVENTION"
      via: "op.f() constraint names"
      pattern: "op\\.f\\("
---

<objective>
Create the persistence foundation for the Job framework: four ORM models, the closed
status enum and the stable failure/cancellation code vocabulary, the Alembic migration
that materializes them in PostgreSQL, and a migration enforcement test.

Purpose: Every other Phase 17 plan reads or writes these tables and imports these enums.
This plan is the single definition site for the entire code vocabulary — downstream plans
reference these names and never define their own.
Output: `jobs`, `job_dependencies`, `job_events`, `job_logs` tables + migration + enforcement test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-job-framework/17-CONTEXT.md
@.planning/phases/17-job-framework/17-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the Job code vocabulary and the Job ORM model</name>
  <files>src/trading_platform/db/models/job.py</files>
  <read_first>
    - src/trading_platform/db/models/strategy_run.py (the StrEnum + `values_callable=_enum_values` + `validate_strings=True` idiom, UUID identity, timestamp columns, JSON snapshot columns, `__table_args__` Index shape)
    - src/trading_platform/db/base.py (Base, TimestampedModel, NAMING_CONVENTION)
    - .planning/phases/17-job-framework/17-CONTEXT.md (D-01, D-03, D-05, D-09, D-11, D-12 and the `<specifics>` stable-code list)
  </read_first>
  <action>
Create `src/trading_platform/db/models/job.py` following `strategy_run.py`'s structure exactly.

Define a module-level `_enum_values(enum_cls: type[StrEnum]) -> list[str]` helper (same as `strategy_run.py`) and four `StrEnum` classes. These names and values are the canonical vocabulary for the whole phase — do not abbreviate or rename them:

`JobStatus`, PostgreSQL type name `job_status`: QUEUED="queued", RUNNING="running", SUCCEEDED="succeeded", FAILED="failed", CANCELLED="cancelled". No other members. This closed set is JOB-01.

`JobFailureReason`, PostgreSQL type name `job_failure_reason`: HANDLER_ERROR="handler_error", WORKER_LOST="worker_lost", LEASE_EXPIRED="lease_expired", CANCELLATION_TIMEOUT="cancellation_timeout". Per D-01, `worker_lost` and `lease_expired` are FAILED reasons, never a cancellation cause. Per D-09, `cancellation_timeout` is a FAILED reason.

`JobCancellationCause`, PostgreSQL type name `job_cancellation_cause`: OPERATOR_REQUEST="operator_request", DEPENDENCY_FAILED="dependency_failed", DEPENDENCY_CANCELLED="dependency_cancelled". Per D-04 these are the only legitimate paths to CANCELLED.

Then define `class Job(TimestampedModel, Base)` with `__tablename__ = "jobs"` and these columns:

Identity and type: `id` (Uuid primary key, default uuid.uuid4), `job_type` (String(64), not null), `payload` (JSON, not null, default dict).

Lifecycle: `status` (Enum(JobStatus, name="job_status", values_callable=_enum_values, validate_strings=True), not null, default JobStatus.QUEUED), `queued_at` (DateTime(timezone=True), server_default=func.now(), not null), `started_at` (nullable), `completed_at` (nullable).

Lease/claim (JOB-02 support, populated in plan 17-07): `lease_owner` (String(128), nullable), `lease_expires_at` (DateTime(timezone=True), nullable), `heartbeat_at` (DateTime(timezone=True), nullable).

Terminal outcome (D-01, D-03): `failure_reason` (Enum(JobFailureReason, ...), nullable), `failure_message` (Text, nullable), `outcome_uncertain` (Boolean, not null, default False, server_default false), `result_summary` (JSON, not null, default dict).

Cancellation (D-08, D-10): `cancellation_requested_at` (DateTime(timezone=True), nullable), `cancellation_requested_by` (String(128), nullable), `cancellation_reason` (Text, nullable), `cancellation_acknowledged_at` (DateTime(timezone=True), nullable), `cancellation_cause` (Enum(JobCancellationCause, ...), nullable).

Dependency causal chain (D-05): `blocking_job_id` (Uuid, ForeignKey("jobs.id", ondelete="SET NULL"), nullable), `blocking_job_status` (Enum(JobStatus, ...) nullable — reuse the same enum type, pass `create_type=False` semantics by relying on the shared `name="job_status"`), `root_cause_job_id` (Uuid, ForeignKey("jobs.id", ondelete="SET NULL"), nullable).

Progress (D-11, D-12): `progress_percent` (Integer, nullable), `progress_step` (String(255), nullable), `progress_current` (Integer, nullable), `progress_total` (Integer, nullable), `progress_updated_at` (DateTime(timezone=True), nullable). All nullable so an unstarted Job has no progress and a FAILED/CANCELLED Job keeps its last snapshot untouched (D-12) — nothing in this model resets progress on terminal transition.

`__table_args__` (names must match NAMING_CONVENTION so `op.f(...)` in the migration agrees):
CheckConstraint on `progress_percent IS NULL OR (progress_percent >= 0 AND progress_percent <= 100)` named `job_progress_percent_range`;
Index `ix_jobs_status_queued_at` on ("status", "queued_at") for the queue-claim query;
Index `ix_jobs_status_lease_expires_at` on ("status", "lease_expires_at") for the lease-expiry sweep;
Index `ix_jobs_job_type_status` on ("job_type", "status").

Add relationships for the self-referential FKs using the `remote_side` / `foreign_keys` idiom from `paper_order.py` (`blocking_job`, `root_cause_job`), each with `foreign_keys=[...]` since there are two self-FKs on one table and SQLAlchemy cannot disambiguate them otherwise.

Add a module docstring stating that this file is the single definition site for the Phase 17 status/failure/cancellation vocabulary.
  </action>
  <verify>
    <automated>python -c "from trading_platform.db.models.job import Job, JobStatus, JobFailureReason, JobCancellationCause; assert [m.value for m in JobStatus] == ['queued','running','succeeded','failed','cancelled']; assert {m.value for m in JobFailureReason} == {'handler_error','worker_lost','lease_expired','cancellation_timeout'}; assert {m.value for m in JobCancellationCause} == {'operator_request','dependency_failed','dependency_cancelled'}; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `JobStatus` has exactly five members with values queued, running, succeeded, failed, cancelled — the assertion in the verify command passes.
    - `JobFailureReason` contains `worker_lost`, `lease_expired`, `cancellation_timeout`, `handler_error` and `JobCancellationCause` contains none of those four values.
    - `grep -c "validate_strings=True" src/trading_platform/db/models/job.py` returns at least 4 (status, failure_reason, cancellation_cause, blocking_job_status).
    - `Job.__table__.c.outcome_uncertain.nullable is False` and its default resolves to False.
    - `Job.__table__.c.progress_percent.nullable is True` and a CheckConstraint whose name contains `progress_percent_range` exists in `Job.__table__.constraints`.
    - Every one of these column names exists on `Job.__table__.c`: job_type, payload, status, queued_at, started_at, completed_at, lease_owner, lease_expires_at, heartbeat_at, failure_reason, failure_message, outcome_uncertain, result_summary, cancellation_requested_at, cancellation_requested_by, cancellation_reason, cancellation_acknowledged_at, cancellation_cause, blocking_job_id, blocking_job_status, root_cause_job_id, progress_percent, progress_step, progress_current, progress_total, progress_updated_at.
  </acceptance_criteria>
  <done>`job.py` imports cleanly, the four enums are closed and correctly valued, and the Job table carries lifecycle, lease, terminal-outcome, cancellation, causal-chain and progress columns.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dependency, event, and log models and export them</name>
  <files>src/trading_platform/db/models/job_dependency.py, src/trading_platform/db/models/job_event.py, src/trading_platform/db/models/job_log.py, src/trading_platform/db/models/__init__.py</files>
  <read_first>
    - src/trading_platform/db/models/job.py (just created — import JobStatus from here, do not redefine)
    - src/trading_platform/db/models/order_event.py (append-only transition-record shape: from_state/to_state/event_type/outcome/event_at/details plus composite indexes)
    - src/trading_platform/db/models/execution_event.py (append-only log-record shape: event_type/severity/event_at/message/details JSON)
    - src/trading_platform/db/models/paper_order.py (self-referential FK + relationship idiom, `supersedes_paper_order_id`)
    - src/trading_platform/db/models/__init__.py (existing barrel export ordering and `__all__` style)
    - .planning/phases/17-job-framework/17-CONTEXT.md (D-06, D-07 through D-10, D-13, D-14)
  </read_first>
  <action>
Create `job_dependency.py`: `class JobDependency(TimestampedModel, Base)`, `__tablename__ = "job_dependencies"`. Columns: `id` (Uuid pk, default uuid4), `job_id` (Uuid, ForeignKey("jobs.id", ondelete="CASCADE"), not null), `depends_on_job_id` (Uuid, ForeignKey("jobs.id", ondelete="CASCADE"), not null). `__table_args__`: UniqueConstraint("job_id", "depends_on_job_id") named `uq_job_dependencies_job_id_depends_on_job_id`; CheckConstraint `job_id <> depends_on_job_id` named `job_not_self_dependent` (which NAMING_CONVENTION renders as `ck_job_dependencies_job_not_self_dependent`); Index `ix_job_dependencies_depends_on_job_id` on ("depends_on_job_id",) for reverse dependent lookups. Add `job` and `depends_on_job` relationships with explicit `foreign_keys=[...]` (two FKs to the same table require disambiguation). Add a docstring noting D-06: this edge set is immutable after submission; cycle rejection lives in `jobs/dependencies.py`, and this CheckConstraint is only the self-dependency backstop.

Create `job_event.py`: define `class JobEventType(StrEnum)`, PostgreSQL type name `job_event_type`, with members SUBMITTED="submitted", CLAIMED="claimed", SUCCEEDED="succeeded", FAILED="failed", CANCELLATION_REQUESTED="cancellation_requested", CANCELLED="cancelled", LEASE_EXPIRED="lease_expired", WORKER_LOST="worker_lost", CANCELLATION_TIMEOUT="cancellation_timeout", DEPENDENCY_RESOLVED="dependency_resolved". Define `class JobTransitionOutcome(StrEnum)`, type name `job_transition_outcome`: ACCEPTED="accepted", REJECTED="rejected". Then `class JobEvent(TimestampedModel, Base)`, `__tablename__ = "job_events"`, mirroring `order_event.py`: `id` (Uuid pk), `job_id` (Uuid FK jobs.id ondelete CASCADE, not null), `from_status` (Enum JobStatus, nullable — a submission event has no prior state), `to_status` (Enum JobStatus, nullable — a rejected transition has no resulting state), `event_type` (Enum JobEventType, not null), `outcome` (Enum JobTransitionOutcome, not null), `event_at` (DateTime(timezone=True), not null). D-10 fields: `requested_by` (String(128), nullable), `reason` (Text, nullable), `requested_at` (DateTime(timezone=True), nullable), `acknowledged_at` (DateTime(timezone=True), nullable), `terminal_cause` (String(64), nullable — carries the `JobFailureReason` or `JobCancellationCause` value as a string so one column serves both). `details` (JSON, not null, default dict). `__table_args__`: Index `ix_job_events_job_id_event_at` on ("job_id", "event_at"); Index `ix_job_events_job_id_event_type` on ("job_id", "event_type").

Create `job_log.py`: `class JobLog(TimestampedModel, Base)`, `__tablename__ = "job_logs"`. Columns per D-13: `id` (Uuid pk), `job_id` (Uuid FK jobs.id ondelete CASCADE, not null), `sequence` (Integer, not null — the deterministic per-Job ordering key, since `logged_at` timestamps can collide), `logged_at` (DateTime(timezone=True), not null), `level` (String(16), not null), `event_code` (String(64), not null), `message` (Text, not null), `handler_type` (String(64), not null), `context` (JSON, not null, default dict). `__table_args__`: UniqueConstraint("job_id", "sequence") named `uq_job_logs_job_id_sequence`; Index `ix_job_logs_job_id_sequence` on ("job_id", "sequence"). Docstring must state D-13 (ordering is by `sequence`, not `logged_at`), D-14 (no pruning or compaction exists in Phase 17 — rows live for the lifetime of the Job record), and that `context` is written only after passing through `trading_platform.core.log_sanitizer.sanitize` (enforced by the writer in plan 17-04, not by this model).

Extend `src/trading_platform/db/models/__init__.py`: add alphabetically-placed imports for `Job`, `JobStatus`, `JobFailureReason`, `JobCancellationCause` from `.job`; `JobDependency` from `.job_dependency`; `JobEvent`, `JobEventType`, `JobTransitionOutcome` from `.job_event`; `JobLog` from `.job_log`. Add all ten names to `__all__` preserving the file's existing sort order.
  </action>
  <verify>
    <automated>python -c "from trading_platform.db.models import Job, JobDependency, JobEvent, JobEventType, JobLog, JobStatus, JobTransitionOutcome, JobFailureReason, JobCancellationCause; from trading_platform.db.base import Base; t=Base.metadata.tables; assert {'jobs','job_dependencies','job_events','job_logs'} <= set(t); assert any('not_self_dependent' in (c.name or '') for c in t['job_dependencies'].constraints); assert any(u.name=='uq_job_logs_job_id_sequence' for u in t['job_logs'].constraints if u.name); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0: all four tables register on `Base.metadata`, the self-dependency CheckConstraint exists, and the `(job_id, sequence)` unique constraint exists.
    - `JobDependency.__table__.c.job_id.foreign_keys` and `.depends_on_job_id.foreign_keys` both target `jobs.id`, and both FKs have `ondelete="CASCADE"`.
    - `job_event.py` contains no `class JobStatus` definition — `grep -c "class JobStatus" src/trading_platform/db/models/job_event.py` returns 0; it imports JobStatus from `trading_platform.db.models.job`.
    - `JobLog.__table__.c.sequence.nullable is False` and `JobLog.__table__.c.handler_type.nullable is False`.
    - `JobEvent.__table__.c` includes requested_by, reason, requested_at, acknowledged_at, terminal_cause.
    - `python -c "from trading_platform.db import models; assert 'Job' in models.__all__ and 'JobLog' in models.__all__ and 'JobDependency' in models.__all__ and 'JobEvent' in models.__all__"` exits 0.
  </acceptance_criteria>
  <done>All four Job tables are registered on the shared metadata and exported from the models barrel with no duplicate enum definitions.</done>
</task>

<task type="auto">
  <name>Task 3: Alembic migration and migration enforcement test</name>
  <files>alembic/versions/0018_phase17_job_framework.py, tests/test_db_migrations.py</files>
  <read_first>
    - alembic/versions/0009_phase5_order_lifecycle.py (multi-table create with enum types, `op.f(...)` constraint names, FK ondelete, index creation, downgrade ordering)
    - alembic/versions/0017_phase11_query_performance_indices.py (current head: `revision = "0017_phase11_query_perf_indices"`, and the documented ≤32-char revision-id constraint)
    - src/trading_platform/db/base.py (NAMING_CONVENTION — `op.f(...)` names must match what SQLAlchemy autogenerates)
    - src/trading_platform/db/models/job.py, job_dependency.py, job_event.py, job_log.py (the exact columns/constraints to materialize)
    - tests/test_db_migrations.py (the `migrated_database` fixture and `test_alembic_upgrade_creates_phase5_paper_order_tables` as the assertion template)
  </read_first>
  <action>
Create `alembic/versions/0018_phase17_job_framework.py` with `revision = "0018_phase17_job_framework"` (25 chars, within the documented 32-char `alembic_version.version_num` limit) and `down_revision = "0017_phase11_query_perf_indices"`.

`upgrade()` creates, in FK-safe order: `jobs`, then `job_dependencies`, `job_events`, `job_logs`. Use `sa.Enum(..., name="job_status")` etc. so PostgreSQL native enum types `job_status`, `job_failure_reason`, `job_cancellation_cause`, `job_event_type`, `job_transition_outcome` are created. Because `job_status` is referenced by four columns across two tables, create the type explicitly once (a `sa.Enum` object with `create_type=False` reused on subsequent columns, or an explicit `postgresql.ENUM(...).create(op.get_bind())` before the first `create_table`) — a naive repeat will raise `DuplicateObject`. Every column, constraint, and index must mirror Task 1 and Task 2 exactly, with constraint names produced via `op.f(...)` matching NAMING_CONVENTION.

`downgrade()` drops `job_logs`, `job_events`, `job_dependencies`, `jobs` in reverse order, then explicitly drops the five enum types via `sa.Enum(name=...).drop(op.get_bind())` or `op.execute("DROP TYPE IF EXISTS ...")` — Postgres does not drop enum types with their tables.

Add one test function `test_alembic_upgrade_creates_phase17_job_tables(migrated_database: str) -> None` to `tests/test_db_migrations.py`, reusing the existing `migrated_database` fixture (do not create a new fixture). It must assert:
that `{"jobs", "job_dependencies", "job_events", "job_logs"}` is a subset of `inspector.get_table_names()`;
that the `job_status` enum labels equal exactly `{"queued","running","succeeded","failed","cancelled"}` (this is the JOB-01 database-level proof) and that `job_failure_reason` labels equal `{"handler_error","worker_lost","lease_expired","cancellation_timeout"}` and `job_cancellation_cause` labels equal `{"operator_request","dependency_failed","dependency_cancelled"}`;
that the `jobs` column set is a superset of the full column list from Task 1;
that `job_logs` has a unique constraint over `("job_id","sequence")`;
that inserting a `Job` row with `status="stale"` (a valid value for a *different* table's enum, deliberately chosen to prove enum isolation) raises a database error — use `session_scope` and `pytest.raises`.

Add a second test `test_phase17_job_dependency_rejects_self_edge(migrated_database: str) -> None` that persists one Job, then attempts a `JobDependency` with `job_id == depends_on_job_id` and asserts the insert raises (the D-06 self-dependency backstop), following the round-trip style of `test_phase7_idempotent_intent_schema_supports_predecessor_links`.
  </action>
  <verify>
    <automated>pytest tests/test_db_migrations.py -k "phase17" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_db_migrations.py -k "phase17" -x -q` passes with 2 tests collected and 0 failures.
    - `grep -c 'revision = "0018_phase17_job_framework"' alembic/versions/0018_phase17_job_framework.py` returns 1, and the revision string is 32 characters or fewer.
    - `grep -c 'down_revision = "0017_phase11_query_perf_indices"' alembic/versions/0018_phase17_job_framework.py` returns 1.
    - `alembic downgrade -1` followed by `alembic upgrade head` completes without error against a test database (enum types are dropped and recreated cleanly).
    - `pytest tests/test_db_migrations.py -q` shows no regression in previously passing tests.
  </acceptance_criteria>
  <done>Migration 0018 chains off 0017, creates all four tables with native enum types, is reversible, and two enforcement tests prove the closed status enum and the self-dependency rejection at the database level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application → PostgreSQL | Job status, failure reason, and cancellation cause values cross into persistent storage; only the closed enum sets may cross |
| Job payload → `jobs.payload` JSON | Caller-supplied operation parameters are stored verbatim in this phase (submission validation is Phase 18) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | `jobs.status` column | mitigate | Native PostgreSQL enum `job_status` with `validate_strings=True`; enforcement test asserts labels equal exactly the five-member set and that an out-of-set literal raises |
| T-17-01-02 | Tampering | `job_dependencies` self-edge | mitigate | `ck_job_dependencies_job_not_self_dependent` CheckConstraint plus a dedicated migration test (D-06) |
| T-17-01-03 | Information disclosure | `job_logs.context`, `jobs.payload` JSON | accept (deferred to 17-04) | This plan defines the columns only; the sanitization obligation is enforced at the single write path in plan 17-04 via `core.log_sanitizer.sanitize`. Documented in the `job_log.py` docstring so no second write path is invented |
| T-17-01-04 | Repudiation | Job terminal outcome | mitigate | `job_events` is append-only with `requested_by`, `requested_at`, `acknowledged_at`, `terminal_cause`; no UPDATE path exists on this table |
| T-17-01-05 | Denial of service | unbounded `job_logs` growth | accept | D-14 explicitly excludes pruning/compaction from Phase 17; read-side volume safeguards land in plan 17-08 |
| T-17-01-SC | Tampering | package installs | accept | This plan installs no new packages — SQLAlchemy and Alembic are already project dependencies |
</threat_model>

<verification>
- `pytest tests/test_db_migrations.py -q` passes.
- `python -c "from trading_platform.db import models"` imports without error.
- `alembic upgrade head` then `alembic downgrade -1` then `alembic upgrade head` is clean.
</verification>

<success_criteria>
- Four Job tables exist in PostgreSQL with native enum types.
- `JobStatus` is closed at five members at both the Python and PostgreSQL layers, proven by test.
- The stable code vocabulary (`worker_lost`, `lease_expired`, `cancellation_timeout`, `dependency_failed`, `dependency_cancelled`) is defined exactly once, in `job.py`.
- Migration 0018 is reversible and enforcement-tested.
</success_criteria>

<output>
Create `.planning/phases/17-job-framework/17-01-SUMMARY.md` when done
</output>
