---
phase: 12-structural-refactor-and-tooling
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/trading_platform/services/config/validation.py
  - src/trading_platform/services/config/secrets.py
  - src/trading_platform/core/config_validation.py
  - src/trading_platform/core/startup.py
  - src/trading_platform/api/app.py
  - src/trading_platform/worker/__main__.py
  - src/trading_platform/services/bootstrap.py
  - tests/test_config_validation.py
  - tests/test_startup_validation.py
autonomous: true
requirements: [STRUCT-06, STRUCT-08]
must_haves:
  truths:
    - "Config validation logic lives under services/config/{validation,secrets}.py and the old core/config_validation.py is deleted"
    - "All imports of config validation resolve through the new services/config paths"
    - "There is exactly one canonical settings surface (trading_platform.core.settings); no competing settings module exists"
    - "The full suite passes with zero new or modified assertions"
  artifacts:
    - path: "src/trading_platform/services/config/validation.py"
      provides: "validate_config, ExecutionMode, ConfigValidationError (moved from core/config_validation.py)"
      contains: "def validate_config"
    - path: "src/trading_platform/services/config/secrets.py"
      provides: "Per-mode required-secret / endpoint checks (CFG-01/02/03 semantic layer)"
      contains: "def"
  key_links:
    - from: "src/trading_platform/core/startup.py"
      to: "src/trading_platform/services/config/validation.py"
      via: "import validate_config / ConfigValidationError / ExecutionMode"
      pattern: "from trading_platform.services.config.validation import"
    - from: "src/trading_platform/worker/__main__.py"
      to: "src/trading_platform/services/config/validation.py"
      via: "import"
      pattern: "from trading_platform.services.config"
---

<objective>
Reorganize config logic under its declared service boundary and confirm the single canonical settings surface.

STRUCT-06: move `core/config_validation.py` into `services/config/{validation,secrets}.py`, splitting the pure shape/orchestration layer (validation) from the per-mode required-secret / endpoint-mode checks (secrets). Delete the old module; update every import site.
STRUCT-08: confirm `trading_platform.core.settings` is the one canonical settings surface, remove any duplicate/redundant settings-load patterns, and record the confirmation.

Purpose: Config validation currently lives under `core/` but the requirement places it under `services/config/`. This move plus the settings confirmation closes the config-organization requirements with zero behavior change.
Output: `services/config/validation.py`, `services/config/secrets.py`, deleted `core/config_validation.py`, updated importers.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/trading_platform/core/config_validation.py

<interfaces>
Current public surface of core/config_validation.py (to be relocated verbatim):
```python
class ExecutionMode(str, Enum): BACKTEST="backtest"; PAPER="paper"; LIVE="live"
class ConfigValidationError(Exception): ...   # carries failures: list[tuple[str,str]]
def validate_config(payload: dict[str, Any], *, mode: ExecutionMode = ExecutionMode.BACKTEST) -> Settings: ...
def _semantic_failures(settings: Settings, *, mode: ExecutionMode) -> list[tuple[str,str]]: ...  # CFG-01/02/03
```
Consumers of `core/config_validation.py` today (all must be repointed): `core/startup.py`, `api/app.py`, `worker/__main__.py`, `services/bootstrap.py`, plus tests `test_config_validation.py`, `test_startup_validation.py`.

Settings surface (STRUCT-08): `core/settings.py` is ALREADY the sole `BaseSettings`/`Settings` definition. Public: `Settings`, `load_settings`, `build_settings_payload`, `get_strategy_config`, `clear_settings_cache`, `PROJECT_ROOT`. grep across `src/` finds no competing settings class. STRUCT-08 here is a CONFIRMATION + tidy, not a deletion of a rival module.
</interfaces>

<key_facts>
- Split boundary: `validation.py` owns `ExecutionMode`, `ConfigValidationError`, `_dotted_path`, `_translate_pydantic_errors`, and `validate_config` (the pydantic-shape pass + orchestration). `secrets.py` owns the CFG-01/02/03 semantic layer currently in `_semantic_failures` + `_PAPER_BASE_URL_MARKER` (required-secret presence and paper/live endpoint-mode checks). `validation.py` imports `secrets.py` and calls its semantic-check function. This keeps the exact two-pass behavior (pydantic shape pass, then semantic pass) byte-for-byte.
- `secrets.py` is deliberately thin (the required-secret + endpoint checks). That is correct — do not invent extra content.
- The old `core/config_validation.py` MUST be deleted (requirement: "old scattered module definitions are deleted"). Provide NO backward-compat shim; repoint every importer to the new path.
- Updating import statements in src AND tests is required here and is NOT an assertion change (see contract). Do not modify any assertion body.
</key_facts>

<zero_behavior_change_contract>
STRUCT-02 no-behavior-change refactor. Proof = the existing suite green before and after with ZERO new/removed/modified assertions. No new tests. Import-line edits (including in test files, to repoint at the new module path) are permitted; assertion bodies are frozen.
</zero_behavior_change_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STRUCT-06 — create services/config/validation.py + secrets.py, delete core/config_validation.py, repoint importers</name>
  <files>src/trading_platform/services/config/validation.py, src/trading_platform/services/config/secrets.py, src/trading_platform/core/config_validation.py, src/trading_platform/core/startup.py, src/trading_platform/api/app.py, src/trading_platform/worker/__main__.py, src/trading_platform/services/bootstrap.py, tests/test_config_validation.py, tests/test_startup_validation.py</files>
  <action>
    1. Create `services/config/validation.py` containing `ExecutionMode`, `ConfigValidationError`, `_dotted_path`, `_translate_pydantic_errors`, and `validate_config`. `validate_config`'s semantic pass calls into `secrets.py`.
    2. Create `services/config/secrets.py` containing the semantic CFG-01/02/03 layer: move `_semantic_failures` (rename to a clear public-ish name, e.g. `semantic_failures`) and `_PAPER_BASE_URL_MARKER`. `validation.py` imports and calls it.
    3. Delete `core/config_validation.py` entirely (no shim).
    4. Repoint every importer to `from trading_platform.services.config.validation import ...`: `core/startup.py`, `api/app.py`, `worker/__main__.py`, `services/bootstrap.py`.
    5. Repoint test imports in `tests/test_config_validation.py` and `tests/test_startup_validation.py` to the new path. Change ONLY the import lines — assertions stay identical.
    6. Confirm nothing still references the old module: `grep -rn "core.config_validation\|core/config_validation" src/ tests/` returns nothing.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/pytest tests/test_config_validation.py tests/test_startup_validation.py tests/test_app_boot.py tests/test_dry_run.py -q</automated>
  </verify>
  <done>services/config/{validation,secrets}.py exist with the moved logic; core/config_validation.py deleted; all importers + tests repointed; grep for old path is clean; targeted tests green.</done>
</task>

<task type="auto">
  <name>Task 2: STRUCT-08 — confirm single canonical settings surface</name>
  <files>src/trading_platform/core/settings.py</files>
  <action>
    Confirm and document that `trading_platform.core.settings` is the ONE canonical settings surface:
    1. `grep -rnE "BaseSettings|class .*Settings\b" src/ --include=*.py` — verify `core/settings.py` is the only module defining the settings model hierarchy (it is, per the interfaces block). If any competing settings module or duplicate `Settings`/`BaseSettings` definition is found that this grep surfaces, remove the duplicate and repoint its importers to `core/settings.py`.
    2. If there is genuinely no competing module (expected outcome), make NO code change — record in the SUMMARY that STRUCT-08 was satisfied by confirmation (single surface already canonical), citing the grep result. Do not fabricate a deletion that has no target.
    3. If, and only if, you find redundant/competing `load_settings()`-style duplication (e.g., a second cache or a shadow loader), consolidate onto the canonical `load_settings`/`clear_settings_cache` pair.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/pytest -q 2>&1 | tail -3</automated>
  </verify>
  <done>Grep evidence recorded showing exactly one settings surface; no competing settings module remains; full suite green at baseline count.</done>
</task>

</tasks>

<verification>
- STRUCT-06: `grep -rn "core.config_validation" src/ tests/` empty; `services/config/validation.py` and `services/config/secrets.py` exist; targeted config/startup/boot tests green.
- STRUCT-08: grep shows one settings surface; documented in SUMMARY.
- Zero-behavior-change: full suite pass count equals 12-BASELINE.md; no assertion bodies changed.
</verification>

<success_criteria>
- Config validation reorganized under services/config/{validation,secrets}.py; old module deleted; all imports resolve through new paths.
- Single canonical settings surface confirmed (or duplicate removed).
- Full suite green at baseline count with no assertion changes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-structural-refactor-and-tooling/12-02-SUMMARY.md`.
</output>
