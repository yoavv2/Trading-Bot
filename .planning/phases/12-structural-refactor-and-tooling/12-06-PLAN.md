---
phase: 12-structural-refactor-and-tooling
plan: 06
type: execute
wave: 6
depends_on: ["12-05"]
files_modified:
  - src/trading_platform/worker/__main__.py
  - src/trading_platform/worker/parser.py
  - src/trading_platform/worker/commands/__init__.py
  - src/trading_platform/worker/commands/bootstrap.py
  - src/trading_platform/worker/commands/ingest.py
  - src/trading_platform/worker/commands/backtest.py
  - src/trading_platform/worker/commands/risk_check.py
  - src/trading_platform/worker/commands/paper_execute.py
  - src/trading_platform/worker/commands/reconcile.py
  - src/trading_platform/worker/commands/operator.py
autonomous: true
requirements: [STRUCT-03, STRUCT-02]
must_haves:
  truths:
    - "worker/__main__.py contains only parser construction wiring, command dispatch, and top-level error handling — no business logic remains in the entrypoint"
    - "Each worker subcommand's domain logic lives in worker/commands/{bootstrap,ingest,backtest,risk_check,paper_execute,reconcile}.py (plus operator.py)"
    - "Every worker CLI subcommand still runs and produces identical behavior"
    - "The full suite passes with zero new or modified assertions, equal to the captured baseline (STRUCT-02 proven end-to-end)"
  artifacts:
    - path: "src/trading_platform/worker/__main__.py"
      provides: "Routing-only entrypoint: build parser, parse args, dispatch to command handler, handle top-level errors"
      contains: "def main"
    - path: "src/trading_platform/worker/commands/paper_execute.py"
      provides: "submit-paper-orders, run-paper-session, sync-paper-state command handlers"
      contains: "def"
    - path: "src/trading_platform/worker/commands/bootstrap.py"
      provides: "serve + dry-run command handlers"
      contains: "def"
  key_links:
    - from: "src/trading_platform/worker/__main__.py"
      to: "src/trading_platform/worker/commands/__init__.py"
      via: "command → handler dispatch mapping"
      pattern: "commands"
    - from: "src/trading_platform/worker/commands/paper_execute.py"
      to: "src/trading_platform/services/execution/submit_orders.py"
      via: "import of run_paper_order_submission / run_paper_session"
      pattern: "from trading_platform.services.execution"
---

<objective>
Split the 795-line `worker/__main__.py` into bounded command modules under `worker/commands/`, leaving the entrypoint as routing only (< ~100 lines). This is STRUCT-03. It runs LAST among the code-move plans so every command module imports the FINAL service paths (execution package from 12-03/12-04, reconciliation package from 12-05, config package from 12-02).

This plan also owns STRUCT-02: because it is the last refactor to land, its green-suite-at-baseline result is the end-to-end proof that the entire Phase-12 refactor introduced zero behavior change.

Purpose: The entrypoint has accreted ~15 subcommands with full domain logic inline. STRUCT-03 requires it be pure routing with domain commands extracted to named modules.
Output: `worker/commands/` package with the six named modules + operator.py, `worker/parser.py`, and a slimmed `__main__.py`.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/12-structural-refactor-and-tooling/12-BASELINE.md

<interfaces>
Current `worker/__main__.py` subcommands (parser at build_parser, ~lines 40-210) and their handler functions, mapped to target command modules:

`commands/bootstrap.py`:
- serve → run_placeholder_worker (212); dry-run → run_dry_bootstrap (237)

`commands/ingest.py`:
- ingest-bars → run_ingest_bars (622); sync-metadata → run_sync_metadata (658); sync-sessions → run_sync_sessions (709)

`commands/backtest.py`:
- backtest → run_backtest_command (254); report-backtest → run_report_backtest_command (286); report-strategy-analytics → run_report_strategy_analytics_command (299)

`commands/risk_check.py`:
- evaluate-risk → run_evaluate_risk_command (325)

`commands/paper_execute.py`:
- submit-paper-orders → run_submit_paper_orders_command (385); run-paper-session → run_paper_session_command (418); sync-paper-state → run_sync_paper_state_command (453); plus shared helper _handle_concurrent_run_lock_denied (354)

`commands/reconcile.py`:
- reconcile-paper-execution → run_reconcile_paper_execution_command (482)

`commands/operator.py` (the two operator subcommands that fall outside the six named domain modules — see key_facts):
- operator-control → run_operator_control_command (512) + _run_kill_switch_action (552)
- operator-status → run_operator_status_command (600)

`worker/parser.py`:
- build_parser (40) — the ~170-line argparse construction

`worker/__main__.py` (remains):
- main (741) — reduced to: build parser, parse args, look up handler in a {command: handler} dispatch map, call it, handle top-level errors. Target < 100 lines.
</interfaces>

<key_facts>
- The STRUCT-03 requirement names six modules. The `operator-control`/`operator-status` subcommands do not belong to any of the six; leaving them inline would keep domain logic in the entrypoint and violate the "<100 lines, routing only" criterion. Resolution: add `commands/operator.py` as a legitimate seventh sibling module. State this explicitly in the SUMMARY so the plan-checker does not read the extra module as scope creep — the entrypoint-must-be-pure-routing criterion forces a home for these commands.
- `build_parser` (~170 lines) is not "routing" and cannot stay in `__main__.py` without breaking the <100-line budget. Move it to `worker/parser.py`. `__main__.main()` imports `build_parser` and a dispatch map.
- Recommended dispatch shape: each command module exposes its `run_*` handler(s) with the SAME signatures they have today (`(args: argparse.Namespace) -> None` etc.). `commands/__init__.py` (or `__main__.py`) builds a `{subcommand_string: handler}` dict; `main()` does `handler = DISPATCH[args.command]; handler(args)`. Preserve the exact SystemExit/exit-code behavior (e.g. CONCURRENT_RUN_LOCK_EXIT_CODE mapping in paper_execute).
- Move handlers VERBATIM. Repoint their service imports to the FINAL package paths: `trading_platform.services.execution` (submit_orders/sync_orders/idempotency), `trading_platform.services.reconciliation`, `trading_platform.services.config.validation`. Grep the moved bodies for any old paths and fix.
- Preserve the module-level constants the handlers rely on (e.g. `CONCURRENT_RUN_LOCK_EXIT_CODE`) — move them to where they are used or a shared `commands/__init__.py`.
- Import-line edits in src AND tests are required and are NOT assertion changes. Assertion bodies frozen. No new tests.
</key_facts>

<zero_behavior_change_contract>
STRUCT-02 no-behavior-change refactor — and this plan is where STRUCT-02 is proven and marked Complete, because it is the final code move. Proof = the FULL existing suite green at exactly the 12-BASELINE.md count with ZERO new/removed/modified assertions. No new tests. Repointing imports permitted; assertion bodies frozen. Before marking STRUCT-02 Complete, run the full suite and confirm the pass count equals the baseline captured in 12-01.
</zero_behavior_change_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract parser + command modules from __main__.py</name>
  <files>src/trading_platform/worker/parser.py, src/trading_platform/worker/commands/__init__.py, src/trading_platform/worker/commands/bootstrap.py, src/trading_platform/worker/commands/ingest.py, src/trading_platform/worker/commands/backtest.py, src/trading_platform/worker/commands/risk_check.py, src/trading_platform/worker/commands/paper_execute.py, src/trading_platform/worker/commands/reconcile.py, src/trading_platform/worker/commands/operator.py</files>
  <action>
    1. Create `worker/commands/` package. Move each handler function verbatim into its target module per the interfaces mapping, including the shared helpers (_handle_concurrent_run_lock_denied → paper_execute.py; _run_kill_switch_action → operator.py).
    2. Move `build_parser` into `worker/parser.py`.
    3. In each moved handler, repoint service imports to the FINAL package paths (execution/reconciliation/config packages). Grep each new file for stale `paper_execution`/`order_state_machine`/`order_identity`/`reconciliation_matcher`/`reconciliation_types`/`core.config_validation` references and fix.
    4. Move shared module-level constants (e.g. CONCURRENT_RUN_LOCK_EXIT_CODE) to the module that uses them or `commands/__init__.py`.
    5. Do NOT yet delete the bodies from `__main__.py` if it helps keep it importable mid-task — but by end of Task 2 `__main__.py` must contain none of the moved bodies.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -c "from trading_platform.worker.parser import build_parser; from trading_platform.worker.commands import bootstrap, ingest, backtest, risk_check, paper_execute, reconcile, operator; print('ok')"</automated>
  </verify>
  <done>parser.py + all seven command modules exist with verbatim handlers; imports resolve through final service paths; import smoke test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce __main__.py to routing-only dispatch and prove the baseline</name>
  <files>src/trading_platform/worker/__main__.py</files>
  <action>
    1. Rewrite `__main__.py` to: import `build_parser` from `worker.parser`, import handlers from `worker.commands`, build a `{command_string: handler}` dispatch map, and `main()` parses args, resolves the handler, calls it, and handles top-level errors — nothing else. Preserve exit-code behavior exactly.
    2. Confirm no moved handler body remains in `__main__.py`.
    3. Success is architectural purity, NOT a line-count target: confirm that NO handler/business logic remains in `__main__.py` (no `run_*`/`_run_*` handler definitions, no domain imports beyond the command modules + parser). A routing-only file of 110-120 lines is a PASS; do NOT add indirection solely to hit a number. `wc -l` is an informational sanity signal only.
    4. Run the FULL suite and confirm the pass count equals 12-BASELINE.md (STRUCT-02 end-to-end proof). Spot-check that at least one CLI command still dispatches: e.g. `PYTHONPATH=src .venv/bin/python -m trading_platform.worker dry-run --strategy trend_following_daily` behaves as before (or `--help` lists all subcommands).
  </action>
  <verify>
    <automated>! grep -qE '^\s*(async )?def (run_|_run_)' src/trading_platform/worker/__main__.py && PYTHONPATH=src .venv/bin/python -m trading_platform.worker --help >/dev/null && PYTHONPATH=src .venv/bin/pytest -q 2>&1 | tail -3</automated>
  </verify>
  <done>__main__.py contains only parser wiring, dispatch, and top-level error handling — no handler bodies remain (grep proves it); every subcommand dispatches (`worker --help`); full suite green at the 12-BASELINE.md count with zero assertion changes.</done>
</task>

</tasks>

<verification>
- STRUCT-03: no handler/business logic remains in `__main__.py` (only parser wiring + dispatch + top-level error handling); `worker --help` still lists all subcommands. Line count is informational, not a gate.
- STRUCT-02: full suite (`PYTHONPATH=src .venv/bin/pytest -q`) pass count EQUALS 12-BASELINE.md; no assertion added/removed/modified across the whole phase.
</verification>

<success_criteria>
- worker/__main__.py is pure routing (only parser wiring + dispatch + error handling, no business logic); domain commands live in worker/commands/{six named + operator}.py; parser in worker/parser.py.
- Full suite green at the captured baseline count — proving the entire Phase-12 refactor is zero-behavior-change (STRUCT-02).
</success_criteria>

<output>
After completion, create `.planning/phases/12-structural-refactor-and-tooling/12-06-SUMMARY.md`. STRUCT-03 and STRUCT-02 may be marked Complete on this plan.
</output>
