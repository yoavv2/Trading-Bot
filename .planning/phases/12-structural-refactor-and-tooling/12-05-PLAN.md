---
phase: 12-structural-refactor-and-tooling
plan: 05
type: execute
wave: 5
depends_on: ["12-04"]
files_modified:
  - src/trading_platform/services/reconciliation/__init__.py
  - src/trading_platform/services/reconciliation/snapshot.py
  - src/trading_platform/services/reconciliation/matcher.py
  - src/trading_platform/services/reconciliation/findings.py
  - src/trading_platform/services/reconciliation/report.py
  - src/trading_platform/services/reconciliation.py
  - src/trading_platform/services/reconciliation_matcher.py
  - src/trading_platform/services/reconciliation_types.py
  - src/trading_platform/worker/__main__.py
  - src/trading_platform/services/operator_reads.py
  - src/trading_platform/services/operator_status.py
  - tests/test_reconciliation_matcher.py
  - tests/test_reconciliation_types.py
  - tests/test_execution_reconciliation.py
autonomous: true
requirements: [STRUCT-05]
must_haves:
  truths:
    - "Reconciliation logic lives under services/reconciliation/{snapshot,matcher,findings,report}.py"
    - "Old scattered reconciliation modules (reconciliation.py, reconciliation_matcher.py, reconciliation_types.py) are deleted"
    - "All imports of reconciliation entrypoints resolve through the new package paths"
    - "The full suite passes with zero new or modified assertions"
  artifacts:
    - path: "src/trading_platform/services/reconciliation/matcher.py"
      provides: "match_snapshots + comparison helpers (moved from reconciliation_matcher.py)"
      contains: "def match_snapshots"
    - path: "src/trading_platform/services/reconciliation/report.py"
      provides: "reconcile_paper_execution orchestrator + apply_reconciliation_corrections + report materialization"
      contains: "def reconcile_paper_execution"
    - path: "src/trading_platform/services/reconciliation/findings.py"
      provides: "ReconciliationFinding closed enum + Finding types (from reconciliation_types.py)"
      contains: "ReconciliationFinding"
    - path: "src/trading_platform/services/reconciliation/snapshot.py"
      provides: "Local*Snapshot typed dataclasses + ReconciliationIdentity (from reconciliation_types.py)"
      contains: "Snapshot"
  key_links:
    - from: "src/trading_platform/services/reconciliation/report.py"
      to: "src/trading_platform/services/reconciliation/matcher.py"
      via: "import match_snapshots"
      pattern: "from trading_platform.services.reconciliation.matcher import"
    - from: "src/trading_platform/services/reconciliation/matcher.py"
      to: "src/trading_platform/services/config/tolerances.py"
      via: "import MONEY_TOLERANCE, QUANTITY_TOLERANCE"
      pattern: "from trading_platform.services.config.tolerances import"
---

<objective>
Reorganize reconciliation logic into the package declared by STRUCT-05: `services/reconciliation/{snapshot,matcher,findings,report}.py`. Three current modules split cleanly along the boundary the Phase-9 rewrite already established (typed snapshots, closed-enum findings, pure matcher, read-only orchestrator/report).

Purpose: STRUCT-05 requires the four-file reconciliation package. `reconciliation_types.py` splits into `snapshot.py` (typed snapshots) + `findings.py` (finding enum/types); `reconciliation_matcher.py` becomes `matcher.py`; `reconciliation.py` becomes `report.py` (the orchestrator + materialized report).
Output: `services/reconciliation/` package; old three modules deleted; importers repointed.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/trading_platform/services/reconciliation_types.py

<interfaces>
Current reconciliation modules and their split targets:
- `reconciliation_types.py` (197 lines): typed `Local*Snapshot` dataclasses + `ReconciliationIdentity` → `snapshot.py`; the closed 5-member `ReconciliationFinding` enum + `Finding` types → `findings.py`. (Move each definition to the file whose name matches its role.)
- `reconciliation_matcher.py` (452 lines): `match_snapshots`, `match_snapshots_with_comparisons`, comparison helpers, `_decimal_differs` → `matcher.py`. Its tolerance imports already come from `services/config/tolerances.py` (12-01) — keep that.
- `reconciliation.py` (769 lines): the read-only orchestrator `reconcile_paper_execution`, `apply_reconciliation_corrections`, the materialized-report builder, threshold/divergence evaluations → `report.py`.

Cross-module imports to preserve: `reconciliation.py` today imports `ExecutionOrderStatus, OrderSide` from `services.execution` (now the package, 12-03), `apply_order_transition`-family from what is now `execution.transition`, `match_snapshots` from the matcher, and the snapshot/finding types. Repoint all to the new package layout.

Known consumers to repoint (grep to confirm full set): `worker/__main__.py`, `operator_reads.py`, `operator_status.py`, tests `test_reconciliation_matcher.py`, `test_reconciliation_types.py`, `test_execution_reconciliation.py`.
</interfaces>

<key_facts>
- Package `__init__.py` re-exports the public surface so consumers import from `trading_platform.services.reconciliation`: `reconcile_paper_execution`, `apply_reconciliation_corrections`, `match_snapshots`, `match_snapshots_with_comparisons`, `ReconciliationFinding`, the `Local*Snapshot` dataclasses, `ReconciliationIdentity`, and the report/finding types. Provide `__all__`.
- Old `reconciliation.py`, `reconciliation_matcher.py`, `reconciliation_types.py` MUST be deleted (no shims). Creating the `reconciliation/` package shadows the old `reconciliation.py` module — ensure the old file is deleted so Python resolves the package.
- `matcher.py` must keep importing tolerances from `services/config/tolerances.py` (12-01), not redefine them.
- The Phase-9 semantics are frozen: read-only reconcile, correction as a separate entrypoint, closed finding enum, O(n) matcher. Verbatim move preserves them — do not adjust logic.
- Import-line edits in src AND tests are required and are NOT assertion changes. Assertion bodies frozen. No new tests.
</key_facts>

<zero_behavior_change_contract>
STRUCT-02 no-behavior-change refactor. Proof = existing suite green before and after with ZERO new/removed/modified assertions. No new tests. Repointing imports (incl. tests) permitted; assertion bodies frozen.
</zero_behavior_change_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the reconciliation package (snapshot, findings, matcher, report)</name>
  <files>src/trading_platform/services/reconciliation/__init__.py, src/trading_platform/services/reconciliation/snapshot.py, src/trading_platform/services/reconciliation/findings.py, src/trading_platform/services/reconciliation/matcher.py, src/trading_platform/services/reconciliation/report.py, src/trading_platform/services/reconciliation.py, src/trading_platform/services/reconciliation_matcher.py, src/trading_platform/services/reconciliation_types.py</files>
  <action>
    1. Create the `services/reconciliation/` package.
    2. `snapshot.py` ← the `Local*Snapshot` dataclasses + `ReconciliationIdentity` from reconciliation_types.py (verbatim).
    3. `findings.py` ← `ReconciliationFinding` enum + `Finding` types from reconciliation_types.py (verbatim).
    4. `matcher.py` ← full content of reconciliation_matcher.py (verbatim); repoint its snapshot/finding imports to `.snapshot`/`.findings`; keep tolerance import from `services/config/tolerances.py`.
    5. `report.py` ← full content of reconciliation.py (verbatim); repoint imports to `.matcher`, `.snapshot`, `.findings`, and the execution package (`execution.contracts`/`execution.transition`).
    6. `__init__.py` ← re-export the public surface with `__all__`.
    7. Sequencing (create -> move -> repoint -> verify -> delete-last): FORCED EXCEPTION — delete `services/reconciliation.py` NOW, at package creation, because its filename collides with the new `services/reconciliation/` package directory name and cannot survive as a shim. For the two distinctly-named modules, DEFER deletion to Task 2: turn `services/reconciliation_matcher.py` and `services/reconciliation_types.py` into TEMPORARY re-export shims (`from trading_platform.services.reconciliation.matcher import *`; `from ...snapshot import *` + `from ...findings import *`) so the project stays buildable. These shims are deleted in Task 2 after verification — they are NOT shipped.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -c "from trading_platform.services.reconciliation import reconcile_paper_execution, apply_reconciliation_corrections, match_snapshots, ReconciliationFinding; print('ok')" && PYTHONPATH=src .venv/bin/python -m trading_platform.worker --help >/dev/null && echo "cli ok"</automated>
  </verify>
  <done>reconciliation package exists with all four modules + re-exporting __init__; reconciliation.py deleted; reconciliation_matcher.py/reconciliation_types.py are temporary shims; import smoke AND `worker --help` both succeed.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint consumers and run affected suites</name>
  <files>src/trading_platform/worker/__main__.py, src/trading_platform/services/operator_reads.py, src/trading_platform/services/operator_status.py, tests/test_reconciliation_matcher.py, tests/test_reconciliation_types.py, tests/test_execution_reconciliation.py</files>
  <action>
    1. Grep: `grep -rn "services.reconciliation_matcher\|services.reconciliation_types\|services/reconciliation_matcher\|services/reconciliation_types\|from trading_platform.services.reconciliation import" src/ tests/`. Repoint each to the new package layout (`.matcher`, `.snapshot`, `.findings`, `.report`, or the package re-export). (The Task 1 temporary shims keep these imports working until you repoint them.)
    2. Repoint the three test files' import lines only (assertions frozen). `test_reconciliation_types.py` will now import snapshot/finding types from `services.reconciliation.snapshot` / `.findings`.
    3. Run the targeted suites (verify below) and confirm no stale references remain.
    4. DELETE-LAST: only after the grep is clean and the targeted suites are green, delete the temporary shims `services/reconciliation_matcher.py` and `services/reconciliation_types.py`. Re-run the verify to confirm nothing regressed.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/pytest tests/test_reconciliation_matcher.py tests/test_reconciliation_types.py tests/test_execution_reconciliation.py -q</automated>
  </verify>
  <done>All importers + tests repointed; grep for old module paths clean; reconciliation suites green with no assertion changes.</done>
</task>

</tasks>

<verification>
- `grep -rn "reconciliation_matcher\|reconciliation_types" src/ tests/` returns nothing referencing the deleted modules.
- `services/reconciliation.py` (module file) no longer exists; the `reconciliation/` package resolves.
- Zero-behavior-change: full suite pass count equals 12-BASELINE.md; no assertion changes.
</verification>

<success_criteria>
- Reconciliation reorganized under services/reconciliation/{snapshot,matcher,findings,report}.py; old modules deleted; imports resolve through new paths.
- Full suite green at baseline count with no assertion changes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-structural-refactor-and-tooling/12-05-SUMMARY.md`.
</output>
