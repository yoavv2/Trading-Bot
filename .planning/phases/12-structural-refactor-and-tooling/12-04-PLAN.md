---
phase: 12-structural-refactor-and-tooling
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - src/trading_platform/services/execution/submit_orders.py
  - src/trading_platform/services/execution/sync_orders.py
  - src/trading_platform/services/execution/_paper_common.py
  - src/trading_platform/services/execution/__init__.py
  - src/trading_platform/services/paper_execution.py
  - src/trading_platform/worker/__main__.py
  - src/trading_platform/services/operator_reads.py
  - src/trading_platform/services/operator_status.py
  - src/trading_platform/services/stale_runs.py
  - tests/test_paper_execution.py
  - tests/test_paper_preflight_query_count.py
  - tests/test_execution_reconciliation.py
autonomous: true
requirements: [STRUCT-04]
must_haves:
  truths:
    - "Paper order submission logic lives in execution/submit_orders.py and broker-state sync logic lives in execution/sync_orders.py"
    - "The old services/paper_execution.py module is deleted; its public entrypoints resolve through the execution package"
    - "All imports of paper-execution entrypoints resolve through the new execution package paths"
    - "The full suite passes with zero new or modified assertions"
  artifacts:
    - path: "src/trading_platform/services/execution/submit_orders.py"
      provides: "run_paper_order_submission, run_paper_session, build_client_order_id, session-plan + intent-decision logic"
      contains: "def run_paper_order_submission"
    - path: "src/trading_platform/services/execution/sync_orders.py"
      provides: "sync_paper_state and broker order/fill/position/account sync helpers"
      contains: "def sync_paper_state"
  key_links:
    - from: "src/trading_platform/worker/__main__.py"
      to: "src/trading_platform/services/execution/submit_orders.py"
      via: "import of run_paper_order_submission / run_paper_session"
      pattern: "from trading_platform.services.execution"
    - from: "src/trading_platform/services/execution/__init__.py"
      to: "src/trading_platform/services/execution/sync_orders.py"
      via: "re-export of sync_paper_state"
      pattern: "sync_paper_state"
---

<objective>
Complete STRUCT-04 by splitting the 2117-line `services/paper_execution.py` into the two remaining declared execution modules: `submit_orders.py` (submission + session orchestration + intent decisions) and `sync_orders.py` (broker-state sync: orders, fills, positions, account). Shared dataclasses and helpers used by both sides move to a `_paper_common.py` module inside the package.

This is the risk concentration of Phase 12. The proof of correctness is the unchanged existing suite (especially `tests/test_paper_execution.py`) staying green with zero assertion changes.

Purpose: STRUCT-04 requires `services/execution/{submit_orders,sync_orders,...}.py`. The submit and sync paths currently co-habit one oversized module; separating them along the existing internal seam completes the boundary.
Output: `submit_orders.py`, `sync_orders.py`, `_paper_common.py`; deleted `paper_execution.py`; package `__init__` re-exports; all importers repointed.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/trading_platform/services/paper_execution.py

<interfaces>
Explicit function → target-file mapping for the paper_execution.py split (line numbers are from the current file; move each definition verbatim):

SHARED → `_paper_common.py` (imported by both submit_orders and sync_orders):
- Dataclasses (72-176): PaperExecutionCandidate, PaperExecutionRunReport, PaperSessionRunReport, PaperSessionPlan, PaperIntentDecision, PaperStateSyncReport
- `_ensure_symbol` (2109), `_broker_transition_event` (1925), `_record_intent_decision_event` (1902)
- Any module-level constant referenced by both sides (identify during move)

SUBMIT side → `submit_orders.py`:
- resolve_submission_session (177), build_client_order_id (191)
- run_paper_order_submission (210), _run_paper_order_submission_guarded (263), run_paper_session (778)
- _resolve_source_risk_run (1097), _risk_event_side_priority (1150), _candidate_from_risk_event (1154)
- _load_submission_candidates (1174), _load_auto_resolve_candidates (1195), _load_paper_order_index (1266)
- _build_paper_session_plan (1310), _build_intent_decision (1405)
- _resolve_paper_intent_decision (1486), _resolve_paper_intent_decision_from_index (1541)
- _create_paper_execution_run (1584), _update_paper_execution_run (1624), _paper_order_payload (1661)
- _finalize_blocked_paper_execution_run (1697), _finalize_mid_run_kill_switch_halt (1766)
- _is_resubmittable_order (1810), _broker_has_touched_order (1821)
- schedule_reconciliation_after_partial_failure (1835)

SYNC side → `sync_orders.py`:
- sync_paper_state (1016)
- _sync_paper_orders (1941), _ingest_paper_fills (1992), _sync_positions_from_broker (2029), _record_broker_account_snapshot (2084)

Known consumers of paper_execution entrypoints (repoint all in Task 2; grep to confirm the full set):
`worker/__main__.py`, `operator_reads.py`, `operator_status.py`, `stale_runs.py`, tests `test_paper_execution.py`, `test_paper_preflight_query_count.py`, `test_execution_reconciliation.py`.
</interfaces>

<key_facts>
- `_paper_common.py` (leading underscore = package-internal) is the deliberate, documented home for the dataclasses and cross-cutting helpers shared by submit and sync. State this in the SUMMARY so the plan-checker does not read it as an undeclared extra surface — the STRUCT-04 named files are submit_orders/sync_orders/transition/idempotency; a shared-helper module for the split is a legitimate implementation detail, not a scope addition.
- `run_paper_session` orchestrates both submission and post-submit concerns; it lives in `submit_orders.py` (it is a submission-session runner) and imports sync helpers from `sync_orders.py` if needed. Watch for a circular import between submit_orders and sync_orders — if one appears, resolve by moving the truly-shared helper into `_paper_common.py`, not by cross-importing private names. The Task 1 import-smoke verify is designed to catch exactly this circularity.
- Package `__init__.py` MUST re-export the public entrypoints so consumers import from `trading_platform.services.execution`: `run_paper_order_submission`, `run_paper_session`, `sync_paper_state`, `build_client_order_id`, `resolve_submission_session`, `schedule_reconciliation_after_partial_failure`, and the public dataclasses/reports. Add to the `__all__` created in 12-03.
- Old `services/paper_execution.py` MUST be deleted (no shim) as the final step of Task 1. The package re-export is the compatibility surface. Do the split in ONE task (create all three modules + delete the old file together) — do NOT leave a half-moved paper_execution.py between tasks, because its residual functions would reference dataclasses now living in _paper_common.py and would not import.
- Preserve every hard invariant that STATE.md records for this file (lock-before-writes, running-row-first, reclaim-after-running-write, broker I/O outside any open transaction, durable independent commits, the 2-query preflight bound from PERF-01). A verbatim move preserves them; do not "improve" any logic.
- Import-line edits in src AND tests are required and are NOT assertion changes. Assertion bodies are frozen. No new tests.
</key_facts>

<zero_behavior_change_contract>
STRUCT-02 no-behavior-change refactor and the highest-risk one in the phase. Proof = existing suite green before and after with ZERO new/removed/modified assertions. Run `tests/test_paper_execution.py` and `tests/test_paper_preflight_query_count.py` (the PERF-01 2-query invariant) in Task 2. No characterization tests. Repointing imports (incl. tests) is permitted; assertion bodies frozen.
</zero_behavior_change_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split paper_execution.py into submit_orders + sync_orders + _paper_common; delete the old module</name>
  <files>src/trading_platform/services/execution/_paper_common.py, src/trading_platform/services/execution/submit_orders.py, src/trading_platform/services/execution/sync_orders.py, src/trading_platform/services/execution/__init__.py, src/trading_platform/services/paper_execution.py</files>
  <action>
    Do the entire split atomically in this task (mirrors 12-03/12-05 — create everything, delete the old module, verify by import-smoke; consumer repointing is Task 2):
    1. Create `_paper_common.py` with the SHARED dataclasses and helpers listed in the interfaces mapping, moved verbatim.
    2. Create `submit_orders.py` with the SUBMIT-side functions (verbatim); import shared names from `_paper_common` and execution contracts/transition/idempotency from the package.
    3. Create `sync_orders.py` with the SYNC-side functions (verbatim); import shared names from `_paper_common`.
    4. Update `execution/__init__.py` `__all__` + re-exports to expose all public paper-execution entrypoints (see key_facts).
    5. Delete `services/paper_execution.py` entirely.
    6. If a circular import between submit_orders and sync_orders surfaces in the smoke test, relocate the genuinely-shared helper into `_paper_common.py`.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -c "from trading_platform.services.execution import run_paper_order_submission, run_paper_session, sync_paper_state, build_client_order_id, PaperExecutionCandidate; print('ok')"</automated>
  </verify>
  <done>_paper_common.py, submit_orders.py, sync_orders.py exist with the mapped functions; paper_execution.py deleted; __init__ re-exports all public entrypoints; import-smoke (incl. no circular import) passes.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint all consumers + tests and run the affected suites</name>
  <files>src/trading_platform/worker/__main__.py, src/trading_platform/services/operator_reads.py, src/trading_platform/services/operator_status.py, src/trading_platform/services/stale_runs.py, tests/test_paper_execution.py, tests/test_paper_preflight_query_count.py, tests/test_execution_reconciliation.py</files>
  <action>
    1. Grep every consumer: `grep -rn "services.paper_execution\|services/paper_execution\|from trading_platform.services.paper_execution" src/ tests/`. Repoint each to `trading_platform.services.execution` (package re-export) or the specific submodule. Consumers include worker/__main__.py, operator_reads.py, operator_status.py, stale_runs.py and the three test files.
    2. Repoint test imports (import lines only — assertions frozen).
    3. Confirm the grep from step 1 returns nothing after repointing.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/pytest tests/test_paper_execution.py tests/test_paper_preflight_query_count.py tests/test_execution_reconciliation.py -q</automated>
  </verify>
  <done>All consumers + tests repointed; grep for old module path clean; targeted suites (incl. PERF-01 preflight 2-query invariant) green with no assertion changes.</done>
</task>

</tasks>

<verification>
- `grep -rn "paper_execution" src/ tests/` returns nothing referencing the deleted module (matches on the new submodule names are fine).
- `services/paper_execution.py` no longer exists; submit_orders.py + sync_orders.py + _paper_common.py exist.
- Zero-behavior-change: full suite (`PYTHONPATH=src .venv/bin/pytest -q`) pass count equals 12-BASELINE.md; PERF-01 2-query invariant test green; no assertion changes.
- STRUCT-04 is complete once this plan lands (transition + idempotency from 12-03; submit_orders + sync_orders here).
</verification>

<success_criteria>
- Execution package holds all four declared modules (submit_orders, sync_orders, transition, idempotency); old paper_execution.py and standalone modules deleted; imports resolve through new paths.
- Every hard execution invariant preserved (verbatim move); full suite green at baseline count with no assertion changes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-structural-refactor-and-tooling/12-04-SUMMARY.md`. STRUCT-04 may be marked Complete on this plan.
</output>
