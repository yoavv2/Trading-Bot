---
phase: 12-structural-refactor-and-tooling
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - src/trading_platform/services/execution/__init__.py
  - src/trading_platform/services/execution/contracts.py
  - src/trading_platform/services/execution/transition.py
  - src/trading_platform/services/execution/idempotency.py
  - src/trading_platform/services/execution.py
  - src/trading_platform/services/order_state_machine.py
  - src/trading_platform/services/order_identity.py
  - src/trading_platform/services/reconciliation.py
  - src/trading_platform/services/paper_execution.py
  - tests/test_order_state_machine.py
  - tests/test_alpaca_execution.py
autonomous: true
requirements: [STRUCT-04]
must_haves:
  truths:
    - "services.execution is a package; transition logic lives in transition.py and idempotency/client-order-id logic lives in idempotency.py"
    - "The execution ABC and order enums resolve through the execution package, not a standalone execution.py module"
    - "All imports of order_state_machine and order_identity resolve through the new execution package paths"
    - "The full suite passes with zero new or modified assertions"
  artifacts:
    - path: "src/trading_platform/services/execution/transition.py"
      provides: "apply_order_transition and the closed transition map (moved from order_state_machine.py)"
      contains: "def"
    - path: "src/trading_platform/services/execution/idempotency.py"
      provides: "deterministic client_order_id derivation (moved from order_identity.py)"
      contains: "def"
    - path: "src/trading_platform/services/execution/__init__.py"
      provides: "Public re-exports (OrderSide, ExecutionOrderStatus, ExecutionService, transition + idempotency entrypoints)"
      contains: "import"
  key_links:
    - from: "src/trading_platform/services/reconciliation.py"
      to: "src/trading_platform/services/execution/transition.py"
      via: "import of order-transition symbols"
      pattern: "from trading_platform.services.execution"
---

<objective>
Convert `services.execution` from a single module into the package declared by STRUCT-04, and relocate the low-risk, clearly-separable execution pieces: the transition state machine and the idempotency (client_order_id) logic.

This is STRUCT-04 part 1 of 2. The heavy `paper_execution.py` submit/sync split is 12-04. Splitting the renames out first gives 12-04 a stable execution package to land into.

Purpose: STRUCT-04 requires `services/execution/{submit_orders,sync_orders,transition,idempotency}.py`. `transition` and `idempotency` already exist as standalone modules (`order_state_machine.py`, `order_identity.py`) and move cleanly; the execution ABC/enums become the package's shared contracts.
Output: `services/execution/` package with `__init__.py`, `contracts.py`, `transition.py`, `idempotency.py`; old standalone modules deleted; importers repointed.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
Modules being reorganized (current standalone files):
- `services/execution.py` (87 lines): `OrderSide`, `OrderType`, `OrderTimeInForce`, `ExecutionOrderStatus` (StrEnums), `OrderIntent`, `OrderSubmissionResult` (dataclasses), `ExecutionService` (ABC), `PlaceholderExecutionService`.
- `services/order_state_machine.py` (291 lines): the closed transition map + `apply_order_transition` + `IllegalOrderTransition` (ORDER-01..07 kernel).
- `services/order_identity.py` (152 lines): deterministic `client_order_id` derivation (IDEM-01).

Known consumers to repoint (grep to find all — this list is the floor, not the ceiling):
- `services.execution` symbols: `reconciliation.py` (`ExecutionOrderStatus, OrderSide`), `paper_execution.py`, `alpaca.py`, `reconciliation_types.py`, tests.
- `order_state_machine`: `reconciliation.py`, `paper_execution.py`, `tests/test_order_state_machine.py`.
- `order_identity`: `paper_execution.py`, tests.
</interfaces>

<key_facts>
- Target package layout after this plan: `services/execution/__init__.py`, `contracts.py` (the ABC + enums + OrderIntent/OrderSubmissionResult from old execution.py), `transition.py` (from order_state_machine.py), `idempotency.py` (from order_identity.py). `submit_orders.py` and `sync_orders.py` are added by 12-04 — do NOT create them here.
- `__init__.py` MUST re-export the widely-used public names (`OrderSide`, `OrderType`, `OrderTimeInForce`, `ExecutionOrderStatus`, `OrderIntent`, `OrderSubmissionResult`, `ExecutionService`, `PlaceholderExecutionService`, plus the transition entrypoint `apply_order_transition`/`IllegalOrderTransition` and the idempotency entrypoint) so `from trading_platform.services.execution import OrderSide` keeps resolving. This minimizes churn and is how "imports resolve through the new path" is satisfied.
- Old standalone modules `services/execution.py`, `services/order_state_machine.py`, `services/order_identity.py` MUST be deleted (no shim modules). Package `__init__` re-export is the compatibility surface, not a leftover module.
- Import-line edits in src AND tests are required and are NOT assertion changes. Assertion bodies are frozen.
</key_facts>

<zero_behavior_change_contract>
STRUCT-02 no-behavior-change refactor. The proof is the existing suite green before and after with ZERO new/removed/modified assertions. No new tests. Repointing imports (including in test files) is permitted; assertion bodies must not change. STRUCT-04 is NOT marked complete on this plan — 12-04 completes it.
</zero_behavior_change_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the execution package — contracts, transition, idempotency</name>
  <files>src/trading_platform/services/execution/__init__.py, src/trading_platform/services/execution/contracts.py, src/trading_platform/services/execution/transition.py, src/trading_platform/services/execution/idempotency.py, src/trading_platform/services/execution.py, src/trading_platform/services/order_state_machine.py, src/trading_platform/services/order_identity.py</files>
  <action>
    1. Create the `services/execution/` package directory.
    2. `contracts.py` ← the entire content of the old `services/execution.py` (enums, OrderIntent, OrderSubmissionResult, ExecutionService ABC, PlaceholderExecutionService), verbatim.
    3. `transition.py` ← the entire content of `services/order_state_machine.py`, verbatim (update any intra-move imports of execution enums to `from trading_platform.services.execution.contracts import ...` or rely on package re-export).
    4. `idempotency.py` ← the entire content of `services/order_identity.py`, verbatim.
    5. `__init__.py` ← re-export the public names listed in key_facts from contracts/transition/idempotency, with an `__all__`.
    6. Delete the three old standalone module files (`execution.py`, `order_state_machine.py`, `order_identity.py`).
    Note: creating `services/execution/` shadows the old `services/execution.py` — ensure the old file is deleted so Python resolves the package.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/python -c "from trading_platform.services.execution import OrderSide, ExecutionOrderStatus, ExecutionService, apply_order_transition; from trading_platform.services.execution.transition import apply_order_transition; from trading_platform.services.execution.idempotency import *; print('ok')"</automated>
  </verify>
  <done>execution package exists with contracts/transition/idempotency + re-exporting __init__; old three modules deleted; import smoke test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint all consumers and run the affected suites</name>
  <files>src/trading_platform/services/reconciliation.py, src/trading_platform/services/paper_execution.py, tests/test_order_state_machine.py, tests/test_alpaca_execution.py</files>
  <action>
    1. Grep for every remaining reference to the deleted module paths: `grep -rn "services.order_state_machine\|services/order_state_machine\|services.order_identity\|services/order_identity" src/ tests/`. Repoint each to `trading_platform.services.execution.transition` / `...idempotency` (or the package re-export).
    2. Verify `from trading_platform.services.execution import ...` still resolves for all prior consumers (reconciliation.py line ~38, etc.) — package `__init__` re-export should make these work unchanged; if any imported a name not re-exported, add it to `__all__`.
    3. Repoint test imports in `tests/test_order_state_machine.py` and `tests/test_alpaca_execution.py`. Change ONLY import lines.
    4. Confirm no stale references remain (the grep from step 1 returns nothing).
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/pytest tests/test_order_state_machine.py tests/test_alpaca_execution.py tests/test_execution_reconciliation.py tests/test_paper_execution.py -q</automated>
  </verify>
  <done>All importers repointed; grep for old module paths clean; affected suites green with no assertion changes.</done>
</task>

</tasks>

<verification>
- `grep -rn "order_state_machine\|order_identity" src/ tests/` returns nothing referencing the deleted standalone modules.
- execution package resolves; `services/execution.py` file no longer exists.
- Zero-behavior-change: full suite (`PYTHONPATH=src .venv/bin/pytest -q`) pass count equals 12-BASELINE.md; no assertion changes.
</verification>

<success_criteria>
- transition and idempotency live under the execution package; execution ABC/enums resolve through it; old standalone modules deleted.
- Full suite green at baseline count with no assertion changes.
- STRUCT-04 remains open (completed by 12-04).
</success_criteria>

<output>
After completion, create `.planning/phases/12-structural-refactor-and-tooling/12-03-SUMMARY.md`.
</output>
