---
phase: 12-structural-refactor-and-tooling
plan: 07
type: execute
wave: 7
depends_on: ["12-06"]
files_modified:
  - .pre-commit-config.yaml
  - pyproject.toml
tools_scope_note: "ruff format may reformat many existing files; mypy fixes are annotation-only"
autonomous: true
requirements: [TOOL-01, TOOL-02]
must_haves:
  truths:
    - "A pre-commit hook runs ruff lint + format and blocks the commit when they fail"
    - "A pre-commit hook runs mypy over the execution, reconciliation, and config modules and blocks the commit on type errors in those modules"
    - "pre-commit run --all-files passes on the current tree"
    - "The full test suite still passes"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "ruff (lint+format) and mypy pre-commit hooks that block on failure"
      contains: "ruff"
    - path: "pyproject.toml"
      provides: "[tool.ruff] and [tool.mypy] configuration + ruff/mypy/pre-commit dev dependencies"
      contains: "tool.ruff"
  key_links:
    - from: ".pre-commit-config.yaml"
      to: "pyproject.toml"
      via: "ruff/mypy read their config from pyproject"
      pattern: "ruff"
---

<objective>
Wire the merge-blocking quality gates as LOCAL pre-commit hooks (the operator-locked decision — NOT GitHub Actions CI; this repo has no CI infrastructure and none is added this phase):
- TOOL-01: ruff lint + format, blocking on pre-commit.
- TOOL-02: mypy over the execution, reconciliation, and config modules, blocking on pre-commit.

This runs last so the tools target the FINAL module structure produced by 12-02 through 12-06.

Purpose: STRUCT refactors reorganized the code; TOOL requirements make lint/format and type errors block a commit so the new boundaries stay clean.
Output: `.pre-commit-config.yaml`, `[tool.ruff]`/`[tool.mypy]` config + dev deps in `pyproject.toml`.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@pyproject.toml

<key_facts>
- LOCAL pre-commit only. No `.github/workflows`, no CI YAML. The gate mechanism is `.pre-commit-config.yaml` + installed git hook.
- No `.pre-commit-config.yaml` exists today; ruff/mypy/pre-commit are NOT installed. Add them to `pyproject.toml [project.optional-dependencies] dev` and install into `.venv`.
- Python target: `requires-python = ">=3.12"`. Source lives under `src/` (package-dir `{"" = "src"}`); tests under `tests/`. Ruff and mypy must know `src` is the package root (`PYTHONPATH=src` is how everything runs).
- TOOL-02 mypy scope is EXACTLY the execution, reconciliation, and config modules — after Phase-12 moves these are:
  - `src/trading_platform/services/execution/` (submit_orders, sync_orders, transition, idempotency, contracts, _paper_common)
  - `src/trading_platform/services/reconciliation/` (snapshot, matcher, findings, report)
  - `src/trading_platform/services/config/` (validation, secrets, tolerances)
  mypy need not (and per the requirement, is not required to) pass over the whole repo — scope it to these three packages so the gate is achievable and targeted.
- ruff (TOOL-01) applies repo-wide (lint + format). Running `ruff format` will likely produce a large formatting diff across existing files — that is acceptable (formatting is not a behavior change and touches no assertions). Do it as part of this plan so the very first hook run is green.
- This is the ONLY Phase-12 plan permitted to change file formatting broadly. It adds no test assertions; the suite must stay green.
</key_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TOOL-01 — ruff config, format pass, and blocking pre-commit hook</name>
  <files>pyproject.toml, .pre-commit-config.yaml</files>
  <action>
    1. Add `ruff`, `mypy`, and `pre-commit` to `pyproject.toml [project.optional-dependencies] dev`; install into `.venv` (`.venv/bin/pip install -e ".[dev]"` or pin versions).
    2. Add a `[tool.ruff]` section to pyproject: set `target-version = "py312"`, `src = ["src", "tests"]`, `line-length` (choose a sane value, e.g. 100 — the codebase already uses `# noqa: E501` in places, so match existing width), and a reasonable default lint rule set (pycodestyle/pyflakes/isort at minimum). Keep the initial rule set pragmatic so the first run can pass after formatting.
    3. Run `.venv/bin/ruff check --fix src tests` and `.venv/bin/ruff format src tests` to bring the tree into compliance. Address remaining lint findings (or narrowly `# noqa` genuinely intentional ones, matching the existing eslint-disable-style discipline in STATE.md).
    4. Create `.pre-commit-config.yaml` with a ruff hook (lint) and a ruff-format hook, both from the official `astral-sh/ruff-pre-commit` repo, configured to read pyproject. These hooks must exit non-zero (block the commit) on any violation.
    5. Install the git hook: `.venv/bin/pre-commit install`.
    6. Confirm the full test suite is still green after the format pass (`PYTHONPATH=src .venv/bin/pytest -q`, count equals 12-BASELINE.md).
  </action>
  <verify>
    <automated>.venv/bin/ruff check src tests && .venv/bin/ruff format --check src tests</automated>
  </verify>
  <done>ruff check + format-check pass on the tree; .pre-commit-config.yaml has blocking ruff lint + format hooks; git hook installed; suite green.</done>
</task>

<task type="auto">
  <name>Task 2: TOOL-02 — mypy over execution/reconciliation/config, blocking on pre-commit</name>
  <files>pyproject.toml, .pre-commit-config.yaml</files>
  <action>
    1. Add a `[tool.mypy]` section to pyproject: `python_version = "3.12"`, `mypy_path = "src"`, `explicit_package_bases = true` (or `namespace_packages`), and a pragmatic strictness (at minimum `warn_unused_ignores`, `no_implicit_optional`; full `strict` only if the three target packages already pass — do not weaken behavior to force strict). Restrict the checked paths to the three package groups (via `files = [...]` for the three package dirs, or per-hook args).
    2. Run `.venv/bin/mypy` over exactly `services/execution`, `services/reconciliation`, `services/config`. Resolve reported type errors with ANNOTATION-ONLY changes (add/adjust type hints, `cast`, typed `TypedDict`/`dataclass` fields). Do NOT change runtime behavior or any test assertion. If a genuine untyped third-party import blocks the gate, add a targeted `[[tool.mypy.overrides]]` `ignore_missing_imports` for that module only.
    3. Add a mypy hook to `.pre-commit-config.yaml` (local hook running the venv mypy, or the mirrors-mypy repo) scoped to the three packages, exiting non-zero on type errors so it blocks the commit.
    4. Prove the gate blocks: run `.venv/bin/pre-commit run --all-files` — it must pass on the clean tree. Then verify blocking behavior by introducing a deliberate type error in one execution module, running the mypy hook, confirming non-zero exit, and reverting.
    5. Re-run the full test suite; confirm green at the baseline count (annotation-only changes must not alter behavior).
  </action>
  <verify>
    <automated>.venv/bin/mypy src/trading_platform/services/execution src/trading_platform/services/reconciliation src/trading_platform/services/config && PYTHONPATH=src .venv/bin/pytest -q 2>&1 | tail -3</automated>
  </verify>
  <done>mypy passes over the three package groups; a blocking mypy pre-commit hook exists and rejects a deliberate type error; suite green at baseline.</done>
</task>

</tasks>

<verification>
- TOOL-01: `.venv/bin/pre-commit run ruff --all-files` and `... ruff-format --all-files` pass; both block on a deliberate violation.
- TOOL-02: `.venv/bin/pre-commit run mypy --all-files` passes over the three package groups and blocks on a deliberate type error.
- `.venv/bin/pre-commit run --all-files` is green on the final tree; full suite green at 12-BASELINE.md count.
</verification>

<success_criteria>
- A local pre-commit gate blocks commits on ruff lint/format failure (TOOL-01) and on mypy type errors in execution/reconciliation/config modules (TOOL-02).
- Full test suite still green; no test assertions changed.
</success_criteria>

<output>
After completion, create `.planning/phases/12-structural-refactor-and-tooling/12-07-SUMMARY.md`. TOOL-01 and TOOL-02 may be marked Complete on this plan.
</output>
