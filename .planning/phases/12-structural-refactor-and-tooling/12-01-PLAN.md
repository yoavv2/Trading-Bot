---
phase: 12-structural-refactor-and-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_platform/services/config/tolerances.py
  - src/trading_platform/services/reconciliation.py
  - src/trading_platform/services/reconciliation_matcher.py
  - .planning/phases/12-structural-refactor-and-tooling/12-BASELINE.md
autonomous: true
requirements: [STRUCT-01, STRUCT-07]
must_haves:
  truths:
    - "The Tier-0 correctness gate is confirmed GREEN before any Tier-3 refactor code is written"
    - "The full existing test suite passes and its exact pass count is captured as the phase baseline"
    - "Reconciliation money/quantity tolerances resolve from one typed module, not per-file duplicated constants"
    - "The full suite still passes after tolerance extraction with zero new or modified assertions"
  artifacts:
    - path: "src/trading_platform/services/config/tolerances.py"
      provides: "Single typed source for MONEY_TOLERANCE and QUANTITY_TOLERANCE"
      contains: "MONEY_TOLERANCE"
    - path: ".planning/phases/12-structural-refactor-and-tooling/12-BASELINE.md"
      provides: "Captured pytest baseline count all later Phase-12 plans must reproduce"
      contains: "passed"
  key_links:
    - from: "src/trading_platform/services/reconciliation.py"
      to: "src/trading_platform/services/config/tolerances.py"
      via: "import of MONEY_TOLERANCE"
      pattern: "from trading_platform.services.config.tolerances import"
    - from: "src/trading_platform/services/reconciliation_matcher.py"
      to: "src/trading_platform/services/config/tolerances.py"
      via: "import of MONEY_TOLERANCE, QUANTITY_TOLERANCE"
      pattern: "from trading_platform.services.config.tolerances import"
---

<objective>
Establish the Phase-12 refactor preconditions and land the first, lowest-risk structural move.

This plan does two things:
1. **STRUCT-01 gate** — prove the Tier-0 correctness kernel is verified complete BEFORE any Tier-3 refactor lands, and capture the current full-suite pass count as the immutable baseline every subsequent Phase-12 plan must reproduce unchanged.
2. **STRUCT-07** — extract the scattered reconciliation tolerance constants into one typed config module and delete the duplicated definitions.

Purpose: A refactor that changes behavior is a bug. This plan pins the "before" state (green suite) so every later plan can prove "after == before", and it retires the first piece of duplication under that guarantee.
Output: `services/config/tolerances.py`, updated reconciliation modules, and `12-BASELINE.md`.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/00-VERIFY.md

<key_facts>
- STRUCT-01 verification is against REAL artifacts, NOT the REQUIREMENTS.md checkboxes. `.planning/milestones/v1.1-paused/REQUIREMENTS.md` was last edited 2026-04-18 and its Tier-0 rows (ORDER/IDEM/LOCK/RECON) still show `[ ]`/Pending even though STATE.md records Phases 7-11 complete. Do NOT gate on those checkboxes — they are stale. Gate on: `.planning/00-VERIFY.md` Status is GREEN (it is), AND the full suite runs green right now.
- Test command: `PYTHONPATH=src .venv/bin/pytest -q` (see Makefile). The 00-VERIFY snapshot was `148 passed`; Phases 8-11 added tests, so the current count is higher — capture the ACTUAL number this run produces.
- Duplicated tolerance constants today:
  - `services/reconciliation.py:59` → `_MONEY_TOLERANCE = Decimal("0.01")`
  - `services/reconciliation_matcher.py:58` → `_MONEY_TOLERANCE = Decimal("0.01")`
  - `services/reconciliation_matcher.py:59` → `_QUANTITY_TOLERANCE = Decimal("0.000001")`
- `MONEY_SCALE = Decimal("0.000001")` in backtest_reporting.py / backtesting.py / portfolio.py is a Decimal quantization SCALE, not a comparison tolerance. It is OUT OF SCOPE for STRUCT-07 — do not touch it.
- `services/config/` does not exist yet; create the package (`__init__.py` + `tolerances.py`). Later plans (12-02) add `validation.py`/`secrets.py` to the same package.
</key_facts>

<zero_behavior_change_contract>
This is a Tier-3 no-behavior-change refactor (STRUCT-02). The proof of correctness is the UNCHANGED existing test suite staying green — before and after — with ZERO new, removed, or modified assertions. Do NOT add characterization tests, new test files, or new assertions for the tolerance move. Editing an `import` statement (in src or tests) is NOT an assertion change and is permitted; changing the body of any `assert`/`pytest.raises`/expected-value is forbidden.
</zero_behavior_change_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STRUCT-01 gate — verify Tier-0 green and capture the phase baseline</name>
  <files>.planning/phases/12-structural-refactor-and-tooling/12-BASELINE.md</files>
  <action>
    Confirm the Tier-0 precondition before writing any refactor code:
    1. Confirm `.planning/00-VERIFY.md` Status line reads GREEN (read the file; it does).
    2. Run the full suite: `PYTHONPATH=src .venv/bin/pytest -q`. It MUST be all-green (0 failed, 0 error). Capture the exact summary line (e.g. `N passed in Xs`).
    3. Write `12-BASELINE.md` recording: the exact pass count `N`, the command used, the date, and a one-line statement that this count is the invariant every Phase-12 plan must reproduce unchanged (no new/modified assertions).
    If the suite is NOT green, STOP — do not proceed to Task 2. Report the failure; Tier-3 refactor is blocked by STRUCT-01 until it is green.
    NOTE the known environmental flake in STATE.md blockers: a `pg_terminate_backend` teardown `InsufficientPrivilege` can non-deterministically fail one unrelated market-data test under parallel load. If (and only if) the single failure is that exact teardown-privilege flake, re-run once to confirm green; document it if it recurs.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/pytest -q 2>&1 | tail -3</automated>
  </verify>
  <done>Full suite green; 12-BASELINE.md exists recording the exact pass count and command.</done>
</task>

<task type="auto">
  <name>Task 2: STRUCT-07 — extract reconciliation tolerances into one typed module</name>
  <files>src/trading_platform/services/config/__init__.py, src/trading_platform/services/config/tolerances.py, src/trading_platform/services/reconciliation.py, src/trading_platform/services/reconciliation_matcher.py</files>
  <action>
    1. Create `services/config/__init__.py` (empty package marker) and `services/config/tolerances.py` with typed module constants:
       `MONEY_TOLERANCE: Final[Decimal] = Decimal("0.01")` and `QUANTITY_TOLERANCE: Final[Decimal] = Decimal("0.000001")`, with a short docstring naming them the single source of reconciliation comparison tolerances. Keep the EXACT same Decimal values — byte-for-byte, no rounding-behavior change.
    2. In `reconciliation.py`: delete the local `_MONEY_TOLERANCE` definition (line ~59) and import `MONEY_TOLERANCE` from `trading_platform.services.config.tolerances`; update the ~4 usages (`_decimal_differs(..., tolerance=_MONEY_TOLERANCE)`) to reference the imported name.
    3. In `reconciliation_matcher.py`: delete local `_MONEY_TOLERANCE` and `_QUANTITY_TOLERANCE` (lines ~58-59) and import both from the new module; update usages.
    4. Do NOT touch `MONEY_SCALE` anywhere. Do NOT change any tolerance VALUE. This is a pure relocation.
    Confirm no stale `_MONEY_TOLERANCE`/`_QUANTITY_TOLERANCE` definitions remain: `grep -rn "_MONEY_TOLERANCE\|_QUANTITY_TOLERANCE" src/` should return nothing after the move.
  </action>
  <verify>
    <automated>PYTHONPATH=src .venv/bin/pytest tests/test_reconciliation_matcher.py tests/test_execution_reconciliation.py tests/test_reconciliation_types.py -q</automated>
  </verify>
  <done>tolerances.py owns both constants; old definitions deleted (grep clean); reconciliation tests green with zero assertion changes.</done>
</task>

</tasks>

<verification>
- STRUCT-01: `.planning/00-VERIFY.md` GREEN confirmed; full suite green; baseline count captured in 12-BASELINE.md.
- STRUCT-07: `grep -rn "_MONEY_TOLERANCE\|_QUANTITY_TOLERANCE" src/` returns nothing; both consumers import from `services/config/tolerances.py`.
- Zero-behavior-change (STRUCT-02 property): full suite pass count equals the captured baseline; no assertion added/removed/modified. Run `PYTHONPATH=src .venv/bin/pytest -q` and compare to 12-BASELINE.md.
</verification>

<success_criteria>
- Tier-0 gate proven green and baseline captured before any refactor code lands.
- One typed tolerances module is the sole source of the reconciliation tolerances; duplicates deleted.
- Full suite pass count unchanged from baseline; no assertions changed.
</success_criteria>

<output>
After completion, create `.planning/phases/12-structural-refactor-and-tooling/12-01-SUMMARY.md`.
</output>
