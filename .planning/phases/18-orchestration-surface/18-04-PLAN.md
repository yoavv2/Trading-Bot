---
phase: 18-orchestration-surface
plan: 04
type: execute
wave: 3
depends_on: [18-03]
files_modified:
  - src/trading_platform/api/dependencies.py
  - src/trading_platform/api/routes/jobs.py
  - tests/test_job_api.py
  - tests/test_job_mutation_api.py
autonomous: true
requirements: [ORCH-01, ORCH-03, ORCH-04]
must_haves:
  truths:
    - "Clients submit only a registered generic Job through `POST /api/v1/jobs`; unsupported types receive typed 422 before persistence (D-04, D-05)."
    - "Both mutation routes require `Idempotency-Key`; missing keys receive typed 400 and perform no mutation (D-06)."
    - "New submission is 202; exact submission replay is 200 with `Idempotency-Replayed: true`; mismatch is typed 409 naming the original Job (D-09, D-10, D-19)."
    - "`POST /api/v1/jobs/{job_id}/cancel` exposes Phase 17 cancellation with D-12 through D-15 outcomes and the shared compact reference (D-11, D-12, D-13, D-14, D-15, D-20)."
    - "Mutation responses expose only the compact relative-link Job reference; clients observe state/progress/logs/events through existing GET routes (D-16, D-17, D-18)."
  artifacts:
    - path: "src/trading_platform/api/routes/jobs.py"
      provides: "Thin POST submission/cancellation adapters plus unchanged GET observation routes"
      exports: ["submit_job", "cancel_job"]
    - path: "src/trading_platform/api/dependencies.py"
      provides: "JobRegistry and JobOrchestrationService providers"
    - path: "tests/test_job_mutation_api.py"
      provides: "HTTP status/header/body/no-write contract tests"
    - path: "tests/test_job_api.py"
      provides: "Existing read contract plus exact route-method fence"
  key_links:
    - from: "src/trading_platform/api/routes/jobs.py"
      to: "src/trading_platform/orchestration/job_mutations.py"
      via: "Depends-injected JobOrchestrationService only"
      pattern: "JobOrchestrationService"
    - from: "mutation response links"
      to: "src/trading_platform/api/routes/jobs.py GET routes"
      via: "stable relative /api/v1/jobs/{job_id} paths"
      pattern: "/api/v1/jobs/"
---

<objective>
Expose the generic idempotent submission and cancellation contracts as thin FastAPI adapters over the application orchestration layer.

Purpose: Make HTTP the one public Job mutation surface while retaining Phase 17's transport-agnostic observation routes. HTTP code parses, selects statuses/headers, and translates typed outcomes only; it contains no registry, fingerprint, transaction, lifecycle, or domain-operation logic.
Output: Two POST routes, dependency providers, exact typed response semantics, and real-PostgreSQL API contract tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-orchestration-surface/18-CONTEXT.md
@.planning/phases/18-orchestration-surface/18-RESEARCH.md
@.planning/phases/18-orchestration-surface/18-PATTERNS.md
@.planning/phases/18-orchestration-surface/18-UI-SPEC.md
@.planning/phases/18-orchestration-surface/18-03-SUMMARY.md
@src/trading_platform/api/dependencies.py
@src/trading_platform/api/routes/jobs.py
@src/trading_platform/jobs/registry.py
@tests/test_job_api.py

<interfaces>
Consume Plan 03 exactly:
- `JobOrchestrationService.submit(job_type=..., payload=..., idempotency_key=...) -> MutationResult`.
- `JobOrchestrationService.cancel(job_id=..., reason=..., idempotency_key=...) -> MutationResult`.
- `MutationResult.reference.to_dict()` returns the exact compact shape; adapters do not rebuild it.
- Typed exception fields drive deliberate HTTP mappings.

HTTP request/error contract selected under allowed discretion:
- `SubmitJobRequest`: normalized nonblank `job_type` (max 64) and `payload` constrained to a JSON object; no production operation literals or dependencies field.
- `CancelJobRequest`: optional `reason`; service performs trim/blank/max-500 normalization before mutation.
- Error JSON uses `detail.code` with stable values `missing_idempotency_key`, `invalid_idempotency_key`, `unknown_job_type`, `invalid_cancellation_reason`, `job_not_found`, `job_not_cancellable`, and locked `idempotency_key_conflict`. Conflict detail also includes `original_job_id`; terminal detail includes `job_id` and `status`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Job registry/orchestration dependencies and thin POST adapters</name>
  <read_first>
    - tests/test_job_api.py
    - src/trading_platform/api/dependencies.py
    - src/trading_platform/api/routes/jobs.py
    - src/trading_platform/orchestration/job_mutations.py
    - src/trading_platform/jobs/registry.py
    - src/trading_platform/api/routes/analytics.py
    - .planning/phases/18-orchestration-surface/18-UI-SPEC.md
  </read_first>
  <files>src/trading_platform/api/dependencies.py, src/trading_platform/api/routes/jobs.py, tests/test_job_api.py</files>
  <action>In `api/dependencies.py`, alias the existing strategy registry builder clearly, add `get_job_registry(request)` that returns an injected `app.state.job_registry` when present or `jobs.registry.build_default_registry(settings)` otherwise, and add `get_job_orchestration_service(request)` constructing `JobOrchestrationService(settings, registry)`. In `api/routes/jobs.py`, remove the obsolete read-only scope prose but leave all five GET behaviors unchanged. Define request models and add `POST ""` plus `POST "/{job_id}/cancel"`. Parse `Idempotency-Key` as optional with explicit alias so absence reaches typed service mapping rather than FastAPI's automatic 422. Return Plan 03's reference dictionary directly. Set submission default 202; on `result.replayed`, set 200 plus exact header `Idempotency-Replayed: true`. Cancellation returns 200 for accepted/no-op/current-reference outcomes and may set the same replay header only for exact-key replay. Map only the named typed exceptions to the exact 400/404/409/422 detail contract; do not query ORM rows, resolve registries, hash payloads, call `submit_job`/`request_cancellation`, import domain operation services, or derive absolute URLs in routes. In this task, replace the obsolete read-only fence in `tests/test_job_api.py` with the aggregated exact route-method contract so existing Job API tests remain runnable immediately after adding POST routes.</action>
  <acceptance_criteria>
    - OpenAPI/runtime routes include `POST /api/v1/jobs` and `POST /api/v1/jobs/{job_id}/cancel` alongside all existing GET paths.
    - A route source scan finds calls only to `JobOrchestrationService.submit/cancel` for mutation behavior and no imports from `trading_platform.services` domain-operation modules, `trading_platform.worker`, or SQLAlchemy ORM.
    - Missing header maps to 400 rather than FastAPI 422; unknown registered type maps to 422; conflicts map to 409; missing cancellation target maps to 404.
    - The production fallback registry remains `build_default_registry()` and therefore has zero Phase 19 handlers per D-01/D-02.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_job_api.py -x -q</automated>
  </verify>
  <done>The API exposes exactly two Job mutations as HTTP-only adapters over one shared application service and preserves every existing observation route.</done>
</task>

<task type="auto">
  <name>Task 2: Pin all HTTP statuses, headers, compact bodies, and zero-write rejections</name>
  <read_first>
    - tests/test_job_mutation_api.py
    - tests/test_job_api.py
    - tests/test_job_runner.py
    - tests/test_job_orchestration.py
    - src/trading_platform/api/routes/jobs.py
    - src/trading_platform/api/dependencies.py
  </read_first>
  <files>tests/test_job_api.py, tests/test_job_mutation_api.py</files>
  <action>Create `tests/test_job_mutation_api.py` with the existing isolated migrated-PostgreSQL/TestClient fixture style and a test-only registry handler; inject it through app state/dependency without changing the production default. Cover exact HTTP contracts: 202 new submission; 200 exact replay with lowercase-readable `Idempotency-Replayed` value `true`; 409 changed payload/type with `detail.code == "idempotency_key_conflict"` and original Job ID; 400 absent/blank/oversized key; 422 unregistered type and invalid payload; 200 queued/running/pending/CANCELLED cancellation behaviors; trimmed/blank/over-500 reasons; 404 missing target; 409 fresh SUCCEEDED/FAILED; endpoint-key independence. For each rejection assert exact Job/JobMutation/event/request-fact row counts. Assert every success body has exactly the UI-SPEC compact fields and fixed relative links, with no embedded progress/log/events. Replace `test_jobs_router_exposes_no_mutating_verbs` in `tests/test_job_api.py` with an aggregated exact route-method contract: collection has GET+POST, detail/progress/logs/events remain GET, cancel is POST, and no PUT/PATCH/DELETE exists.</action>
  <acceptance_criteria>
    - New submit: 202 and one queued Job; exact replay: 200/header true/same Job and no second event; changed identity: typed 409/original Job ID/no write.
    - Missing header is typed 400 with zero Job/mutation rows; unknown type is typed 422 with zero rows.
    - Cancellation normalization/status/repeat/terminal/not-found cases match D-11 through D-15 and preserve first-request fields.
    - The compact response has no keys beyond `job_id,job_type,status,links`, and every link starts with `/api/v1/jobs/` rather than a scheme/host.
    - Route introspection proves the exact allowed method set and rejects any PUT/PATCH/DELETE exposure.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_job_mutation_api.py tests/test_job_api.py -x -q</automated>
  </verify>
  <done>Every locked HTTP result is executable as an automated assertion, including no-write failures and transport-independent linked observation.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / Trust Boundaries

| Boundary | Description |
|---|---|
| HTTP client → FastAPI | Untrusted headers, JSON bodies, path UUIDs, and job types enter the mutation surface. |
| FastAPI adapter → orchestration | Typed service outcomes must map without leaking internals or bypassing validation. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-18-15 | Tampering | High | missing/malformed mutation inputs | mitigate | ASVS V5 boundary models plus explicit missing-header 400 and zero-write tests. |
| T-18-16 | Elevation of Privilege | Critical | unsupported `job_type` | mitigate | Registry resolution before persistence; 422/no-row test; production default remains empty. |
| T-18-17 | Information Disclosure | Medium | typed error payloads | mitigate | Return stable codes and required IDs/status only; do not serialize SQL/tracebacks/settings. |
| T-18-18 | Spoofing | High | Host/proxy-derived links | mitigate | Route returns service-built fixed relative links; exact body assertions reject schemes/hosts. |
| T-18-19 | Repudiation | High | cancellation HTTP repeats | mitigate | API tests assert first requester/reason/timestamp/event count survive replay/fresh repeat. |
| T-18-SC | Tampering | Low | package supply chain | accept | No new dependency or install task. |

<severity_gate>ASVS L1 V5 controls and all Critical/High mitigations are blocking. Any unexpected 500, mutation on a rejected request, production handler registration, or absolute link stops completion.</severity_gate>
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_job_mutation_api.py tests/test_job_api.py tests/test_job_orchestration.py -x -q`
- `.venv/bin/python -m ruff check src/trading_platform/api/dependencies.py src/trading_platform/api/routes/jobs.py tests/test_job_mutation_api.py tests/test_job_api.py`
- `.venv/bin/python -m mypy src/trading_platform/api/dependencies.py src/trading_platform/api/routes/jobs.py`
</verification>

<success_criteria>
- Both mutation endpoints require and enforce endpoint-scoped idempotency.
- HTTP status/header/error bodies exactly implement D-04 through D-20.
- Route code contains parsing and typed outcome mapping only.
- Existing linked reads remain the exclusive state/progress/log/event observation mechanism.
- No console/UI file is created or modified.
</success_criteria>

<output>
Create `.planning/phases/18-orchestration-surface/18-04-SUMMARY.md` when done.
</output>
