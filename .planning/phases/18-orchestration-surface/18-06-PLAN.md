---
phase: 18-orchestration-surface
plan: 06
type: execute
wave: 4
depends_on: [18-04, 18-05]
files_modified:
  - src/trading_platform/api/app.py
  - tests/test_app_boot.py
  - tests/test_startup_validation.py
  - tests/test_job_mutation_e2e.py
autonomous: true
requirements: [ORCH-01, ORCH-02, ORCH-03, ORCH-04]
must_haves:
  truths:
    - "A test-only registered type traverses HTTP POST → one queued Job → worker handler execution → terminal Job observable through linked GET detail/progress/log/event endpoints (D-01)."
    - "The production registry remains empty of backtest/risk/paper/reconciliation/market-data/broker-sync handlers until Phase 19 (D-01, D-02)."
    - "The mutation-capable API fails startup before serving when PostgreSQL is unreachable; it does not retain Phase 17's read-only DB-optional rationale."
    - "New submission/replay/ref links stay idempotent and transport-independent throughout the full worker lifecycle (D-09, D-16, D-17, D-18, D-19)."
    - "No console route, component, hook, form, styling, design-system, or push/polling implementation is changed in Phase 18."
  artifacts:
    - path: "src/trading_platform/api/app.py"
      provides: "Mutation-ready DB-required startup and injectable/default Job registry lifecycle"
    - path: "tests/test_job_mutation_e2e.py"
      provides: "Complete Phase 18 submit-execute-observe proof with test-only handler"
    - path: "tests/test_app_boot.py"
      provides: "Fail-fast startup configuration assertion"
    - path: "tests/test_startup_validation.py"
      provides: "Updated API/run-jobs entrypoint gate coverage after CLI removal"
  key_links:
    - from: "src/trading_platform/api/app.py"
      to: "src/trading_platform/jobs/registry.py"
      via: "injected test registry or empty production default"
      pattern: "job_registry"
    - from: "tests/test_job_mutation_e2e.py"
      to: "src/trading_platform/jobs/runner.py"
      via: "same JobRegistry instance passed to API and run_worker_loop"
      pattern: "run_worker_loop"
    - from: "mutation response links"
      to: "existing Job GET routes"
      via: "TestClient follows returned relative links after worker completion"
      pattern: "links"
---

<objective>
Close Phase 18 with mutation-ready API startup and a full submit → execute → observe proof using only a test-registered handler.

Purpose: Prove the generic orchestration surface works end to end without pulling Phase 19 production operations or any console UI forward. Make database availability an explicit boot invariant now that the API mutates durable state.
Output: DB-required startup, controlled registry injection, updated startup tests, full real-PostgreSQL HTTP/worker/read E2E test, and complete source coverage audit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-orchestration-surface/18-CONTEXT.md
@.planning/phases/18-orchestration-surface/18-RESEARCH.md
@.planning/phases/18-orchestration-surface/18-PATTERNS.md
@.planning/phases/18-orchestration-surface/18-UI-SPEC.md
@.planning/phases/17-job-framework/17-08-SUMMARY.md
@.planning/phases/17-job-framework/17-09-SUMMARY.md
@.planning/phases/18-orchestration-surface/18-04-SUMMARY.md
@.planning/phases/18-orchestration-surface/18-05-SUMMARY.md
@src/trading_platform/api/app.py
@src/trading_platform/jobs/contracts.py
@src/trading_platform/jobs/registry.py
@src/trading_platform/jobs/runner.py
@tests/test_app_boot.py
@tests/test_startup_validation.py
@tests/test_job_runner.py

<interfaces>
- Change `create_app()` compatibly to `create_app(*, job_registry: JobRegistry | None = None) -> FastAPI`.
- An injected registry is stored at `app.state.job_registry` and used by Plan 04's provider.
- With no injection, lifespan builds `jobs.registry.build_default_registry(settings)`; D-01/D-02 require its job-type list to stay empty in Phase 18.
- API lifespan calls `enforce_startup_config(mode=ExecutionMode.BACKTEST, require_database=True)` before setting bootstrapped state or serving routes.
- The E2E handler is defined only in `tests/test_job_mutation_e2e.py`; `job_type = "phase18_e2e_probe"`; it reports progress, writes one structured log, and returns a JSON-safe result.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Require database-ready API startup and support explicit test registry injection</name>
  <read_first>
    - src/trading_platform/api/app.py
    - src/trading_platform/api/dependencies.py
    - src/trading_platform/core/startup.py
    - src/trading_platform/jobs/registry.py
    - tests/test_app_boot.py
    - tests/test_startup_validation.py
  </read_first>
  <files>src/trading_platform/api/app.py, tests/test_app_boot.py, tests/test_startup_validation.py</files>
  <action>Replace `api.app.lifespan`'s read-only/DB-optional comment and `require_database=False` call with the mutation-surface invariant `require_database=True`. Add keyword-only optional `job_registry` to `create_app`; store an injected registry on app state, and during lifespan create the empty production default only when none was injected. Do not register handlers or catch/suppress startup failure. In `test_app_boot.py`, patch `api.app.enforce_startup_config` only for the no-real-DB foundation endpoint test, return its loaded Settings, and assert the call uses BACKTEST plus `require_database=True`; retain endpoint assertions. In `test_startup_validation.py`, replace the removed `submit-paper-orders` parser ordering test with retained `run-jobs --once`: invalid DB/config exits before `run_worker_loop` is called. Replace the claim that API boot succeeds without DB with an assertion that mutation-capable startup rejects an unreachable DB even when Alpaca keys are empty, and add `run_jobs_command` to the static entrypoint gate list. Keep the standalone `enforce_startup_config(require_database=False)` function test because other read-only/internal callers may still opt out; only API policy changes.</action>
  <acceptance_criteria>
    - Source and patched-call assertions both show `enforce_startup_config(mode=ExecutionMode.BACKTEST, require_database=True)` in API lifespan.
    - Startup failure occurs before `app.state.bootstrapped = True`, registry construction, or route serving.
    - `create_app(job_registry=test_registry)` preserves that exact instance through lifespan; `create_app()` uses an empty default registry.
    - `run-jobs` remains startup-gated and never enters `run_worker_loop` after gate failure.
    - No test or comment continues to describe no-DB API boot as supported Phase 18 behavior.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_app_boot.py tests/test_startup_validation.py -x -q</automated>
  </verify>
  <done>The API refuses to expose a write surface without PostgreSQL and supports test-only registry injection without contaminating production registration.</done>
</task>

<task type="auto">
  <name>Task 2: Prove POST submission through worker execution and linked terminal observation</name>
  <read_first>
    - tests/test_job_mutation_e2e.py
    - tests/test_job_runner.py
    - tests/test_job_api.py
    - tests/test_job_mutation_api.py
    - src/trading_platform/jobs/contracts.py
    - src/trading_platform/jobs/runner.py
    - src/trading_platform/api/app.py
    - src/trading_platform/api/routes/jobs.py
  </read_first>
  <files>tests/test_job_mutation_e2e.py</files>
  <action>Create a real migrated-PostgreSQL E2E test module with a local `_Phase18E2EHandler` and one `JobRegistry` instance injected into `create_app(job_registry=registry)` and passed unchanged to `run_worker_loop`. The handler accepts `{"message": "hello"}`, reports an intermediate progress snapshot, writes one structured `phase18_e2e` log via `JobContext.log`, and returns a JSON-safe echo result. POST `/api/v1/jobs` with a key, assert 202/queued/compact relative links and one persisted Job; replay before execution, assert 200/header true/same body and still one Job. Run one worker Job. Follow the exact returned `self`, `progress`, `logs`, and `events` links through TestClient and assert SUCCEEDED, 100% terminal progress, handler log presence, and submitted/claimed/succeeded events. Separately assert `build_default_registry().list_job_types() == []` and that no production operation literal is registered. Do not add this handler under `src/`, test console behavior, or choose polling/push transport.</action>
  <acceptance_criteria>
    - One test executes the complete `POST → QUEUED → RUNNING → SUCCEEDED → linked GETs` chain with the same injected registry and real DB.
    - Pre-execution replay returns the identical compact body, 200, and replay header without adding a Job/event or running the handler.
    - The handler executes exactly once; final detail is succeeded; progress is 100; logs include `phase18_e2e`; events include submitted, claimed, succeeded.
    - All four response links are directly usable relative paths and no mutation response embeds the fetched resources.
    - Production `build_default_registry()` remains empty and no Phase 19 handler/module is introduced.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_job_mutation_e2e.py -x -q</automated>
  </verify>
  <done>D-01's complete generic flow is proven over HTTP, worker execution, PostgreSQL persistence, and API-only observation without any production operation handler.</done>
</task>

<task type="auto">
  <name>Task 3: Run phase-wide contract, security, boundary, and scope-fence verification</name>
  <read_first>
    - .planning/phases/18-orchestration-surface/18-CONTEXT.md
    - .planning/phases/18-orchestration-surface/18-RESEARCH.md
    - .planning/phases/18-orchestration-surface/18-PATTERNS.md
    - .planning/phases/18-orchestration-surface/18-UI-SPEC.md
    - tests/test_job_mutation_migration.py
    - tests/test_job_orchestration.py
    - tests/test_job_mutation_api.py
    - tests/test_orchestration_boundaries.py
    - tests/test_job_mutation_e2e.py
  </read_first>
  <files>tests/test_job_mutation_e2e.py</files>
  <action>Run every Phase 18 focused test plus Phase 17 Job regression suites, then the full pytest suite, ruff check/format, and configured mypy gate. Use a diff/path assertion to prove no path under `console/` changed and no production handler file/registration was added. Search active source (excluding comments/tests where appropriate) for removed CLI command registration, absolute Job-reference URL construction, route-local Job lifecycle/persistence calls, and imports from `services` to `jobs`; each count must be zero. Treat any unresolved OWASP ASVS L1 High/Critical finding, flaky duplicate race, full-suite regression, boundary failure, schema mismatch, or UI-scope change as blocking rather than documenting it as acceptable.</action>
  <acceptance_criteria>
    - All focused Phase 18 and Phase 17 Job tests pass against PostgreSQL.
    - Full pytest, ruff check, ruff format check, and configured mypy gates pass.
    - Git diff names zero files under `console/` and zero production operation-handler files.
    - Removed CLI registration, route-local mutation, absolute-link, and domain-service-upward-import scans return zero actionable matches.
    - Every High/Critical threat mitigation in Plans 01-06 has an automated passing assertion.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_job_mutation_migration.py tests/test_job_dependencies.py tests/test_job_cancellation.py tests/test_job_orchestration.py tests/test_job_api.py tests/test_job_mutation_api.py tests/test_orchestration_boundaries.py tests/test_job_import_boundary.py tests/test_job_registry.py tests/test_job_runner.py tests/test_job_mutation_e2e.py tests/test_app_boot.py tests/test_startup_validation.py tests/test_operator_controls.py tests/test_concurrency_guard_e2e.py -x -q &amp;&amp; .venv/bin/python -m pytest -x -q &amp;&amp; .venv/bin/python -m ruff check . &amp;&amp; .venv/bin/python -m ruff format --check . &amp;&amp; .venv/bin/python -m mypy src/trading_platform/services/execution src/trading_platform/services/reconciliation src/trading_platform/services/config src/trading_platform/jobs src/trading_platform/orchestration src/trading_platform/api src/trading_platform/worker</automated>
  </verify>
  <done>Phase 18 is fully green, security-gated, boundary-enforced, production-handler-free, and backend-only.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / Trust Boundaries

| Boundary | Description |
|---|---|
| Process startup → mutation routes | Serving while PostgreSQL is unavailable would advertise operations that cannot persist safely. |
| Test registry → production app | A proof handler must never leak into production registration. |
| HTTP submission → worker → read API | Identity, lifecycle, progress, logs, and events cross multiple components and must remain linked to one Job. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-18-25 | Denial of Service | High | API lifespan | mitigate | Fail-fast DB preflight before bootstrapped state; startup ordering test. |
| T-18-26 | Elevation of Privilege | Critical | default JobRegistry | mitigate | Test-only constructor injection; explicit production-empty assertion; no src handler. |
| T-18-27 | Tampering | Critical | submit/worker identity chain | mitigate | Same registry and persisted Job ID followed through one-run worker and all linked GET assertions. |
| T-18-28 | Repudiation | High | E2E event/log trail | mitigate | Assert submitted/claimed/succeeded events and structured handler log for the one Job. |
| T-18-29 | Information Disclosure | Medium | full verification | mitigate | Existing log sanitizer remains in the Job context path; no secret values used or asserted. |
| T-18-SC | Tampering | Low | package supply chain | accept | No package installation anywhere in Phase 18. |

<severity_gate>ASVS L1 High/Critical findings block the phase. The API may not boot DB-optional, the test handler may not exist in production, and E2E identity/audit assertions may not be skipped.</severity_gate>
</threat_model>

<source_coverage>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature / contract | Plan(s) | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | HTTP is the single idempotent Job orchestration surface with compact observation and no parallel CLI logic | 03-06 | COVERED | Service, routes, CLI fence, E2E. |
| REQ | ORCH-01 | HTTP-only manual operation surface | 04-06 | COVERED | Generic POST + direct mutating CLI removal; production handlers remain D-02 deferred. |
| REQ | ORCH-02 | Thin API/CLI adapters over shared layers | 02, 03, 05 | COVERED | Session primitives, application layer, AST enforcement. |
| REQ | ORCH-03 | Every mutation endpoint is idempotent | 01-04, 06 | COVERED | DB uniqueness, transaction composition, service/API/E2E tests. |
| REQ | ORCH-04 | Compact Job reference observable via API reads | 03, 04, 06 | COVERED | Relative links and linked E2E reads. |
| ROADMAP | SC-1 | Every manual operation invoked only through HTTP | 04, 05 | COVERED | D-01/D-02 handoff means Phase 18 proves generic type only; no production operation is directly invocable. |
| ROADMAP | SC-2 | Structural test proves no duplicated API/CLI business logic | 05 | COVERED | Exact parser/dispatch and AST boundaries. |
| ROADMAP | SC-3 | Same-key replay returns original Job | 01, 03, 04, 06 | COVERED | Sequential/concurrent/API/E2E. |
| ROADMAP | SC-4 | API-only state/progress/log observation | 03, 04, 06 | COVERED | Compact links followed after execution. |
| ROADMAP | SC-5 | Operator cancellation calls Phase 17 framework | 02-04 | COVERED | Exact `request_cancellation` entry point and route. |
| CONTEXT | D-01 | Test-only submit-execute-observe; production registry empty | 04, 05, 06 | COVERED | No src handler. |
| CONTEXT | D-02 | Six production handlers remain Phase 19 | 05, 06 | COVERED | Explicit scope fence and registry assertion. |
| CONTEXT | D-03 | Remove direct operation CLI; retain worker infrastructure | 05 | COVERED | Exact retained/removed sets. |
| CONTEXT | D-04 | Generic POST `/api/v1/jobs` with registered type/payload | 03, 04 | COVERED | Service and route contract. |
| CONTEXT | D-05 | Unknown type typed 422 before persistence | 03, 04 | COVERED | Resolve-before-transaction plus no-write API test. |
| CONTEXT | D-06 | Idempotency-Key required; missing typed 400/no mutation | 03, 04 | COVERED | Explicit service/route mapping. |
| CONTEXT | D-07 | Endpoint-scoped key uniqueness | 01, 03 | COVERED | Named endpoint/key constraint and scope tests. |
| CONTEXT | D-08 | Canonical submission/cancellation identity | 03 | COVERED | Sorted JSON SHA-256 over normalized operation. |
| CONTEXT | D-09 | Exact replay original Job/no second operation | 01, 03, 04, 06 | COVERED | Race and E2E assertions. |
| CONTEXT | D-10 | Changed identity typed 409/code/original Job/no mutation | 03, 04 | COVERED | Typed exception and exact HTTP detail. |
| CONTEXT | D-11 | POST cancel route, no deletion | 03, 04 | COVERED | Route/service; no delete path. |
| CONTEXT | D-12 | Optional trimmed/null/max-500 reason | 03, 04 | COVERED | Normalize before fingerprint/write. |
| CONTEXT | D-13 | Call request_cancellation; preserve Phase 17 semantics | 02, 03 | COVERED | Caller-session exact function. |
| CONTEXT | D-14 | Pending/CANCELLED repeats return current ref/no overwrite | 03, 04 | COVERED | Exact/fresh-key tests. |
| CONTEXT | D-15 | Fresh terminal conflict/current status; absent 404 | 03, 04 | COVERED | Typed service/HTTP outcomes. |
| CONTEXT | D-16 | One compact Job-reference shape | 03, 04 | COVERED | Single builder. |
| CONTEXT | D-17 | Stable relative observation links | 03, 04, 06 | COVERED | Fixed paths, E2E follow. |
| CONTEXT | D-18 | No embedded progress/log/events | 03, 04, 06 | COVERED | Exact key-set assertions. |
| CONTEXT | D-19 | 202 new; 200/header true replay | 04, 06 | COVERED | API and E2E. |
| CONTEXT | D-20 | Cancellation returns same compact reference | 03, 04 | COVERED | Shared builder. |
| RESEARCH | R-01 | DB-constrained transactional idempotency and concurrent proof | 01-03 | COVERED | Named constraint/savepoints/race tests. |
| RESEARCH | R-02 | Session-owned submit/cancel primitives | 02 | COVERED | Compatibility APIs. |
| RESEARCH | R-03 | Resolve services→jobs boundary explicitly | 03, 05 | COVERED | Allowed orchestration package; blanket unchanged. |
| RESEARCH | R-04 | FastAPI typed statuses/headers and compact reference | 04 | COVERED | Exact contract. |
| RESEARCH | R-05 | Explicit mutation-ready DB startup | 06 | COVERED | Fail fast. |
| RESEARCH | R-06 | No new packages; stdlib canonical digest | 01-06 | COVERED | No install tasks. |
| RESEARCH | R-07 | ASVS V5 input validation and race/audit threat controls | 01-06 | COVERED | Per-plan blocking threat models/tests. |
| UI-SPEC | UI-01 | Backend-only; no console/design-system/browser changes | 01-06 | COVERED | File lists exclude `console/`; final diff gate. |
| UI-SPEC | UI-02 | Submit/cancel/compact/rejection HTTP contracts | 03, 04, 06 | COVERED | Exact API and E2E tests. |
| UI-SPEC | UI-03 | No polling/push choice | 03, 04, 06 | COVERED | Relative links only. |
| UI-SPEC | UI-04 | Adapter boundary and test-only handler | 05, 06 | COVERED | AST enforcement and test-local handler. |

Deferred exclusions confirmed absent: Phase 19 production handlers/console triggers/retry; Phase 20 scheduler; Phase 21 full audit/status UI. No unplanned source item remains.
</source_coverage>

<verification>
- Focused command in Task 3 passes.
- `git diff --name-only -- console` returns no paths.
- `.venv/bin/python -m pytest -x -q` passes with zero regressions.
- `.venv/bin/python -m ruff check .`, `.venv/bin/python -m ruff format --check .`, and configured mypy scope pass.
</verification>

<success_criteria>
- API boot requires a reachable PostgreSQL database.
- One test-only type completes the full HTTP submission, worker execution, and linked API observation chain exactly once.
- Production default registry remains empty and all Phase 19/20/21 work remains absent.
- All ORCH requirements, five roadmap criteria, D-01 through D-20, applicable research constraints, ASVS L1 controls, and UI-SPEC fences are covered by executable assertions.
</success_criteria>

<output>
Create `.planning/phases/18-orchestration-surface/18-06-SUMMARY.md` when done.
</output>
