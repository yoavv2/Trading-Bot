---
phase: 18-orchestration-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_platform/jobs/dependencies.py
  - src/trading_platform/jobs/cancellation.py
  - tests/test_job_dependencies.py
  - tests/test_job_cancellation.py
autonomous: true
requirements: [ORCH-02, ORCH-03]
must_haves:
  truths:
    - "Job creation can participate in the same caller-owned transaction as idempotency reservation without changing the existing standalone submit behavior (D-09)."
    - "Eligible cancellation still calls `jobs.cancellation.request_cancellation` while allowing its mutation key and audit changes to commit or roll back atomically (D-13)."
    - "Queued cancellation remains immediate, running cancellation remains cooperative, and first requester/reason facts remain immutable (D-13, D-14)."
  artifacts:
    - path: "src/trading_platform/jobs/dependencies.py"
      provides: "Caller-session-capable submit_job compatibility API"
      contains: "def submit_job("
    - path: "src/trading_platform/jobs/cancellation.py"
      provides: "Caller-session-capable request_cancellation compatibility API"
      contains: "def request_cancellation("
    - path: "tests/test_job_dependencies.py"
      provides: "Commit/rollback ownership tests for Job insertion"
    - path: "tests/test_job_cancellation.py"
      provides: "Commit/rollback ownership and Phase 17 cancellation regression tests"
  key_links:
    - from: "src/trading_platform/jobs/dependencies.py"
      to: "src/trading_platform/db/session.py"
      via: "standalone compatibility wrapper opens session_scope only when no caller session is supplied"
      pattern: "session_scope"
    - from: "src/trading_platform/jobs/cancellation.py"
      to: "src/trading_platform/jobs/lifecycle.py"
      via: "request_cancellation continues routing state changes through apply_job_transition"
      pattern: "apply_job_transition"
---

<objective>
Make Phase 17 submission and cancellation primitives composable inside a Phase 18 service-owned transaction.

Purpose: Idempotency reservation plus Job mutation cannot be atomic while `submit_job` and `request_cancellation` always open and commit independent sessions. Preserve all Phase 17 public behavior while adding explicit caller-owned session participation; do not duplicate lifecycle or cancellation logic in Phase 18.
Output: Backward-compatible session-aware primitives and real-PostgreSQL transaction-ownership regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-orchestration-surface/18-CONTEXT.md
@.planning/phases/18-orchestration-surface/18-RESEARCH.md
@.planning/phases/18-orchestration-surface/18-PATTERNS.md
@.planning/phases/17-job-framework/17-06-SUMMARY.md
@src/trading_platform/db/session.py
@src/trading_platform/jobs/dependencies.py
@src/trading_platform/jobs/cancellation.py
@tests/test_job_dependencies.py
@tests/test_job_cancellation.py

<interfaces>
Existing signatures to extend compatibly:
- `submit_job(*, job_type: str, payload: Mapping[str, Any], depends_on: Sequence[UUID] = (), settings: Settings | DatabaseSettings | None = None) -> UUID`
- `request_cancellation(*, job_id: UUID, requested_by: str, reason: str | None = None, settings: Settings | DatabaseSettings | None = None) -> CancellationResult`
- `session_scope(settings)` commits on normal exit and rolls back on exception.

Required compatible signatures after this plan:
- Both functions add keyword-only `session: Session | None = None`.
- When `session is None`, behavior and transaction ownership stay exactly as Phase 17 shipped.
- When a `Session` is supplied, the function uses it, calls `flush()`, never commits/closes it, and the caller owns commit/rollback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make submit_job caller-session-capable without duplicating insertion semantics</name>
  <read_first>
    - src/trading_platform/jobs/dependencies.py
    - src/trading_platform/db/session.py
    - tests/test_job_dependencies.py
    - .planning/phases/17-job-framework/17-05-SUMMARY.md
  </read_first>
  <files>src/trading_platform/jobs/dependencies.py, tests/test_job_dependencies.py</files>
  <action>Refactor the existing `submit_job` body so one internal Session-accepting path owns UUID generation, dependency validation/deduplication, `Job` insertion, `JobDependency` insertion, the single `SUBMITTED` `JobEvent`, and flush. Extend `submit_job` with keyword-only `session: Session | None = None`: invoke that path directly when supplied, otherwise preserve the current `session_scope(settings)` wrapper and return contract. Do not change dependency immutability, lifecycle vocabulary, event contents, UUID generation, or add commit/delete/retry behavior. Add tests proving both modes produce one identical queued Job/event and that an exception raised by the caller after session-mode submission rolls back Job, dependency, and event rows together.</action>
  <acceptance_criteria>
    - Every Job submitted in either mode has one `SUBMITTED` event and the same deduplicated dependency behavior.
    - `submit_job(..., session=session)` does not commit: a forced caller exception leaves zero new Job, dependency, and event rows.
    - `submit_job(..., settings=settings)` still commits and all existing Phase 17 dependency tests remain green.
    - The source contains no `session.commit()` and no second copy of the row/event construction sequence.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_job_dependencies.py -x -q</automated>
  </verify>
  <done>Phase 18 can create a Job inside its own atomic transaction while every existing direct framework caller retains Phase 17 behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Make request_cancellation caller-session-capable while preserving first-request audit facts</name>
  <read_first>
    - src/trading_platform/jobs/cancellation.py
    - src/trading_platform/jobs/lifecycle.py
    - src/trading_platform/db/session.py
    - tests/test_job_cancellation.py
    - .planning/phases/17-job-framework/17-06-SUMMARY.md
  </read_first>
  <files>src/trading_platform/jobs/cancellation.py, tests/test_job_cancellation.py</files>
  <action>Extend the public `request_cancellation` function with keyword-only `session: Session | None = None` and factor its current row-lock/branch/event/transition logic into one Session-owned path. When a caller session is supplied, use it and flush without commit; otherwise retain the existing `session_scope(settings)` compatibility behavior. This exact function remains the mandatory D-13 entry point used by Phase 18. Preserve queued immediate cancellation plus dependency cascade, running cooperative request, second-running-request no-op, terminal `JobNotCancellableError`, first requester/reason/timestamp immutability, and the sole-writer lifecycle rule. Add rollback tests for queued and running cancellation and re-run all Phase 17 cancellation cases.</action>
  <acceptance_criteria>
    - `request_cancellation(..., session=session)` followed by a forced caller rollback restores the queued/running Job and leaves no cancellation event or request facts.
    - Normal caller-session commit produces the same `CancellationResult`, Job status/request fields, and event rows as the standalone settings mode.
    - A second running request still returns `accepted=False` and does not overwrite requester, reason, or timestamp.
    - No direct `.status =` assignment, `session.commit()`, or duplicate cancellation state machine is introduced.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_job_cancellation.py -x -q</automated>
  </verify>
  <done>The exact Phase 17 cancellation primitive can be composed atomically with idempotency state without weakening queued/running/terminal semantics or audit immutability.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / Trust Boundaries

| Boundary | Description |
|---|---|
| Orchestration caller → Job framework | A higher layer lends a transaction to lifecycle primitives; ownership ambiguity could create partial commits. |
| Cancellation request → Job/audit rows | A repeated or rolled-back request must not fabricate or overwrite audit facts. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-18-05 | Tampering | High | `submit_job` transaction ownership | mitigate | One insertion path; caller-session rollback test proves Job/event/dependencies cannot partially commit. |
| T-18-06 | Repudiation | High | `request_cancellation` audit fields | mitigate | Preserve first-request no-overwrite branch and assert requester/reason/timestamp immutability. |
| T-18-07 | Elevation of Privilege | High | `Job.status` writes | mitigate | Keep all status transitions through `apply_job_transition`; source gate forbids direct assignment. |
| T-18-08 | Denial of Service | Medium | row locks | mitigate | Reuse the existing bounded single-Job row lock; do not add cross-table unbounded lock scans. |
| T-18-SC | Tampering | Low | package supply chain | accept | No package installation occurs. |

<severity_gate>ASVS L1 transaction-integrity controls are blocking. Any High threat without a passing rollback/no-overwrite/sole-writer assertion stops completion.</severity_gate>
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_job_dependencies.py tests/test_job_cancellation.py -x -q`
- `.venv/bin/python -m ruff check src/trading_platform/jobs/dependencies.py src/trading_platform/jobs/cancellation.py tests/test_job_dependencies.py tests/test_job_cancellation.py`
- `.venv/bin/python -m mypy src/trading_platform/jobs/dependencies.py src/trading_platform/jobs/cancellation.py`
</verification>

<success_criteria>
- Standalone callers observe zero behavior change.
- Caller-session mode flushes but never commits or closes the supplied Session.
- Submission and cancellation can be rolled back together with future idempotency rows.
- Phase 17 lifecycle, dependency, cancellation, and audit invariants remain test-enforced.
</success_criteria>

<output>
Create `.planning/phases/18-orchestration-surface/18-02-SUMMARY.md` when done.
</output>
