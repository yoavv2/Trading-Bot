---
phase: 03-backtest-and-reporting
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - Makefile
  - src/trading_platform/services/market_data_access.py
  - src/trading_platform/services/backtesting.py
  - src/trading_platform/worker/__main__.py
  - scripts/run_backtest.py
  - tests/test_backtest_runner.py
autonomous: true
requirements:
  - REQ-05
  - REQ-06
user_setup: []
must_haves:
  truths:
    - "The platform can run a deterministic daily-bar backtest end to end against persisted sessions and bars."
    - "The runner never fills on the signal session and never opens overlapping positions for a symbol that is already held."
    - "Backtest execution persists the expected signal, trade, and equity artifacts under one run record."
  artifacts:
    - "A dedicated backtest runner service exists for deterministic daily-bar simulation."
    - "A CLI and worker entrypoint exist for running backtests from the operator workflow."
    - "Deterministic runner tests cover no-lookahead fills, duplicate-entry suppression, and repeat-run stability."
  key_links:
    - "The runner consumes `strategy.generate_signals(...)` and persisted market sessions rather than bypassing the Phase 2 strategy boundary."
    - "The worker and CLI entrypoints both flow through the same runner service instead of duplicating simulation logic."
    - "Automated tests prove next-session-open fills, duplicate-entry suppression, and repeat-run determinism against seeded PostgreSQL data."
---

<objective>
Build the first real research execution path by implementing the deterministic daily-bar backtest runner, wiring it into the operator surface, and proving the simulation rules through seeded tests.

Purpose: This plan turns the Phase 3 schema foundation into a reproducible backtest workflow while keeping reporting and export concerns in a separate follow-up plan.
Output: A deterministic backtest runner service, a CLI or worker entrypoint for executing runs, and automated tests covering the core simulation behavior.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backtest-and-reporting/03-RESEARCH.md
@.planning/phases/03-backtest-and-reporting/03-01-SUMMARY.md
@src/trading_platform/services/market_data_access.py
@src/trading_platform/strategies/base.py
@src/trading_platform/strategies/signals.py
@src/trading_platform/strategies/trend_following_daily/strategy.py
@src/trading_platform/worker/__main__.py
@tests/test_dry_run.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the deterministic daily-session backtest runner</name>
  <files>src/trading_platform/services/market_data_access.py, src/trading_platform/services/backtesting.py</files>
  <action>Implement a lightweight internal simulator that iterates persisted market sessions in order, evaluates `TrendFollowingDailyV1` through the existing `generate_signals` interface, and converts actionable entry or exit signals into next-session-open fills. Add the read helpers the runner genuinely needs, such as session iteration or next-session lookup, but keep them focused on persisted-data queries rather than provider logic. Track cash, open positions, realized PnL, unrealized equity, and session-level equity history using the backtest schema from `03-01`.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_backtest_runner.py -q` passes for the core runner behavior.</verify>
  <done>The codebase has one deterministic runner service that can simulate a backtest over persisted daily sessions without lookahead.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the runner into the worker and CLI surfaces with duplicate-entry guards</name>
  <files>Makefile, src/trading_platform/services/backtesting.py, src/trading_platform/worker/__main__.py, scripts/run_backtest.py</files>
  <action>Expose the runner through both a standalone CLI and a worker command such as `make backtest`, `scripts/run_backtest.py`, and `trading-platform-worker backtest`. The execution flow must enforce one open position per symbol: ignore repeated `LONG` signals while a position is already open, and only process `EXIT` when that symbol has an active position to close. Persist all generated artifacts through the shared `strategy_runs` root from `03-01`. Do not import the future Phase 4 risk engine, portfolio guardrails, or broker abstractions into this path.</action>
  <verify>`PYTHONPATH=src .venv/bin/python scripts/run_backtest.py --help` exits successfully and `PYTHONPATH=src .venv/bin/python -m trading_platform.worker backtest --help` exits successfully.</verify>
  <done>The operator can execute the deterministic runner through the standard CLI-first workflow, and the path cannot over-enter the same symbol repeatedly.</done>
</task>

<task type="auto">
  <name>Task 3: Prove determinism, no-lookahead fills, and dry-run compatibility</name>
  <files>tests/test_backtest_runner.py</files>
  <action>Create deterministic tests that seed symbols, sessions, and daily bars into the temporary PostgreSQL pattern already used in this repo, then verify: repeated runs over the same date range produce identical persisted results, fills occur on the next available session rather than the signal session, duplicate `LONG` signals do not open overlapping positions for a symbol that is already held, and the shared worker changes do not regress the existing dry-run path. Keep the validation local and data-driven; no live network calls or manual setup should be required.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_backtest_runner.py tests/test_dry_run.py -q` passes.</verify>
  <done>The backtest runner is regression-covered, deterministic, explicit about duplicate-entry suppression, and compatible with the existing dry-run worker flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_backtest_runner.py tests/test_dry_run.py -q` passes
- [ ] `PYTHONPATH=src .venv/bin/python scripts/run_backtest.py --help` exits successfully
- [ ] `PYTHONPATH=src .venv/bin/python -m trading_platform.worker backtest --help` exits successfully
- [ ] Re-running the same seeded backtest produces the same persisted trades and equity snapshots
- [ ] Repeated `LONG` signals never create overlapping positions for a symbol that is already open
- [ ] The runner never fills a trade on the same session that generated the signal
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The platform can run deterministic daily-bar backtests and persist the artifacts needed for later reporting
- The runner never opens a second overlapping position for a symbol that is already held
- Fill timing, fee assumptions, and slippage assumptions are explicit enough that the operator can explain what the simulator actually did
  </success_criteria>

<output>
After completion, create `.planning/phases/03-backtest-and-reporting/03-02-SUMMARY.md`
</output>
