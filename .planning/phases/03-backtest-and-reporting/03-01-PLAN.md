---
phase: 03-backtest-and-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/app.yaml
  - src/trading_platform/core/settings.py
  - src/trading_platform/db/models/strategy_run.py
  - src/trading_platform/db/models/backtest_signal.py
  - src/trading_platform/db/models/backtest_trade.py
  - src/trading_platform/db/models/backtest_equity_snapshot.py
  - src/trading_platform/db/models/__init__.py
  - alembic/versions/0004_phase3_backtest_foundation.py
  - tests/test_db_migrations.py
autonomous: true
requirements:
  - REQ-05
  - REQ-06
  - REQ-11
user_setup: []
must_haves:
  truths:
    - "Phase 3 has a typed, versioned backtest configuration surface instead of hidden simulator constants."
    - "The database schema can persist backtest runs and their artifacts under a `strategy_runs` root without breaking the existing dry-run path."
    - "Backtest persistence remains normalized and auditable rather than collapsing execution outputs into opaque JSON blobs."
  artifacts:
    - "A typed backtest settings block exists and loads through the existing YAML-plus-env settings path."
    - "Alembic head creates the Phase 3 backtest tables and the `strategy_runs` root can distinguish backtests from dry runs."
    - "Regression coverage proves the shared run-model changes do not break the existing dry-run execution path."
  key_links:
    - "Every persisted backtest artifact is anchored to a `strategy_runs` root record so later plans can query runs consistently."
    - "Typed settings, ORM models, and the Alembic revision all agree on the same backtest assumption schema."
    - "Dry-run regression tests protect the shared `strategy_runs` foundation before the real backtest runner is added."
---

<objective>
Build the Phase 3 foundation by adding typed backtest settings, extending the run model, and landing normalized persistence for backtest artifacts.

Purpose: This plan isolates the schema and compatibility work so the actual runner can land in a smaller follow-up plan without mixing migration risk, CLI wiring, and simulator logic in one execution unit.
Output: Typed backtest settings, Phase 3 ORM models, and an Alembic revision that prepares PostgreSQL for deterministic backtest artifacts.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backtest-and-reporting/03-RESEARCH.md
@.planning/phases/02-data-and-strategy/02-03-SUMMARY.md
@src/trading_platform/core/settings.py
@src/trading_platform/db/models/strategy_run.py
@src/trading_platform/services/bootstrap.py
@tests/test_db_migrations.py
@tests/test_dry_run.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed backtest settings and extend the shared run model safely</name>
  <files>config/app.yaml, src/trading_platform/core/settings.py, src/trading_platform/db/models/strategy_run.py</files>
  <action>Extend the runtime settings with an explicit backtest block covering at least initial capital, fill timing, allocation model, fee assumptions, and slippage assumptions. Reuse `strategy_runs` as the root run record by adding a backtest-specific run type and any minimal metadata needed for Phase 3. Keep this work limited to the shared settings and run root so the schema foundation can be validated without introducing runner logic yet.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_dry_run.py -q` passes after the settings and shared run-model updates.</verify>
  <done>The codebase can represent a backtest run in typed config and in the shared run model without regressing the existing dry-run path.</done>
</task>

<task type="auto">
  <name>Task 2: Add normalized backtest artifact tables and the Phase 3 migration</name>
  <files>src/trading_platform/db/models/backtest_signal.py, src/trading_platform/db/models/backtest_trade.py, src/trading_platform/db/models/backtest_equity_snapshot.py, src/trading_platform/db/models/__init__.py, alembic/versions/0004_phase3_backtest_foundation.py</files>
  <action>Add child tables keyed to `strategy_run_id` for the artifacts Phase 3 must persist later: signal events, simulated trades, and session-level equity snapshots. Keep these schemas narrowly focused on backtest inspection and replay; do not introduce broker lifecycle, paper-order, or Phase 4 risk tables. Wire the new models into the package exports and land the migration as a clean follow-on to the current Alembic chain.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_db_migrations.py -q` passes and confirms the new tables land at Alembic head.</verify>
  <done>PostgreSQL is ready to store normalized backtest artifacts under a shared run root, and the migration path remains clean.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the foundation with migration and dry-run regression coverage</name>
  <files>tests/test_db_migrations.py</files>
  <action>Extend the existing migration coverage so it asserts the new backtest tables and run-type changes are present, then run the existing dry-run regression suite to prove the shared `strategy_runs` changes did not break the worker bootstrap flow. Keep this task focused on compatibility and schema confidence, not on runner behavior that belongs to the next plan.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_db_migrations.py tests/test_dry_run.py -q` passes.</verify>
  <done>The Phase 3 foundation is in place and the shared run infrastructure is still safe to build on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_db_migrations.py tests/test_dry_run.py -q` passes
- [ ] Alembic head creates the Phase 3 backtest tables cleanly
- [ ] The shared `strategy_runs` path still supports the existing dry-run worker flow
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The codebase has a safe, typed, and migratable foundation for deterministic backtest execution
- Existing dry-run behavior remains intact after the shared run-model changes
  </success_criteria>

<output>
After completion, create `.planning/phases/03-backtest-and-reporting/03-01-SUMMARY.md`
</output>
