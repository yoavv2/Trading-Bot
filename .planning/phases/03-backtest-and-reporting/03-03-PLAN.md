---
phase: 03-backtest-and-reporting
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - Makefile
  - src/trading_platform/db/models/backtest_metric.py
  - src/trading_platform/db/models/__init__.py
  - src/trading_platform/services/backtest_reporting.py
  - src/trading_platform/worker/__main__.py
  - alembic/versions/0005_phase3_backtest_reporting.py
  - scripts/export_backtest_report.py
  - tests/test_backtest_reporting.py
  - tests/test_db_migrations.py
autonomous: true
requirements:
  - REQ-05
  - REQ-06
user_setup: []
must_haves:
  truths:
    - "The operator can inspect a trustworthy run summary, trade ledger, and equity-curve export without reading raw logs or writing SQL manually."
    - "Backtest reports make the run assumptions visible, including fee and slippage settings, so reported metrics are explainable."
    - "Metrics and exports are deterministic because they are derived from persisted Phase 3 run artifacts, not hidden in-memory state."
  artifacts:
    - "A backtest reporting service exists for summary metrics, report rendering, and machine-readable exports."
    - "Run-level metrics are persisted or snapshotted in a normalized form tied to the run record."
    - "CLI commands exist to render or export backtest reports for research inspection."
  key_links:
    - "The reporting layer reads only persisted backtest artifacts from 03-02 and does not re-simulate trades during report generation."
    - "Run summaries expose the same assumption snapshot captured during execution so metrics remain tied to the exact run configuration."
    - "Automated tests prove that markdown or JSON summaries and CSV exports remain stable for seeded runs, including no-trade edge cases."
---

<objective>
Turn persisted backtest data into research-ready inspection outputs by computing run metrics, rendering trustworthy summaries, and exporting machine-readable artifacts for later analysis.

Purpose: This plan completes Phase 3 by making backtest results usable for strategy evaluation instead of leaving them trapped in raw database rows.
Output: A backtest reporting service, persisted run metrics, and CLI-driven summary and export commands for research inspection.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backtest-and-reporting/03-RESEARCH.md
@.planning/phases/03-backtest-and-reporting/03-02-SUMMARY.md
@src/trading_platform/db/models/strategy_run.py
@src/trading_platform/services/backtesting.py
@src/trading_platform/worker/__main__.py
@tests/test_db_migrations.py
@tests/test_dry_run.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compute and persist run-level backtest metrics from stored artifacts</name>
  <files>src/trading_platform/db/models/backtest_metric.py, src/trading_platform/db/models/__init__.py, alembic/versions/0005_phase3_backtest_reporting.py, tests/test_db_migrations.py</files>
  <action>Build the first normalized metrics layer for backtests by computing run-level summaries from the persisted trades and equity snapshots produced in `03-02`. Persist enough structured output to support inspection without recomputation surprises, including at least total return, max drawdown, trade count, win rate, average win, average loss, profit factor, exposure percentage, and average holding period. Keep the scope backtest-specific; do not try to generalize this into the full cross-platform analytics service deferred to Phase 6.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_db_migrations.py tests/test_backtest_reporting.py -q` passes after the metrics schema and calculation updates.</verify>
  <done>Every completed backtest run has a stable, queryable metrics summary tied to the exact persisted run artifacts and assumption snapshot that produced it.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI-first report rendering and export flows for research inspection</name>
  <files>Makefile, src/trading_platform/services/backtest_reporting.py, src/trading_platform/worker/__main__.py, scripts/export_backtest_report.py</files>
  <action>Create a reporting layer that can render one backtest run into a human-readable summary and export the research artifacts the operator actually needs: a run summary, a trade ledger, and an equity-curve export. Provide both a standalone CLI and a worker path that can output markdown or JSON summaries plus CSV exports, and support selecting a run by run ID or the most recent run for a strategy. Keep the operator surface file- and terminal-first; avoid pulling forward FastAPI report endpoints or dashboard-specific response models.</action>
  <verify>`PYTHONPATH=src .venv/bin/python scripts/export_backtest_report.py --help` exits successfully and `PYTHONPATH=src .venv/bin/python -m trading_platform.worker report-backtest --help` exits successfully.</verify>
  <done>The operator can render and export backtest results from the terminal without opening the database directly, and the report clearly surfaces the assumptions used by the run.</done>
</task>

<task type="auto">
  <name>Task 3: Lock reporting behavior with deterministic tests and edge-case coverage</name>
  <files>tests/test_backtest_reporting.py</files>
  <action>Add deterministic tests that seed completed runs and verify the reporting layer end to end: summary metrics match known expected values, assumptions appear in the rendered report, CSV exports contain the expected columns and row counts, and no-trade runs or all-flat runs still produce a valid summary without divide-by-zero or missing-field failures. Keep these tests fixture-driven and independent of live data providers or manual review.</action>
  <verify>`PYTHONPATH=src .venv/bin/pytest tests/test_backtest_reporting.py tests/test_backtest_runner.py tests/test_db_migrations.py tests/test_dry_run.py -q` passes.</verify>
  <done>Backtest reporting is stable, regression-covered, and useful for research inspection rather than just technically present.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH=src .venv/bin/pytest tests/test_backtest_reporting.py tests/test_backtest_runner.py tests/test_db_migrations.py tests/test_dry_run.py -q` passes
- [ ] `PYTHONPATH=src .venv/bin/python scripts/export_backtest_report.py --help` exits successfully
- [ ] `PYTHONPATH=src .venv/bin/python -m trading_platform.worker report-backtest --help` exits successfully
- [ ] A seeded run can be exported into a human-readable summary plus machine-readable trade and equity files
- [ ] Reported metrics and assumptions match the persisted run data rather than recomputed ad-hoc state
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The operator can inspect trustworthy backtest reports and exports without reading raw logs
- Phase 3 leaves behind persisted, explainable research outputs that Phase 6 can later expose through APIs without redesigning the run model
  </success_criteria>

<output>
After completion, create `.planning/phases/03-backtest-and-reporting/03-03-SUMMARY.md`
</output>
