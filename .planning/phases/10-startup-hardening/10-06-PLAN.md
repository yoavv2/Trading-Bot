---
phase: 10-startup-hardening
plan: 06
type: execute
wave: 3
depends_on: ["10-02", "10-03", "10-04", "10-05"]
files_modified:
  - src/trading_platform/services/paper_execution.py
  - src/trading_platform/services/order_state_machine.py
  - src/trading_platform/services/concurrency_guard.py
  - src/trading_platform/services/operator_status.py
  - src/trading_platform/services/reconciliation.py
  - src/trading_platform/worker/__main__.py
  - src/trading_platform/services/bootstrap.py
  - src/trading_platform/api/app.py
  - src/trading_platform/core/logging.py
  - tests/test_log_enforcement.py
autonomous: true
requirements: [LOG-01, LOG-06]
must_haves:
  truths:
    - "Execution, reconciliation, and config/startup path modules obtain loggers ONLY through the approved get_logger wrapper — direct logging.getLogger is forbidden there and enforced by an import-boundary test"
    - "Every emission from those paths reaches the logger through the sanitizer (via emit_structured_log and a formatter-level backstop)"
    - "An enforcement test asserts no emitted log line under default config contains password=, api_key=, an Authorization: header value, or a full broker order ID"
  artifacts:
    - path: "tests/test_log_enforcement.py"
      provides: "import-boundary test (no logging.getLogger in enumerated paths) + emitted-line scan for secrets/full order IDs"
      contains: "logging.getLogger"
    - path: "src/trading_platform/core/logging.py"
      provides: "JsonLogFormatter applies sanitize() as a defense-in-depth backstop so even a stray direct emission is scrubbed"
      contains: "sanitize"
  key_links:
    - from: "execution/reconciliation/config path modules"
      to: "get_logger"
      via: "loggers obtained via the wrapper; no direct logging.getLogger in these modules"
      pattern: "get_logger"
    - from: "JsonLogFormatter.format"
      to: "sanitize"
      via: "formatter-level backstop scrubs the serialized payload before output"
      pattern: "sanitize"
---

<objective>
Enforce single-logger discipline and prove log sanitization end-to-end: migrate every execution, reconciliation, and config/startup path module off direct logging.getLogger onto the approved get_logger wrapper (LOG-01), add a defense-in-depth sanitizer backstop in the JSON formatter, and ship the two enforcement tests — an import-boundary test forbidding logging.getLogger in those paths, and an emitted-line scan asserting no default-config log line leaks credentials or a full broker order ID (LOG-06).

This is the LAST plan in the phase: it runs after the DB-transaction (10-04) and CFG-startup (10-05) edits land, so it migrates the FINAL version of the shared files (paper_execution.py, worker/__main__.py, bootstrap.py, api/app.py) rather than colliding with them.

Purpose: LOG-06 is the observable invariant the whole LOG track exists to guarantee — enforceable only once there is one logger surface and one sanitization chokepoint, both in place from 10-02.
Output: migrated modules, formatter backstop, import-boundary + emitted-line enforcement tests.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<key_facts>
- Consumes 10-02's contract: `from trading_platform.core.logging import get_logger` (approved wrapper) and the sanitize chokepoint already inside emit_structured_log.
- LOG-01 scope is EXACTLY "execution, reconciliation, and config paths" — do NOT migrate data-path modules (polygon.py, calendar.py, ingestion.py, market_data_access.py); they are out of LOG-01's declared scope. In-scope modules verified to use logging.getLogger today: execution — paper_execution.py, order_state_machine.py, concurrency_guard.py, operator_status.py, worker/__main__.py; reconciliation — reconciliation.py; config/startup — services/bootstrap.py, api/app.py (plus core/startup.py and core/config_validation.py from Wave-2 plans, which should already avoid direct getLogger).
- emit_structured_log(logger, level, message, **fields) already routes context through sanitize (10-02). Direct logger.info(...)/logger.exception(...) calls passing extra={"context": ...} (e.g. bootstrap.run_dry_bootstrap, api/app.py lifespan) currently BYPASS the sanitizer — the formatter backstop closes that gap.
- JsonLogFormatter.format (core/logging.py lines 17-29) builds payload["context"] = context and payload["message"] = record.getMessage(). Applying sanitize to the assembled payload there scrubs any stray unsanitized emission (defense in depth for LOG-06).
- Broker order IDs appear in emitted context (e.g. paper_execution success/transition logging) — the last-6 masking from 10-02 must be what shows up in emitted lines.
</key_facts>

<interfaces>
From src/trading_platform/core/logging.py (10-02):
  get_logger(name) -> logging.Logger  # the one approved logger factory
  emit_structured_log(logger, level, message, *, ...) -> None  # routes context through sanitize
  sanitize importable from trading_platform.core.log_sanitizer
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate execution + reconciliation path modules onto get_logger</name>
  <files>src/trading_platform/services/paper_execution.py, src/trading_platform/services/order_state_machine.py, src/trading_platform/services/concurrency_guard.py, src/trading_platform/services/operator_status.py, src/trading_platform/services/reconciliation.py</files>
  <action>
    In each module, replace every logging.getLogger("...") / logging.getLogger(__name__) acquisition with get_logger("...") imported from trading_platform.core.logging, preserving the existing logger NAME strings (downstream log routing depends on them). Leave logging.WARNING/logging.INFO level constants untouched (they are level ints, not logger acquisition). Where a direct logger.info/warning(...) passes extra={"context": {...}} with potentially sensitive fields, prefer routing through emit_structured_log for consistency, but the Task 2 formatter backstop guarantees sanitization regardless. Do not change message strings or emission semantics — this is a mechanical acquisition swap.
  </action>
  <verify>
    <automated>python -m pytest tests/test_execution_reconciliation.py tests/test_paper_execution.py -q</automated>
  </verify>
  <done>No logging.getLogger remains in the five execution/reconciliation modules; loggers acquired via get_logger with identical names; related tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate config/startup modules + formatter sanitize backstop + import-boundary test (LOG-01)</name>
  <files>src/trading_platform/worker/__main__.py, src/trading_platform/services/bootstrap.py, src/trading_platform/api/app.py, src/trading_platform/core/logging.py, tests/test_log_enforcement.py</files>
  <action>
    Migrate worker/__main__.py, services/bootstrap.py, and api/app.py off direct logging.getLogger onto get_logger, preserving logger names. In core/logging.py, modify JsonLogFormatter.format to pass the assembled payload (message + context) through sanitize(...) before json.dumps — a defense-in-depth backstop so any stray direct emission is scrubbed (honoring the default-mask broker-id behavior). Add tests/test_log_enforcement.py with an import-boundary test: statically scan (AST or source read) the enumerated in-scope module files and assert none contains a logging.getLogger call (allowing plain import logging for level constants). Embed the exact in-scope module list so the test fails if a future edit reintroduces a raw getLogger in an execution/reconciliation/config path.
  </action>
  <verify>
    <automated>python -m pytest tests/test_log_enforcement.py -x -q -k import_boundary</automated>
  </verify>
  <done>All config/startup modules use get_logger; the formatter applies the sanitize backstop; the import-boundary test passes and forbids logging.getLogger across the enumerated paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LOG-06 emitted-line enforcement test</name>
  <files>tests/test_log_enforcement.py</files>
  <behavior>
    - Under default config (debug-unmask flag OFF), driving a representative execution emission (e.g. emit_structured_log / a paper-execution success log carrying a broker_order_id, plus a payload seeded with password/api_key/Authorization fields) produces log output where NO emitted line contains the substrings "password=", "api_key=", an Authorization header value, or the full broker order ID.
    - The full broker order ID's leading characters are absent; only the last-6 mask appears.
    - When the debug-unmask flag is explicitly ON, the full order ID may appear (proving the default is the masked path, not an accident of the test data).
  </behavior>
  <action>
    Add an enforcement test that installs a capturing handler on the root logger with JsonLogFormatter (matching production config via configure_logging), emits records through the real emission path carrying a seeded secret payload and a known full broker order id, captures the serialized JSON lines, and asserts none contains password=, api_key=, the Authorization header value, or the full order id — only the last-6 mask. Add a companion assertion that with the debug-unmask flag set the full id is permitted, so the default-masked behavior is proven distinct.
  </action>
  <verify>
    <automated>python -m pytest tests/test_log_enforcement.py -x -q</automated>
  </verify>
  <done>Emitted-line scan proves no default-config log line leaks credentials or a full broker order id; masked-by-default vs debug-unmask distinction is asserted.</done>
</task>

</tasks>

<verification>
- python -m pytest tests/test_log_enforcement.py -x -q passes (import-boundary + emitted-line scan).
- grep -rn "logging.getLogger" across the enumerated in-scope modules returns nothing.
- grep -n "sanitize" src/trading_platform/core/logging.py shows the formatter backstop.
- Full suite python -m pytest -q green (no regression from logger-acquisition swaps).
</verification>

<success_criteria>
- Execution/reconciliation/config paths use only get_logger; enforced by the import-boundary test (LOG-01).
- No emitted log line under default config leaks credentials or a full broker order id (LOG-06).
</success_criteria>

<output>
After completion, create `.planning/phases/10-startup-hardening/10-06-SUMMARY.md`
</output>
