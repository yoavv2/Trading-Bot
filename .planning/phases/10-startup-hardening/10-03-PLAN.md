---
phase: 10-startup-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_platform/db/session.py
  - src/trading_platform/db/__init__.py
  - src/trading_platform/services/concurrency_guard.py
  - tests/test_db_lifecycle.py
  - .planning/PROJECT.md
autonomous: true
requirements: [DB-01, DB-02, DB-03]
must_haves:
  truths:
    - "Exactly one documented connection-lifecycle model governs the engine and session factory, and the choice is recorded as a Key Decision"
    - "No competing caching mechanism coexists — the codebase does not contain both an @lru_cache-based engine getter and the dict-cache mechanism"
    - "Engine and session access flows through a single canonical import path; other import paths are removed"
    - "The test suite can still point the engine at the test vs local database (the reloadable model is preserved)"
  artifacts:
    - path: "src/trading_platform/db/session.py"
      provides: "the single documented reloadable engine/session manager (module docstring declares the model)"
      contains: "Lifecycle model"
    - path: "tests/test_db_lifecycle.py"
      provides: "import-boundary test asserting one canonical engine/session import path + reloadability preserved"
      contains: "def test_"
    - path: ".planning/PROJECT.md"
      provides: "Key Decision entry recording the chosen lifecycle model (DB-01)"
      contains: "lifecycle"
  key_links:
    - from: "all engine/session consumers"
      to: "trading_platform.db.session"
      via: "single canonical import path; trading_platform.db package no longer re-exports a competing surface"
      pattern: "from trading_platform.db.session import"
    - from: "clear_engine_cache"
      to: "test suite DB swap"
      via: "reloadable manager preserves test vs local DB pointing"
      pattern: "clear_engine_cache"
---

<objective>
Consolidate the DB connection lifecycle into one documented model, resolve the caching-mechanism duality so only the chosen mechanism remains, and force all engine/session access through a single canonical import path — with the choice recorded as a Key Decision. This is deliberately conservative: `db/session.py` is already an explicit reloadable manager (keyed engine/session-factory caches so tests can swap databases). The work is to FORMALIZE and DOCUMENT that model as canonical, eliminate the redundant/second import surface, and pin it with an import-boundary test — not to rewrite the engine layer.

Purpose: DB-01/02/03 require one connection-lifecycle model, no competing mechanism, and one import path — the foundation the transaction-integrity work (plan 10-04) builds on.
Output: documented canonical manager in `db/session.py`, deduplicated import path, Key Decision entry, import-boundary test.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<key_facts>
- `db/session.py` (verified full read): engine + session-factory are cached in module dicts `_ENGINE_CACHE` / `_SESSION_FACTORY_CACHE`, keyed by `(database.url, database.echo)`. `clear_engine_cache()` disposes+clears both. There is NO `@lru_cache` on any engine getter — the only `@lru_cache` in the codebase is on `load_settings` in settings.py (verified by grep; git history shows session.py has a single original commit). So the literal "duality" of DB-02 is a caching-STYLE inconsistency (settings singleton via lru_cache vs. keyed reloadable dict for the engine), not two competing engine mechanisms.
- HARD CONSTRAINT: the keyed `(url, echo)` cache exists so the 215-test suite can point at the test DB vs the local DB. Do NOT collapse to a process-immutable singleton and do NOT strip `clear_engine_cache`/`clear_settings_cache` — that would break the suite. This pushes DB-01 to the "explicit reloadable manager" option.
- Two import surfaces currently coexist: `from trading_platform.db.session import ...` (used by ~all services) AND `from trading_platform.db import ...` (the package `__init__` re-exports build_engine/get_engine/get_session_factory/session_scope/check_database_connection/clear_engine_cache). `concurrency_guard.py` imports `get_engine` from `.db.session`. Pick ONE canonical path (recommend `trading_platform.db.session`, the dominant one) and remove the competing re-export surface (or vice-versa) so DB-03's "other paths are removed" holds.
- Consumers of `session_scope` (verified via grep): bootstrap, ingestion, backtesting, reconciliation, paper_execution, risk, order_state_machine, backtest_reporting, analytics, operator_controls, operator_reads, worker/__main__. concurrency_guard uses `get_engine`. Any import-path change must update all of these consistently.
</key_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize db/session.py as the one documented reloadable manager + Key Decision entry (DB-01, DB-02)</name>
  <files>src/trading_platform/db/session.py, .planning/PROJECT.md</files>
  <action>
    Add a module-level docstring to `db/session.py` titled with an explicit "Lifecycle model:" section declaring the chosen model: an EXPLICIT RELOADABLE MANAGER — engine and session factory are cached per `(url, echo)` and reset via `clear_engine_cache()`; this is deliberately reloadable (not a process-immutable singleton) so tests and multi-DB entrypoints can rebind. State that this keyed dict-cache is the SINGLE authorized caching mechanism for the engine/session lifecycle and that `@lru_cache`-style memoization of engines is forbidden (resolving the DB-02 duality by declaration + code). Confirm by grep that no `@lru_cache` decorates any engine/session getter (there is none — record that the duality is resolved with the dict-cache retained). Add a Key Decision entry to `.planning/PROJECT.md` Key Decisions table: model chosen (explicit reloadable manager), why (test-suite DB-swap requirement), and that engine memoization uses the keyed dict cache exclusively.
  </action>
  <verify>
    <automated>python -c "import ast,sys; src=open('src/trading_platform/db/session.py').read(); assert 'Lifecycle model' in src; assert 'lru_cache' not in src, 'no engine lru_cache allowed'; print('ok')"</automated>
  </verify>
  <done>db/session.py declares one lifecycle model in its docstring; no @lru_cache engine getter exists; PROJECT.md records the Key Decision.</done>
</task>

<task type="auto">
  <name>Task 2: Single canonical import path + import-boundary test (DB-03)</name>
  <files>src/trading_platform/db/__init__.py, src/trading_platform/services/concurrency_guard.py, tests/test_db_lifecycle.py</files>
  <action>
    Choose `trading_platform.db.session` as the canonical engine/session import path (the dominant existing usage). In `db/__init__.py`, remove the competing engine/session re-exports (keep only what is genuinely a package-level need such as `Base`), so that engine/session symbols are importable from exactly one path. Update any consumer that imported engine/session symbols from the package surface (`from trading_platform.db import ...`) to import from `trading_platform.db.session` — verify `concurrency_guard.py` already uses `.db.session` (it does) and fix any straggler found by grep. Add `tests/test_db_lifecycle.py` with: (a) an import-boundary test that scans `src/trading_platform/**/*.py` and asserts every engine/session symbol import (`get_engine`, `get_session_factory`, `session_scope`, `build_engine`, `clear_engine_cache`, `check_database_connection`) comes from `trading_platform.db.session` and NOT from `trading_platform.db`; (b) a reloadability test that builds an engine, calls `clear_engine_cache()`, and asserts a fresh engine is produced (test-DB-swap capability preserved).
  </action>
  <verify>
    <automated>python -m pytest tests/test_db_lifecycle.py -x -q && python -m pytest -q</automated>
  </verify>
  <done>All engine/session imports use the single canonical `trading_platform.db.session` path; the package no longer re-exports a competing surface; import-boundary + reloadability tests pass; full suite green.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_db_lifecycle.py -x -q` passes.
- `grep -rn "from trading_platform.db import" src/ | grep -vE "import Base|models"` returns no engine/session imports.
- `grep -rn "lru_cache" src/trading_platform/db/` returns nothing.
- Full suite `python -m pytest -q` green (no import regressions across the ~12 consumer modules).
- PROJECT.md Key Decisions contains the lifecycle-model entry (DB-01).
</verification>

<success_criteria>
- One documented reloadable lifecycle model; Key Decision recorded.
- No competing caching mechanism; engine memoization uses the keyed dict cache only.
- One canonical import path; test-suite DB-swap capability preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/10-startup-hardening/10-03-SUMMARY.md`
</output>
