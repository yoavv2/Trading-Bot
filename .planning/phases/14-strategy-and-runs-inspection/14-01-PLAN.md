---
phase: 14-strategy-and-runs-inspection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - console/src/app/strategy/page.tsx
  - console/src/components/strategy/StrategyOverviewPanel.tsx
  - console/src/app/layout.tsx
autonomous: true
requirements: [STRA-01, STRA-02]
must_haves:
  truths:
    - "Operator can navigate to a Strategy screen from the top nav on every console page"
    - "Operator sees TrendFollowingDailyV1 with an explicit enabled/disabled status"
    - "Operator sees the strategy config summary: universe (tickers), indicators/entry rules, exit rules, and risk params"
    - "When /api/v1/strategies/{id} fails, the screen shows an endpoint-named ErrorState, never a blank or fake render"
    - "The panel shows an as-of timestamp and a working Refresh control"
  artifacts:
    - path: "console/src/app/strategy/page.tsx"
      provides: "Strategy overview route composing the overview panel"
      min_lines: 10
    - path: "console/src/components/strategy/StrategyOverviewPanel.tsx"
      provides: "Client panel fetching /api/v1/strategies/trend_following_daily via useApiQuery, rendering enabled status + config summary"
      min_lines: 40
    - path: "console/src/app/layout.tsx"
      provides: "Top nav links to /strategy and /runs added"
      contains: "/strategy"
  key_links:
    - from: "console/src/components/strategy/StrategyOverviewPanel.tsx"
      to: "/api/v1/strategies/trend_following_daily"
      via: "useApiQuery"
      pattern: "useApiQuery.*strategies/trend_following_daily"
    - from: "console/src/app/layout.tsx"
      to: "/strategy and /runs routes"
      via: "next/link"
      pattern: "href=\"/strategy\""
---

<objective>
Build the Strategy Overview screen: the operator can see `TrendFollowingDailyV1`'s enabled/disabled status and its full config summary (universe, indicators/entry rules, exit rules, risk params) on a dedicated `/strategy` route, and reach it (and the runs screen) from the top nav.

Purpose: STRA-01/STRA-02 — make the strategy's current declared state inspectable without reading code or the DB.
Output: `/strategy` route + `StrategyOverviewPanel` + nav links for both new Phase 14 routes.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-console-foundation-and-system-status/13-04-SUMMARY.md

# Console foundation you MUST reuse (do NOT re-implement fetch/error/freshness):
@console/src/lib/useApiQuery.ts
@console/src/lib/api.ts
@console/src/components/ErrorState.tsx
@console/src/components/FetchMeta.tsx
@console/src/components/status/StatusPanel.tsx
@console/src/app/layout.tsx

<constraints>
- FRONTEND ONLY. No backend files. Consume only the EXISTING `GET /api/v1/strategies/{strategy_id}` route.
- NO component may call `fetch()` directly — every fetch goes through `useApiQuery`/`fetchApi`. Enforced by grep.
- Every failure renders via `ErrorState`; every data view shows `FetchMeta` freshness + Refresh. Never a placeholder/default value on failure.
- This is a NEW customized Next.js (v16.2.10). Before writing route/layout code, read the relevant guide under `console/node_modules/next/dist/docs/` if any App Router / metadata / Link convention is unfamiliar. Heed deprecation notices. Match the exact conventions already used in `console/src/app/layout.tsx` and `console/src/app/page.tsx`.
</constraints>

<interfaces>
<!-- VERIFIED response shape of GET /api/v1/strategies/{strategy_id} (from strategies route + StrategyMetadata.to_public_dict). -->
<!-- strategy_id to use: "trend_following_daily" -->
Response body:
```ts
type StrategyDetail = {
  strategy: {
    strategy_id: string;          // "trend_following_daily"
    display_name: string;         // e.g. "Trend Following Daily V1"
    version: string;
    enabled: boolean;             // STRA-01 status
    description: string;
    config_reference: string;
    universe: string[];           // tickers
    universe_size: number;
    indicators: Record<string, unknown>;  // entry-rule params (shape is strategy-defined; render generically)
    risk: Record<string, unknown>;         // risk params
    exits: Record<string, unknown>;        // exit rules
  };
  operator_reads: Record<string, unknown>; // link catalog — you may ignore for this panel
};
```
NOTE: `indicators`/`risk`/`exits` are open-ended dicts. Render them generically (key/value rows), NOT with hardcoded field names, so the panel stays honest if the config changes.

<!-- Local composition pattern (from StatusPanel, 13-04): each screen builds its OWN screen-local panel from the lib primitives. Do NOT import StatusPanel across screens — it is scoped to the status screen. Build a strategy-local panel using useApiQuery + FetchMeta + ErrorState directly, following the same shape. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strategy overview panel + route</name>
  <files>console/src/components/strategy/StrategyOverviewPanel.tsx, console/src/app/strategy/page.tsx</files>
  <action>
Create `StrategyOverviewPanel.tsx` — a `"use client"` component that calls `useApiQuery<StrategyDetail>("/api/v1/strategies/trend_following_daily")` and renders a panel with the SAME honest chrome pattern as StatusPanel: a header with the panel title + `<FetchMeta asOf={result?.asOf ?? null} loading={loading} onRefresh={refetch} />`, a `Loading…` state when `result` is null, an `<ErrorState failure={result} />` branch when `result.ok === false`, and the data render when `result.ok === true`.

Data render (STRA-01 + STRA-02):
- Header: `display_name` + `version`, and a prominent enabled/disabled badge derived from `strategy.enabled` (green "ENABLED" when true, zinc/grey "DISABLED" when false). This badge is STRA-01 — it MUST be visually unambiguous.
- Description + `config_reference` (monospace).
- Universe (STRA-02): render `universe` tickers as a chip/inline list, plus `universe_size`.
- Three generic key/value sections for `indicators` (label it "Entry / Indicators"), `exits` (label "Exit Rules"), and `risk` (label "Risk Params"). Render each dict generically by mapping `Object.entries(...)` to rows — do NOT hardcode individual field names, and stringify nested objects/arrays with `JSON.stringify(value)` in a `<pre>`/monospace cell so any config shape renders honestly.

Do NOT import `StatusPanel` from the status screen (it is scoped there per 13-04). Compose directly from `useApiQuery`/`FetchMeta`/`ErrorState`.

Create `console/src/app/strategy/page.tsx` — a route that renders an `<h1>Strategy</h1>` heading and `<StrategyOverviewPanel />` inside a `<main className="flex-1 p-6">`, matching the layout/spacing conventions in `console/src/app/page.tsx`.
  </action>
  <verify>
    <automated>cd console && npm run build && npm run lint && grep -rnE "(^|[^a-zA-Z.])fetch\(" src/app/strategy src/components/strategy | grep -v fetchApi | grep -v "// " ; test $? -ne 0</automated>
  </verify>
  <done>`/strategy` route builds and lints clean; StrategyOverviewPanel fetches via useApiQuery (no direct fetch()), renders an enabled/disabled badge + universe + indicators/exits/risk sections, and branches to ErrorState on failure with a FetchMeta header.</done>
</task>

<task type="auto">
  <name>Task 2: Add Strategy + Runs nav links to global layout</name>
  <files>console/src/app/layout.tsx</files>
  <action>
In `console/src/app/layout.tsx`, extend the existing `<nav>` (which currently has the "Operator Console" label and a "System Status" link to `/`) by adding two `next/link` entries using the exact same className/hover styling as the existing System Status link:
- `<Link href="/strategy">Strategy</Link>`
- `<Link href="/runs">Runs</Link>`

This plan OWNS all Phase 14 nav additions (both links) so parallel Wave-1 plans do not conflict on `layout.tsx`. The `/runs` route is created by Plan 14-02 and `/runs/[runId]` by 14-03; a nav href to a route that lands later in the same phase is fine (Next does not validate href targets at build time). Do not touch anything else in the layout.
  </action>
  <verify>
    <automated>cd console && grep -q 'href="/strategy"' src/app/layout.tsx && grep -q 'href="/runs"' src/app/layout.tsx && npm run build && npm run lint</automated>
  </verify>
  <done>Top nav on every screen shows Strategy and Runs links; build + lint pass.</done>
</task>

</tasks>

<verification>
- `cd console && npm run build` succeeds; `npm run lint` clean.
- `grep -rnE "fetch\(" console/src/app/strategy console/src/components/strategy | grep -v fetchApi` returns nothing (no direct fetch bypass).
- Behavioral (covered live in 14-05 checkpoint): visiting `/strategy` shows the enabled/disabled badge, universe, and config sections; stopping the API makes the panel render an endpoint-named ErrorState (`/api/v1/strategies/trend_following_daily`), not a blank.
</verification>

<success_criteria>
- STRA-01: TrendFollowingDailyV1's enabled/disabled status is unambiguously visible.
- STRA-02: universe, entry/indicator params, exit rules, and risk params are all visible.
- Screen inherits the honest fetch/error/as-of pattern and the global kill-switch banner. Nav reaches Strategy and Runs.
</success_criteria>

<output>
After completion, create `.planning/phases/14-strategy-and-runs-inspection/14-01-SUMMARY.md`
</output>
