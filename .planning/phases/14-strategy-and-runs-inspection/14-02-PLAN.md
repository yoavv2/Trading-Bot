---
phase: 14-strategy-and-runs-inspection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - console/src/app/runs/page.tsx
  - console/src/components/runs/RunsTable.tsx
  - console/src/components/runs/RunFilters.tsx
autonomous: true
requirements: [RUNS-01, RUNS-02]
must_haves:
  truths:
    - "Operator sees a runs table with rows spanning backtest / risk_evaluation / paper_execution (and other) run types"
    - "Each row shows run type, status, session date, a run timestamp, and an error indication"
    - "Operator can filter the table by run type and by status, applied server-side"
    - "Each row links to that run's detail page at /runs/{run_id}"
    - "When /api/v1/runs fails, the table area shows an endpoint-named ErrorState; empty result shows an explicit 'no runs' state"
    - "The table shows an as-of timestamp and a working Refresh control"
  artifacts:
    - path: "console/src/app/runs/page.tsx"
      provides: "Runs route holding filter state and composing filters + table"
      min_lines: 15
    - path: "console/src/components/runs/RunsTable.tsx"
      provides: "Table rendering /api/v1/runs items with per-row link to /runs/{run_id}"
      min_lines: 40
    - path: "console/src/components/runs/RunFilters.tsx"
      provides: "run_type + status select controls driving the query string"
      min_lines: 20
  key_links:
    - from: "console/src/components/runs/RunsTable.tsx"
      to: "/api/v1/runs"
      via: "useApiQuery with run_type/status query params"
      pattern: "useApiQuery.*runs"
    - from: "console/src/components/runs/RunsTable.tsx"
      to: "/runs/{run_id} detail route"
      via: "next/link per row"
      pattern: "runs/\\$\\{|runs/`|href=.*run_id"
---

<objective>
Build the Runs screen: a filterable table over `GET /api/v1/runs` spanning all run types, showing status, session date, run timestamp, and error indication, with server-side filtering by run type and status, and each row drilling into `/runs/{run_id}`.

Purpose: RUNS-01/RUNS-02 — the runs index and the entry point to the run-detail audit trail.
Output: `/runs` route + `RunsTable` + `RunFilters`.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-console-foundation-and-system-status/13-04-SUMMARY.md

# Console foundation you MUST reuse:
@console/src/lib/useApiQuery.ts
@console/src/lib/api.ts
@console/src/components/ErrorState.tsx
@console/src/components/FetchMeta.tsx
@console/src/components/status/LatestRunPanel.tsx

<constraints>
- FRONTEND ONLY. No backend files. Consume only the EXISTING `GET /api/v1/runs` route with its native `run_type` + `status` query params.
- NO direct `fetch()` — everything via `useApiQuery`/`fetchApi`. Every failure renders `ErrorState`; every data view shows `FetchMeta` + Refresh.
- NEW customized Next.js (v16.2.10). Consult `console/node_modules/next/dist/docs/` for any unfamiliar App Router / `next/link` / client-component convention before writing. Match `console/src/app/page.tsx` conventions.
- HONESTY (RUNS-01 "created_at"): the runs serializer exposes `started_at`, `completed_at`, `as_of_session` — it does NOT expose a distinct `created_at` field, and adding one would be an unauthorized backend change. Satisfy the RUNS-01 "created_at" column with `started_at` (label the column "Started"), and add a short comment in RunsTable noting this substitution. Do NOT invent or fake a created_at value.
</constraints>

<interfaces>
<!-- VERIFIED response shape of GET /api/v1/runs (build_collection_response + _serialize_run_summary). -->
<!-- LatestRunPanel already consumes this exact shape — mirror its RunItem type. -->
```ts
type RunItem = {
  run_id: string;
  strategy_id: string;
  display_name: string;
  run_type: string;      // one of the RunType enum values below
  status: string;        // "pending" | "running" | "succeeded" | "failed"
  trigger_source: string;
  as_of_session: string | null;   // session date (RUNS-01 "session date")
  started_at: string;             // ISO — use as the RUNS-01 run timestamp
  completed_at: string | null;
  error_message: string | null;   // RUNS-01 "error indication"
  // parameters_snapshot / result_summary also present but not needed for the table
};
type RunsResponse = { filters: Record<string, unknown>; count: number; items: RunItem[] };
```

<!-- VERIFIED native query params on /api/v1/runs: run_type, status, strategy_id, session_start, session_end, limit (1..100, default 20). -->
<!-- VERIFIED enum values (server-side filter option lists — use these EXACT strings): -->
RunType values:   dry_bootstrap | backtest | risk_evaluation | paper_execution | reconciliation | operator_control
RunStatus values: pending | running | succeeded | failed

<!-- strategy_id defaults to trend_following_daily server-side (LatestRunPanel omits it successfully); you may omit strategy_id. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RunFilters + query-string builder</name>
  <files>console/src/components/runs/RunFilters.tsx</files>
  <action>
Create `RunFilters.tsx` (`"use client"`): two `<select>` controls — "Run type" and "Status" — each with an "All" option (empty value) plus one `<option>` per enum value from the interfaces block (RunType and RunStatus exact strings). Style them to match the console's dark zinc theme (see existing components for the palette). The component is controlled: it takes `runType`, `status`, and `onChange(next: { runType: string; status: string })` props and calls `onChange` when either select changes. Keep it presentational — it does NOT fetch. The parent (page) owns state and builds the endpoint query string, so filtering is applied SERVER-SIDE (RUNS-02): when a value is non-empty it becomes `run_type=<v>` / `status=<v>` on the `/api/v1/runs` request; "All" omits that param.
  </action>
  <verify>
    <automated>cd console && npm run build && npm run lint</automated>
  </verify>
  <done>RunFilters renders run_type + status selects (All + every enum value), is fully controlled, and does not fetch. Build + lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: RunsTable + /runs route with server-side filtering and drill-down links</name>
  <files>console/src/components/runs/RunsTable.tsx, console/src/app/runs/page.tsx</files>
  <action>
Create `console/src/app/runs/page.tsx` (`"use client"`): holds filter state `{ runType: "", status: "" }`, renders an `<h1>Runs</h1>` heading, `<RunFilters .../>`, and `<RunsTable runType={runType} status={status} />`, inside `<main className="flex-1 p-6">` matching `page.tsx` conventions.

Create `RunsTable.tsx` (`"use client"`): builds the endpoint string from props — start with `/api/v1/runs?limit=100`, append `&run_type=${runType}` only when `runType` is non-empty and `&status=${status}` only when `status` is non-empty (use `URLSearchParams` or explicit concatenation) — so the SERVER does the filtering (RUNS-02). Call `useApiQuery<RunsResponse>(endpoint)`.

Render with the honest chrome (same pattern as LatestRunPanel/StatusPanel): a header with a title + `<FetchMeta asOf={result?.asOf ?? null} loading={loading} onRefresh={refetch} />`; `Loading…` when `result` is null; `<ErrorState failure={result} />` when `result.ok === false`; when `result.ok === true` render:
- If `count === 0`: explicit "No runs match these filters." empty state (NOT a blank).
- Else a table with columns: Run type (`run_type`), Status (color-coded like LatestRunPanel's `statusColor`), Session (`as_of_session ?? "—"`), Started (`started_at` — see honesty note below), Error. The Error column shows a red indicator/short text when `error_message` is non-null (e.g. "error" badge or truncated message), and "—" otherwise. RUNS-01 error indication.
- Each row is (or contains) a `next/link` to `/runs/${run.run_id}` so the operator drills into the detail page (route created in 14-03). Make the whole row or a "View" cell the link.

HONESTY: add a code comment on the Started column noting "RUNS-01 created_at satisfied by started_at; the runs read-surface exposes no distinct created_at and adding one would be an unauthorized backend change." Do not render a fabricated created_at.

Reuse the `statusColor` idea from LatestRunPanel (duplicate the small helper locally; it is trivial and screen-scoped).
  </action>
  <verify>
    <automated>cd console && npm run build && npm run lint && grep -q "runs/" src/components/runs/RunsTable.tsx && grep -rnE "(^|[^a-zA-Z.])fetch\(" src/app/runs src/components/runs | grep -v fetchApi ; test $? -ne 0</automated>
  </verify>
  <done>`/runs` renders a filterable table; changing a filter re-issues `/api/v1/runs` with the matching query param (server-side); rows link to `/runs/{run_id}`; empty and error states are explicit; no direct fetch(). Build + lint pass.</done>
</task>

</tasks>

<verification>
- `cd console && npm run build` succeeds; `npm run lint` clean.
- `grep -rnE "fetch\(" console/src/app/runs console/src/components/runs | grep -v fetchApi` returns nothing.
- Filter changes alter the request query string (verify by reading the endpoint-building code) — filtering is server-side, not a client `.filter()` over a full fetch.
- Behavioral (14-05 checkpoint): table lists multiple run types; selecting run_type=backtest / status=failed narrows rows; a failed run shows an error indicator; clicking a row opens `/runs/{id}`.
</verification>

<success_criteria>
- RUNS-01: runs table across run types with status, session date, run timestamp (started_at), and error indication.
- RUNS-02: run_type + status filters applied server-side via native query params.
- Rows drill into the run detail route. Honest handling of the missing created_at field.
</success_criteria>

<output>
After completion, create `.planning/phases/14-strategy-and-runs-inspection/14-02-SUMMARY.md`
</output>
