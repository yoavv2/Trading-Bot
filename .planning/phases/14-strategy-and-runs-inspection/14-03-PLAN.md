---
phase: 14-strategy-and-runs-inspection
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - console/src/components/runs/detail/runScopedFilter.ts
  - console/src/components/runs/detail/runScopedFilter.test.ts
  - console/src/components/runs/detail/CappedDisclosure.tsx
  - console/src/components/runs/detail/RunHeaderPanel.tsx
  - console/src/components/runs/detail/SignalsRiskPanel.tsx
  - console/src/app/runs/[runId]/page.tsx
autonomous: true
requirements: [RUNS-03, RUNS-04]
must_haves:
  truths:
    - "Operator opening /runs/{run_id} sees the run header: display name, run type, status, session, timestamps, and verbatim error_message"
    - "Operator sees this run's signals (direction + reason) derived from risk-events filtered to this run"
    - "Operator sees this run's risk decisions including BLOCKED trades with human-readable decision reasons"
    - "When the strategy-wide risk-events fetch hits the API's 100-row cap, a visible disclosure warns rows for this run may be truncated — including when the filtered result is empty (an old run) so 'empty' is never mistaken for 'this run had none'"
    - "Each section shows an as-of timestamp + Refresh and renders an endpoint-named ErrorState on failure"
  artifacts:
    - path: "console/src/components/runs/detail/runScopedFilter.ts"
      provides: "Pure helper: filter items by run_id and detect the MAX_LIMIT cap (capped = rawCount >= 100)"
      exports: ["filterByRun", "isCapped"]
      min_lines: 15
    - path: "console/src/components/runs/detail/runScopedFilter.test.ts"
      provides: "vitest coverage of filter + cap-detection incl the empty-and-capped case"
      min_lines: 25
    - path: "console/src/components/runs/detail/CappedDisclosure.tsx"
      provides: "Reusable truncation-disclosure banner (consumed by 14-03 and 14-04)"
      min_lines: 12
    - path: "console/src/components/runs/detail/RunHeaderPanel.tsx"
      provides: "Run summary header from /api/v1/runs/{runId}; exposes run_type/strategy_id to sibling panels via the page"
      min_lines: 30
    - path: "console/src/components/runs/detail/SignalsRiskPanel.tsx"
      provides: "Signals + risk decisions from /api/v1/operations/risk-events filtered to the run"
      min_lines: 40
    - path: "console/src/app/runs/[runId]/page.tsx"
      provides: "Run detail route: reads runId param, fetches run, composes header + signals/risk panels, passes runId/strategyId/runType contract to children"
      min_lines: 20
  key_links:
    - from: "console/src/components/runs/detail/SignalsRiskPanel.tsx"
      to: "/api/v1/operations/risk-events"
      via: "useApiQuery, then filterByRun client-side"
      pattern: "operations/risk-events"
    - from: "console/src/app/runs/[runId]/page.tsx"
      to: "/api/v1/runs/{runId}"
      via: "useApiQuery for the run header"
      pattern: "api/v1/runs/"
    - from: "console/src/components/runs/detail/SignalsRiskPanel.tsx"
      to: "console/src/components/runs/detail/runScopedFilter.ts"
      via: "filterByRun + isCapped"
      pattern: "filterByRun|isCapped"
---

<objective>
Build the run-detail page shell and its first audit sections. The page reads the `runId` route param, fetches the run summary, and composes: (1) a run header, and (2) a Signals & Risk Decisions section that pulls strategy-wide risk-events and filters them to this run — surfacing per-session signals (RUNS-03) and risk decisions incl. blocked-trade reasons (RUNS-04). This plan ALSO establishes the two shared run-detail primitives (`runScopedFilter` pure helper + `CappedDisclosure` banner) that Plan 14-04 reuses.

Purpose: RUNS-03/RUNS-04 + the honest MAX_LIMIT truncation-disclosure mechanism the whole detail page depends on.
Output: `/runs/[runId]` route shell, RunHeaderPanel, SignalsRiskPanel, CappedDisclosure, and the vitest-tested runScopedFilter helper.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-console-foundation-and-system-status/13-03-SUMMARY.md
@.planning/phases/13-console-foundation-and-system-status/13-04-SUMMARY.md

# Console foundation you MUST reuse:
@console/src/lib/useApiQuery.ts
@console/src/lib/api.ts
@console/src/components/ErrorState.tsx
@console/src/components/FetchMeta.tsx
@console/src/components/status/StatusPanel.tsx
@console/src/components/status/LatestRunPanel.tsx
@console/vitest.config.ts
@console/src/lib/api.test.ts

<constraints>
- FRONTEND ONLY. No backend files. Consume ONLY existing routes: `GET /api/v1/runs/{run_id}` and `GET /api/v1/operations/risk-events`.
- NO direct `fetch()`. Every failure to `ErrorState`; every data view shows `FetchMeta` + Refresh.
- NEW customized Next.js (v16.2.10). The dynamic route param API (`params`) may differ from your training data — READ `console/node_modules/next/dist/docs/` for the current App-Router dynamic-segment / `params` convention (it may be async) BEFORE writing `[runId]/page.tsx`. Heed deprecation notices.
- CRITICAL — MAX_LIMIT / no run_id server filter (VERIFIED): `/api/v1/operations/risk-events` has NO `run_id` query param and caps at `limit` <= 100. Single-run views MUST fetch strategy-wide (`limit=100`) and filter by `run_id` in the browser. This SILENTLY DROPS rows for a run once the strategy exceeds 100 matching rows. You MUST NOT silently truncate: when the raw response returned the cap (>= 100 items) you MUST render the `CappedDisclosure` banner ABOVE the section — INCLUDING when the filtered result is empty (an old run falls outside the 100 most-recent window and yields zero matches; that must read "may be truncated," never "this run had no signals"). Adding a server-side run_id filter is an UNAUTHORIZED backend change — do not propose one.
- vitest is node-env, `src/**/*.test.ts` only. Put the tested logic in a pure `.ts` helper (not the .tsx component).
</constraints>

<interfaces>
<!-- Page shell CONTRACT this plan defines and 14-04 consumes. -->
<!-- After the run resolves, child audit panels receive: -->
```ts
type RunDetailContext = {
  runId: string;        // from route param
  strategyId: string;   // run.strategy_id
  runType: string;      // run.run_type — 14-04 MetricsPanel needs this to pick backtest_run_id vs paper_run_id
};
```
<!-- VERIFIED GET /api/v1/runs/{run_id} response: { run: RunSummary, artifact_counts: {...} } -->
```ts
type RunSummary = {
  run_id: string; strategy_id: string; display_name: string;
  run_type: string; status: string; trigger_source: string;
  as_of_session: string | null; started_at: string; completed_at: string | null;
  parameters_snapshot: Record<string, unknown>;
  result_summary: Record<string, unknown> | null;
  error_message: string | null;
};
type RunDetailResponse = {
  run: RunSummary;
  artifact_counts: {
    backtest_signals: number; backtest_trades: number; backtest_equity_snapshots: number;
    risk_events: number; paper_orders: number; paper_fills: number; execution_events: number;
  };
};
```
<!-- VERIFIED GET /api/v1/operations/risk-events response: build_collection_response { filters, count, items: RiskEventItem[] } -->
```ts
type RiskEventItem = {
  risk_event_id: string;
  run_id: string;              // <-- filter key (client-side)
  strategy_id: string;
  run_type: string; run_status: string;
  symbol: string;
  session_date: string;
  signal_direction: string | null;   // RUNS-03 signal
  signal_reason: string | null;      // RUNS-03 signal
  outcome: string;                    // RUNS-04 risk decision outcome (e.g. approved/blocked)
  decision_code: string | null;      // RUNS-04
  decision_reason: string | null;    // RUNS-04 human-readable blocked reason
  reference_price: string | null; proposed_quantity: string | null; proposed_notional: string | null;
  risk_metadata: Record<string, unknown> | null;
};
```
<!-- Query it as: /api/v1/operations/risk-events?strategy_id=<strategyId>&limit=100 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runScopedFilter pure helper (TDD) + CappedDisclosure banner</name>
  <files>console/src/components/runs/detail/runScopedFilter.ts, console/src/components/runs/detail/runScopedFilter.test.ts, console/src/components/runs/detail/CappedDisclosure.tsx</files>
  <behavior>
    - filterByRun(items, runId) returns only items whose run_id === runId.
    - filterByRun on an empty array returns [].
    - isCapped(rawCount) is true when rawCount >= 100 (the API max), false below 100.
    - Combined: given 100 raw items where NONE match the runId (old run outside window), filterByRun returns [] AND isCapped(100) returns true (the empty-and-capped case that must trigger the disclosure).
    - Given 12 raw items where 3 match, filterByRun returns those 3, isCapped(12) returns false (no disclosure).
  </behavior>
  <action>
Write `runScopedFilter.test.ts` FIRST (RED) covering the behaviors above, then implement `runScopedFilter.ts` (GREEN):
- `export function filterByRun<T extends { run_id: string }>(items: T[], runId: string): T[]`
- `export function isCapped(rawCount: number): boolean` returns `rawCount >= 100`. Add a named const `OPERATIONS_MAX_LIMIT = 100` and a comment tying it to the backend `MAX_LIMIT` cap.
Keep it a pure `.ts` module (node-env vitest can import it).

Then create `CappedDisclosure.tsx` — a small presentational banner (amber/warning styling, distinct from the red ErrorState) that renders ONLY when passed `capped === true`. Props: `{ capped: boolean; rowNoun: string; matchedCount: number }`. Copy MUST convey both the cap and the possibility of missing rows, and must read correctly even when `matchedCount === 0`, e.g.: "Showing {matchedCount} {rowNoun} matched for this run. The API returned its maximum of 100 most-recent {rowNoun} for this strategy, so older {rowNoun} for this run may not appear here." Return `null` when `capped` is false.
  </action>
  <verify>
    <automated>cd console && npx vitest run src/components/runs/detail/runScopedFilter.test.ts && npm run lint</automated>
  </verify>
  <done>All runScopedFilter tests pass (incl empty-and-capped); CappedDisclosure renders only when capped, with copy safe for matchedCount 0. Lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Run detail route shell + RunHeaderPanel + SignalsRiskPanel</name>
  <files>console/src/app/runs/[runId]/page.tsx, console/src/components/runs/detail/RunHeaderPanel.tsx, console/src/components/runs/detail/SignalsRiskPanel.tsx</files>
  <action>
Create `console/src/app/runs/[runId]/page.tsx` — the run detail route. Read the `runId` route param using the CURRENT customized-Next convention (confirm in `node_modules/next/dist/docs/`; it may be an async `params`). Because data fetching uses the `useApiQuery` client hook, the page should resolve `runId` then render a `"use client"` detail component that owns the `/api/v1/runs/{runId}` fetch. That component:
- Renders `<RunHeaderPanel runId={runId} />` (which itself owns the run fetch + freshness), OR fetches the run once at this level and passes the resolved `run` down — your call, but the run must be fetched via useApiQuery, not directly.
- Once the run resolves (`result.ok`), renders the child audit panels with the RunDetailContext: `<SignalsRiskPanel runId={runId} strategyId={run.strategy_id} />`, passing `strategyId={run.strategy_id}` and (reserved for 14-04) making `run.run_type` available.
- Leave a clearly-commented placeholder marking exactly where 14-04's `<OrdersFillsPanel>` and `<MetricsPanel>` attach, using the same `runId` / `strategyId` / `runType` context, so 14-04 is a drop-in.
Do not render child panels before a resolved strategyId (otherwise their query string would be malformed).

Create `RunHeaderPanel.tsx` (`"use client"`, props `{ runId: string }`, and it must OWN its freshness display): `useApiQuery<RunDetailResponse>('/api/v1/runs/' + runId)`. Render the run summary honestly — `display_name` + color-coded status badge, run_type, trigger_source, `as_of_session`, started_at, completed_at, and the verbatim `error_message` in a monospace `<pre>` when non-null. Also surface `artifact_counts` (esp. `risk_events`, `paper_orders`, `paper_fills`, `backtest_signals`) as small count chips. Include a `FetchMeta` header + `ErrorState` branch (endpoint `/api/v1/runs/{runId}`).

Create `SignalsRiskPanel.tsx` (`"use client"`, props `{ runId: string; strategyId: string }`): `useApiQuery<{count:number; items: RiskEventItem[]}>` on `/api/v1/operations/risk-events?strategy_id=<strategyId>&limit=100`. On success compute `matched = filterByRun(data.items, runId)` and `capped = isCapped(data.count)` (use `data.count`, the raw pre-filter count). Render — in THIS order — the `FetchMeta` header, then `<CappedDisclosure capped={capped} rowNoun="risk events" matchedCount={matched.length} />` ABOVE the content (so the truncation warning shows even when `matched` is empty), then two labelled sub-sections built from `matched`:
  - "Signals": per matched row show `symbol`, `signal_direction`, `signal_reason`, `session_date`; show "—" when direction+reason are both null. (RUNS-03)
  - "Risk Decisions": per matched row show `symbol`, `outcome`, `decision_code`, and human-readable `decision_reason`. Visually EMPHASIZE blocked outcomes (red text/badge when `outcome` indicates blocked/rejected) so blocked trades and reasons stand out. (RUNS-04)
On `result.ok === false`: `<ErrorState failure={result} />`. When `matched.length === 0` and NOT capped: explicit "No risk events recorded for this run." When `matched.length === 0` and capped: rely on the CappedDisclosure plus a neutral "No matching rows in the 100 most-recent" line — NOT "this run had none".

Do NOT import the status-screen `StatusPanel`; compose from the lib primitives directly (screen-scoped, per 13-04).
  </action>
  <verify>
    <automated>cd console && npm run build && npm run lint && grep -q "operations/risk-events" src/components/runs/detail/SignalsRiskPanel.tsx && grep -q "filterByRun" src/components/runs/detail/SignalsRiskPanel.tsx && bash -c 'if grep -rnE "(^|[^a-zA-Z.])fetch\(" "src/app/runs/[runId]" src/components/runs/detail | grep -v fetchApi; then exit 1; fi'</automated>
  </verify>
  <done>`/runs/{id}` builds and lints; RunHeaderPanel shows the run summary + artifact counts + verbatim error; SignalsRiskPanel fetches risk-events, filters to the run via filterByRun, shows the CappedDisclosure when count hits 100 (even empty), and separates Signals from Risk Decisions with blocked emphasis; no direct fetch().</done>
</task>

</tasks>

<verification>
- `cd console && npm run build` succeeds; `npm run lint` clean; `npx vitest run` passes (incl runScopedFilter empty-and-capped test).
- `grep -rnE "fetch\(" "console/src/app/runs/[runId]" console/src/components/runs/detail | grep -v fetchApi` returns nothing.
- Behavioral (14-05 checkpoint): opening a run shows header + signals + risk decisions; a blocked risk event shows its decision_reason emphasized; forcing the cap (or an old run) shows the amber truncation disclosure, and an empty old run reads "may be truncated," not "no signals."
</verification>

<success_criteria>
- RUNS-03: run detail shows the run's signals (direction + reason) scoped to that run.
- RUNS-04: run detail shows risk decisions incl. blocked trades with human-readable reasons.
- MAX_LIMIT truncation is surfaced honestly (never silently dropped), including the empty-and-capped case.
- Shared `runScopedFilter` + `CappedDisclosure` primitives exist for 14-04 to reuse.
</success_criteria>

<output>
After completion, create `.planning/phases/14-strategy-and-runs-inspection/14-03-SUMMARY.md`
</output>
