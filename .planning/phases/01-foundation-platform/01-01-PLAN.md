---
phase: 01-foundation-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - pyproject.toml
  - Makefile
  - docker-compose.yml
  - .env.example
  - config/app.yaml
  - config/strategies/trend_following_daily.yaml
  - src/trading_platform/__init__.py
  - src/trading_platform/api/app.py
  - src/trading_platform/api/routes/health.py
  - src/trading_platform/api/routes/system.py
  - src/trading_platform/core/settings.py
  - src/trading_platform/core/logging.py
  - src/trading_platform/worker/__init__.py
  - tests/test_app_boot.py
autonomous: true
requirements:
  - REQ-01
  - REQ-11
  - REQ-12
user_setup: []
must_haves:
  truths:
    - "The local stack can be booted with a short documented command sequence built around Docker Compose."
    - "FastAPI starts through a lifespan-managed bootstrap path and exposes `/health`, `/ready`, and a minimal versioned system surface."
    - "Runtime and strategy configuration load from files plus environment overrides with typed validation."
  artifacts:
    - ".planning/ROADMAP.md aligns Phase 1 success criteria with the locked minimal-schema scope."
    - "pyproject.toml defines the Python package, core dependencies, and local developer tooling entrypoints."
    - "docker-compose.yml, .env.example, and Makefile provide a reproducible local-first operator workflow."
    - "The `src/trading_platform/api` and `src/trading_platform/core` packages exist with a real application bootstrap path."
  key_links:
    - "Docker Compose injects configuration expected by the typed settings loader."
    - "The FastAPI app bootstrap uses the centralized logging and settings modules instead of import-time side effects."
    - "The roadmap wording no longer implies that full trading persistence must land in Phase 1."
---

<objective>
Establish the Phase 1 scaffold: align the planning docs with the locked scope, create the Python project skeleton, and deliver a local boot flow with typed settings, structured logging, and a minimal FastAPI boundary.

Purpose: This creates the operational and architectural foundation the later plans build on, without pulling persistence or strategy execution concerns into the wrong layer.
Output: Local-first scaffolding, validated config loading, Docker Compose boot flow, and a bootable FastAPI app with health and readiness endpoints.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-platform/01-CONTEXT.md
@.planning/phases/01-foundation-platform/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile Phase 1 roadmap scope and scaffold the repository foundation</name>
  <files>.planning/ROADMAP.md, pyproject.toml, Makefile, docker-compose.yml, .env.example, config/app.yaml, config/strategies/trend_following_daily.yaml, src/trading_platform/__init__.py, src/trading_platform/worker/__init__.py</files>
  <action>Patch the Phase 1 section in `.planning/ROADMAP.md` so its persistence language matches the locked context and research: Phase 1 proves the platform with a minimal schema rather than implementing every future trading table. Then scaffold the Python package, dependency manifest, Docker Compose stack, environment example, checked-in config files, and task-runner commands. Use a `src/` layout, keep the stack local-first, and do not introduce Redis, cloud services, or broker/data integrations. The Make targets should cover `up`, `down`, `migrate`, and a placeholder `dry-run` flow even if later plans fill in the implementation.</action>
  <verify>`python -m compileall src` succeeds after scaffolding, and `docker compose config` validates the Compose file without errors.</verify>
  <done>The repo has a real Python package layout, a documented local operator workflow, and the roadmap no longer conflicts with the agreed Phase 1 schema scope.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed settings, structured logging, and the FastAPI bootstrap surface</name>
  <files>src/trading_platform/core/settings.py, src/trading_platform/core/logging.py, src/trading_platform/api/app.py, src/trading_platform/api/routes/health.py, src/trading_platform/api/routes/system.py</files>
  <action>Implement a typed settings loader using file-first config plus environment overrides, following the research recommendation to use `pydantic-settings`. Add centralized logging configuration using the Python stdlib with structured output. Build the FastAPI app around a `lifespan` bootstrap path that wires settings and logging explicitly and avoids import-time side effects. Expose `GET /health`, `GET /ready`, and a minimal versioned route such as `GET /api/v1/system` or equivalent. Keep `/ready` shaped for a dependency-aware check even if the database ping is only fully wired in Plan 02.</action>
  <verify>Start the app with the project entrypoint and confirm `curl` returns `200` for `/health` and the version/system route. `GET /ready` must return a structured response that clearly distinguishes readiness from simple liveness.</verify>
  <done>The app starts through a real bootstrap path, configuration and logging are centralized, and the minimal API surface is live and testable.</done>
</task>

<task type="auto">
  <name>Task 3: Add smoke coverage for app boot and config loading</name>
  <files>tests/test_app_boot.py</files>
  <action>Create minimal tests that prove the settings loader can build a runtime configuration from files and environment values, and that the FastAPI application bootstraps cleanly enough to serve the minimal endpoints. Keep the tests focused on foundation behavior, not future business logic. If additional fixtures or helper modules are needed, create them under `tests/` without expanding the test surface into persistence or strategy execution yet.</action>
  <verify>`pytest tests/test_app_boot.py -q` passes.</verify>
  <done>Phase 1 has an automated smoke check for app boot and config loading, giving later plans a stable foundation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose config` passes
- [ ] `pytest tests/test_app_boot.py -q` passes
- [ ] The API serves `GET /health`, `GET /ready`, and the version/system route locally
- [ ] Make targets or equivalent documented commands exist for `up`, `down`, `migrate`, and `dry-run`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository boots as a real local-first service scaffold instead of a pile of placeholder files
- Phase 1 planning docs and implementation scope are aligned before persistence work begins
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-platform/01-01-SUMMARY.md`
</output>
