---
phase: 01-foundation-platform
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/trading_platform/db/base.py
  - src/trading_platform/db/session.py
  - src/trading_platform/db/models/strategy.py
  - src/trading_platform/db/models/strategy_run.py
  - src/trading_platform/db/models/__init__.py
  - src/trading_platform/api/routes/health.py
  - src/trading_platform/api/routes/system.py
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - alembic/versions/0001_phase1_foundation.py
  - scripts/migrate.py
  - scripts/seed_phase1.py
  - tests/test_db_migrations.py
autonomous: true
requirements:
  - REQ-06
  - REQ-11
  - REQ-12
user_setup: []
must_haves:
  truths:
    - "PostgreSQL connectivity, sessions, and migrations are real rather than implied by placeholders."
    - "The database schema is intentionally minimal for Phase 1 and centered on strategy metadata plus dry-run persistence."
    - "`/ready` performs an actual dependency-aware database check."
  artifacts:
    - "A centralized SQLAlchemy base, session factory, and model package exist under `src/trading_platform/db`."
    - "Alembic is configured with an initial migration that creates the minimal Phase 1 tables."
    - "Migration and seed scripts exist for operator use."
  key_links:
    - "The FastAPI readiness route uses the same database/session setup the CLI and scripts use."
    - "The initial migration reflects the same naming conventions and model metadata used by the ORM."
    - "Seed/bootstrap data creates a real persisted foothold for the later dry-run plan."
---

<objective>
Implement the real persistence layer for Phase 1: SQLAlchemy models, PostgreSQL session management, Alembic migrations, and the minimal seed/bootstrap flow needed to prove the platform is not static scaffolding.

Purpose: This plan turns the service shell from Plan 01 into a system with durable state and a credible migration path, while deliberately staying within the locked minimal-schema scope.
Output: Real database integration, an initial migration, minimal persisted Phase 1 entities, and database-backed readiness and smoke checks.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-platform/01-CONTEXT.md
@.planning/phases/01-foundation-platform/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the SQLAlchemy foundation and minimal Phase 1 models</name>
  <files>src/trading_platform/db/base.py, src/trading_platform/db/session.py, src/trading_platform/db/models/strategy.py, src/trading_platform/db/models/strategy_run.py, src/trading_platform/db/models/__init__.py</files>
  <action>Implement the persistence boundary using synchronous SQLAlchemy 2.x with typed declarative mappings, centralized metadata naming conventions, and a shared session factory. Model only the Phase 1 entities that the context permits: `strategies`, `strategy_runs`, and a lightweight events table only if it materially improves auditability for the dry-run proof. Do not reintroduce broad trading tables from later phases. Ensure model names, statuses, timestamps, and metadata fields are explicit and suited to deterministic dry-run persistence.</action>
  <verify>`python -m compileall src` succeeds, and a small import or smoke script can create the SQLAlchemy metadata without runtime errors.</verify>
  <done>The repo has a real database package with minimal, typed Phase 1 models and session management ready for migrations.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Alembic, create the initial migration, and wire readiness to the database</name>
  <files>alembic.ini, alembic/env.py, alembic/script.py.mako, alembic/versions/0001_phase1_foundation.py, src/trading_platform/api/routes/health.py, src/trading_platform/api/routes/system.py, scripts/migrate.py</files>
  <action>Set up Alembic around the SQLAlchemy metadata from Task 1 and create the initial migration for the minimal Phase 1 schema. Add a migration entrypoint script and wire the API readiness check to perform a real database dependency test, not just a static success response. Keep the migration path standard and boring: no custom migration framework, no async plumbing, and no future-phase tables hidden behind comments or no-op migrations.</action>
  <verify>Run the migration command against the local Postgres setup and confirm `alembic upgrade head` succeeds. `GET /ready` must return success only when the database is reachable.</verify>
  <done>The database can be migrated from scratch, and the API readiness surface reflects actual dependency health.</done>
</task>

<task type="auto">
  <name>Task 3: Add minimal seeding and migration smoke tests</name>
  <files>scripts/seed_phase1.py, tests/test_db_migrations.py</files>
  <action>Create an idempotent seed/bootstrap script that inserts at least the initial strategy metadata record needed by the later dry-run flow. Add tests or smoke checks that exercise migration application and minimal persistence expectations. The tests should prove the schema exists and can hold the Phase 1 records without turning into a broad integration suite.</action>
  <verify>`pytest tests/test_db_migrations.py -q` passes, and running the seed script twice does not create duplicate strategy metadata records.</verify>
  <done>Phase 1 now has a repeatable migration path and a minimal persisted dataset that later plans can build on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `alembic upgrade head` passes against the local Postgres service
- [ ] `pytest tests/test_db_migrations.py -q` passes
- [ ] The seed/bootstrap script is idempotent
- [ ] `GET /ready` fails when the database is unavailable and succeeds when it is reachable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Persistence is real and limited to the agreed Phase 1 proof scope
- The platform has a clean migration path that later phases can extend without rework
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-platform/01-02-SUMMARY.md`
</output>
