---
phase: 01-foundation-platform
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/trading_platform/strategies/base.py
  - src/trading_platform/strategies/registry.py
  - src/trading_platform/strategies/trend_following_daily/__init__.py
  - src/trading_platform/strategies/trend_following_daily/strategy.py
  - src/trading_platform/services/bootstrap.py
  - src/trading_platform/services/data.py
  - src/trading_platform/services/risk.py
  - src/trading_platform/services/execution.py
  - src/trading_platform/services/analytics.py
  - src/trading_platform/api/routes/strategies.py
  - scripts/dry_run.py
  - tests/test_strategy_registry.py
  - tests/test_dry_run.py
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-06
user_setup: []
must_haves:
  truths:
    - "At least one strategy can be discovered, described, and resolved through a real registry."
    - "A dry strategy bootstrap run persists a `strategy_run` record and logs the result without any market-data or broker integration."
    - "The CLI remains the primary operator surface while the API exposes a thin read boundary for strategy visibility."
  artifacts:
    - "A base strategy contract and registry exist under `src/trading_platform/strategies`."
    - "Placeholder interfaces for data, risk, execution, and analytics exist under `src/trading_platform/services`."
    - "A `scripts/dry_run.py` entrypoint and registry/dry-run tests exist."
  key_links:
    - "The dry-run path reads typed strategy config, resolves a strategy from the registry, and persists status through the database layer."
    - "The API strategy route reflects the same registry metadata used by the CLI dry-run path."
    - "Structured logs capture startup and dry-run lifecycle events in a form an operator can inspect."
---

<objective>
Implement the Phase 1 extensibility proof: strategy contracts, registry, placeholder platform-service interfaces, and the dry execution path that persists a bootstrap run for the initial strategy module.

Purpose: This proves the project is a real strategy platform rather than an API shell with a database, while still avoiding market data, brokers, risk logic, or backtesting work that belongs in later phases.
Output: Strategy registry, placeholder service contracts, thin strategy visibility route, and a CLI dry-run flow that persists and logs a strategy run.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-platform/01-CONTEXT.md
@.planning/phases/01-foundation-platform/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement strategy contracts, registry, and placeholder platform interfaces</name>
  <files>src/trading_platform/strategies/base.py, src/trading_platform/strategies/registry.py, src/trading_platform/strategies/trend_following_daily/__init__.py, src/trading_platform/strategies/trend_following_daily/strategy.py, src/trading_platform/services/data.py, src/trading_platform/services/risk.py, src/trading_platform/services/execution.py, src/trading_platform/services/analytics.py</files>
  <action>Implement a base strategy contract with explicit metadata and configuration hooks, then build a registry that can discover or register the initial `trend_following_daily` strategy module. Add placeholder interfaces for data, risk, execution, and analytics that make the architecture boundaries real without pretending those systems are implemented. Keep the initial strategy intentionally empty or mocked: it should be resolvable, configurable, and describable, but it must not ingest market data, generate signals, or call a broker.</action>
  <verify>`pytest tests/test_strategy_registry.py -q` passes, and a registry smoke check can list the initial strategy with name, version, and enabled/config metadata.</verify>
  <done>The strategy layer is real, pluggable, and clearly separated from the placeholder services around it.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dry bootstrap flow and persist strategy runs</name>
  <files>src/trading_platform/services/bootstrap.py, scripts/dry_run.py</files>
  <action>Implement the dry execution path described in the locked context: load app and strategy configuration, resolve the strategy from the registry, create a `strategy_run` in an explicit mode such as `dry_run` or `bootstrap`, persist status transitions, and emit structured logs for start, success, and failure. Use the real database layer from Plan 02. Keep the CLI as the primary operator surface. Do not add HTTP-triggered mutation unless it is strictly needed to complete the proof.</action>
  <verify>Run the dry-run command for `trend_following_daily` and confirm it exits successfully, writes structured logs, and persists a strategy run record with timestamps and status in PostgreSQL.</verify>
  <done>The operator can prove the platform works end to end by executing a dry strategy run and inspecting both logs and persisted state.</done>
</task>

<task type="auto">
  <name>Task 3: Expose strategy visibility and add dry-run persistence tests</name>
  <files>src/trading_platform/api/routes/strategies.py, tests/test_strategy_registry.py, tests/test_dry_run.py</files>
  <action>Add a thin read-only strategy route, such as `GET /strategies`, that reflects the registry metadata without introducing strategy management workflows. Expand tests to cover registry behavior and the dry-run persistence path, including failure-safe behavior if a strategy cannot be resolved or configuration is invalid. Keep the API read-only in this phase unless a write endpoint is required to complete the agreed proof.</action>
  <verify>`pytest tests/test_strategy_registry.py tests/test_dry_run.py -q` passes, and `curl` against `GET /strategies` returns the registered strategy metadata.</verify>
  <done>The platform exposes its initial strategy boundary through both CLI and API surfaces, with tests proving dry-run persistence and registry behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest tests/test_strategy_registry.py tests/test_dry_run.py -q` passes
- [ ] Running the dry-run script persists a `strategy_run` record for the registered strategy
- [ ] Structured logs show the dry-run lifecycle clearly enough to inspect success or failure
- [ ] `GET /strategies` returns registry-backed strategy metadata
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The platform proves real strategy extensibility without leaking into later-phase trading logic
- A single operator can inspect a dry strategy run through persisted state and logs with confidence
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-platform/01-03-SUMMARY.md`
</output>
