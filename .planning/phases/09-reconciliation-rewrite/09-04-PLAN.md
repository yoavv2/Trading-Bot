---
phase: 09-reconciliation-rewrite
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - src/trading_platform/services/reconciliation.py
  - src/trading_platform/services/paper_execution.py
  - src/trading_platform/services/analytics.py
  - src/trading_platform/services/operator_status.py
  - src/trading_platform/services/operator_reads.py
  - tests/test_execution_reconciliation.py
  - tests/test_paper_execution.py
  - tests/test_analytics_service.py
autonomous: true
requirements: [RECON-04]
must_haves:
  truths:
    - "Corrective action (sync-failure/threshold state mutation + in-flight recovery) is a separate explicit entrypoint that reconcile never calls"
    - "The read-only reconcile path and the corrective path share no code that mutates execution state"
    - "Downstream consumers (analytics, operator_status, operator_reads) read the closed-enum finding taxonomy without breaking"
  artifacts:
    - path: "src/trading_platform/services/reconciliation.py"
      provides: "apply_reconciliation_corrections() explicit corrective entrypoint distinct from reconcile"
      contains: "def apply_reconciliation_corrections"
    - path: "src/trading_platform/services/paper_execution.py"
      provides: "session runner invokes correction as a distinct step from read-only reconcile"
      contains: "reconcile_paper_execution"
  key_links:
    - from: "apply_reconciliation_corrections"
      to: "PaperOrder.sync_failure_count / recover_inflight_paper_orders"
      via: "the only path that writes execution state; never called by reconcile_paper_execution"
      pattern: "sync_failure_count"
    - from: "analytics/operator_status/operator_reads"
      to: "ExecutionEvent.event_type"
      via: "consume enum-name taxonomy (MISSING_LOCAL, ...); no old string event_types referenced"
      pattern: "event_type"
---

<objective>
Complete the read-only/correction separation (RECON-04): relocate the execution-state
mutation that used to run inside reconcile into an explicit, separately-invoked corrective
entrypoint, rewire the paper-session runner to call reconcile (read-only) and correction
(mutating) as distinct steps on distinct code paths, and migrate every downstream consumer
to the closed-enum finding taxonomy. Re-home the mutation tests that 09-03 skipped onto the
new corrective entrypoint.

Purpose: RECON-04 — corrective action is a separate explicit step invoked after findings are
reviewed; reconciliation and correction never share a mutating code path. Plus close the
consumer-migration loop the taxonomy change opened (analytics/operator_status/operator_reads).
Output: `apply_reconciliation_corrections`, rewired session runner, migrated consumers,
full green suite.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/trading_platform/services/reconciliation.py
@src/trading_platform/services/paper_execution.py

<interfaces>
<!-- Session runner today (paper_execution.py ~819-840) does: load_broker_state -> recover_inflight ->
     reconcile. recover_inflight_paper_orders ALREADY mutates and is already a separate function; the new
     corrective entrypoint owns the sync-failure INCREMENT that 09-03 removed from reconcile (the read-only threshold blocking evaluation stays in reconcile per D2). -->
<!-- Consumers of finding taxonomy (must be migrated to enum names MISSING_LOCAL / MISSING_BROKER /
     QUANTITY_MISMATCH / PRICE_MISMATCH / STATE_MISMATCH):
       analytics.py:257-266  -> recent_execution_findings serializes event_type
       analytics.py:506-507  -> markdown lists event_type
       operator_status.py:217 -> renders event['event_type']
       operator_reads.py:478  -> serializes execution_event.event_type
     These read event_type as an opaque string, so they keep working, BUT their TESTS
     (test_analytics_service.py, test_api_reads.py) may assert specific old string values — update those. -->
</interfaces>

<decisions>
<!-- RECON-04 minimal reading (chosen): reconcile is pure; correction is a distinct entrypoint reconcile
     never calls. Keep the session runner's recover->reconcile flow (recover stays a separate function,
     reconcile stays read-only, new corrective mutation is its own function). We do NOT introduce a
     human-review gate before correction (that would be a paper-trading behavior change beyond RECON-04's
     testable invariant). Surface this choice in the SUMMARY. -->
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract apply_reconciliation_corrections() as the explicit mutating entrypoint</name>
  <files>src/trading_platform/services/reconciliation.py, tests/test_execution_reconciliation.py</files>
  <action>
    Create `apply_reconciliation_corrections(strategy_id, *, findings|report, settings=None, registry=None,
    checked_at=None)` — a NEW explicit entrypoint that owns ALL execution-state mutation formerly buried in
    reconcile: the `_apply_sync_failure_state` behavior — WRITING PaperOrder.sync_failure_count / last_sync_error /
    last_sync_failure_at (the INCREMENT). It takes the read-only report's
    findings (or re-derives per-order error messages from them) and applies the mutation in its own
    `session_scope`. `reconcile_paper_execution` MUST NOT call it. IMPORTANT (per 09-03 decision D2): the
    repeated-failure THRESHOLD *evaluation* that feeds blocks_execution stays read-only in reconcile — this
    corrective entrypoint owns only the WRITE/increment, NOT the blocking decision. Do not re-introduce
    blocking logic here. Keep `recover_inflight_paper_orders` as-is
    (it is already a separate mutating function).
    Re-home the tests 09-03 skipped: un-skip `test_reconciliation_persists_blocking_findings_and_updates_sync_failure_state`,
    `..._resets_sync_failures`, `..._blocks_after_repeated_submission_failures` and rewrite them to (1) call
    read-only `reconcile_paper_execution`, then (2) call `apply_reconciliation_corrections` and assert the
    sync-failure/threshold MUTATION now happens there — proving the behavior moved rather than vanished.
    Add a test asserting `reconcile_paper_execution` alone leaves sync_failure_count untouched (the RECON-04
    separation, complementary to 09-03's no-mutation test).
  </action>
  <verify>
    <automated>python -m pytest tests/test_execution_reconciliation.py -x -q</automated>
  </verify>
  <done>apply_reconciliation_corrections exists and is the only path mutating sync-failure/threshold state; reconcile never calls it; relocated tests pass against the new entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire paper-session runner to call reconcile then correction as distinct steps</name>
  <files>src/trading_platform/services/paper_execution.py, tests/test_paper_execution.py</files>
  <action>
    In the session runner (~lines 819-840), keep `load_broker_state` -> `recover_inflight_paper_orders`
    (recovery/correction) as a distinct step, then call `reconcile_paper_execution` (now read-only), then
    invoke `apply_reconciliation_corrections` as its own explicit step AFTER the read-only report is produced.
    The session-blocking gate (`reconciliation_report.blocks_execution` +
    `block_on_unresolved_reconciliation`) is unchanged. Ensure no code path both reconciles and mutates in one
    call. Update `tests/test_paper_execution.py` if it asserted the old single-call reconcile-mutates behavior;
    add/confirm a test that the runner still blocks the session when the read-only report says blocks_execution.
  </action>
  <verify>
    <automated>python -m pytest tests/test_paper_execution.py -x -q</automated>
  </verify>
  <done>Session runner performs read-only reconcile and explicit correction as separate calls; blocking behavior preserved; paper-execution tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate downstream consumers + full-suite regression</name>
  <files>src/trading_platform/services/analytics.py, src/trading_platform/services/operator_status.py, src/trading_platform/services/operator_reads.py, tests/test_analytics_service.py</files>
  <action>
    Confirm analytics.py / operator_status.py / operator_reads.py read `event_type` as an opaque string and
    render the new enum-name taxonomy correctly (they surface findings; they should not branch on specific old
    string values). Fix any place that hard-codes an old event_type string (e.g. a special-case for
    `reconciliation_clean` — replace with the finding_count==0 / empty-findings signal from 09-03's report).
    Update `tests/test_analytics_service.py` (and `tests/test_api_reads.py` if it asserts finding strings) to
    the enum-name taxonomy. Then run the FULL suite and fix any remaining fallout from the taxonomy/mutation
    move. This task closes the consumer-migration loop the rewrite opened.
  </action>
  <verify>
    <automated>python -m pytest -q</automated>
  </verify>
  <done>All consumers render the closed-enum taxonomy; no code or test references old string event_types except intentionally; full repo suite is green.</done>
</task>

</tasks>

<verification>
- `python -m pytest -q` passes (whole repo — no regressions).
- `grep -rn "reconciliation_clean\|order_status_mismatch\|position_mismatch\|order_missing" src/ tests/` returns nothing (old string taxonomy fully removed) except deliberate references.
- `grep -n "apply_reconciliation_corrections\|_apply_sync_failure_state" src/trading_platform/services/reconciliation.py` confirms mutation lives only in the corrective entrypoint and reconcile never calls it.
</verification>

<success_criteria>
- Correction is a separate explicit entrypoint; reconcile and correction never share a mutating code path (RECON-04).
- Session runner invokes read-only reconcile and explicit correction as distinct steps; blocking preserved.
- All downstream consumers and tests migrated to the closed-enum taxonomy; full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-reconciliation-rewrite/09-04-SUMMARY.md` — record the RECON-04 minimal-reading decision (no human-review gate added) and confirm Phase 9 requirement coverage RECON-01..09.
</output>
