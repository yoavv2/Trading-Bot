---
phase: 09-reconciliation-rewrite
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/trading_platform/services/reconciliation.py
  - tests/test_execution_reconciliation.py
autonomous: true
requirements: [RECON-01, RECON-02, RECON-03, RECON-09]
must_haves:
  truths:
    - "Running reconciliation performs zero writes to execution state (paper_orders, positions, account_snapshots)"
    - "Broker snapshot drives current-quantity findings; local DB drives intent/history findings"
    - "Reconciliation always emits one materialized report (StrategyRun + ExecutionEvents) with closed-enum findings tied to their source snapshots"
    - "A clean/flat reconcile still emits the materialized report with zero findings (not a fake 'clean' finding)"
    - "blocks_execution is preserved: it still trips on the read-only account-divergence AND read-only repeated-failure-threshold evaluations"
  artifacts:
    - path: "src/trading_platform/services/reconciliation.py"
      provides: "read-only reconcile_paper_execution wired to typed snapshots + match_snapshots + materialized report"
      contains: "match_snapshots"
    - path: "tests/test_execution_reconciliation.py"
      provides: "no-mutation invariant test + closed-enum report assertions + clean-run report test + threshold-still-blocks test"
      exports: []
  key_links:
    - from: "reconcile_paper_execution"
      to: "reconciliation_matcher.match_snapshots"
      via: "typed local+broker snapshots passed to pure matcher"
      pattern: "match_snapshots\\("
    - from: "reconcile_paper_execution"
      to: "ExecutionEvent rows"
      via: "one materialized report per run; findings serialized via Finding.to_event_dict"
      pattern: "ExecutionEvent\\("
    - from: "reconcile_paper_execution"
      to: "PaperOrder/Position/AccountSnapshot"
      via: "READ-ONLY select only — no attribute writes, no _apply_sync_failure_state"
      pattern: "select\\("
---

<objective>
Rewrite `reconcile_paper_execution` into a strictly read-only orchestrator: load broker and
local state as the typed snapshots (09-01), run the pure indexed matcher (09-02), and
materialize exactly one report (StrategyRun row + ExecutionEvent rows) whose findings are
closed-enum values tied to their source snapshots. Remove the execution-state MUTATION
(`_apply_sync_failure_state` increment) from reconcile — that write relocates to the
corrective path in 09-04 — while PRESERVING the read-only blocking evaluations
(account divergence and repeated-failure threshold) so today's session-blocking safety
guard is not silently dropped.

Purpose: RECON-01 (broker = source of truth for quantities), RECON-02 (local = source of
truth for intent/history), RECON-03 (read-only — zero execution-state writes), RECON-09
(one materialized report, findings tied to source snapshots).
Output: read-only `reconcile_paper_execution` + no-mutation, clean-run, and threshold-still-blocks tests.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/trading_platform/services/reconciliation.py
@src/trading_platform/services/reconciliation_types.py
@src/trading_platform/services/reconciliation_matcher.py

<interfaces>
<!-- Preserve these callable signatures/return contract — paper_execution.py:830 and worker/__main__.py:489 call them. -->
```python
def reconcile_paper_execution(strategy_id=None, *, as_of_session, settings=None, registry=None,
    broker_client=None, broker_state=None, recovered_order_count=0,
    trigger_source="paper_reconciliation") -> ReconciliationReport
```
<!-- ReconciliationReport.to_dict() and its .blocks_execution / .run_id / .findings are consumed by
     paper_execution.py (session gate at line 842) and operator_status/analytics. Keep the report shape;
     only the finding taxonomy inside it changes to enum .name values.
     Finding.to_event_dict() -> {"event_type": category.name, "severity", "blocks_execution",
                                  "message", "paper_order_id", "details"} (from 09-01).
     Today's threshold blocking (reconciliation.py:610-652): submission_attempt_count >= failure_threshold
     OR sync_failure_count + 1 >= failure_threshold each set blocks_execution=True and feed the session gate. -->
</interfaces>

<decisions>
<!-- SCOPE DECISIONS baked in here, surfaced (not buried) per requirement-fidelity: -->
<!-- D1 (account-snapshot): cash/equity/buying_power divergence is NOT one of the 5 closed finding
     categories, so it does NOT enter the `findings` list. It stays a READ-ONLY comparison in this
     orchestrator and, if divergent, is recorded as a report-summary flag (`account_divergence: {...}`)
     that STILL contributes to `blocks_execution`. PRESERVE today's non-blocking branches: a never-synced
     account with a flat book (`account_snapshot_not_yet_synced`) must remain NON-blocking; only a genuine
     value divergence blocks. -->
<!-- D2 (repeated-failure threshold — SPLIT read from write, symmetric with D1): the threshold has a READ
     half and a WRITE half.
       READ half (STAYS in reconcile, read-only): evaluate the SAME predicates today's code uses
         (submission_attempt_count >= failure_threshold for SUBMISSION_FAILED orders;
          sync_failure_count + 1 >= failure_threshold for orders with a sync error THIS run) as a pure read,
         producing a `threshold_breach` report-summary flag that STILL feeds blocks_execution. This preserves
         the session-blocking safety guard — dropping it would silently stop blocking a strategy past its
         repeated-failure threshold, which is unacceptable for this platform.
       WRITE half (MOVES to 09-04 corrective path): incrementing PaperOrder.sync_failure_count and setting
         last_sync_error / last_sync_failure_at. Reconcile MUST NOT perform this write. -->
<!-- D3 (clean state): no synthetic `reconciliation_clean` finding. A clean run emits the materialized
     report with an EMPTY findings tuple and blocks_execution=False (RECON-08/09). The "clean" signal lives
     in result_summary (finding_count=0), not in a fake finding. -->
</decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load typed local+broker snapshots, drive the pure matcher, and compute read-only blocking signals</name>
  <files>src/trading_platform/services/reconciliation.py</files>
  <action>
    In `reconcile_paper_execution`, replace the `_build_findings(...)` call and the direct use of raw ORM
    objects with: (a) load broker state (already typed via BrokerStateSnapshot); (b) inside `session_scope`,
    run the existing read-only `select(...)` queries for PaperOrder / PaperFill / Position / AccountSnapshot,
    then PROJECT each ORM row into the typed 09-01 snapshots (`LocalOrderSnapshot`, `LocalFillSnapshot`,
    `LocalPositionSnapshot`, `LocalAccountSnapshot`) — do not pass ORM instances beyond this projection
    boundary; (c) call `match_snapshots(...)` to get `tuple[Finding, ...]`.
    DELETE `_build_findings`, `_order_error_messages`, and the CALL to `_apply_sync_failure_state` from this
    module's reconcile path. Leave a `# increment moved to corrective path (09-04)` breadcrumb. The
    sync-failure/threshold INCREMENT must not run inside reconcile anymore (09-04 relocates it).
    Add TWO read-only blocking evaluations (no row writes):
      - `account_divergence` (D1): reuse today's cash/buying_power/equity/gross_exposure/open_positions delta
        logic PLUS preserve the `account_snapshot_not_yet_synced` NON-blocking branch (never-synced + flat book
        => not blocking). Produce an `account_divergence` dict only for genuine divergence.
      - `threshold_breach` (D2): evaluate today's predicates as a PURE READ over the loaded local snapshots
        (submission_attempt_count >= failure_threshold; sync_failure_count + 1 >= failure_threshold for orders
        with a sync error this run) => a `threshold_breach` list/flag. NO increment, NO row write.
    RECON-01/02: broker snapshot supplies current quantities/positions/fills; local supplies intent/history —
    ensure MISSING_LOCAL/MISSING_BROKER directionality reflects this (already handled by the matcher).
  </action>
  <verify>
    <automated>python -m pytest tests/test_execution_reconciliation.py -x -q</automated>
  </verify>
  <done>reconcile uses typed snapshots + match_snapshots; _build_findings removed; no _apply_sync_failure_state call in reconcile; account + threshold evaluations are read-only (no row writes).</done>
</task>

<task type="auto">
  <name>Task 2: Materialize one closed-enum report, preserve blocks_execution, and add invariant/clean/threshold tests</name>
  <files>src/trading_platform/services/reconciliation.py, tests/test_execution_reconciliation.py</files>
  <action>
    Persist exactly ONE materialized report per run (RECON-09): keep the StrategyRun (RECONCILIATION type)
    create/update, and write one ExecutionEvent per `Finding` via `Finding.to_event_dict()` so
    `event_type` is the enum `.name` (MISSING_LOCAL / MISSING_BROKER / QUANTITY_MISMATCH / PRICE_MISMATCH /
    STATE_MISMATCH). A clean run writes the report with zero ExecutionEvent findings and
    `result_summary.finding_count=0` (decision D3 — NO synthetic `reconciliation_clean` finding).
    Set `blocks_execution = bool(findings) or bool(account_divergence) or bool(threshold_breach)` (D1+D2) —
    this preserves today's session-blocking guard. Persist `account_divergence` and `threshold_breach` into
    `result_summary` so the report explains WHY it blocks. Ensure each persisted finding ties to its source
    snapshot: include the identity `(symbol, account, side)` and source ids (paper_order_id / broker_order_id /
    broker_fill_id) in `details`, and keep the report's `run_id`/`as_of_session`.
    Update the ReconciliationReport reconstruction to read enum-named event_types.
    Then update/add tests in `tests/test_execution_reconciliation.py`:
      - Update assertions expecting old string event_types to enum names. SPECIFICALLY fix
        `test_reconciliation_prefers_client_order_id_when_version_chain_exists`: its
        `assert "order_status_mismatch" not in ...` is now VACUOUSLY true — change it to
        `assert "STATE_MISMATCH" not in {event_types}` so it still tests something.
      - ADD a no-mutation test (RECON-03): capture PaperOrder.sync_failure_count/last_sync_error, Position rows,
        and the AccountSnapshot BEFORE `reconcile_paper_execution`, run it against a DIVERGENT broker fixture,
        assert those execution-state rows are unchanged afterward, while the StrategyRun report + ExecutionEvent
        findings WERE written.
      - ADD a clean-run test (RECON-09/D3): a clean/flat reconcile still emits the materialized StrategyRun
        report with `result_summary.finding_count == 0`, ZERO ExecutionEvent findings, and blocks_execution=False.
      - ADD a threshold-still-blocks test (D2): with a SUBMISSION_FAILED order past `failure_threshold` and an
        otherwise-aligned broker, `reconcile_paper_execution` returns blocks_execution=True WITHOUT mutating
        sync_failure_count (the block comes from the read-only threshold_breach evaluation).
    NOTE: tests asserting the OLD in-reconcile sync-failure MUTATION
    (`test_reconciliation_persists_blocking_findings_and_updates_sync_failure_state`,
    `test_reconciliation_persists_clean_event_and_resets_sync_failures`) will now fail on their mutation
    assertions because the INCREMENT relocates to 09-04. Mark those
    `@pytest.mark.skip(reason="sync-failure increment relocates to corrective path in 09-04")` with a
    breadcrumb so 09-04 re-homes them onto the corrective entrypoint. (The clean-run REPORT behavior is covered
    by the new clean-run test above, so no clean-report coverage is lost by skipping the old one.)
  </action>
  <verify>
    <automated>python -m pytest tests/test_execution_reconciliation.py -x -q</automated>
  </verify>
  <done>One report per run with enum-named findings tied to source snapshots; clean run = empty findings + report; blocks_execution still trips on account divergence and threshold breach; no-mutation, clean-run, and threshold-still-blocks tests pass; relocated mutation tests explicitly skipped with breadcrumb.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_execution_reconciliation.py -q` passes (with the 2 mutation-relocation tests skipped).
- `grep -n "_apply_sync_failure_state\|_build_findings" src/trading_platform/services/reconciliation.py` shows no CALL inside `reconcile_paper_execution`.
- No-mutation test proves PaperOrder/Position/AccountSnapshot rows are unchanged across a reconcile of divergent state.
- Threshold-still-blocks test proves a repeated-failure order still trips blocks_execution without mutation.
- `python -m pytest tests/test_paper_execution.py -q` still passes (session gate still reads blocks_execution).
</verification>

<success_criteria>
- reconcile is read-only over execution state; zero writes to paper_orders/positions/account_snapshots (RECON-03).
- Broker drives current-quantity findings, local drives intent/history findings (RECON-01, RECON-02).
- Exactly one materialized report per run; findings are closed-enum, tied to source snapshots; clean = empty report (RECON-09).
- blocks_execution behavior preserved via read-only account_divergence AND threshold_breach flags (no safety regression).
</success_criteria>

<output>
After completion, create `.planning/phases/09-reconciliation-rewrite/09-03-SUMMARY.md` — explicitly record decisions D1/D2/D3, confirming the threshold blocking guard is preserved (read-only) while its increment relocates to 09-04.
</output>
