---
phase: 08-concurrency-guard
plan: 05
type: execute
wave: 4
depends_on: ["08-04"]
files_modified:
  - src/trading_platform/worker/__main__.py
  - tests/test_concurrency_guard_e2e.py
autonomous: true
requirements: [LOCK-01, LOCK-06]
must_haves:
  truths:
    - "Running submit-paper-orders (or run-paper-session) while another session holds the tuple's lock exits with the reserved non-zero code and no traceback, plus a WARNING log naming the tuple"
    - "After a run holding the lock crashes (connection dropped mid-run), a subsequent run acquires the lock cleanly with no manual intervention and reclaims the crashed run's leftover running row as STALE"
  artifacts:
    - path: "src/trading_platform/worker/__main__.py"
      provides: "ConcurrentRunLockedError → reserved exit code + WARNING, no traceback, for both paper commands"
      contains: "ConcurrentRunLockedError"
    - path: "tests/test_concurrency_guard_e2e.py"
      provides: "crash/restart lock-release proof + CLI exit-code test"
  key_links:
    - from: "run_submit_paper_orders_command / run_paper_session_command"
      to: "SystemExit(CONCURRENT_RUN_LOCK_EXIT_CODE)"
      via: "except ConcurrentRunLockedError → log WARNING → raise SystemExit(code)"
      pattern: "CONCURRENT_RUN_LOCK_EXIT_CODE"
---

<objective>
Map the typed lock denial to a dedicated non-zero CLI exit code (no traceback) for both side-effecting worker commands, and prove the crash-release guarantee end-to-end: a crashed lock-holder's lock auto-releases and a subsequent run acquires cleanly and reclaims the leftover running row.

Purpose: A scheduler/operator must distinguish "another run holds it" from a real crash by exit code alone, and LOCK-06 requires a restart/crash test that confirms automatic release + clean reacquire without manual intervention. This is the phase's verification capstone.
Output: Exit-code wiring in the worker CLI + a crash/restart e2e test.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-concurrency-guard/08-CONTEXT.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- From 08-02 — services/concurrency_guard.py -->
```python
class ConcurrentRunLockedError(RuntimeError): strategy_id: str; session_date: date
CONCURRENT_RUN_LOCK_EXIT_CODE = 3
def session_run_lock(*, strategy_id, session_date, settings=None): ...
def advisory_lock_key(strategy_id, session_date) -> int: ...
```
<!-- From 08-03 — services/stale_runs.py -->
```python
def find_stale_runs(session, *, timeout_minutes) -> list[StrategyRun]: ...
def reclaim_stale_runs(session, *, strategy_public_id, session_date, timeout_minutes, reclaiming_run_id=None) -> list[uuid.UUID]: ...
```
<!-- Existing worker CLI — src/trading_platform/worker/__main__.py -->
<!--   run_submit_paper_orders_command(args) -> None  (calls run_paper_order_submission) -->
<!--   run_paper_session_command(args) -> None         (calls run_paper_session) -->
<!--   main() dispatches on args.command and returns None; SystemExit(int) sets the process code with no traceback -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map ConcurrentRunLockedError to the reserved exit code in the worker CLI</name>
  <files>src/trading_platform/worker/__main__.py</files>
  <action>
    Import `ConcurrentRunLockedError` and `CONCURRENT_RUN_LOCK_EXIT_CODE` from `trading_platform.services.concurrency_guard`.
    In BOTH `run_submit_paper_orders_command` and `run_paper_session_command`, wrap the `run_paper_order_submission` / `run_paper_session` call in `try/except ConcurrentRunLockedError as exc:`. In the handler:
    - Emit a WARNING structured log (`event="paper_command_lock_denied"`, fields: strategy_id, session_date, command name) via the existing `emit_structured_log` / logger pattern used elsewhere in the file. (The service layer in 08-04 also logs the denial; a second CLI-level WARNING that states the exit-code decision is intended.)
    - Print a single concise human line to stderr (no JSON dump, no traceback) explaining another session holds the lock for the tuple.
    - `raise SystemExit(CONCURRENT_RUN_LOCK_EXIT_CODE)` — this sets the process exit code and produces no traceback.
    Do NOT let the exception reach argparse (which would exit 2). A normal successful run must still exit 0.
  </action>
  <verify>
    <automated>pytest tests/test_concurrency_guard_e2e.py -x -k "exit_code"</automated>
  </verify>
  <done>Both paper commands exit with code 3 and no traceback when the lock is held, log a WARNING naming the tuple, and still exit 0 on success.</done>
</task>

<task type="auto">
  <name>Task 2: Crash/restart e2e — auto-release + clean reacquire + stale reclaim</name>
  <files>tests/test_concurrency_guard_e2e.py</files>
  <action>
    Create `tests/test_concurrency_guard_e2e.py` (self-provisioned temp Postgres, same fixture pattern as `tests/test_paper_execution.py`; import `build_default_registry`, a `FakeExecutionService`, and seed a strategy + a succeeded risk_evaluation run with approved candidates so submission has work — reuse/adapt the seeding helpers from `tests/test_paper_execution.py`).

    Test A — exit code (supports Task 1): construct `args` for `submit-paper-orders`, hold the tuple's advisory lock on a raw connection (`pg_try_advisory_lock(advisory_lock_key(...))`), invoke `run_submit_paper_orders_command(args)` and assert it raises `SystemExit` with `.code == CONCURRENT_RUN_LOCK_EXIT_CODE`; assert no new paper_execution run row and no broker submission occurred.

    Test B — crash-release + reclaim (LOCK-06): simulate a crashed holder by (1) opening a raw connection, taking the advisory lock, and INSERTING a StrategyRun paper_execution row at status=running with started_at ~40 min in the past and parameters_snapshot.as_of_session = the tuple's session_date (committed on a separate connection so it is durable), then (2) force-closing the lock-holding connection WITHOUT unlocking (the crash). Then run a real `run_paper_order_submission(...)` for the same tuple with a `FakeExecutionService`. Assert: it acquires the lock cleanly (no ConcurrentRunLockedError, no hang), the old durable running row is now status=STALE with a reclaim ExecutionEvent, and the new run reaches a terminal succeeded state — all with zero manual intervention.
  </action>
  <verify>
    <automated>pytest tests/test_concurrency_guard_e2e.py -x</automated>
  </verify>
  <done>A held lock forces exit code 3 with no side effects; a crashed holder's lock auto-releases, the leftover running row is reclaimed STALE, and the next run completes cleanly — LOCK-06 proven end-to-end.</done>
</task>

</tasks>

<verification>
- `submit-paper-orders` and `run-paper-session` exit with the reserved code (no traceback) under contention; exit 0 on success.
- A dropped lock-holder connection auto-releases the lock; the next run acquires cleanly and reclaims the crashed run as STALE.
</verification>

<success_criteria>
The lock-denied path is operator/scheduler-distinguishable via a dedicated exit code (LOCK-01 exit path), and the crash/restart guarantee is proven by an automated end-to-end test (LOCK-06). With 08-01..08-04, the phase goal — at most one side-effecting run per (strategy_id, session_date), lock before side effects, released on all exit paths including crash, stale runs detectable and cleanly reclaimed — is fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/08-concurrency-guard/08-05-SUMMARY.md`
</output>
