---
phase: 08-concurrency-guard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_platform/services/concurrency_guard.py
  - tests/test_concurrency_guard.py
autonomous: true
requirements: [LOCK-01, LOCK-06]
must_haves:
  truths:
    - "A second attempt to hold the same (strategy_id, session_date) advisory lock while the first holds it fails immediately with a typed ConcurrentRunLockedError — non-blocking, no hang"
    - "The advisory lock is held for the whole guarded region on ONE dedicated connection and is released on normal exit"
    - "When the lock-holding connection drops (simulated crash), PostgreSQL auto-releases the lock and a fresh acquisition succeeds cleanly"
  artifacts:
    - path: "src/trading_platform/services/concurrency_guard.py"
      provides: "ConcurrentRunLockedError, advisory_lock_key(), session_run_lock() context manager, CONCURRENT_RUN_LOCK_EXIT_CODE"
      exports: ["ConcurrentRunLockedError", "session_run_lock", "advisory_lock_key", "CONCURRENT_RUN_LOCK_EXIT_CODE"]
      min_lines: 40
    - path: "tests/test_concurrency_guard.py"
      provides: "contention + crash-release integration tests against real Postgres"
  key_links:
    - from: "session_run_lock()"
      to: "PostgreSQL pg_try_advisory_lock / pg_advisory_unlock"
      via: "dedicated held connection"
      pattern: "pg_try_advisory_lock"
---

<objective>
Build the greenfield advisory-lock primitive: a typed denial exception, a deterministic key derivation from `(strategy_id, session_date)`, and a `session_run_lock()` context manager that takes a non-blocking session-level PostgreSQL advisory lock on ONE dedicated connection held for the whole guarded region, releasing on normal exit and auto-releasing on crash via connection drop.

Purpose: This is the contract every side-effecting run acquires before doing anything. The loser-of-the-lock invariant ("exits cleanly, no hang, typed error") and the crash-release guarantee (LOCK-06) live here, isolated and independently testable before integration.
Output: `services/concurrency_guard.py` + a Postgres integration test proving non-blocking contention and crash-release.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-concurrency-guard/08-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Engine/session helpers to reuse — src/trading_platform/db/session.py -->
```python
def get_engine(settings: Settings | DatabaseSettings | None = None) -> Engine: ...
# session_scope() commits/rolls back a pooled Session; NOT used to HOLD the lock.
```
<!-- Settings entry point — src/trading_platform/core/settings.py -->
```python
def load_settings() -> Settings: ...  # settings.database.url, settings.execution.safety.*
```
<!-- Custom-exception convention (frozen dataclass over a builtin) — CONVENTIONS.md -->
```python
@dataclass(frozen=True)
class UnknownStrategyError(KeyError):
    strategy_id: str
    def __str__(self) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConcurrentRunLockedError + advisory_lock_key + exit-code constant</name>
  <files>src/trading_platform/services/concurrency_guard.py, tests/test_concurrency_guard.py</files>
  <behavior>
    - advisory_lock_key(strategy_id, session_date) returns a stable signed 64-bit int (fits BIGINT for pg_try_advisory_lock(key bigint)); same inputs → same key; different session_date → different key.
    - ConcurrentRunLockedError carries strategy_id (str) and session_date (date); str(err) is human-readable and names both; it is an Exception subclass tests assert by class, not string.
    - CONCURRENT_RUN_LOCK_EXIT_CODE is a module constant, a small non-zero int distinct from argparse's usage exit code 2 (use 3).
  </behavior>
  <action>
    Create `src/trading_platform/services/concurrency_guard.py` (module docstring, `from __future__ import annotations`).
    - `CONCURRENT_RUN_LOCK_EXIT_CODE = 3`.
    - `def advisory_lock_key(strategy_id: str, session_date: date) -> int`: build a canonical string `f"{strategy_id}:{session_date.isoformat()}"`, hash with `hashlib.blake2b(..., digest_size=8)`, interpret the 8 bytes as a signed big-endian int (`int.from_bytes(digest, "big", signed=True)`). Add an inline comment noting advisory locks share a global keyspace but this repo has no other advisory-lock users today (Claude's-discretion key derivation per CONTEXT).
    - `@dataclass(frozen=True) class ConcurrentRunLockedError(RuntimeError)` with `strategy_id: str`, `session_date: date`, and `__str__` returning e.g. `"Another session holds the run lock for strategy '{strategy_id}' session {session_date}."`.
    In `tests/test_concurrency_guard.py` add pure unit tests (no DB): key determinism, key varies by session_date, and `str(ConcurrentRunLockedError(...))` names both fields.
  </action>
  <verify>
    <automated>pytest tests/test_concurrency_guard.py -x -k "key or error"</automated>
  </verify>
  <done>Key derivation is deterministic and BIGINT-safe; the typed error is class-assertable and self-describing; exit-code constant is 3.</done>
</task>

<task type="auto">
  <name>Task 2: session_run_lock() context manager + contention + crash-release tests</name>
  <files>src/trading_platform/services/concurrency_guard.py, tests/test_concurrency_guard.py</files>
  <action>
    Add `@contextmanager def session_run_lock(*, strategy_id: str, session_date: date, settings: Settings | None = None) -> Iterator[None]` to `concurrency_guard.py`:
    - Resolve settings via `load_settings()` if None. Open ONE dedicated raw connection from `get_engine(settings)` (e.g. `engine.connect()`), set it to autocommit / begin no long transaction — the connection exists only to HOLD the lock, not to run the guarded writes.
    - Execute `SELECT pg_try_advisory_lock(:key)` with `advisory_lock_key(...)`. If it returns false, close the connection and `raise ConcurrentRunLockedError(strategy_id, session_date)` — NON-BLOCKING, no retry, no hang.
    - If true, `yield`. In a `finally`, execute `SELECT pg_advisory_unlock(:key)` then close/dispose the dedicated connection. If the process crashes instead of unlocking, the connection drop makes Postgres auto-release the lock (this is the LOCK-06 mechanism — do NOT rely on the finally alone).
    - Keep the guarded body's own DB writes on separate pooled `session_scope` connections (callers do that in 08-04); this context manager does not wrap those in a transaction.
    Add integration tests to `tests/test_concurrency_guard.py` (self-provisioned temp Postgres, same fixture pattern as `tests/test_paper_execution.py`):
    - Contention: enter `session_run_lock(...)` for a tuple, then assert a second `session_run_lock(...)` for the SAME tuple raises `ConcurrentRunLockedError` immediately (guard the test with a short timeout expectation / the call must not block); a DIFFERENT session_date acquires fine concurrently.
    - Release-on-exit: after the first context exits, a new acquisition of the same tuple succeeds.
    - Crash-release: acquire the raw advisory lock on a manually-opened connection, then force-close that connection WITHOUT unlocking (simulate crash); assert `session_run_lock(...)` for the same tuple then acquires cleanly (proving Postgres auto-released on connection drop).
  </action>
  <verify>
    <automated>pytest tests/test_concurrency_guard.py -x</automated>
  </verify>
  <done>Second concurrent acquisition raises ConcurrentRunLockedError without blocking; lock releases on normal exit; and a dropped holder-connection auto-releases so a fresh run acquires cleanly.</done>
</task>

</tasks>

<verification>
- `session_run_lock` is non-blocking: contention raises the typed error rather than waiting.
- Lock is released on normal context exit AND on simulated crash (connection drop).
- Key derivation is stable and BIGINT-safe.
</verification>

<success_criteria>
The advisory-lock primitive exists in isolation with a typed non-blocking denial (LOCK-01 mechanism) and a proven crash-release guarantee (LOCK-06 mechanism), ready for the submission entrypoint to acquire in 08-04.
</success_criteria>

<output>
After completion, create `.planning/phases/08-concurrency-guard/08-02-SUMMARY.md`
</output>
