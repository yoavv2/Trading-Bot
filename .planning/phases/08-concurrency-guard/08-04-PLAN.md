---
phase: 08-concurrency-guard
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/trading_platform/services/paper_execution.py
  - tests/test_paper_execution.py
autonomous: true
requirements: [LOCK-01, LOCK-02, LOCK-03, LOCK-05]
must_haves:
  truths:
    - "run_paper_order_submission acquires the (strategy_id, session_date) advisory lock before any broker call or state-affecting DB write"
    - "The winning run's FIRST persisted write is a StrategyRun row at status=running with started_at set — before any stale reclaim or other write"
    - "A second concurrent attempt for the same tuple raises ConcurrentRunLockedError and writes zero rows / makes zero broker calls before exiting"
    - "After writing its own running row, the run reclaims any pre-existing stale running row for the tuple (marked STALE + audited) and continues; the fresh running row cannot self-reclaim because its started_at is inside the timeout window"
    - "The running row and per-order writes commit independently so a crash leaves a durable running row for later stale detection; the lock is held on a separate dedicated connection"
  artifacts:
    - path: "src/trading_platform/services/paper_execution.py"
      provides: "lock-guarded, reordered run_paper_order_submission (running-row-first, no pre-lock pending insert)"
      contains: "session_run_lock"
  key_links:
    - from: "run_paper_order_submission()"
      to: "session_run_lock() + reclaim_stale_runs()"
      via: "with session_run_lock(...) wrapping the guarded region; running-row-first, then reclaim"
      pattern: "session_run_lock|reclaim_stale_runs"
    - from: "guarded region entry"
      to: "StrategyRun status=running as first persisted write"
      via: "direct RUNNING insert replacing the pre-lock PENDING insert"
      pattern: "StrategyRunStatus.RUNNING"
---

<objective>
Wire the advisory lock (08-02) and stale reclaim (08-03) into `run_paper_order_submission`, the single side-effecting entrypoint. Reorder so the lock is acquired first, the first persisted write is a `running` StrategyRun row (removing the current pre-lock `pending` insert), stale predecessors are reclaimed immediately after that row exists, and kill-switch/disabled handling happens after the run row exists. The loser of the lock exits cleanly having written nothing.

Purpose: This is the reorder that makes LOCK-01/02/03/05 literally true. The current code inserts a `pending` row and runs kill-switch checks BEFORE any lock — a direct violation. `run_paper_session` and `submit-paper-orders` inherit the guard because they call this function.
Output: Reordered, lock-guarded `run_paper_order_submission` + integration tests.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-concurrency-guard/08-CONTEXT.md
@src/trading_platform/services/paper_execution.py

<interfaces>
<!-- From 08-02 — src/trading_platform/services/concurrency_guard.py -->
```python
@contextmanager
def session_run_lock(*, strategy_id: str, session_date: date, settings: Settings | None = None) -> Iterator[None]: ...
@dataclass(frozen=True)
class ConcurrentRunLockedError(RuntimeError):
    strategy_id: str
    session_date: date
CONCURRENT_RUN_LOCK_EXIT_CODE = 3
```
<!-- From 08-03 — src/trading_platform/services/stale_runs.py -->
```python
def reclaim_stale_runs(session, *, strategy_public_id: str, session_date: date,
                       timeout_minutes: int, reclaiming_run_id: uuid.UUID | None = None) -> list[uuid.UUID]: ...
# Reclaim filters status=RUNNING AND started_at < now()-timeout, so a row created "now" is never matched.
```
<!-- Existing helpers already in paper_execution.py to REORDER around (do not rewrite their bodies except _create_paper_execution_run's status) -->
<!--   _create_paper_execution_run(...)         inserts a StrategyRun row (currently status=PENDING) -->
<!--   _update_paper_execution_run(...)          updates status/result_summary/completed_at, returns PaperExecutionRunReport -->
<!--   _finalize_blocked_paper_execution_run(...) sets FAILED + writes an ExecutionEvent (used by kill-switch / disabled) -->
<!-- Stale timeout: resolved_settings.execution.safety.stale_run_timeout_minutes (added in 08-01) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock-guard and reorder run_paper_order_submission</name>
  <files>src/trading_platform/services/paper_execution.py</files>
  <action>
    Restructure `run_paper_order_submission` so the guarded region has this exact ordering:

    1. Resolve settings/registry/strategy/metadata (pure, no writes). Do NOT create any StrategyRun row yet and do NOT load kill-switch/control state yet.
    2. Wrap everything below in `with session_run_lock(strategy_id=strategy_id, session_date=as_of_session, settings=resolved_settings):`. Put a `try/except ConcurrentRunLockedError` AROUND the `with` so that on lock denial you emit a WARNING structured log (`event="paper_execution_lock_denied"`, fields: strategy_id, session_date, trigger_source) and re-raise — the caller/CLI (08-05) maps it to the reserved exit code. Because the `with` raises before its body runs, the loser writes ZERO rows and makes ZERO broker calls.
    3. FIRST persisted write for THIS run (must be the literal first write after lock acquisition, per LOCK-03 / Success Criterion 2): create the StrategyRun row at `status=StrategyRunStatus.RUNNING` (not PENDING; started_at already server_default now()). Change `_create_paper_execution_run` to insert `status=StrategyRunStatus.RUNNING`, OR create the row RUNNING directly then drop the separate later `_update_paper_execution_run(..., status=RUNNING)` call. This commits on its own pooled connection (durable) — see the durability note below.
    4. AFTER the RUNNING row for this run exists, call `reclaim_stale_runs(session, strategy_public_id=strategy_id, session_date=as_of_session, timeout_minutes=resolved_settings.execution.safety.stale_run_timeout_minutes, reclaiming_run_id=<this run's id>)` inside its own committed `session_scope`. This flips any crashed predecessor to STALE and audits it, then the run continues. The row just created in step 3 is NOT reclaimed: its `started_at≈now()` is outside the `started_at < now()-timeout` staleness window, so it cannot self-reclaim. (Reclaim intentionally runs AFTER the RUNNING write so the running-row write remains the literal first persisted action.)
    5. AFTER reclaim, load kill-switch + control state and keep the existing blocked branches (`_finalize_blocked_paper_execution_run` for kill-switch-tripped and for strategy-disabled) — they now transition the existing RUNNING row to FAILED with the ExecutionEvent audit, unchanged in behavior.
    6. Proceed with the existing broker submission loop and finalization exactly as today. Broker `submit_order` calls MUST remain outside any open DB transaction (they already are — keep that).

    DURABILITY / CRASH-CORRECTNESS (critical — do not violate): the advisory lock is held on the dedicated connection INSIDE `session_run_lock`. The run row and per-order writes MUST continue to commit on their own short-lived `session_scope` connections (as today) — do NOT consolidate the whole guarded body into a single open transaction on the lock connection. A mid-run crash must leave a durable `running` row so 08-03 stale detection can later reclaim it. Add an inline comment stating this rationale. The precise dedicated-connection wrapper shape is Claude's discretion per CONTEXT; the invariants (lock-before-writes, running-row-first, reclaim-after-running-write, durable independent commits, broker I/O outside txn) are not.

    Remove the now-dead pre-lock `pending` insert path. `run_paper_session` needs no change — it calls this function and inherits the guard; its lock-free reconciliation/preflight reads stay lock-free per CONTEXT.
  </action>
  <verify>
    <automated>pytest tests/test_paper_execution.py -x -k "submission or paper"</automated>
  </verify>
  <done>Lock acquired before any write/broker call; the RUNNING run row is the literal FIRST persisted write; stale predecessors reclaimed AFTER that row exists (fresh row not self-reclaimed); kill-switch/disabled handled after reclaim; existing happy-path tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Concurrency integration tests for the guarded entrypoint</name>
  <files>tests/test_paper_execution.py</files>
  <action>
    Add integration tests (reuse the existing `migrated_paper_db` fixture and `FakeExecutionService` in this file):
    - Loser writes nothing: hold the tuple's advisory lock (enter `session_run_lock(...)` from the test, or open a raw connection and `pg_try_advisory_lock` the derived key), then call `run_paper_order_submission(...)` for the SAME tuple and assert it raises `ConcurrentRunLockedError` AND that the StrategyRun paper_execution row count for the tuple is unchanged (zero new rows) AND the `FakeExecutionService.submitted_intents` list is empty (zero broker calls).
    - Running-row-first (LOCK-03): run a normal submission with a `FakeExecutionService` whose `submit_order` asserts, at broker-call time, that a StrategyRun row for this run already exists at status=running with started_at set (capture run state via a query inside the fake or via an injected callback). Simplest: after a successful submission, assert the run progressed running→succeeded and that NO pending-status row was ever the terminal state; and assert a fresh submission against a DB with a pre-seeded 40-min-old RUNNING row for the same tuple marks that old row STALE (query it) while the new run succeeds and the new run's own row is NOT marked stale.
    - Kill-switch honored after lock AND lock released after finalize (LOCK-06 kill-switch release): trip the kill switch, run submission, assert the run row exists (created RUNNING) then finalized blocked/FAILED with the kill-switch ExecutionEvent — proving the check runs AFTER lock acquisition and row creation, not before. THEN assert the lock is FREE: after the kill-switch-blocked run finalizes FAILED, a second `session_run_lock(strategy_id=..., session_date=...)` acquisition on the SAME tuple SUCCEEDS (enters the context without raising `ConcurrentRunLockedError`), proving the lock was released on the kill-switch-blocked exit path.
  </action>
  <verify>
    <automated>pytest tests/test_paper_execution.py -x</automated>
  </verify>
  <done>Tests prove: lock loser writes zero rows and makes zero broker calls (LOCK-01/02); winner's row is running-first and is not self-reclaimed (LOCK-03); stale predecessor reclaimed to STALE after the new running row exists (LOCK-05); kill-switch honored post-lock AND the lock is free for a subsequent acquisition after the blocked run finalizes (LOCK-06 kill-switch release).</done>
</task>

</tasks>

<verification>
- No DB write or broker call occurs before the advisory lock is confirmed (loser-writes-nothing test).
- First persisted write is a running StrategyRun row; reclaim runs after it and never self-reclaims the fresh row.
- Stale predecessor rows are reclaimed after the new running row exists; the new run continues.
- After a kill-switch-blocked run finalizes, the tuple's lock is free for a subsequent acquisition.
- Full existing `tests/test_paper_execution.py` suite still green.
</verification>

<success_criteria>
`run_paper_order_submission` is guarded end-to-end: lock-before-side-effects (LOCK-02), running-row-first as the literal first persisted write (LOCK-03), clean typed denial with zero writes for the loser (LOCK-01), and stale reclaim after the run row exists (LOCK-05), with the lock proven released on the kill-switch-blocked exit path (LOCK-06). CLI exit-code mapping and the crash/restart end-to-end proof land in 08-05.
</success_criteria>

<output>
After completion, create `.planning/phases/08-concurrency-guard/08-04-SUMMARY.md`
</output>
