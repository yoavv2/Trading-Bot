---
phase: 08-concurrency-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_platform/db/models/strategy_run.py
  - src/trading_platform/core/settings.py
  - alembic/versions/0016_phase8_stale_run_status.py
  - tests/test_stale_run_config.py
autonomous: true
requirements: [LOCK-04]
must_haves:
  truths:
    - "StrategyRunStatus.STALE is a valid closed-enum value in Python and in the PostgreSQL strategy_run_status enum after migration to head"
    - "The stale timeout is read from execution.safety config (default 30 minutes), not a hardcoded constant"
  artifacts:
    - path: "src/trading_platform/db/models/strategy_run.py"
      provides: "STALE member on StrategyRunStatus closed enum"
      contains: "STALE ="
    - path: "alembic/versions/0016_phase8_stale_run_status.py"
      provides: "PG enum migration adding 'stale' to strategy_run_status"
      contains: "ADD VALUE IF NOT EXISTS 'stale'"
    - path: "src/trading_platform/core/settings.py"
      provides: "stale_run_timeout_minutes on ExecutionSafetySettings"
      contains: "stale_run_timeout_minutes"
  key_links:
    - from: "ExecutionSafetySettings.stale_run_timeout_minutes"
      to: "stale-run detection query (08-03) and integration (08-04)"
      via: "resolved_settings.execution.safety.stale_run_timeout_minutes"
      pattern: "stale_run_timeout_minutes"
---

<objective>
Add the closed-enum representation and externalized configuration that stale-run detection and reclaim depend on: a `STALE` value on `StrategyRunStatus` (with the PostgreSQL enum migration) and a `stale_run_timeout_minutes` setting on the execution safety block.

Purpose: Run liveness must stay a single authoritative closed-enum column (no free strings), and the timeout must be tunable without a code change — the foundation every other Phase 8 plan builds on.
Output: Updated enum + migration `0016`, new safety setting, and a migration/config test.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-concurrency-guard/08-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/codebase/TESTING.md

<interfaces>
<!-- Existing enum to extend — src/trading_platform/db/models/strategy_run.py -->
```python
class StrategyRunStatus(StrEnum):
    PENDING = "pending"
    RUNNING = "running"
    SUCCEEDED = "succeeded"
    FAILED = "failed"
```

<!-- Existing safety block to extend — src/trading_platform/core/settings.py -->
```python
class ExecutionSafetySettings(BaseModel):
    """Safety thresholds that gate new paper execution when state is ambiguous."""
    repeated_failure_threshold: int = Field(default=3, ge=1)
    block_on_unresolved_reconciliation: bool = True
```

<!-- Proven PG-enum ADD VALUE pattern already used in this repo -->
<!-- alembic/versions/0004_phase3_backtest_foundation.py:19 -->
```python
op.execute("ALTER TYPE strategy_run_type ADD VALUE IF NOT EXISTS 'backtest'")
```
<!-- Current migration head: revision "0015_phase7_kill_switch" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add STALE to StrategyRunStatus + alembic migration 0016</name>
  <files>src/trading_platform/db/models/strategy_run.py, alembic/versions/0016_phase8_stale_run_status.py</files>
  <action>
    In `strategy_run.py`, add `STALE = "stale"` to the `StrategyRunStatus` StrEnum (after `FAILED`). The `Enum(..., values_callable=_enum_values, validate_strings=True)` column mapping picks the new value up automatically — no column change needed.

    Create `alembic/versions/0016_phase8_stale_run_status.py` following the repo's proven enum pattern:
    - `revision = "0016_phase8_stale_run_status"`, `down_revision = "0015_phase7_kill_switch"`, `branch_labels = None`, `depends_on = None`.
    - Module docstring: "Phase 8 concurrency guard: add STALE to the strategy_run_status enum for stale-run reclaim."
    - `upgrade()`: `op.execute("ALTER TYPE strategy_run_status ADD VALUE IF NOT EXISTS 'stale'")`. This is safe inside alembic's transaction because the value is added but NOT used as a literal in the same migration (matches 0004/0007/0008 usage on this PG version).
    - `downgrade()`: PostgreSQL cannot drop a single enum value in place; make downgrade a documented no-op (leave `'stale'` present) with an inline comment explaining that removing an enum value requires a full type recreation and is intentionally not performed here. Do NOT attempt a partial ALTER TYPE ... DROP.
  </action>
  <verify>
    <automated>python -c "from trading_platform.db.models import StrategyRunStatus; assert StrategyRunStatus.STALE.value == 'stale'"</automated>
  </verify>
  <done>StrategyRunStatus.STALE exists in Python; migration 0016 chains from 0015 and adds 'stale' to the PG enum idempotently; downgrade is a documented no-op.</done>
</task>

<task type="auto">
  <name>Task 2: Externalize stale_run_timeout_minutes + migration/config test</name>
  <files>src/trading_platform/core/settings.py, tests/test_stale_run_config.py</files>
  <action>
    In `settings.py`, add `stale_run_timeout_minutes: int = Field(default=30, ge=1)` to `ExecutionSafetySettings`, alongside `repeated_failure_threshold`. Add/extend the docstring to note it is the wall-clock threshold on `started_at` past which a `running` run is considered stale.

    Create `tests/test_stale_run_config.py` following the repo integration-test pattern (self-provisioned temp Postgres DB via the `_admin_connection_settings`/`_connect_admin`/`_set_database_env` helpers copied from `tests/test_paper_execution.py`, `command.upgrade(build_alembic_config(), "head")`, `clear_settings_cache()`/`clear_engine_cache()` teardown). Include:
    - A config unit test (no DB): `load_settings().execution.safety.stale_run_timeout_minutes == 30` by default, and an env override (`TRADING_PLATFORM_EXECUTION__SAFETY__STALE_RUN_TIMEOUT_MINUTES=5`) yields `5` after `clear_settings_cache()`.
    - A migration test: after upgrading a fresh temp DB to head, insert a `StrategyRun` row with `status=StrategyRunStatus.STALE` (via `session_scope`) and read it back, asserting the PG enum accepts `'stale'` (proves the migration landed).
  </action>
  <verify>
    <automated>pytest tests/test_stale_run_config.py -x</automated>
  </verify>
  <done>Default timeout is 30, env override works, and a STALE-status StrategyRun row round-trips against a migrated DB.</done>
</task>

</tasks>

<verification>
- `StrategyRunStatus.STALE` importable and equal to `"stale"`.
- `alembic upgrade head` succeeds from a clean DB and admits `'stale'` for `strategy_run_status`.
- `execution.safety.stale_run_timeout_minutes` defaults to 30 and is env-overridable.
</verification>

<success_criteria>
The closed-enum STALE value and the externalized 30-minute timeout exist and are proven against a migrated PostgreSQL database. No detection or reclaim logic yet — this plan only lays the representation + config foundation (LOCK-04).
</success_criteria>

<output>
After completion, create `.planning/phases/08-concurrency-guard/08-01-SUMMARY.md`
</output>
