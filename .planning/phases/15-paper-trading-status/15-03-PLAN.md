---
phase: 15-paper-trading-status
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Operator confirms the /paper screen renders all four PAPR surfaces on one page: account snapshot, latest reconciliation + findings, current positions, and open orders"
    - "Operator confirms that with no broker data every panel shows its explicit honest empty state (no snapshot recorded / no reconciliation / no open positions / no open orders) — never fabricated zeros or a blank render"
    - "Operator confirms the open-orders and positions filters DISCLOSE hidden non-open/terminal rows (reveal control) and never silently drop rows; the 100-row truncation note appears for an empty-and-capped list rather than a false definitive 'none'"
    - "Operator confirms every panel degrades to an ErrorState naming its exact endpoint when the API is down, and the analytics section names /api/v1/analytics/strategies/trend_following_daily"
  artifacts: []
  key_links: []
---

<objective>
Live operator verification of the complete Phase 15 Paper Trading Status screen against a running FastAPI backend — the same end-to-end sign-off pattern that closed Phases 13 (13-04) and 14 (14-05). No new code; this plan is the honesty gate for the paper-trading surface, and specifically for the empty-state and filter-disclosure behavior that automated build/lint cannot prove against real (or absent) broker data.

Purpose: prove the PAPR requirements are TRUE for the operator, not just green in CI. Closes the behavioral checks that plans 15-01/15-02 defer to "the 15-03 checkpoint."
Output: operator sign-off, or a precise punch-list to feed `/gsd:plan-phase 15 --gaps`.

Requirement coverage note: this plan owns no requirements field — all four IDs (PAPR-01..04) are implemented and covered by plans 15-01/15-02. This plan verifies them live.

Data-availability note (from STATE.md): Alpaca paper credentials are not configured, so account snapshot, positions, and open orders will very likely be EMPTY. That is expected — the primary thing this checkpoint verifies is that empty renders honestly (explicit empty states, not zeros/blanks) and that failure renders as endpoint-named ErrorStates. If the operator later configures a live paper feed, the same steps confirm real data renders.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/13-console-foundation-and-system-status/13-04-SUMMARY.md
@.planning/phases/15-paper-trading-status/15-01-PLAN.md
@.planning/phases/15-paper-trading-status/15-02-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Operator verifies Phase 15 paper-trading status end-to-end</name>
  <action>Pause execution and present the verification steps below to the operator against a live FastAPI backend. Do not mark the Phase 15 PAPR requirements verified without operator sign-off.</action>
  <what-built>
Phase 15 built the single Paper Trading Status screen at `/paper` on the Phase 13 console foundation (all frontend-only, consuming existing FastAPI read routes):
- Account snapshot (PAPR-04) and latest reconciliation + strategy-wide recent findings (PAPR-03), both drawn from ONE `/api/v1/analytics/strategies/trend_following_daily` fetch.
- Current (open) positions (PAPR-01) from `/api/v1/operations/positions`, open-by-default with honest disclosure of hidden non-open rows.
- Open orders (PAPR-02) from `/api/v1/operations/orders`, filtered client-side to open lifecycle statuses ({pending_submission, submitted, partially_filled}) with honest disclosure of hidden terminal orders and a 100-row truncation note.
Automated build/lint + no-direct-fetch grep gates were enforced in 15-01/15-02; this plan is the live-data sign-off.
  </what-built>
  <how-to-verify>
Preconditions (same startup as the 13-04 / 14-05 verifications):
- Start the FastAPI backend (the read API the console proxies to).
- In a second shell: `make console` (runs `cd console && npm run dev`).
- Open http://localhost:3000 and click "Paper Trading" in the top nav (confirm the nav link is present on every screen).

1. ONE SCREEN, FOUR SURFACES: confirm `/paper` shows, on a single page, the Account & Reconciliation section plus Positions and Open Orders panels. Each has its own "as of …" timestamp and a Refresh control; press Refresh on each and confirm the timestamp advances.
2. ACCOUNT SNAPSHOT (PAPR-04): if a snapshot exists, confirm Total equity, Cash, and Buying power render (plus gross exposure / open positions / snapshot source / snapshot-at). If NO snapshot exists, confirm the panel reads an explicit "No account snapshot recorded yet." — NOT a card of zeros and NOT a blank.
3. RECONCILIATION (PAPR-03): if a reconciliation exists, confirm status, finding/blocking counts, and an unambiguous blocks_execution badge (red "BLOCKS EXECUTION" vs zinc "does not block execution"). Confirm the findings list heading STATES ITS SCOPE (strategy-wide, most-recent) and does NOT claim to be "this reconciliation's findings"; blocks_execution rows are emphasized. If none exists, confirm "No reconciliation has been recorded yet."
4. POSITIONS (PAPR-01): if open positions exist, confirm symbol, quantity, avg entry price, cost basis render. If none, confirm "No open positions." If any non-open (closed) positions exist, confirm the "Hiding N non-open position(s)" disclosure appears with a reveal control that shows the closed rows with a Status column — confirm nothing is silently dropped.
5. OPEN ORDERS (PAPR-02): if open orders exist, confirm only OPEN lifecycle statuses show (pending_submission / submitted / partially_filled) with symbol, side, qty, status, and monospace client_order_id. If none, confirm "No open orders." If terminal orders (filled/canceled/…) exist, confirm the "Hiding N non-open order(s)" disclosure + reveal control surfaces them with their terminal Status — confirm nothing is silently dropped. If the raw response is capped at 100, confirm the truncation note appears so an empty open list is never a false definitive "none".
6. HONEST FAILURE (CONS-02 inheritance): stop the FastAPI backend and press Refresh on each section. Confirm the Account & Reconciliation section renders a single ErrorState naming `/api/v1/analytics/strategies/trend_following_daily`, the Positions panel names `/api/v1/operations/positions`, and the Open Orders panel names `/api/v1/operations/orders` (each with HTTP status or "unreachable") — never a blank or fake-success render — and the global kill-switch banner shows its amber "unknown" state. Restart the API, Refresh, and confirm full recovery.
  </how-to-verify>
  <resume-signal>Type "approved" once all six steps pass, or list the specific failures (panel + expected vs actual) so gaps can be planned via `/gsd:plan-phase 15 --gaps`.</resume-signal>
  <verify>Operator responds "approved" after completing steps 1-6 against live data.</verify>
  <done>Operator confirmed live: the /paper screen composes account snapshot, reconciliation + scope-labelled findings, current positions, and open orders; every empty case renders an explicit honest empty state (no zeros/blanks); positions/orders filters disclose and reveal hidden rows with the 100-row truncation note for empty-and-capped lists; and every panel degrades to an endpoint-named ErrorState on API down with correct recovery.</done>
</task>

</tasks>

<verification>
Operator-driven; no automated command. The automated build/lint + no-direct-fetch gates were enforced in plans 15-01/15-02.
</verification>

<success_criteria>
- All six verification steps pass against live data, OR a precise punch-list is produced for `/gsd:plan-phase 15 --gaps`.
- Phase 15 goal is TRUE: the operator can check the live paper-trading state — what's open (positions), what the broker says (open orders), the latest reconciliation, and what the account looks like (equity/cash/buying power) — on one screen, with every empty/missing/failed case surfaced honestly rather than faked.
</success_criteria>

<output>
After completion, create `.planning/phases/15-paper-trading-status/15-03-SUMMARY.md`
</output>
