---
phase: 15-paper-trading-status
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - console/src/components/paper/PositionsPanel.tsx
  - console/src/components/paper/OpenOrdersPanel.tsx
  - console/src/app/paper/page.tsx
autonomous: true
requirements: [PAPR-01, PAPR-02]
must_haves:
  truths:
    - "The positions panel fetches /api/v1/operations/positions?strategy_id=trend_following_daily and shows symbol, quantity, average_entry_price, and cost_basis for current (open) positions (PAPR-01)"
    - "The positions panel defaults to open positions (status === 'open'); any hidden non-open positions are disclosed with a count and a reveal control — nothing is silently dropped; when there are zero open positions it shows an explicit 'no open positions' state, never a blank"
    - "The open-orders panel fetches /api/v1/operations/orders?strategy_id=trend_following_daily and shows only open orders (status in {pending_submission, submitted, partially_filled}) with symbol, side, quantity, status, and client_order_id (PAPR-02)"
    - "Orders excluded by the open-status filter are disclosed with a count and a reveal-all control; when the raw response reached the 100-row cap a truncation disclosure is shown so an empty open list never falsely reads as a definitive 'no open orders'"
    - "When /api/v1/operations/positions or /api/v1/operations/orders fails, the corresponding panel renders an ErrorState naming that exact endpoint, never a blank or fake-success render"
    - "Each panel shows an as-of timestamp and a working Refresh control that re-fetches its endpoint; both panels are composed into the existing /paper page below the analytics section"
  artifacts:
    - path: "console/src/components/paper/PositionsPanel.tsx"
      provides: "Self-fetching current-positions panel (PAPR-01): useApiQuery to /api/v1/operations/positions, open-by-default with non-open disclosure/reveal, explicit empty state"
      min_lines: 45
    - path: "console/src/components/paper/OpenOrdersPanel.tsx"
      provides: "Self-fetching open-orders panel (PAPR-02): useApiQuery to /api/v1/operations/orders, open-status filter with hidden-count disclosure/reveal + 100-row truncation disclosure"
      min_lines: 50
    - path: "console/src/app/paper/page.tsx"
      provides: "Paper trading route extended to compose PositionsPanel + OpenOrdersPanel below the analytics section"
      contains: "PositionsPanel"
  key_links:
    - from: "console/src/components/paper/PositionsPanel.tsx"
      to: "/api/v1/operations/positions"
      via: "useApiQuery"
      pattern: "useApiQuery.*operations/positions"
    - from: "console/src/components/paper/OpenOrdersPanel.tsx"
      to: "/api/v1/operations/orders"
      via: "useApiQuery"
      pattern: "useApiQuery.*operations/orders"
    - from: "console/src/app/paper/page.tsx"
      to: "PositionsPanel + OpenOrdersPanel"
      via: "import + render"
      pattern: "<PositionsPanel"
---

<objective>
Complete the Paper Trading Status screen at `/paper`: add the current-positions panel (PAPR-01) and the open-orders panel (PAPR-02), each self-fetching its own operations endpoint, and compose them into the existing `/paper` page below the analytics section built in 15-01.

Purpose: PAPR-01/PAPR-02 — make what is open (positions) and what the broker has live (open orders) inspectable on the one paper-trading screen.
Output: `PositionsPanel` + `OpenOrdersPanel` + `/paper` page composing all four PAPR panels.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-paper-trading-status/15-01-PLAN.md

# Console foundation you MUST reuse (do NOT re-implement fetch/error/freshness):
@console/src/lib/useApiQuery.ts
@console/src/lib/api.ts
@console/src/components/ErrorState.tsx
@console/src/components/FetchMeta.tsx
@console/src/app/paper/page.tsx

# The exact self-fetching table-panel pattern to mirror (own fetch + FetchMeta/ErrorState + table + capped/empty disclosure):
@console/src/components/runs/detail/OrdersFillsPanel.tsx

<constraints>
- FRONTEND ONLY. No backend files. Consume only the EXISTING `GET /api/v1/operations/positions` and `GET /api/v1/operations/orders` routes. strategy_id to use: `trend_following_daily`.
- NO component may call `fetch()` directly — every fetch goes through `useApiQuery`/`fetchApi`. Enforced by grep in verify.
- Every failure renders via `ErrorState`; every data view shows `FetchMeta` freshness + Refresh. Never a placeholder/default value on failure OR on empty data.
- Empty is the PRIMARY path: per STATE.md, Alpaca paper credentials are not configured, so positions and orders will very likely be empty. The honest empty states are core deliverables.
- HONESTY on filtering: neither endpoint filters to "open" server-side (the `status` query param filters by RUN status, not order/position status). You filter client-side, so you MUST disclose what you hid — never silently drop rows. Follow the OrdersFillsPanel precedent for the 100-row cap (`limit=100`): if the raw response `count` reached the cap, show a "may be truncated" note so an empty filtered list is never presented as a definitive "none".
- Do NOT import `StatusPanel` (status-screen scoped) or the `runs/detail` components (run-detail scoped, incl. `CappedDisclosure`/`runScopedFilter`). Read OrdersFillsPanel as the PATTERN to copy, but build paper-local panels composing directly from `useApiQuery`/`FetchMeta`/`ErrorState` and inline the small disclosure text.
- This is a NEW customized Next.js (v16.2.10). If any App Router / client-component / hooks convention is unfamiliar, read the relevant guide under `console/node_modules/next/dist/docs/` and mirror the existing OrdersFillsPanel conventions. Every `useApiQuery` call must be unconditional (rules-of-hooks) — put a fetching section in its own child component if you need conditional rendering, exactly as OrdersFillsPanel/MetricsPanel do.
</constraints>

<interfaces>
<!-- VERIFIED collection envelope (build_collection_response, src/trading_platform/api/dependencies.py:81). -->
```ts
type CollectionResponse<T> = {
  filters: Record<string, unknown>;
  count: number;   // number of items returned (compare to the limit to detect the cap)
  items: T[];
};
```

<!-- VERIFIED position item (OperatorReadService.list_positions, src/trading_platform/services/operator_reads.py:273). -->
```ts
type PositionItem = {
  position_id: string;
  strategy_id: string;
  symbol: string;
  status: string;                 // "open" | "closed"  — PAPR-01 "current" = open
  quantity: number;               // _decimal_value -> float
  average_entry_price: number;
  cost_basis: number;
  opened_session_date: string | null;
  closed_session_date: string | null;
  opened_at: string | null;
  closed_at: string | null;
};
```

<!-- VERIFIED order item (OperatorReadService.list_paper_orders, src/trading_platform/services/operator_reads.py:175). -->
<!-- Full shape also mirrored in console/src/components/runs/detail/OrdersFillsPanel.tsx OrderItem — reuse those field names. -->
```ts
type OrderItem = {
  order_id: string;
  run_id: string;
  strategy_id: string;
  symbol: string;
  session_date: string;
  side: string;
  quantity: number;
  order_type: string;
  status: string;                 // OrderLifecycleState value — see OPEN_STATUSES below
  broker_status: string | null;
  client_order_id: string;        // PAPR-02 intent id
  broker_order_id: string | null;
  submitted_at: string | null;
  filled_at: string | null;
  last_submission_error: string | null;
  last_sync_error: string | null;
  // ...other fields exist (run_type, run_status, intent_context, ...) — ignore what PAPR-02 doesn't need
};
```

<!-- VERIFIED OrderLifecycleState values (src/trading_platform/db/models/order_event.py:19). -->
OPEN (non-terminal) order statuses — the ONLY ones the open-orders panel shows by default:
  { "pending_submission", "submitted", "partially_filled" }
Everything else is terminal/ambiguous and goes in the revealed bucket:
  { "filled", "canceled", "rejected", "expired", "submission_failed", "unknown" }
Define these as a `const OPEN_STATUSES = new Set([...])` so the filter is explicit and auditable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Current-positions panel (PAPR-01)</name>
  <files>console/src/components/paper/PositionsPanel.tsx</files>
  <action>
Create `PositionsPanel.tsx` — a `"use client"` self-fetching panel mirroring the OrdersFillsPanel section pattern (section wrapper: header with title "Positions" + `<FetchMeta ... />`; `Loading…` when `result` is null; `<ErrorState failure={result} title="Failed to load positions" />` when `!result.ok`; a content child on success). Copy the `PositionItem` + `CollectionResponse<T>` types from the `<interfaces>` block.

Fetch `useApiQuery<CollectionResponse<PositionItem>>("/api/v1/operations/positions?strategy_id=trend_following_daily&limit=100")`.

Content (PAPR-01 "current positions"):
- Compute `open = items.filter(p => p.status === "open")` and `hiddenCount = items.length - open.length`.
- Default view shows ONLY `open` positions in a table: columns Symbol, Quantity, Avg entry price, Cost basis, Opened (`opened_session_date ?? opened_at ?? "—"`).
- If `open.length === 0`: explicit empty state `No open positions.` (plus, if `hiddenCount > 0`, keep the disclosure/reveal below so the operator knows closed rows exist).
- Disclosure (honesty — nothing silently dropped): when `hiddenCount > 0`, render an inline note like `Hiding {hiddenCount} non-open position(s).` with a reveal toggle (a `useState` boolean + a button) that, when on, renders ALL `items` (open + closed) with a Status column so closed rows are clearly labelled. When revealed, the note flips to a "show open only" control.
- Truncation honesty: if `data.count >= 100`, render a note `Showing the 100 most-recent positions; older rows may be truncated.` so an empty open list is never presented as a definitive "no positions ever".

Keep every `useApiQuery` call unconditional; if you split into a fetching section + content child, follow the OrdersFillsPanel structure.
  </action>
  <verify>
    <automated>cd console && npm run build && npm run lint && grep -rnE "(^|[^a-zA-Z.])fetch\(" src/components/paper/PositionsPanel.tsx | grep -v fetchApi | grep -v "// " ; test $? -ne 0</automated>
  </verify>
  <done>PositionsPanel fetches /api/v1/operations/positions via useApiQuery, shows open positions by default with symbol/quantity/avg-entry/cost-basis, discloses+reveals any hidden non-open rows, shows an explicit empty state and a 100-row truncation note, and branches to an endpoint-named ErrorState. No direct fetch().</done>
</task>

<task type="auto">
  <name>Task 2: Open-orders panel (PAPR-02)</name>
  <files>console/src/components/paper/OpenOrdersPanel.tsx</files>
  <action>
Create `OpenOrdersPanel.tsx` — a `"use client"` self-fetching panel mirroring the same section pattern (header title "Open Orders" + `<FetchMeta ... />`; loading / `<ErrorState failure={result} title="Failed to load orders" />` / content). Copy the `OrderItem` + `CollectionResponse<T>` types from the `<interfaces>` block and define `const OPEN_STATUSES = new Set(["pending_submission", "submitted", "partially_filled"])`.

Fetch `useApiQuery<CollectionResponse<OrderItem>>("/api/v1/operations/orders?strategy_id=trend_following_daily&limit=100")`.

Content (PAPR-02 "open orders"):
- Compute `open = items.filter(o => OPEN_STATUSES.has(o.status))` and `hiddenCount = items.length - open.length`.
- Default view shows ONLY `open` orders in a table: columns Symbol, Side, Qty, Status (`status`{` / `+`broker_status` when present}), Submitted (`submitted_at ?? "—"`), Client order id (`client_order_id`, monospace). Surface `last_submission_error`/`last_sync_error` inline in red when present, as OrdersFillsPanel does.
- If `open.length === 0`: explicit empty state `No open orders.` — BUT if `data.count >= 100` show instead/additionally the truncation note `Showing the 100 most-recent orders; older open orders may be truncated.` so an empty open list is never a false definitive "none".
- Disclosure (honesty): when `hiddenCount > 0`, render an inline note `Hiding {hiddenCount} non-open order(s) (filled/canceled/rejected/expired/…).` with a `useState` reveal toggle that renders ALL `items` with the Status column making terminal states obvious; toggle flips back to "show open only".

Keep every `useApiQuery` call unconditional (fetching section + content child if needed).
  </action>
  <verify>
    <automated>cd console && npm run build && npm run lint && grep -q 'pending_submission' src/components/paper/OpenOrdersPanel.tsx && grep -rnE "(^|[^a-zA-Z.])fetch\(" src/components/paper/OpenOrdersPanel.tsx | grep -v fetchApi | grep -v "// " ; test $? -ne 0</automated>
  </verify>
  <done>OpenOrdersPanel fetches /api/v1/operations/orders via useApiQuery, shows only OPEN_STATUSES orders by default with symbol/side/qty/status/client_order_id, discloses+reveals hidden terminal orders, shows an explicit empty state + 100-row truncation note, and branches to an endpoint-named ErrorState. No direct fetch().</done>
</task>

<task type="auto">
  <name>Task 3: Compose positions + open orders into the /paper page</name>
  <files>console/src/app/paper/page.tsx</files>
  <action>
Modify the existing `console/src/app/paper/page.tsx` (created in 15-01) to import and render `<PositionsPanel />` and `<OpenOrdersPanel />` below the existing `<PaperAnalyticsSection />`, inside the same `<main>`. Add spacing consistent with the page (e.g. wrap the sections in a `space-y-6` container or add `mt-6` between them). Do NOT touch `layout.tsx` (15-01 owns the nav edit) and do NOT modify the analytics section. The result is the single Paper Trading Status screen composing all four PAPR panels (account + reconciliation from 15-01, positions + open orders here).
  </action>
  <verify>
    <automated>cd console && grep -q '<PositionsPanel' src/app/paper/page.tsx && grep -q '<OpenOrdersPanel' src/app/paper/page.tsx && npm run build && npm run lint</automated>
  </verify>
  <done>`/paper` composes PaperAnalyticsSection + PositionsPanel + OpenOrdersPanel in one screen; build + lint pass; layout.tsx untouched.</done>
</task>

</tasks>

<verification>
- `cd console && npm run build` succeeds; `npm run lint` clean.
- `grep -rnE "fetch\(" console/src/components/paper | grep -v fetchApi` returns nothing (no direct fetch bypass).
- Behavioral (covered live in 15-03 checkpoint): visiting `/paper` shows Positions and Open Orders panels with honest empty states when the broker has no data; hidden non-open rows are disclosed with a reveal control; stopping the API makes each panel render an ErrorState naming its exact operations endpoint.
</verification>

<success_criteria>
- PAPR-01: current (open) positions are visible with symbol/quantity/avg-entry/cost-basis, an explicit empty state, and honest disclosure of any hidden non-open rows.
- PAPR-02: open orders (open lifecycle statuses only) are visible with symbol/side/qty/status/client_order_id, an explicit empty state, honest disclosure of hidden terminal orders, and a 100-row truncation note.
- All four PAPR panels live on the single `/paper` screen; each inherits the honest fetch/error/as-of pattern and the global kill-switch banner.
</success_criteria>

<output>
After completion, create `.planning/phases/15-paper-trading-status/15-02-SUMMARY.md`
</output>
