---
phase: 15-paper-trading-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - console/src/components/paper/PaperAccountPanel.tsx
  - console/src/components/paper/PaperReconciliationPanel.tsx
  - console/src/components/paper/PaperAnalyticsSection.tsx
  - console/src/app/paper/page.tsx
  - console/src/app/layout.tsx
autonomous: true
requirements: [PAPR-03, PAPR-04]
must_haves:
  truths:
    - "The /paper route is reachable from a 'Paper Trading' link present in the top nav on every console screen"
    - "The account panel shows total_equity, cash, and buying_power from .paper.latest_account_snapshot (PAPR-04); when .paper.latest_account_snapshot is null it shows an explicit 'no account snapshot recorded' state — never fabricated zeros or a blank"
    - "The reconciliation panel shows .paper.latest_reconciliation's status, finding_count, blocking_count, and blocks_execution (PAPR-03); when .paper.latest_reconciliation is null it shows an explicit 'no reconciliation recorded' state"
    - "The reconciliation panel lists .paper.recent_execution_findings under a heading whose text states the scope is strategy-wide most-recent execution findings (NOT the reconciliation run's own findings), highlighting rows where blocks_execution is true"
    - "When /api/v1/analytics/strategies/trend_following_daily fails, the section renders a single ErrorState naming that exact endpoint, never a blank or fake-success render"
    - "The section shows an as-of timestamp and a working Refresh control that re-fetches the analytics endpoint"
  artifacts:
    - path: "console/src/components/paper/PaperAnalyticsSection.tsx"
      provides: "Client section owning a single useApiQuery to /api/v1/analytics/strategies/trend_following_daily, rendering FetchMeta/ErrorState chrome once and passing the .paper slice to the account + reconciliation panels"
      min_lines: 40
    - path: "console/src/components/paper/PaperAccountPanel.tsx"
      provides: "Presentational account-snapshot panel (PAPR-04) with an explicit null-snapshot empty state"
      min_lines: 25
    - path: "console/src/components/paper/PaperReconciliationPanel.tsx"
      provides: "Presentational reconciliation panel (PAPR-03) rendering latest_reconciliation summary + scope-labelled recent_execution_findings, with an explicit null empty state"
      min_lines: 30
    - path: "console/src/app/paper/page.tsx"
      provides: "Paper trading route composing the analytics section (positions + open-orders panels added in 15-02)"
      min_lines: 8
    - path: "console/src/app/layout.tsx"
      provides: "Top nav 'Paper Trading' link to /paper added"
      contains: "/paper"
  key_links:
    - from: "console/src/components/paper/PaperAnalyticsSection.tsx"
      to: "/api/v1/analytics/strategies/trend_following_daily"
      via: "useApiQuery"
      pattern: "useApiQuery.*analytics/strategies/trend_following_daily"
    - from: "console/src/app/layout.tsx"
      to: "/paper route"
      via: "next/link"
      pattern: "href=\"/paper\""
---

<objective>
Build the analytics-fed half of the Paper Trading Status screen at `/paper`: the operator sees the latest account snapshot (equity, cash, buying power — PAPR-04) and the latest reconciliation result with its findings surface (PAPR-03), both drawn from the SAME `/api/v1/analytics/strategies/trend_following_daily` response's `.paper` block via a single fetch. Also add the `Paper Trading` nav link and create the `/paper` route (positions + open orders land in 15-02).

Purpose: PAPR-03/PAPR-04 — make the live paper-trading account and reconciliation state inspectable on one screen without reading logs or the DB.
Output: `/paper` route + `PaperAnalyticsSection` (single shared analytics fetch) + `PaperAccountPanel` + `PaperReconciliationPanel` + nav link.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-console-foundation-and-system-status/13-04-SUMMARY.md

# Console foundation you MUST reuse (do NOT re-implement fetch/error/freshness):
@console/src/lib/useApiQuery.ts
@console/src/lib/api.ts
@console/src/components/ErrorState.tsx
@console/src/components/FetchMeta.tsx
@console/src/components/status/StatusPanel.tsx
@console/src/app/layout.tsx
@console/src/app/strategy/page.tsx

# Reference for the exact analytics response shape (already consumes this endpoint):
@console/src/components/runs/detail/MetricsPanel.tsx

<constraints>
- FRONTEND ONLY. No backend files. Consume only the EXISTING `GET /api/v1/analytics/strategies/{strategy_id}` route. strategy_id to use: `trend_following_daily`.
- NO component may call `fetch()` directly — every fetch goes through `useApiQuery`/`fetchApi`. Enforced by grep in verify.
- Every failure renders via `ErrorState`; every data view shows `FetchMeta` freshness + Refresh. Never a placeholder/default value on failure OR on missing data.
- Empty/null is the PRIMARY path, not an edge case: per STATE.md, Alpaca paper credentials are not configured, so `.paper.latest_account_snapshot` and `.paper.latest_reconciliation` will very likely be `null`. The honest null/empty renders are what the operator will actually see first — they are core deliverables, not afterthoughts.
- Backend quirk to respect: the serializer's `_decimal_value(None)` returns `0.0`, so an individual missing decimal field arrives as 0. Do NOT invent your OWN zeros for a null SNAPSHOT OBJECT — a null `latest_account_snapshot` must render the explicit empty state, not a card of zeros.
- Do NOT import `StatusPanel` (scoped to the status screen per 13-04) or `runs/detail` components (scoped to the run-detail screen). Compose directly from `useApiQuery`/`FetchMeta`/`ErrorState`, and inline any small disclosure text.
- This is a NEW customized Next.js (v16.2.10). Before writing route/layout/Link code, read the relevant guide under `console/node_modules/next/dist/docs/` if any App Router / metadata / Link convention is unfamiliar, and match the exact conventions already used in `console/src/app/strategy/page.tsx` and `console/src/app/layout.tsx`. Heed deprecation notices.
</constraints>

<interfaces>
<!-- VERIFIED response shape of GET /api/v1/analytics/strategies/{strategy_id}. -->
<!-- Top-level confirmed against console/src/components/runs/detail/MetricsPanel.tsx (AnalyticsResponse). -->
<!-- The .paper block fields confirmed against src/trading_platform/services/analytics.py:245-267, -->
<!-- _serialize_account_snapshot (~326) and _serialize_reconciliation (~340). -->
```ts
type AccountSnapshot = {
  snapshot_at: string;        // ISO
  snapshot_source: string;
  cash: number;               // _decimal_value -> float
  gross_exposure: number;
  total_equity: number;       // PAPR-04 "equity"
  buying_power: number;       // PAPR-04 "buying power"
  open_positions: number;
};

type Reconciliation = {
  run_id: string;
  status: string;
  as_of_session: string | null;
  finding_count: number;
  blocking_count: number;
  blocks_execution: boolean;  // PAPR-03 — the headline flag
  completed_at: string | null;
};

type ExecutionFinding = {
  event_type: string;
  severity: string;
  blocks_execution: boolean;
  event_at: string;           // ISO
  message: string;
  details: unknown;           // stringify with JSON.stringify in a <pre>/mono cell
};

type PaperBlock = {
  latest_account_snapshot: AccountSnapshot | null;   // PAPR-04 — MAY BE null
  latest_reconciliation: Reconciliation | null;      // PAPR-03 — MAY BE null
  recent_execution_findings: ExecutionFinding[];     // strategy-wide, most-recent N — NOT scoped to the reconciliation run
  // (other count fields exist but are out of scope for this plan)
};

type AnalyticsResponse = {
  strategy: Record<string, unknown>;
  backtest: unknown | null;
  paper: PaperBlock | null;   // whole block MAY be null when no strategy/paper history exists
};
```

HONESTY NOTE (PAPR-03 scope — decided at plan time): `recent_execution_findings` is selected strategy-wide across ALL runs (ordered by event_at desc, limited to N) — it is NOT the latest reconciliation run's own finding rows. `latest_reconciliation.finding_count` IS that one run's count. Therefore render the counts/blocks_execution as the reconciliation RESULT, and render `recent_execution_findings` under a heading that states its true scope (e.g. "Recent execution findings (strategy-wide, most-recent)"). Do NOT label the findings list as "this reconciliation's findings" — that would be a dishonest scope claim and fails the milestone honesty bar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Presentational account + reconciliation panels</name>
  <files>console/src/components/paper/PaperAccountPanel.tsx, console/src/components/paper/PaperReconciliationPanel.tsx</files>
  <action>
Create two presentational components (they receive already-fetched data as props; the parent section in Task 2 owns the fetch and the FetchMeta/ErrorState chrome). Copy the exact type definitions from the `<interfaces>` block above (define/export them from one of these files or a small shared types file inside `components/paper/`, your choice — but no duplication).

`PaperAccountPanel.tsx` — props `{ snapshot: AccountSnapshot | null }` (PAPR-04):
- When `snapshot === null`: render an explicit empty state `<p className="text-sm text-zinc-500">No account snapshot recorded yet.</p>`. Do NOT render a zeros card. This is the primary path today.
- When present: render a `<dl>` (mirror the `grid grid-cols-2 ... text-xs` style used in MetricsPanel's dl) showing, prominently, Total equity (`total_equity`), Cash (`cash`), Buying power (`buying_power`), plus Gross exposure, Open positions, Snapshot source, and Snapshot at (`snapshot_at`). Format numbers verbatim (e.g. `String(value)` or `toLocaleString()`); do NOT hide precision.

`PaperReconciliationPanel.tsx` — props `{ reconciliation: Reconciliation | null; findings: ExecutionFinding[] }` (PAPR-03):
- When `reconciliation === null`: render an explicit empty state `<p className="text-sm text-zinc-500">No reconciliation has been recorded yet.</p>`.
- When present: render a summary `<dl>` with Status, As-of session (`as_of_session ?? "—"`), Finding count, Blocking count, Completed at (`completed_at ?? "—"`), and a prominent `blocks_execution` badge — red "BLOCKS EXECUTION" when true, zinc "does not block execution" when false (this is the headline PAPR-03 flag; make it visually unambiguous, mirroring the enabled/disabled badge convention from StrategyOverviewPanel).
- Below the summary, render the findings list from the `findings` prop under a heading that STATES ITS SCOPE, e.g. `<h3>Recent execution findings (strategy-wide, most-recent)</h3>`. Do NOT call them "this reconciliation's findings". If `findings.length === 0`, show `No recent execution findings.`. Otherwise render a small table with columns: event_at, event_type, severity, message, and a blocks_execution indicator (highlight/red rows where `blocks_execution` is true). Render `details` via `JSON.stringify(details)` in a monospace cell only when present/non-empty.

Neither component fetches or imports StatusPanel/runs-detail components.
  </action>
  <verify>
    <automated>cd console && npm run build && npm run lint && grep -rnE "(^|[^a-zA-Z.])fetch\(" src/components/paper | grep -v fetchApi | grep -v "// " ; test $? -ne 0</automated>
  </verify>
  <done>Both panels compile and lint clean; each renders an explicit null/empty state (no zeros card, no "this reconciliation's findings" mislabel); reconciliation shows a blocks_execution badge and a scope-labelled findings table. No direct fetch().</done>
</task>

<task type="auto">
  <name>Task 2: Analytics section (single shared fetch) + /paper route + nav link</name>
  <files>console/src/components/paper/PaperAnalyticsSection.tsx, console/src/app/paper/page.tsx, console/src/app/layout.tsx</files>
  <action>
Create `PaperAnalyticsSection.tsx` — a `"use client"` component that calls `useApiQuery<AnalyticsResponse>("/api/v1/analytics/strategies/trend_following_daily")` ONCE and shares the result between both panels (this follows the 14-03 precedent of the owner fetching once instead of two panels double-fetching the same endpoint). Render a `<section>` with the same honest chrome as StatusPanel: a header row containing a title (e.g. "Account & Reconciliation") + `<FetchMeta asOf={result?.asOf ?? null} loading={loading} onRefresh={refetch} />`; a `Loading…` state when `result` is null; a single `<ErrorState failure={result} />` branch when `result.ok === false` (one endpoint failure = one honest error covering both sub-panels, since they share the endpoint); and on success, compute `const paper = result.data.paper` and render `<PaperAccountPanel snapshot={paper?.latest_account_snapshot ?? null} />` and `<PaperReconciliationPanel reconciliation={paper?.latest_reconciliation ?? null} findings={paper?.recent_execution_findings ?? []} />`. A null `paper` block must flow through as null snapshot + null reconciliation + empty findings (each panel then shows its explicit empty state).

Create `console/src/app/paper/page.tsx` — mirror `console/src/app/strategy/page.tsx` exactly: a server component rendering `<main className="flex-1 p-6">` with an `<h1 className="mb-4 text-xl font-semibold text-zinc-100">Paper Trading Status</h1>` and `<PaperAnalyticsSection />`. Leave room below it for the positions + open-orders panels that 15-02 will add (do not stub them here).

In `console/src/app/layout.tsx`, add ONE `next/link` entry to the existing `<nav>` using the EXACT same className as the sibling links (`className="text-zinc-400 hover:text-zinc-100"`): `<Link href="/paper">Paper Trading</Link>`, placed after the "Runs" link. This plan OWNS the Phase 15 nav edit (mirrors 14-01 owning the Phase 14 nav) so 15-02 does not touch layout.tsx. Do not change anything else in the layout.
  </action>
  <verify>
    <automated>cd console && grep -q 'href="/paper"' src/app/layout.tsx && grep -q 'analytics/strategies/trend_following_daily' src/components/paper/PaperAnalyticsSection.tsx && npm run build && npm run lint && grep -rnE "(^|[^a-zA-Z.])fetch\(" src/app/paper src/components/paper | grep -v fetchApi | grep -v "// " ; test $? -ne 0</automated>
  </verify>
  <done>`/paper` route builds and lints clean; PaperAnalyticsSection fetches the analytics endpoint once via useApiQuery and feeds both panels; a single ErrorState names the analytics endpoint on failure; a Paper Trading nav link is present in layout.tsx. No direct fetch().</done>
</task>

</tasks>

<verification>
- `cd console && npm run build` succeeds; `npm run lint` clean.
- `grep -rnE "fetch\(" console/src/app/paper console/src/components/paper | grep -v fetchApi` returns nothing (no direct fetch bypass).
- Behavioral (covered live in 15-03 checkpoint): visiting `/paper` shows the account + reconciliation section with honest empty states when no snapshot/reconciliation exists; stopping the API makes the section render an ErrorState naming `/api/v1/analytics/strategies/trend_following_daily`, not a blank.
</verification>

<success_criteria>
- PAPR-04: the latest account snapshot's equity, cash, and buying power are visible, with an explicit empty state when no snapshot exists.
- PAPR-03: the latest reconciliation's result (status, counts, blocks_execution) is visible with an unambiguous blocks_execution badge, plus the strategy-wide recent findings under a scope-honest heading, and an explicit empty state when no reconciliation exists.
- Screen inherits the honest fetch/error/as-of pattern and the global kill-switch banner; nav reaches Paper Trading.
</success_criteria>

<output>
After completion, create `.planning/phases/15-paper-trading-status/15-01-SUMMARY.md`
</output>
