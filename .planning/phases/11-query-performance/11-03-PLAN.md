---
phase: 11-query-performance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/trading_platform/db/models/paper_order.py
  - src/trading_platform/db/models/paper_fill.py
  - src/trading_platform/db/models/position.py
  - alembic/versions/0017_phase11_query_performance_indices.py
  - tests/test_query_index_usage.py
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "Each critical query path (operator reads, reconciliation, order-lifecycle sync) uses a named index confirmed by EXPLAIN"
    - "No critical query falls back to a full sequential scan on a seeded, ANALYZEd table"
    - "Any new index ships via migration 0017 following the existing numbering convention"
  artifacts:
    - path: "alembic/versions/0017_phase11_query_performance_indices.py"
      provides: "Migration adding only the named indices EXPLAIN proves are missing"
      contains: "def upgrade"
    - path: "tests/test_query_index_usage.py"
      provides: "EXPLAIN-based index-usage assertions over seeded+ANALYZEd tables"
      contains: "EXPLAIN"
  key_links:
    - from: "tests/test_query_index_usage.py"
      to: "critical query EXPLAIN plans"
      via: "seed rows + ANALYZE, then assert Index Scan (not Seq Scan)"
      pattern: "Index Scan"
    - from: "alembic/versions/0017_phase11_query_performance_indices.py"
      to: "db/models/*.py Index() declarations"
      via: "migration mirrors the model-level Index additions"
      pattern: "create_index"
---

<objective>
Prove every critical query path uses a named index via EXPLAIN, and add a named covering index
(migration 0017) only where EXPLAIN reveals a sequential scan on a large table (PERF-03).

Critical query paths (real, grounded in the codebase):
- OPERATOR READS — `src/trading_platform/services/operator_reads.py`: runs list (StrategyRun join
  Strategy, order by started_at desc — line 83/92), orders/fills/positions/account/risk/execution-event
  listings (lines 152, 221, 263, 298, 337, 449), kill-switch (line 377), failed-runs (line 405).
- RECONCILIATION — `src/trading_platform/services/reconciliation.py`: PaperOrder / PaperFill / Position /
  AccountSnapshot loads keyed on strategy (lines 160, 235, 296, 302, 309, 317).
- ORDER-LIFECYCLE SYNC — `src/trading_platform/services/paper_execution.py`: `_sync_paper_orders` local
  order load (line 1041), broker-fill dedup `select(PaperFill.broker_fill_id)` full scan (line 1747),
  open-position load by strategy+status (line 1787).

Many composite indices ALREADY exist (do not duplicate): e.g. `ix_paper_orders_strategy_run_id_status`,
`ix_paper_orders_strategy_run_id_symbol_id`, `ix_positions_strategy_id_status`,
`ix_positions_symbol_id_status`, `ix_account_snapshots_strategy_id_snapshot_at`,
`ix_strategy_runs_strategy_id_status`, `ix_paper_fills_symbol_id_filled_at`,
`ix_execution_events_strategy_run_id_event_at`. `PaperOrder.intent_hash`/`client_order_id`/`broker_order_id`
each already have a UniqueConstraint (implicit index). So migration 0017 is expected to be SMALL — add
indices only for paths EXPLAIN shows scanning.

Purpose: PERF-03 requires EXPLAIN-confirmed named index usage on the three critical paths, with seq
scans on large tables absent.
Output: An EXPLAIN-based test (seed + ANALYZE) proving index usage, and migration 0017 adding only the
genuinely-missing named indices.
</objective>

<execution_context>
@/Users/yoavhevroni/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yoavhevroni/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/trading_platform/services/operator_reads.py
@src/trading_platform/services/reconciliation.py
@src/trading_platform/db/models/paper_order.py
@src/trading_platform/db/models/paper_fill.py
@src/trading_platform/db/models/position.py

<interfaces>
<!-- Grounding facts the executor must not re-derive. -->

Migration convention (alembic/versions/): files are `NNNN_phaseX_<slug>.py`, sequential.
Latest is `0016_phase8_stale_run_status.py`. NEXT is `0017_phase11_query_performance_indices.py`.
Each migration exposes `revision`, `down_revision`, `upgrade()`, `downgrade()`. IMPORTANT: revision
ids are the FULL slug, NOT the numeric prefix, and filename != revision id. The head migration
`0016_phase8_stale_run_status.py` has `revision = "0016_phase8_stale_run_status"` and
`down_revision = "0015_phase7_kill_switch"`. So 0017 MUST set
`revision = "0017_phase11_query_performance_indices"` and
`down_revision = "0016_phase8_stale_run_status"` (equal to 0016's exact `revision` string, verbatim —
never the numeric `"0016"`, which matches no revision in this repo).

Model Index() declaration style (from db/models/*.py `__table_args__`):
```python
__table_args__ = (
    UniqueConstraint(...),
    Index("ix_<table>_<cols>", "col_a", "col_b"),
)
```

Existing indices already present (DO NOT re-add):
- paper_orders: (strategy_run_id,status), (strategy_run_id,symbol_id), (supersedes_paper_order_id);
  UNIQUE on intent_hash, client_order_id, broker_order_id.
- paper_fills: (paper_order_id,filled_at), (symbol_id,filled_at).
- positions: (strategy_id,status), (symbol_id,status).
- strategy_runs: (strategy_id,status). account_snapshots: (snapshot_at), (strategy_id,snapshot_at).
- risk_events: (strategy_run_id,session_date), (decision_code). execution_events: three composite.

Candidate GAPS to check with EXPLAIN (add index ONLY if a seq scan appears on a seeded+ANALYZEd table):
- `select(PaperFill.broker_fill_id)` (paper_execution.py:1747) — full-table broker-fill dedup scan; no
  index on `paper_fills.broker_fill_id`. Likely needs `ix_paper_fills_broker_fill_id`.
- StrategyRun ordering by `started_at desc` (operator_reads runs list) — no index leads with started_at;
  only (strategy_id,status) exists.
- `PaperOrder.intended_session_date` predecessor-lineage lookups — no index leads with it.
Treat these as HYPOTHESES to confirm by EXPLAIN, not assumptions.

Running EXPLAIN in a test (SQLAlchemy + Postgres):
```python
from sqlalchemy import text
rows = session.execute(text("EXPLAIN " + compiled_sql)).scalars().all()
plan = "\n".join(rows)
assert "Seq Scan" not in plan   # or: assert "Index Scan" in plan / "Index Only Scan"
```
Compile an ORM `select(...)` to SQL via `stmt.compile(bind=session.get_bind(),
compile_kwargs={"literal_binds": True})`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EXPLAIN-based index-usage test over seeded + ANALYZEd critical queries (suite green)</name>
  <files>tests/test_query_index_usage.py</files>
  <action>
    Create an integration test that, per critical query path, seeds ENOUGH rows and runs `ANALYZE`
    BEFORE `EXPLAIN`. This is essential: Postgres correctly prefers a Seq Scan on tiny tables, so an
    EXPLAIN on an empty/small table would either pass vacuously or fail a perfectly good index. Seed on
    the order of a few thousand rows into each table under test and run `session.execute(text("ANALYZE <table>"))`
    (or bare `ANALYZE`) so the planner has real statistics.

    Use the project's existing DB test fixtures (same engine/session fixtures as tests/test_paper_execution.py
    / tests/test_execution_reconciliation.py). Skip cleanly if the test DB is not Postgres (EXPLAIN plan
    text is Postgres-specific).

    For each of these representative critical queries, compile the real ORM statement (import/rebuild the
    exact `select(...)` used by the service, or call a small extracted query-builder), then assert via a
    helper `assert_uses_index(session, stmt, table_name)` that the EXPLAIN plan contains an
    `Index Scan`/`Index Only Scan` and NOT a `Seq Scan` on the large table (attach the full plan to the
    assert message):
      - Operator runs list: StrategyRun join Strategy filtered by strategy + ordered by started_at desc.
      - Operator orders listing (PaperOrder join StrategyRun join Strategy, ordered).
      - Reconciliation PaperOrder-by-strategy load (reconciliation.py:296) and Position-by-strategy+status
        (reconciliation.py:309).
      - Order-lifecycle sync: `select(PaperFill.broker_fill_id)` dedup (paper_execution.py:1747) and the
        open-position load (paper_execution.py:1787).

    CRITICAL — keep this task's suite GREEN on completion. Run each case once locally to see which already
    use an index and which seq-scan under current schema. For every case that seq-scans TODAY (the ones
    Task 2's migration will fix), mark that specific test `@pytest.mark.xfail(strict=True, reason="PERF-03: index added in migration 0017 / 11-03 Task 2")`. Cases whose index already exists are plain passing
    tests. Result: `pytest tests/test_query_index_usage.py` exits 0 at end of Task 1 (passes + strict-xfails),
    and the xfail set is the exact list of indices Task 2 must add. Do NOT weaken any assertion to make a
    seq-scan case "pass" — xfail is the mechanism, not a relaxed assert.
  </action>
  <verify>
    <automated>python -m pytest tests/test_query_index_usage.py -q</automated>
  </verify>
  <done>Suite exits 0: already-covered paths pass as Index Scans; genuine seq-scan paths are strict-xfail and name the indices Task 2 must add.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0017 — add named indices; flip the xfail cases to passing</name>
  <files>alembic/versions/0017_phase11_query_performance_indices.py, src/trading_platform/db/models/paper_order.py, src/trading_platform/db/models/paper_fill.py, src/trading_platform/db/models/position.py, tests/test_query_index_usage.py</files>
  <action>
    For ONLY the queries Task 1 marked strict-xfail (proven Seq Scan), add a named covering index.
    Add each index in BOTH places, kept consistent:
      1. The model's `__table_args__` `Index("ix_<table>_<cols>", ...)` (so the ORM/metadata knows it).
      2. Migration `0017_phase11_query_performance_indices.py` `upgrade()` via `op.create_index(...)`,
         with a matching `op.drop_index(...)` in `downgrade()`.

    Set `revision="0017_phase11_query_performance_indices"` and
    `down_revision="0016_phase8_stale_run_status"` (the full-slug revision ids this repo uses — the
    down_revision must equal 0016's exact `revision` string verbatim, NOT the numeric prefix `"0016"`).
    Follow the exact structure of an existing migration
    (read `0016_phase8_stale_run_status.py` for the header/imports/style).

    Expected additions (confirm against Task 1's actual xfail set — add nothing that already has a covering
    index; the migration should be small):
      - `ix_paper_fills_broker_fill_id` on `paper_fills.broker_fill_id` if the dedup query (1747) seq-scanned.
      - A started_at-leading index on `strategy_runs` (e.g. `ix_strategy_runs_started_at`) if the operator
        runs-list ordering seq-scanned.
      - Any `intended_session_date`-leading paper_orders index only if a proven critical path needs it.

    Do NOT duplicate existing indices (see the interfaces list). If Task 1 produced NO xfail cases (every
    critical path already indexed), the migration upgrade/downgrade may legitimately be near-empty — record
    that outcome in the SUMMARY rather than inventing indices to justify the migration.

    Then REMOVE the `@pytest.mark.xfail` decorators from the now-fixed cases in tests/test_query_index_usage.py
    so they become plain passing tests (strict-xfail would otherwise fail once they start passing). Every case
    must now show Index Scan / Index Only Scan with no Seq Scan on the large table.
  </action>
  <verify>
    <automated>python -m pytest tests/test_query_index_usage.py tests/test_db_migrations.py -q</automated>
  </verify>
  <done>Migration 0017 applies (upgrade + downgrade) cleanly and mirrors the model Index() additions; all EXPLAIN cases pass as index scans with no remaining xfail and no Seq Scan on large tables.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_query_index_usage.py tests/test_db_migrations.py -q` is green with no remaining xfail.
- Migration `0017` upgrades and downgrades cleanly; every model-level Index added is mirrored in the migration.
- EXPLAIN on each critical path (operator reads, reconciliation, order-lifecycle sync), over seeded+ANALYZEd
  tables, shows a named Index/Index-Only Scan — no full Seq Scan on a large table.
</verification>

<success_criteria>
PERF-03 satisfied: EXPLAIN output for operator reads, reconciliation queries, and order-lifecycle sync
queries shows the named covering index is used; full sequential scans on large tables are absent. Any
newly required index ships via migration 0017 following the existing numbering convention.
</success_criteria>

<output>
After completion, create `.planning/phases/11-query-performance/11-03-SUMMARY.md`. List exactly which
indices migration 0017 added (or state that all critical paths were already covered and why the migration
is minimal).
</output>
